# 로그인 화면 — 시작하자마자 부르는 서버 호출, 예외처리 원본 대조

> 2026-09-16 · 장군님 지적: 「로그인창 실행 하자마자 서버 호출하는 로직에 예외처리가 원본이랑 완전 다르다」
> 대조 대상: 원본 `C:\Users\socce\Documents\Github\U4A_WS3.0.0\www\ws30\ws10_20\Login\Login.js` ↔ 현행 `www/ws30/ws10_20/Login/Login.js`
> **2026-09-16 장군님 지시 「빨리 고쳐 원본처럼」 → ①~⑦ 중 ①②③④⑤⑥⑦ 원본대로 고침.** 오류 창 버튼 모양(§2-4)은 **안 건드렸다**.
> 테스트 = [00_현황판.md](00_현황판.md) · 백업 = `www/ws30/ws10_20/Login/_Login.js.origerrbak`

---

## 1. 정해 주실 것

| 물음 | 제 생각 |
|---|---|
| §2 의 차이를 **원본대로 되돌릴까** | **되돌린다.** 가장 큰 것은 ①번 — 서버가 오류 응답(HTTP 4xx·5xx)을 주면 원본은 「서버 연결 실패」 창을 띄우고 로그인 창을 닫는데, 지금은 **오류를 모른 채 로그인 화면을 그대로 보여 준다.** 사용자가 원인도 모르고 로그인을 시도하게 된다 |
| 원본에 **없는 것을 더한 것**(②·⑤)도 빼나 | **뺀다.** 원본에 없는 동작이다(임의 추가 금지). 누가 왜 넣었는지 주석에 근거가 없다 |
| 콘솔 안내 글이 **원본과 뜻이 반대로** 바뀐 것(③·⑥)도 원본대로 | **원본 글 그대로.** 운영자가 이 글을 보고 서버 설정을 만진다 — 「SAML 삭제」가 「SAML 설정」으로 바뀌어 있어 **정반대 조치**를 하게 된다 |

---

## 2. 차이 목록

### 2-1. 접속 서버 설치 언어 조회 — 로그인 화면이 뜨면 가장 먼저 부르는 서버 호출

원본 4110~4195줄 (`jQuery.ajax`) ↔ 현행 2261~2310줄 (`XMLHttpRequest`)

| # | 무엇 | 원본 | 현행 | 결과 |
|---|---|---|---|---|
| ① ★장군님 실측 확인(2026-09-16, 502 Bad Gateway) — 원본은 「Server connection failed!」 창, 현행은 창 없이 로그인 화면 | **서버가 오류 응답(HTTP 4xx·5xx)을 줄 때** | `jQuery.ajax` 는 **2xx 가 아니면 `error`** 로 간다 → `E999` → 「서버 연결 실패」 창 → 닫으면 로그인 창 닫힘 | `XMLHttpRequest` 의 `onload` 는 **상태 코드와 상관없이** 불린다. 상태 코드를 안 본다 → 본문이 HTML 이면 JSON 읽기 실패 → **`E998`(하위 버전 서버로 간주)** → 언어 입력칸을 보이고 **정상 진행** | **❌ 오류가 숨는다.** 오류 창 없이 로그인 화면이 뜬다. 본문이 JSON 이면 오류 응답을 **정상 결과로 쓴다** |
| ② | 쿠키 같이 보내기 | 없음 (jQuery 기본값 = 안 보냄) | `xhr.withCredentials = true` | 원본에 없는 설정. 실제 영향은 **미확인** |
| ③ | JSON 읽기 실패 때 콘솔 안내 글 | 「로그인 페이지의 언어 선택 영역이 **서버 언어 입력 Input만 나오게** 처리」 · `console.log` | 「언어 선택 영역과 언어 수동 입력 Input을 **숨김처리**」 · `console.error` | **뜻이 반대.** 실제 동작은 입력칸을 **보인다**(원본·현행 같음) — 글만 틀렸다. 등급도 바뀌었다 |
| ④ | 통신 실패 때 오류 원문 | `console.error(error)` 로 **오류 원문**을 남긴다 | 원문을 **안 남긴다**. 시간 초과와 연결 실패를 한 함수로 합쳐 **어느 쪽인지도 안 남는다** | 원인을 못 가린다 |
| ⑥-a | 통신 실패 때 콘솔 안내 글 | 「**접속 정보** 확인 요망!!」 · 「SSO(SAML 2.0) **적용** 서버일 경우, ZU4A_WBC 서비스에 **SAML 삭제**!!」 | 「**서버 상태** 확인 요망!!」 · 「SSO(SAML 2.0) **인증 설정**일 경우, ZU4A_WBC 서비스에 **SAML 설정**!!」 | **❌ 조치가 반대.** 삭제 → 설정 |
| — | 시간 초과 | 5초 → `error` → `E999` | 5초 → `ontimeout` → `E999` | 같음 |

### 2-2. 언어 조회 결과 처리 — 원본 4202~4327줄 ↔ 현행 2313~2363줄

| # | 무엇 | 원본 | 현행 | 결과 |
|---|---|---|---|---|
| — | 오류(`E001`·`E999`·그 외) | 오류 창 + busy 끄기 + **여기서 멈춤**(다음 단계로 안 감) | 같음 | 같음 |
| — | `E998` | 언어 입력칸 보이고 진행 | 같음 | 같음 |
| ⑦ | model 이 없을 때 | 없으면 그냥 진행 (`if (!oModel) return resolve()`) | 확인 없이 바로 씀 | 원본의 막는 줄이 빠졌다 — 실제로 비는 경우가 있는지는 **미확인** |

### 2-3. SSO 자동 로그인 — 원본 4416~4472줄 ↔ 현행 2365~2403줄

| # | 무엇 | 원본 | 현행 | 결과 |
|---|---|---|---|---|
| ⑤ | **응답이 안 올 때** | 시간 제한 **없음** — 응답을 계속 기다린다 | **30초 뒤 강제로 끊고**(원본에 없음) 오류로 기록한 뒤 **로그인을 그대로 진행**한다 | 원본에 없는 동작. 원본은 멈춰 있고, 현행은 30초 뒤 SSO 처리 실패인 채로 로그인을 시도한다 |
| ⑥-b | 통신 실패 때 콘솔 안내 글 | 「SSO **관련** 로그인 처리 실패」 · 「**접속 정보** 확인」 · 「SAML 2.0 **적용** 서버일 경우 … **SAML 삭제**」 | 「SSO **자동** 로그인 처리 실패」 · 「**서버 상태** 확인」 · 「SAML 2.0 **인증 설정**일 경우 … **SAML 설정**」 | **❌ 조치가 반대** |
| — | 응답 내용 확인 | 안 본다 | 안 본다 | 같음 |

### 2-4. 오류 창(서버 연결 실패) — 원본 4021~4105줄 ↔ 현행 2202~2258줄

| 무엇 | 원본 | 현행 | 결과 |
|---|---|---|---|
| 제목·내용·안내 글 | 같은 메시지 번호 | 같은 메시지 번호 | 같음 |
| 버튼 | 「점검사항」(물음표 아이콘) + 닫기(**X 아이콘만, 빨강**) | 「점검사항」(체크리스트 아이콘, **강조색**) + **「Close」 글자 버튼(중립색)** | 모양이 원본과 다르다 — 주석에 바꾼 이유(「버튼까지 빨강이면 과함」)만 있고 **원본 근거는 없다** |
| 닫으면 | 로그인 창 닫힘 | 로그인 창 닫힘 | 같음 |

---

## 3. 원본과 같은 것 (확인함)

- 언어 조회 시간 초과 5초, 시간 초과 → 통신오류 처리.
- 오류가 나면 오류 창을 띄우고 **로그인 시작 처리를 거기서 멈춘다**.
- 오류 창을 닫으면 로그인 창이 닫힌다.
- 테스트 모드 자동 로그인(현행에만 있음)은 **포장본에서는 스위치 자체가 안 만들어진다** (`ServerList_v2/ServerList.js` 1012줄) — 고객사에서는 안 돈다.

---

## 4. 미확인

- ② 쿠키 같이 보내기가 실제 서버 응답에 영향을 주는지.
- ⑦ model 이 비는 경우가 실제로 생기는지.
- 이 차이들을 **누가 언제 넣었는지** — 현행 파일 주석에는 「2026-09-08 복원」 표시만 있고, 안내 글 문장이 바뀐 근거는 없다.
