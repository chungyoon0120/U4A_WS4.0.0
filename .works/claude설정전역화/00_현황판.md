# Claude 설정 전역화 — 현황판

> 작성 2026-09-14 · 상태 **작업·테스트 전부 통과** — 남은 것은 아래 「정해 주실 것」뿐
> 통과한 테스트 그룹은 [00_히스토리.md](00_히스토리.md) 로 옮겼다 · 실측 근거는 문서 하단

---

## 지금 할 것 (장군님)

> **2026-09-14 18:30 — 원점으로 돌아가 있던 저장소 용량을 고쳤다.**
> 프로젝트 안 중복 링크 10개를 제거해 실행 파일 4벌이 저장소에서 빠졌다.
> 전역에 이미 다 있어 **기능 손실 0**(지운 직후 실측으로 확인).
> 항목별 판정·근거 = **[01_정리상태_점검.md](01_정리상태_점검.md)**

### 정해 주실 것

| 정해 주실 것 | 선택지 |
|---|---|
| 다른 세션이 **또 프로젝트 안에 링크를 걸 수 있다.** 무시 목록에 넣어 재발을 막을 수 있다 | ⓐ 막아라 ⓑ 놔둬라 |
| 과거 commit 에 든 실행 파일까지 빼려면 이력을 다시 써야 하고, **이미 원격에 올라가 있어 강제 push 가 필요**하다 | ⓐ 과거분도 지워라 ⓑ 앞으로만 |
| `ui5-mcp-server` 가 전역과 프로젝트 두 곳에 등록돼 있다. 프로젝트 쪽은 켜져 있지 않아 놀고 있다 (다른 세션이 넣은 것이라 안 건드림) | ⓐ 한 곳으로 정리해라 ⓑ 놔둬라 |
| 다른 프로젝트 2곳(`infocg\timeoff` · `infocg_customers`)도 `sap_adt_mcp` 를 **각자 사본**으로 갖고 있다. 지금 전역에 한 벌 생겼으니 거기도 사본을 뺄 수 있다 | ⓐ 거기도 정리해라 ⓑ 놔둬라 |
| `D:\workspace\u4a_ws4_mcp` · `D:\workspace\sap_adt_mcp_server_py` · 다른 프로젝트 것까지 **떠돌이 프로세스**가 아직 살아 있다(9/10 세션 잔여). 이 프로젝트 것은 이미 정리했다 | ⓐ 나머지도 정리해라 ⓑ 놔둬라 |
| `.claude\_skills_backup\` (옛 사본 4파일, 0MB) | ⓐ 지워라 ⓑ 놔둬라 |

---

## 한 일 (2026-09-14 전부 완료)

| Task | 한 일 | 결과 |
|---|---|---|
| 0 | `.claude` 통째 백업 + 설정 4개 백업 | `C:\Users\socce\Documents\_backup\U4A_claude_20260914\` — 486.1MB / 6,322파일 |
| 1 | 끊어진 `yoon-notion` junction 복구 + token 합치기 | 연결 3개 살아 있음, 실제 접속까지 확인 |
| 2 | `skills\download\` 삭제 | −163.7MB |
| 3 | `agy-agent`·`codex-agent` → kit + junction | −163.8MB, 전역 경로에서 외부 CLI 실제 호출 성공 |
| 4 | 검수 명령 경로를 전역으로 고치고 프로젝트 `yoon-notion` 삭제 | −22MB, 고친 경로로 이슈 리포트 DB 읽기 확인 |
| 5 | `notion_multi` + 죽은 명령 파일 삭제 | −44.9MB |
| 6 | `sap_adt_mcp` → kit + 전역 등록 | −47.3MB, 연결 ✔ |
| 7 | `u4a-ws4-mcp` 되살림 (이 프로젝트 전용) | 연결 ✔, `.analy` 22건 읽힘 |
| 8 | 설정 파일 죽은 참조 정리 | 죽은 실행 경로 2줄·없는 서버 참조 제거 |
| 9 | 검증 | **`.claude` 486MB → 44.5MB** (남은 44.4MB = `u4a_ws4_mcp`, 유지 결정) |

### 작업 중 새로 찾아 고친 것

| # | 찾은 것 | 처리 |
|---|---|---|
| 1 | `~\.claude\skills\yoon-notion` 이 **없는 폴더를 가리키는 junction** 이었다 | kit 으로 다시 걸었다 |
| 2 | 검수 명령이 실행 파일 경로를 **프로젝트 경로로 박아** 뒀다 | 전역 경로로 고쳤다 (Task 4-1) |
| 3 | kit `yoon-notion` 의 `SKILL.md` 가 프로젝트 경로만 안내했다 | 전역 경로를 첫 번째 후보로 넣었다 (`_` 백업 남김) |
| 4 | 9/9 세션이 남긴 **떠돌이 프로세스 139개**가 파일을 잡고 있었다 | 이 프로젝트 것 139개 정리 (68 + 71) |
| 5 | `sap-adt` 가 **원래부터 깨져 있었다** — 코드는 `mcp` 1.x 용인데 사본의 `.venv` 에 2.1.1 이 깔려 있었다(백업본도 동일). 의존성이 `mcp>=1.2.0` 로만 적혀 2.x 를 끌어왔다 | 라이브러리는 손대지 않고 `mcp>=1.2.0,<2` 로 **버전을 맞췄다.** 1.30.0 설치 후 연결 ✔ |
| 6 | `u4a-ws4-mcp` 에도 같은 지뢰가 있었다(지금은 1.28.0 이라 돌지만 다음 `uv sync` 에서 깨진다) | `pyproject.toml` 만 `<2` 로 박아 뒀다. `.venv`·`uv.lock` 은 안 건드림 |
| 7 | `sap-adt` 접속 정보가 **이미 전역에 있었다** — `C:\Users\socce\.claude\secrets\sap_adt.env`. 다른 프로젝트 2곳이 이미 그걸 쓰고 D: 원본 `.env` 와 내용도 같다 | Q2 답은 「복사해라」 였으나, 복사하면 접속 정보 사본만 하나 더 는다. **이미 있는 전역 파일을 가리키게** 했다 (사본 안 만듦) |

---

## 결정된 것 (2026-09-14 장군님 지시)

| 항목 | 결정 |
|---|---|
| Q1 `yoon-notion` token `ws4.0.env` | **프로젝트 것이 최신** → kit 에 덮어쓴다 (kit 것은 `_` 접두로 백업) |
| Q2 `sap-adt` 접속 정보 `.env` | 지시는 **복사**였으나, 작업 중 **이미 전역에 같은 내용이 있는 것**을 확인 → 복사하지 않고 `C:\Users\socce\.claude\secrets\sap_adt.env` 를 가리키게 함 (위 「새로 찾아 고친 것」 7번) |
| Q3 `.claude\commands\yoon-notion.md` | **파일째 삭제** — skill 이 같은 일을 다 한다 |
| `sap-adt` MCP | **전역 kit** 으로 옮겨서 모든 프로젝트가 씀 |
| `notion_multi` MCP | **안 씀 — 삭제.** `yoon-notion` skill 로 대체 |
| `u4a-ws4-mcp` MCP | **살림. 이 프로젝트 전용**으로만 등록 |
| `u4a_ws4_mcp` 실체 위치 | **프로젝트 안에 그대로 유지** |
| git 히스토리 202MB | **지금은 안 건드림** — `.gitignore`·`git rm --cached`·history rewrite 전부 보류 |
| 진행 방식 | 계획서 먼저 (이 문서) |

---

## 목표 (달성)

- `.claude` 486MB → **44.5MB** (실측). 남은 44.4MB 는 `u4a_ws4_mcp` 한 덩어리로,
  「프로젝트에 남긴다」는 장군님 결정에 따른 것이다. 그걸 빼면 프로젝트 설정은 0.1MB 다
- 실체는 `C:\Users\socce\AppData\Local\yoon-agent-kit\` 한 벌, 프로젝트엔 설정 파일만 — groupware 와 같은 모양
- 0개였던 MCP server 를 **sap-adt(전역) + u4a-ws4-mcp(이 프로젝트)** 2개로 되살림 — 둘 다 연결 ✔

## 끝난 뒤 모양

```
C:\Users\socce\AppData\Local\yoon-agent-kit\        ← 실체 한 벌
  skills\  agy-agent · codex-agent · yoon-notion · (기존 ui5 skill 29개)
  mcp\     ui5-mcp-server · sap_adt_mcp            ← 새로 들어옴
  runtime\ node22.22.0 · python310

C:\Users\socce\.claude\
  skills\  ... → 전부 kit 으로 가는 junction
  .claude.json  → 최상위 mcpServers 에 sap-adt 등록

U4A_WS4.0.0\
  .mcp.json                 → u4a-ws4-mcp 1개
  .claude\
    mcp\u4a_ws4_mcp\        ← 이 프로젝트 전용, 그대로 유지
    skills\ common-ux · notion-log   ← U4A 전속 2개만
    rules\ commands\ agents\ hooks\ launch.json settings.json settings.local.json
```

---

## 작업 순서

### Task 0 — 백업 (되돌릴 자리 확보)

- [ ] 0-1 `.claude` 통째 백업
  ```powershell
  Compress-Archive -Path "C:\Users\socce\Documents\Github\CHUNGYOON0120\U4A_WS4.0.0\.claude" -DestinationPath "C:\Users\socce\Documents\_backup\U4A_claude_20260914.zip" -CompressionLevel Fastest
  ```
- [ ] 0-2 설정 파일 4개 복사 (`_` 접두 백업)
  - `C:\Users\socce\.claude.json` → `_.claude.json.20260914.bak`
  - `C:\Users\socce\.claude\settings.json` → `_settings.json.20260914.bak`
  - 프로젝트 `.mcp.json` → `_.mcp.json.20260914.bak`
  - 프로젝트 `.claude\settings.local.json` → `_settings.local.json.20260914.bak`
- [ ] 0-3 백업 zip 크기가 더 안 늘어나는 것 확인 후 다음으로 (느린 PC 대비 — 시간으로 못 박지 않는다)

---

### Task 1 — 깨진 `yoon-notion` junction 고치기

**지금 상태(실측):** `C:\Users\socce\.claude\skills\yoon-notion` 은 **끊어진 junction** 이다.
가리키는 곳 `AppData\Local\Packages\Claude_pzs8sxrjxfjjc\LocalCache\Local\yoon-notion-skill\skills\yoon-notion` 가 **없다.**
그래서 지금 실제로 동작하는 건 프로젝트 사본 하나뿐이다. Task 4 에서 그걸 지우므로 이 Task 가 **먼저** 끝나야 한다.

- [ ] 1-1 token 파일 대조 (내용은 찍지 않고 해시만)
  ```powershell
  $p="C:\Users\socce\Documents\Github\CHUNGYOON0120\U4A_WS4.0.0\.claude\skills\yoon-notion\env"
  $k="C:\Users\socce\AppData\Local\yoon-agent-kit\skills\yoon-notion\env"
  Get-ChildItem $p,$k -Filter *.env | Select-Object FullName,Length,LastWriteTime,@{n='Hash';e={(Get-FileHash $_.FullName).Hash.Substring(0,12)}}
  ```
- [ ] 1-2 kit 에 없는 token 파일을 kit 으로 복사 — `me.env` · `u4a.env` · `.default`
      (`u4a.env` 는 이슈 리포트 DB 연결. `.claude\commands\yoon-audit.md` 가 이걸 쓴다)
- [ ] 1-3 `ws4.0.env` — **프로젝트 것이 최신**(장군님 확인). kit 것을 `_ws4.0.env.20260914.bak` 로 백업한 뒤 프로젝트 것으로 덮어쓴다
- [ ] 1-4 **코드는 kit 것을 남긴다** — 실측상 kit 이 최신이다
      - `SKILL.md` kit 9/7 14,289byte > 프로젝트 9/2 13,098byte
      - `notion_cli.py` kit 9/7 54,278byte > 프로젝트 9/2 49,745byte (내용 다름)
      - `yoon-notion.cmd` 내용 다름 / `yoon-notion.sh` 같음 / `runtime` 22MB 양쪽 같음
      → **합치는 방향: 코드=kit, token=프로젝트.** 프로젝트 코드로 kit 을 덮어쓰지 않는다
- [ ] 1-4b 1-4 를 뒤집어야 할 상황 대비 — 프로젝트 `notion_cli.py`·`SKILL.md`·`yoon-notion.cmd` 를 Task 0 백업 zip 에서 꺼낼 수 있는지만 확인하고 넘어간다
- [ ] 1-5 끊어진 junction 교체
  ```powershell
  Remove-Item "C:\Users\socce\.claude\skills\yoon-notion" -Force
  New-Item -ItemType Junction -Path "C:\Users\socce\.claude\skills\yoon-notion" -Target "C:\Users\socce\AppData\Local\yoon-agent-kit\skills\yoon-notion"
  ```
- [ ] 1-6 **검증** — 등록된 노션 연결 목록이 `me` · `u4a` · `ws4.0` 3개 다 나오는지 확인. 안 나오면 여기서 멈춘다(Task 4 진행 금지)

---

### Task 2 — `skills\download\` 삭제 (163.7MB)

`agy-agent` · `codex-agent` 를 그대로 한 벌 더 담아 둔 사본이다. `node.exe` 해시가 네 벌 전부 같다(`bae898add4643fcf`).

- [ ] 2-1 `node.exe` 4개 해시가 같은지 다시 확인
- [ ] 2-2 `.claude\skills\download` 삭제
- [ ] 2-3 참조하는 곳이 없는지 확인
  ```bash
  grep -rn "skills/download\|skills\\\\download" .claude .analy .works --include=*.md --include=*.json
  ```

---

### Task 3 — `agy-agent` · `codex-agent` → kit 이동 (163.8MB)

두 SKILL.md 는 **자기 폴더 밑의 `scripts/node_runtime/node.exe` 를 쓰라고 명시**한다(시스템 node 금지).
폴더째 옮기면 그 관계가 그대로 유지되므로 **SKILL.md 는 한 줄도 안 고친다.**

- [ ] 3-1 `.claude\skills\agy-agent` → `kit\skills\agy-agent` 이동
- [ ] 3-2 `.claude\skills\codex-agent` → `kit\skills\codex-agent` 이동
- [ ] 3-3 junction 2개 생성
  ```powershell
  $k="C:\Users\socce\AppData\Local\yoon-agent-kit\skills"
  New-Item -ItemType Junction -Path "C:\Users\socce\.claude\skills\agy-agent"   -Target "$k\agy-agent"
  New-Item -ItemType Junction -Path "C:\Users\socce\.claude\skills\codex-agent" -Target "$k\codex-agent"
  ```
- [ ] 3-4 **검증** — 새 세션에서 skill 목록에 둘 다 뜨는지, `--prompt` 로 한 번 실제 호출되는지

---

### Task 4 — 프로젝트 `yoon-notion` 사본 삭제 (22MB)  ※ Task 1-6 통과 후에만

**★먼저 고칠 것 (조사에서 나옴):** `.claude\commands\yoon-audit.md` 가 실행 파일 경로를 **프로젝트 경로로 박아** 두고 있다.
그대로 지우면 **검수 왕복 명령이 깨진다.** 삭제보다 경로 수정이 **먼저**다.

| 줄 | 지금 박혀 있는 것 |
|---|---|
| 24 | `.claude/skills/yoon-notion/yoon-notion.cmd` |
| 25 | `& ".claude\skills\yoon-notion\yoon-notion.cmd" <subcmd> --as u4a ...` |
| 26 | `.claude/skills/yoon-notion/yoon-notion.sh <subcmd> --as u4a ...` |

- [ ] 4-1 위 3줄을 **전역 junction 경로**로 고친다
  - PowerShell: `& "$env:USERPROFILE\.claude\skills\yoon-notion\yoon-notion.cmd" <subcmd> --as u4a ...`
  - Bash: `~/.claude/skills/yoon-notion/yoon-notion.sh <subcmd> --as u4a ...`
  - (프로젝트 경로가 아니라 전역을 쓰므로 다른 프로젝트에서도 그대로 돈다)
- [ ] 4-2 고친 경로로 **실제 한 번 실행**해 이슈 리포트 DB 가 읽히는지 확인
- [ ] 4-3 확인되면 `.claude\skills\yoon-notion` 삭제
- [ ] 4-4 남은 프로젝트 경로 참조 확인
  ```bash
  grep -rn "skills/yoon-notion\|skills\\\\yoon-notion" .claude .analy .works --include=*.md --include=*.json
  ```

---

### Task 5 — `notion_multi` MCP 삭제 (44.9MB)

- [ ] 5-1 `.claude\mcp\notion_multi` 삭제
- [ ] 5-2 `.claude\commands\yoon-notion.md` **삭제** (장군님 확인)
      - 이 파일의 `allowed-tools` 가 `mcp__notion-multi__*` 5개를 가리킨다 → 서버가 없어 **지금도 호출하면 전부 실패**
      - kit `yoon-notion` skill 이 같은 일을 다 한다 — 연결 관리 · 대상 페이지 찾기 · 읽기 · 추가/생성 · 수정/삭제
      - 부르는 곳 없음. `yoon-audit.md` 는 이 명령 파일이 아니라 CLI 를 직접 부른다
      - 잃는 것: 슬래시 명령 한 줄로 부르던 편의. skill 로 부르면 된다
- [ ] 5-3 남은 참조 확인
  ```bash
  grep -rn "notion-multi\|notion_multi" .claude .analy .works --include=*.md --include=*.json
  ```

---

### Task 6 — `sap_adt_mcp` → kit 이동 + 전역 등록 (47.3MB)

**어느 것이 최신인지 실측:** 프로젝트 사본(9/1, `server.py` 31,896byte) > `D:\workspace\sap_adt_mcp_server_py`(6/8, 30,961byte). **프로젝트 사본이 최신**이므로 이걸 옮긴다. D: 폴더는 건드리지 않고 그대로 둔다.

- [ ] 6-1 `.claude\mcp\sap_adt_mcp` 의 **`.venv` 를 뺀 나머지**를 `kit\mcp\sap_adt_mcp` 로 복사
      (`.venv` 안에 절대경로가 박혀 있어 옮기면 깨진다 — 옮기지 말고 새로 만든다)
- [ ] 6-2 kit 자리에서 의존성 설치
  ```powershell
  cd "C:\Users\socce\AppData\Local\yoon-agent-kit\mcp\sap_adt_mcp"
  uv sync
  ```
- [ ] 6-3 접속 정보 `.env` **복사** (장군님 확인)
      `D:\workspace\sap_adt_mcp_server_py\.env` → `kit\mcp\sap_adt_mcp\.env`
      - 서버는 ① 환경변수 `SAP_ADT_ENV_FILE` ② 실행 폴더의 `.env` ③ 서버 폴더의 `.env` 순으로 찾는다.
        kit 안에 두면 ③으로 잡히므로 환경변수는 안 넣는다
      - **내용은 열어보지도 찍지도 않는다.** 복사만 하고 줄 수만 확인
      - kit 은 저장소 밖이라 git 에 안 들어간다. D: 원본은 그대로 둔다
- [ ] 6-4 `C:\Users\socce\.claude.json` **최상위** `mcpServers` 에 등록 (지금 비어 있음)
  ```json
  "mcpServers": {
    "sap-adt": {
      "command": "uv",
      "args": ["run", "--project", "C:/Users/socce/AppData/Local/yoon-agent-kit/mcp/sap_adt_mcp", "sap-adt-mcp"]
    }
  }
  ```
- [ ] 6-5 `.claude\mcp\sap_adt_mcp` 삭제
- [ ] 6-6 **검증** — 새 세션에서 `sap-adt` tool 이 잡히는지. 안 잡히면 6-2·6-3 부터 다시

---

### Task 7 — `u4a-ws4-mcp` 되살리기 (프로젝트 전용)

실체는 `.claude\mcp\u4a_ws4_mcp` 에 **그대로 둔다**(장군님 결정). 안 옮기므로 `.venv` 도 그대로 쓴다.
서버는 `.analy` 위치를 ① 환경변수 `U4A_WS4_ANALY_DIR` ② 실행 폴더의 `.analy` ③ 박아 둔 기본값 순으로 찾는다.
Claude Code 는 프로젝트 루트에서 서버를 띄우므로 ②로 자동으로 맞는다 — 환경변수는 안 넣는다.

- [ ] 7-1 `.venv` 가 아직 도는지 확인
  ```powershell
  cd "C:\Users\socce\Documents\Github\CHUNGYOON0120\U4A_WS4.0.0\.claude\mcp\u4a_ws4_mcp"
  uv run python -m py_compile src/u4a_ws4_mcp/server.py
  ```
  실패하면 `uv sync` 로 다시 만든다
- [ ] 7-2 `.mcp.json` 재작성 (지금 `{}` 이다)
  ```json
  {
    "mcpServers": {
      "u4a-ws4-mcp": {
        "command": "uv",
        "args": ["run", "--project", "./.claude/mcp/u4a_ws4_mcp", "u4a-ws4-mcp"]
      }
    }
  }
  ```
- [ ] 7-3 **검증** — 새 세션에서 `.analy` 목차 조회 tool 이 실제로 문서 목록을 돌려주는지
- [ ] 7-4 `.claude\agents\code-reviewer.md` 가 쓰는 tool 이름 3개가 살아난 서버 것과 맞는지 대조

---

### Task 8 — 설정 파일 정리

- [ ] 8-1 `.claude\settings.local.json`
  - `enabledMcpjsonServers` → `["u4a-ws4-mcp"]` (`sap-adt` 는 이제 전역이라 여기 없어도 됨)
  - `disabledMcpjsonServers` 의 `ui5-mcp-server` → 이 프로젝트엔 없는 서버. 제거
  - `permissions.allow` 의 프로젝트 경로 `node.exe` 2줄 → Task 3 으로 경로가 죽음. 제거
- [ ] 8-2 `.claude\_skills_backup\` (4파일) — 옛 사본. 지울지 남길지 확인
- [ ] 8-3 `.claude\_settings.json.20260910.이력게이트전.bak` — 남겨도 무방(작음)

---

### Task 9 — 최종 검증

- [ ] 9-1 Claude Code **완전 재시작** (설정·MCP 는 재시작해야 다시 읽는다)
- [ ] 9-2 skill 목록에 `agy-agent` · `codex-agent` · `yoon-notion` · `common-ux` · `notion-log` 5개가 다 뜨는지
- [ ] 9-3 MCP server 2개(`sap-adt` · `u4a-ws4-mcp`) 연결 확인
- [ ] 9-4 용량 확인 — `.claude` 가 8MB 안쪽인지
- [ ] 9-5 `git status` 로 삭제 건수 확인 (아래 「남는 문제」 1번 참고)
- [ ] 9-6 다른 프로젝트(groupware)에서 skill 이 그대로 보이는지 — 전역화가 먹었다는 증거

---

## 남는 문제 (장군님 결정에 따라 일부러 안 하는 것)

| # | 남는 것 | 이유 |
|---|---|---|
| 1 | **git 에 삭제 5,730건이 잡힌다** | 「git 안 건드림」 결정. 커밋은 지시가 있을 때만 한다. 커밋 전까지 `git status` 가 지저분하다 |
| 2 | `.git` 202MB 그대로 | history rewrite 보류 |
| 3 | `u4a_ws4_mcp\.venv` 1,982파일(44MB)이 계속 git 추적됨 | 「git 안 건드림」 결정. `.gitignore` 를 손대야 빠진다 |
| 4 | kit 안에 `node.exe` 3벌(257MB) 남음 | `agy-agent`·`codex-agent` SKILL.md 가 **자기 폴더 것**을 쓰라고 명시. 남의 도구라 안 고친다 |
| 5 | `D:\workspace\sap_adt_mcp_server_py` (6/8 구버전) 그대로 둠 | 지우라는 지시 없었음 |

### hook 관련 남는 문제 — `.docs/CURRENT.md` 에서 옮겨 옴 (2026-09-14)

장군님 지시로 `.docs/CURRENT.md` 는 **화면 작업만** 담게 정리했다. 아래는 도구 쪽이라 여기로 옮긴다.

| 무엇 | 상세 |
|---|---|
| **세션 시작 hook 이 안 돈다** | `.docs` 를 만들고 「CURRENT.md 를 먼저 읽어라」를 넣어 주는 hook. 전역·프로젝트 양쪽에 등록돼 있는데 **그날 발동 기록이 0건**이었다(실측 2026-09-14). 그래서 그 지시가 새 대화창에 안 들어간다 — 대신 매 턴 도는 쪽(`.claude/rules/always.md`)에 한 줄 넣어 두었다 |
| **세션 시작 hook 문구가 옛 지시** | 그 hook 이 "변경이 전혀 없는 단순 질의에는 이력 파일을 만들지 마라"를 매 세션 주입한다. 2026-09-14 철회된 내용인데 **장군님 지시로 고치지 않았다**(전역 도구라 다른 프로젝트에도 영향) |
| **codex 답변끝 hook 미승인** | 승인 전까지 codex 는 이력을 안 남겨도 답변을 끝낼 수 있다 (**미확인** — 그 뒤 승인했는지 확인 안 됨) |
| **codex 전역 hook 2개 고장** | 없는 파일을 가리켜 매번 실패. **전역 설정이라 손대지 않는다** (**미확인** — 그 뒤 상태 확인 안 됨) |
| **이력 게이트·CURRENT.md 자체가 잘 도는지** | 「질문만 한 대화는 안 남긴다」를 철회하고 CURRENT.md 를 2026-09-14 부터 쌓게 바꿨다. **새 대화창에서 이 문서가 실제로 갱신되는지 지켜볼 것** |

---

## 실측 근거 (2026-09-14 조사)

### groupware 가 쓰는 방식

| 위치 | 내용 |
|---|---|
| `C:\Users\socce\AppData\Local\yoon-agent-kit\` | 실체 한 벌 — `skills\`(32) · `mcp\ui5-mcp-server` · `hooks\claude\SessionStart` · `runtime\node22.22.0`·`python310` |
| `C:\Users\socce\.claude\skills\*` | 전부 **Junction** → kit |
| `C:\Users\socce\.claude\settings.json` | SessionStart hook 이 kit 의 `run.cmd` 호출 |
| groupware 프로젝트 | `.mcp.json` 8줄 + `.claude\settings.json`(hook) + 프로젝트 전용 skill 1개 |

kit 에 `installer\configure.py` 가 있고 agent별 경로표·install·remove 를 다 갖고 있다.
다만 이번 작업은 **끊어진 junction 수습(Task 1)과 token 병합**이 섞여 있어, 손으로 하는 편이 확실하다.
정리가 끝난 뒤 이 도구로 다른 프로젝트에 붙이는 건 그때 판단한다.

### 현재 프로젝트 용량 (`.claude` 486MB · git 추적 5,812파일 · `.git` 202MB)

| 항목 | 용량 | 파일수 | 처리 |
|---|---|---|---|
| `skills\download\` | 163.7MB | 6 | 삭제 (Task 2) |
| `skills\agy-agent\` | 81.9MB | 4 | kit 이동 (Task 3) |
| `skills\codex-agent\` | 81.9MB | 3 | kit 이동 (Task 3) |
| `skills\yoon-notion\` | 22MB | 44 | 삭제 (Task 4) |
| `skills\common-ux`·`notion-log` | 0MB | 2 | 유지 |
| `mcp\sap_adt_mcp` | 47.3MB | 2,259 | kit 이동 (Task 6) |
| `mcp\notion_multi` | 44.9MB | 1,995 | 삭제 (Task 5) |
| `mcp\u4a_ws4_mcp` | 44.4MB | 1,982 | **유지** (Task 7) |
| `rules`·`commands`·`hooks`·`agents` | 0MB | 14 | 유지 |

### 지금 고장 나 있는 것

1. `.mcp.json` 이 `{}` (9/9 21:42 수정) → 이 프로젝트에 등록된 MCP server **0개**
2. `settings.local.json` 의 `enabledMcpjsonServers` 2개가 없는 서버를 가리킴
3. `.claude\agents\code-reviewer.md` 가 `u4a-ws4-mcp` tool 3개를 쓰도록 적혀 있으나 서버가 없어 그 부분 동작 불가
4. `C:\Users\socce\.claude\skills\yoon-notion` 이 **끊어진 junction** (가리키는 폴더 없음)
5. `.claude\commands\yoon-notion.md` 가 폐기된 `notion-multi` MCP tool 5개를 가리킴
   (같은 폴더 `yoon-audit.md` 에는 "`notion-multi` 폐기, `yoon-notion` 으로 이관(2026-09-10)" 이라고 적혀 있다)
6. `.claude\commands\yoon-audit.md` 가 실행 파일 경로를 `.claude/skills/yoon-notion/` 로 **프로젝트 경로로 박아** 둠(24·25·26줄).
   지금은 도는데, Task 4 에서 그 폴더를 지우면 깨진다 → Task 4-1 에서 전역 경로로 먼저 고친다

### `node.exe` 해시 (5벌 전부 동일)

`bae898add4643fcf` — `skills\agy-agent` · `skills\codex-agent` · `skills\download\agy-agent` · `skills\download\codex-agent` · `kit\runtime\node22.22.0`

### `yoon-notion` 사본 3곳

| 위치 | SKILL.md | notion_cli.py | env token | 판정 |
|---|---|---|---|---|
| 프로젝트 `.claude\skills\yoon-notion` | 9/2 · 13,098byte | 9/2 · 49,745byte | `me` · `u4a` · `ws4.0` + `.default` | **token 을 가져온다** |
| kit `skills\yoon-notion` | 9/7 · 14,289byte | 9/7 · 54,278byte | `ws4.0` 만 | **코드가 최신 — 여기로 합친다** |
| 앱 캐시 폴더 | **폴더 없음** | — | — | `~\.claude\skills` junction 이 여기를 가리켜 끊어져 있다 |

합치는 방향 = **코드는 kit, token 은 프로젝트.**
`yoon-notion.cmd` 는 내용이 다르고(kit 것을 쓴다), `yoon-notion.sh` 는 같고, `runtime` 은 22MB 로 양쪽 같다.
프로젝트에만 있는 `__pycache__` 는 안 옮긴다. kit 에만 있는 `.yoon-managed.json` 은 kit 이 자기 것을 표시해 둔 것이라 그대로 둔다.
