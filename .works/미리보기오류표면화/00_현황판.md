# 현황판 — 미리보기 오류 표면화 복원 (2026-08-31)

## 배경
2026-08-14 장군님 지시("오류를 조용히 먹지 말고 드러내라")로 미리보기 화면 파일에서 걷어냈던 처리가,
2026-08-19 그 파일을 원본 최신본에 맞추면서 **원본 상태(조용히 넘김)로 되돌아갔다.**
그 파일은 원작 개발자가 통째로 갱신하는 자리라 **한 줄도 고치지 않고**, 바깥 우리 파일에서 덧대어 복원.

## 무엇을 했나
- 손댄 파일: `www/ws30/ws10_20/js/ws_html5_ws20_prev.js` (원본 자리 **밖**, 우리 파일)
- 미리보기 화면이 뜰 때, 실패를 조용히 먹던 자리에서 실제로 오류를 던지는 함수 5개를 감싸
  **오류코드와 함께 드러낸 뒤 원래대로 다시 던지도록** 함. 원본 흐름·대비책은 그대로.
- 되살린 자리 4곳: UI 부품 정보 읽기 / 화면 묶음 불러오기 / 속성 넣어 UI 만들기 / 속성 직접 적용.
- 백업: `js/_ws_html5_ws20_prev.js.errsurfacebak`

## 한계 (그대로 보고)
- 그 파일을 못 고치므로 **조용히 넘기는 구조 자체는 없앨 수 없다.** 오류를 드러내는 데까지가 한계.
- 속성 넣어 UI 만들 때 **UI 생성 자체가 실패하는 경우**는 감쌀 자리가 없어 못 드러낸다(빈 UI 로 대체되는 건 그대로).

## 테스트

| 코드 | 동작 | 조작·확인 포인트 | O/X(☐) |
|---|---|---|---|
| ES1 | 미리보기가 평소대로 뜬다(회귀 없음) | ① 앱 실행 ② 앱 하나 열어 WS20 디자인 화면 진입 ③ 오른쪽 미리보기에 화면이 정상 표시되는지 확인 | ☐ |
| ES2 | 미리보기에서 UI 고르기·끌어놓기가 평소대로 된다(회귀 없음) | ① 왼쪽 UI 트리에서 UI 하나 클릭 ② 미리보기에 선택 테두리 표시 확인 ③ 팔레트에서 UI 하나를 끌어다 놓아 정상 추가되는지 확인 | ☐ |
