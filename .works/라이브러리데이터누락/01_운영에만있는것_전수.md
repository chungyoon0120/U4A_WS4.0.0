# 운영에는 있는데 개발에 없는 것 — 전수

**최신 조사 2026-09-01 12:49:41(개발) / 12:49:45(운영) KST**
개발 `uha…:10101` (v3.6.4 / 패치 00005) · 운영 `u4a…:10102` (v3.6.4 / 패치 00004)

서버 DB를 직접 조회(읽기 전용)해 라이브러리 6종을 키·값 단위로 전량 대조.
값 묶음은 번호가 서버마다 다르게 매겨져 있어, 속성을 통해 이어붙여 **값 자체로** 비교.

---

## 지금 남은 것 — **UI 1개**

| 없는 UI | 비고 |
|---|---|
| `sap.ui.core.ws.SapPcpWebSocket.SUPPORTED_PROTOCOLS` | 사용중지 표시 항목이라 화면 영향은 낮다 |

**그 외 전부 해결됐다.** 속성·자리정보·선택값 모두 운영에만 있는 항목이 **0건**이다.

---

## 대조 결과 (2026-09-01 12:49)

| 라이브러리 | 개발 | 운영 | **운영에만 있음** |
|---|---|---|---|
| 라이브러리 기본 | 209 | 209 | **0** |
| 코드 정보 | 4,579 | 4,571 | **0** |
| UI 목록 | 1,774 | 1,771 | **1** |
| UI 속성 정보 | 35,561 | 35,485 | **0** |
| 넣을 수 있는 자리 | 9,535 | 9,536 | **0** |
| 값 묶음(선택값) | 2,451 | 2,451 | **0** |

---

## 8/31 → 9/1 변화 — **790건이 채워졌다**

| 항목 | 8/31 10:44~11:05 | **9/1 12:49** |
|---|---|---|
| UI | 1건 없음 | **1건 없음** (그대로) |
| UI 속성 | **23건 없음** | **0건** ✅ |
| 넣을 수 있는 자리 | **100건 없음** | **0건** ✅ |
| 선택값 | **667건 못 고름** | **0건** ✅ |
| 합계 | 791건 | **1건** |

8/31 조사에서 확인했던 것들이 그 사이 개발서버에 채워졌다.

- 입력칸·콤보·날짜 계열의 `enableSelectText` 20건, `formattedValueStateText`, `autoHideMasterPage` 2건 → 채워짐
- 폼 안에 입력 요소를 넣는 자리 정보 46건을 비롯한 100건 → 채워짐
- 목록 항목 계열의 `highlight` 강조색, `showTooltipOn`, `cursorOverStyle` 등 667건 → 고를 수 있게 됨
  (8/31에는 값 묶음이 조각나고 속성이 한 조각만 가리키던 상태였는데, 지금은 연결이 바로잡혔다)

---

## 참고 — 개발에만 있는 것 (개발 중인 신규분으로 보임 · 미확인)

| 구분 | 내용 |
|---|---|
| 새 UI 4개 | `sap.f.GridContainerItemLayoutData` · `sap.f.GridContainerSettings` · `sap.f.ProductSwitch` · `sap.f.ProductSwitchItem` |
| 새 속성 76건 | — |
| 새 자리 정보 33건 | — |
| 새 코드 정보 8건 | 이미지 압축 설정 3건 포함 |

---

## 조사 방법 메모

- 서버 DB를 읽기 전용으로 직접 조회. 6종을 키 단위로 전량 받아 차집합.
- 값 묶음은 번호가 서버마다 다르게 매겨지므로, 속성 → 묶음 번호 → 값 순으로 이어붙여 **값 자체로** 비교해야 한다.
  (번호로 비교하면 실제와 다른 결과가 나온다)
- **조사 결과는 그 시각의 스냅샷이다.** 8/31 조사 중에도 데이터가 바뀌었고(속성 3건 추가), 하루 만에 790건이 채워졌다.
  다시 볼 때는 반드시 재조회할 것.
