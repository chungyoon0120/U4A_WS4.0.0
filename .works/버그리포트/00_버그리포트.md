# 🐞 테스터 버그리포트 — 관리대장

> 테스터가 올리는 이슈를 모으는 **입력 대장**. 실제 수정·테스트 진척은 각 화면 작업폴더의 `00_현황판.md`에서 관리한다(여기서 상태만 요약).
> · 최종 동기화 **2026-08-11**
> · 원본 파일(읽기 전용): `G:\내 드라이브\develop\u4a_ws4.0\issue\U4A WS4.0 이슈사항.pptx`
> · 이 파일은 실시간으로 업데이트됨 → "업데이트됐어" 하시면 다시 읽어 **새로 늘어난 것만** 아래에 추가.

---

## 🆕 새 창에서 이 파일로 버그 고치기

> **작업 순서·규칙·검수까지 전부 → [작업지침서.md](작업지침서.md) 를 먼저 읽는다.**
> 요약: 아래 목록에서 코드(예: BR7) 줄을 읽고 → 그림 필요하면 `python extract_slide.py <슬라이드번호>` 로 그 1장만 뽑아 보고 → 원본(as-is)과 대조해 조사 → 애매하면 먼저 질문 → 원본 1:1 수정 + `node --check`.

---

## 📋 접수 목록

**상태 표기**: 접수(대기) · 수정중 · 테스트대기 · 완료
**분류**: 기능(눌러도/끌어도 동작 안 됨·결과 틀림·기능 부재) / UX(동작은 되지만 조작 편의·크기 조절·성능(안 보이는 줄 안 그리기)·표시)
> 고칠지 말지는 **장군님이 판단해서 지시** → 지시받은 건만 상태를 "수정중"으로 바꿔 진행.
> 분류는 성격에 따른 제 판단 → 다르면 말씀 주세요.

| 코드 | 슬라이드 | 화면 | 테스터 지적 내용 | 분류 | 상태 | 비고 |
|---|---|---|---|---|---|---|
| BR1 | 2p | 바인딩 팝업 | 최소화·최대화 버튼이 없음 | 기능 | ✅ 완료 | |
| BR2 | 3p | 바인딩 팝업 | 바인딩 필드에서 속성(ATTR) 리스트로 끌어다놓기(D&D)가 되지 않음 | 기능 | 접수 | |
| BR3 | 4p | 바인딩 팝업 | 컬럼에 걸러보기(필터) 기능이 누락됨 | 기능 | 접수 | |
| BR4 | 5p | 바인딩 팝업 | 단일 변수는 바인딩 할 수 없어야 하는데 바인딩됨(막아야 함) | 기능 | ✅ 완료 | 원본 거절 복원. 3중 검수 통과 + 실화면 테스트(SV1·SV2) 통과. 이력=바인딩팝업 00_히스토리.md(08-11) |
| BR5 | 6p | 바인딩 팝업 | UI 한 줄 고른 뒤 걸러보기 하면 그 UI의 속성만 걸러지지 않음 | 기능 | 접수 | |
| BR6 | 7p | 바인딩 팝업 | 동일속성 바인딩 화면 하단 표에 가상 스크롤 추가 필요(안 보이는 줄은 안 그리게) | UX | 접수 | 테스터가 문구 교체(이전: "결과 목록 표 안 보이는 영역 안 그리게") |
| BR7 | 8p | 바인딩 팝업 | 표의 컬럼 폭(width)을 바꿀 수 없음 | UX | 접수 | |
| BR8 | 8p | 바인딩 팝업 | 표 컬럼에 걸러보기(필터)가 없음 | 기능 | 접수 | |
| BR9 | 8p | 바인딩 팝업 | 우측 상단 "컬럼 최적화" 버튼이 동작하지 않음 | 기능 | 접수 | |
| BR10 | 9p | 바인딩 팝업 | 화면 커스터마이징 팝업에서 "바인딩 필드"와 "바인딩 추가 속성" 두 영역만 켜면 가운데 세로 구분바로 영역 크기 조절이 안 됨 | UX | 접수 | |
| BR11 | 10p | CTS 팝업 | 목록에서 스크롤 밖 안 보이는 영역까지 다 그려짐 → 안 보이는 줄은 안 그리게(가볍게) 필요 | UX | ✅ 완료 | 목록을 공통 가상 스크롤(보이는 구간만)로 전환, 선택은 행 기준(스크롤해도 유지). 코덱스·안티 검수 통과 + 실화면 테스트(BR11-1~5) 통과. 노션=수정완료. 이력=CTS팝업 00_히스토리.md(08-11) |
| BR12 | 11p | Application 생성 팝업 | WS10에서 앱 생성 시 데이터 세트 항목으로 만들 때, [생성] 버튼의 앱 설명 필수 점검을 없애고, 이어 뜨는 검색 필드 선택 팝업에서 조회한 테이블 설명(Description)을 앱 설명에 자동으로 채움(앱 설명을 직접 입력했으면 그 입력값으로 생성) | 기능 | ✅ 완료 | 데이터 세트 탭 앱 설명 필수 점검을 원본대로(일반 탭만) 복원. 자동 채움은 원래부터 있던 동작(수정 없음). 실화면 테스트(BR12-1~3) 통과. 노션=수정완료. 이력=생성옵션팝업 00_히스토리.md(08-11) |
| BR13 | 12p | UI 추가 팝업 | WS20의 UI 추가 팝업에서 결과 목록 UI를 끌 때, 커서가 팝업 안에 있는데도 팝업 뒤 화면이 놓기 대상으로 잡혀 그쪽에 놓여버림 | 기능 | ✅ 완료 | 끄는 동안 팝업 자신이 계속 앞 막기(예전의 클릭 통과 제거) → 가려진 뒤 화면 놓기·미리보기 파란 놓기표시 모두 차단, 팝업 밖 보이는 트리/미리보기만 놓기. 실화면 테스트(BR13-1~5) 통과. 이력=UI추가팝업 00_히스토리.md(08-11) |
| BR14 | — | UI 추가 팝업 | WS20 UI 추가 팝업에서 앱당 1개만 허용되는 UI(공통코드 UA039)를 중복 추가해도 막지 않음 — 추가 시 이미 있으면 차단하고 안내 메시지 표시해야 함(예: PressTrigger가 1·2·3으로 중복) | 기능 | ✅ 완료 | 원본 UA039 중복 점검 복원(추가 확정 경로). 3중 검수 통과 + 실화면 테스트(BR14-1·BR14-2) 통과. 이력=UI추가팝업 00_히스토리.md(08-11) |
| BR15 | — | Design Tree | WS20 Design Tree에서 UI를 끌 때 놓을 수 없는 줄이 흐리게(놓기 금지) 표시되는데, 트리를 아래로 끌어 자동 스크롤된 뒤 ①놓을 수 있는 줄에 놓아 선택 창이 뜨거나 ②트리 밖에 놓으면 그 흐린 표시가 안 풀리고 여러 줄이 계속 비활성처럼 남음(끌기종료 신호 미도달) | 기능 | ✅ 완료 | 끌기 시작 시 "끝나면 한 번만" 도는 종료 안전망 배선(가상 스크롤이 끌던 줄 지워 신호 유실 대응). 3중 검수 통과 + 실화면 테스트(BR15-1~3) 통과. 이력=디자인트리DnD 00_히스토리.md(08-11) |
| BR16 | — | 미리보기 | 미리보기에서 우클릭 메뉴로 삭제를 고르면 로딩표시(BUSY)가 안 풀림 | 기능 | 테스트대기 | 확인창 전에 켜던 다른창 잠금방송을 삭제확정(예) 후로 이동 + 오류 시 강제 해제. 코덱스·안티 검수 반영. 현황판=.works/미리보기삭제BUSY/00_현황판.md |

> 슬라이드 안 캡처 그림은 아직 안 읽음. 위 내용은 각 장의 글 설명만 뽑은 것.

---

## 🔁 동기화 방법 (내가 반복)

1. 원본 PPT를 다시 읽어 슬라이드별 지적 글을 뽑는다.
2. 위 목록과 대조 → **새로 늘어난 것만** 다음 코드번호(BR7…)로 맨 아래 추가.
3. 이미 있던 줄의 코드·순서·상태는 건드리지 않는다.
