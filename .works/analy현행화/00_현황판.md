# .analy 01~19 현행화 — 완료

> 2026-09-22. 장군님 지시 「문서 현행화」로 **01~19 전부 훑고 고쳤다.** 아래는 무엇을 왜 고쳤는지.
> 고치기 전 원본은 전부 `.analy/_<파일명>.analyupdbak` 로 남겼다(16개).

---

## 지금 할 것

| # | 봐 주실 것 | 왜 |
|---|---|---|
| **1** | `.analy/12_테마_컨버전_전략.md` **3장 테마 카탈로그** 를 한 번 봐 주십시오 | 문서엔 테마가 **5종**이라 적혀 있었는데 실제로는 **11종**이었습니다. 제가 화면 코드에서 읽어 표를 다시 만들었는데, **성격 설명이 맞는지**는 장군님이 보셔야 압니다 |
| **2** | 아직 UI5 로 남은 화면을 **언제 변환할지** | 팝업 몇 개(미리보기 관련·옵션 일부·값도움 OTR·숏컷 생성·UI5 CSS)와 floatingMenu 일부가 남아 있습니다. 문서엔 "남아 있다"고만 적고 개수는 안 적었습니다(적으면 곧 틀립니다) |

---

## 무엇을 고쳤나

### 가장 위험했던 것 — 읽는 AI 를 헛일 시키던 문장

| # | 어긋난 것 | 어떻게 고쳤나 |
|---|---|---|
| 1 | **01~08 의 「AI 컨버전 실행 사양」 절이 전부 "실제 구현은 추후 진행"** 이라고 적혀 있었다. 실제로는 그 화면들이 이미 다 만들어져 돌고 있다 | 절 머리에 **"이미 구현돼 돌고 있다 · 이건 다시 만들라는 지시서가 아니라 대조 기준"** 을 붙였다. 영역마다 실제 상태를 확인해 따로 적었다 |
| 2 | **11 플레이북이 "빈 폴더에서 새로 구축" 전제 + "가장 먼저 읽으세요"** | 맨 위에 **"지금은 구축 단계가 아니다"** + 무엇을 하려 하느냐에 따라 어디부터 읽을지 표를 넣었다. 전략·함정 본문은 지금도 유효하므로 **"깨뜨리지 말 것"** 으로 읽으라고 했다 |
| 3 | **10 이주 매니페스트가 "새 폴더로 옮겨 담아라"** | 맨 위에 **"이주는 끝났다 · 다시 옮기지 마라 · 목표는 소스 동기화가 아니라 기능이 같으면 성공"** 을 넣었다 |
| 4 | **12·13 이 "테마 토큰을 이제 만들어라"** | 둘 다 **"이미 만들어져 돌고 있다"** 로 바꾸고, 색 고칠 때 **어느 파일에서 고쳐야 먹는지**(늦게 로드되는 쪽) 를 짚어 줬다 |
| 5 | **14 가 부트스트랩 제거를 "앞으로 할 일"(S0만 완료)로** | 산출물이 전부 있는 것을 확인하고 **"S0~S5 전부 끝났다 · 이건 기록이다"** 로 바꿨다 |

### 사실이 틀렸던 것

| # | 어긋난 것 | 어떻게 고쳤나 |
|---|---|---|
| 6 | **죽은 링크 4곳** — WS20 권위 스펙이 **없는 파일 이름**을 가리키고 있었다 | 실제 파일로 바꿨다. 확인해 보니 두 링크가 가리키던 건 **한 문서**였고, 나머지 한 파일은 그 문서 18장을 그대로 뺀 **사본**이었다 |
| 7 | **00 문서 색인에 12~19 가 통째로 빠짐** | 8줄을 넣었다. 화면 작업에 꼭 필요한 두 문서는 ★ 로 표시하고, 아래에 **"화면 만들 땐 이 둘을 기능 문서와 항상 같이"** 안내를 붙였다 |
| 8 | **없는 폴더를 「1차 변환 시안」 출처로 지정**(7개 문서) | 전부 **현행 구현 경로**로 바꿨다. 그 시안은 다른 폴더에 남아 있지만 **지금 코드와 안 맞는다**(아래 근거) |
| 9 | **12 의 테마가 5종으로 적혀 있었다. 실제 11종** | 화면 코드에서 목록을 읽어 표를 다시 만들었다. **키 이름도 달랐다** — 문서는 짧은 이름, 실제는 앞에 `horizon_` 이 붙는다. 문서 안 CSS 예시도 전부 실제 키로 고쳤다 |
| 10 | **09 런북의 package.json 이 실제와 4곳 달랐다** | 실제 파일과 **한 글자도 안 틀리게** 맞췄다(버전 1곳, 패키지 3곳). 프로그램으로 다시 대조해 **차이 0** 확인 |
| 11 | **로그 표준이 `.analy` 에서 도달 불가** | 19(예외처리) 머리말에 링크를 넣었다. 오류를 코드로 드러내는 것과 그 자리에 로그를 남기는 것은 한 쌍이라고 적었다 |

### 안 고친 것과 그 이유

| 대상 | 왜 |
|---|---|
| **01~08 본문** | **원본(as-is) UI5 분석 기록**이다. 원본 폴더는 지금도 같은 버전 그대로라 내용이 낡지 않았다. 현재 구현 기준으로 덮으면 **원본 분석 자료가 사라진다** |
| 01~08 의 "분석 대상 버전" | 원본 폴더 실제 버전과 **일치**한다. 맞는 표기다 |
| 01~05 의 원본 경로 | 원본 폴더 기준 경로다. 작업 폴더에 없다고 틀린 게 아니다 |
| **15·16·17·18** | 이미 현행이다. 16 은 busy 공통 파일 분리(9월 15일)까지 반영돼 있고, 17 은 구현 완료 기록이 들어 있다 |
| 16 §2 의 빠진 번호 | 번호를 당기면 다른 문서·기억에 적힌 참조가 전부 깨진다. 그대로 두는 편이 안전하다 |

---

## 근거 — 어떻게 확인했나 (추측 0)

| 확인한 것 | 방법 | 결과 |
|---|---|---|
| 각 영역이 실제로 변환됐나 | 화면 파일에서 UI5 bootstrap 을 세어 봄 | 부팅·로그인·서버리스트·WS10·도움말·ADMIN = **0건**(완료) / 팝업·floatingMenu = 일부 남음 |
| WS20·WS30 구현 여부 | 해당 HTML5 파일 존재 확인 | 트리·속성·미리보기 호스트·드래그앤드롭·에디터 전부 **있음** |
| 테마 실제 목록 | 화면 코드의 테마 목록과 스킨 정의를 직접 읽음 | **11종**, 키 앞에 `horizon_` |
| 런북 package.json | 프로그램으로 실제 파일과 항목별 대조 | 고치기 전 4곳 차이 → 고친 뒤 **0** |
| 시안이 현재와 맞나 | 시안 ↔ 현재 파일 줄 단위 비교 | 서버리스트 화면 뼈대 **다른 줄 217개**, 동작 코드 **5.5배 커짐**, 로그인·WS10 은 **시안 파일명 자체가 없음** → **안 맞음** |
| 문서 링크 | 전수 검사 | **164개 중 깨진 것 0** |

### 내가 한 번 틀릴 뻔한 것

처음에 문서 속 경로를 **작업 폴더에서만** 찾아서 01~05 경로가 무더기로 "없음"으로 나왔습니다.
그건 **원본 분석 문서라 원본 폴더 기준 경로**였습니다. 원본 폴더까지 넣어 다시 검증해 바로잡았습니다.
그대로 보고드렸으면 **거짓 보고**였습니다.

---

## `.codex_tmp` 가 무엇이었나 (장군님 질문)

- 실물 위치 = `C:\Users\socce\Documents\Github\U4A_WS3.0.0_FORK\.codex_tmp`
- 2026-06-10~12 에 만든 **초벌 시안**. 로그인·서버리스트·WS10 의 HTML5 초안(마크업·CSS·JS), 부팅 진입 초안 하나, 변환 전략 메모 하나, 화면 캡처·확인 도구 둘 — 파일 9개
- 문서들이 상대경로로만 적어 둬서 작업 폴더 기준으로 읽으면 못 찾았다
- **현재 코드와 안 맞는다**(위 근거 표) → 문서에서 걷어내고 현행 구현 경로로 바꿨다. **폴더 자체는 건드리지 않았다**
