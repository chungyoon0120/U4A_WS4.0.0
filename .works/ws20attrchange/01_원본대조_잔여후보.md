# WS20 속성영역 — 남은 것

> 원본엔 있는데 HTML5에 없거나 약한 것. **지시가 있을 때만** 원본대로 붙인다(임의 반영 금지).
> 완료분은 [00_히스토리.md](00_히스토리.md).

| 코드 | 남은 것 | 화면 증상 | 원본 근거 | 우선 |
|---|---|---|---|---|
| RM8 | **이미지 압축 설정 창 미변환** | 파일 올리기 2종·목록 올리기의 `imageCompressSettings` 값 칸을 눌러도 켜기/끄기+품질 조절 창이 안 뜨고 오류만 남음 | `design/attributesArea/imageCompress.js` | **높음** |
| RM1 | 펼침목록에 잘못된 값 확정 시 **안내문구 자동표시** 안 함 | 빨간 테두리만 뜨고 문구는 다시 펼쳐야 보임 (글자 입력칸은 정상) | `uiAttributeArea.js` 1436~1439 | 높음 |
| RM2 | 값 범위 점검을 **조용히 건너뜀** | 점검 모듈이 안 뜨면 범위 밖 값도 빨간 표시 없이 통과 | `ws_html5_ws20_attr.js` 3625~3643 | 중 |
| RM6 | 실행 점검의 **조용한 삼킴** 잔존 | 점검 도중 오류가 나면 오류 없이 통과할 여지 | — | 중 |
| RM7 | 문법 점검 중 **로딩표시 멈춤** 위험 | 점검 중 오류 시 로딩표시가 안 풀릴 여지(원본도 같은 구조) | — | 중 |
| RM3 | 오류칸으로 데려갈 때 **위 요약영역 접기 + 줄 선택강조** 누락 | 스크롤만 하고 위 영역이 안 접힘 | `uiAttributeArea.js` 8337~8357 | 낮음 |
| RM4 | 등록 안 된 서버이벤트 오류 시 **그 줄로 이동·포커스** 안 함 | 빨간 테두리만, 화면 이동 없음 | `uiAttributeArea.js` 1126~1129 | 낮음 |
| RM9 | 값 변경 **앞단 조기 종료 2건** 위치가 원본과 다름 | **무증상**. ROOT 화면 문서 속성 미이식 + 끌어놓기 허용 처리가 값 반영 뒤로 이동 | `uiAttributeArea.js` 1795 · 1815 | 낮음 |
