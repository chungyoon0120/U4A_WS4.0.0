# 도움말(Tooltips) 팝업 — 현황판

## 지금 할 것 (미테스트)

### BR58 검수 반영분 재확인 (2026-08-31)

검수(코덱스·안티)에서 지적받은 **실패했을 때의 뒤처리**를 고쳤다. 고친 자리는 다음 넷이다.

- 도움말 창이 다 뜨기 전에 닫히거나 문서를 못 읽으면 **회색 대기 표시가 영영 안 풀리던 것** → 어떤 경우에도 한 번은 풀리도록
- 통합 도움말을 여는 처리가 실패했을 때도 **대기 표시가 남던 것** → 실패를 받아 풀도록
- 통합 도움말이 등록된 서버인데 여는 기능만 없을 때 **엉뚱하게 구버전 도움말이 뜨던 것** → 안 뜨고 오류로 끝나도록
- 도움말 파일 하나가 빠지면 **편집화면 전체가 안 뜨던 것** → 도움말만 안 되고 화면은 정상으로

**★먼저 앱을 껐다 켜야 반영된다.**

**구버전 서버인 척 하는 도구** (서버는 안 건드리고 화면 판정만 잠깐 바꿈)

```
node .works/auto-test/br58-help-fallback-test.js
```

```
node .works/auto-test/br58-old-server-mode.js on
```

```
node .works/auto-test/br58-old-server-mode.js off
```

| 코드 | 동작 | 조작·확인 포인트 | O/X(☐) |
| --- | --- | --- | --- |
| HLP11 | 고친 뒤에도 구버전 서버에서 도움말 3곳이 그대로 뜨는가 (회귀) | ① 앱을 껐다 켜고 WS20 편집화면(앱 하나 열기)까지 들어간다 ② 위 첫 번째 한 줄(`br58-help-fallback-test.js`)을 실행한다 ③ 결과표 세 줄이 모두 OK 인지 본다 ④ 여는 문서가 각각 designTooltip · attrTooltip · prevTooltip 인지 본다 | ☐ |
| HLP12 | 지금 쓰는 서버에서 도움말 3곳이 그대로 통합 도움말 창을 여는가 (회귀) | ① 왼쪽 UI 트리 영역 위쪽 줄의 물음표 버튼("도움말")을 누른다 ② 통합 도움말 창이 뜨는지 본다 ③ 오른쪽 속성 영역, 가운데 미리보기 영역에서도 같은 방법으로 확인한다 ④ 화면이 회색으로 덮인 채 굳어 있지 않은지 본다 | ☐ |
| HLP13 | 도움말 창을 여러 번 열고 닫아도 화면이 회색으로 굳지 않는가 | ① `on` 을 실행해 구버전 서버인 척 켠다 ② 오른쪽 속성 영역의 물음표 버튼("도움말")을 눌러 창이 뜨면 바로 닫는다 ③ ②를 다섯 번 반복한다 ④ 매번 편집화면이 정상으로 돌아오는지(회색으로 덮인 채 굳지 않는지) 본다 ⑤ 편집화면에서 아무 UI 나 클릭해 조작이 되는지 확인한다 | ☐ |
| HLP14 | 도움말 문서가 없을 때 안내가 뜨고 화면이 굳지 않는가 | ① 탐색기에서 `www\ws30\ws10_20\design\html\helper\KO\attrTooltip` 폴더 이름을 `attrTooltip_off` 로 바꾼다 ② 앱을 껐다 켜고 WS20 편집화면까지 들어간다 ③ `on` 을 실행해 구버전 서버인 척 켠다 ④ 오른쪽 속성 영역의 물음표 버튼("도움말")을 누른다 ⑤ **"툴팁 Attribute 영역의 HTML 파일이 존재하지 않습니다."** 안내가 뜨는지 본다 ⑥ 화면이 회색으로 덮인 채 굳지 않는지 본다 ⑦ 폴더 이름을 `attrTooltip` 으로 되돌리고 앱을 껐다 켠다 | ☐ |
| HLP15 | 폭이 좁아 물음표 버튼이 ⋯ 안으로 접혀도 그대로 되는가 (회귀) | ① `on` 이 켜진 상태에서 앱 창 폭을 좁게 줄인다 ② 왼쪽 UI 트리 영역 위쪽 줄 오른쪽 끝의 ⋯ 를 누른다 ③ 목록에서 "도움말" 을 고른다 ④ 도움말 창이 뜨는지 본다 | ☐ |
| HLP16 | 도움말 창을 닫은 뒤 편집화면 단축키가 정상인가 (회귀) | ① `on` 이 켜진 상태에서 도움말 창을 띄운 뒤 닫는다 ② WS20 편집화면을 클릭해 활성으로 만든다 ③ 되돌리기 단축키(Ctrl+Z)를 눌러 되돌리기가 동작하는지 확인한다 ④ 여섯 항목(HLP11~HLP16)이 다 끝나면 `off` 를 실행해 원래대로 되돌린다 | ☐ |

> 통과·완료된 항목은 [00_히스토리.md](00_히스토리.md) 참고.
