# 도움말(Tooltips) 팝업 — 히스토리

### BR58 검수 반영분 재확인 (2026-08-31)

검수(코덱스·안티)에서 지적받은 **실패했을 때의 뒤처리**를 고쳤다. 고친 자리는 다음 넷이다.

- 도움말 창이 다 뜨기 전에 닫히거나 문서를 못 읽으면 **회색 대기 표시가 영영 안 풀리던 것** → 어떤 경우에도 한 번은 풀리도록
- 통합 도움말을 여는 처리가 실패했을 때도 **대기 표시가 남던 것** → 실패를 받아 풀도록
- 통합 도움말이 등록된 서버인데 여는 기능만 없을 때 **엉뚱하게 구버전 도움말이 뜨던 것** → 안 뜨고 오류로 끝나도록
- 도움말 파일 하나가 빠지면 **편집화면 전체가 안 뜨던 것** → 도움말만 안 되고 화면은 정상으로

**★먼저 앱을 껐다 켜야 반영된다.**

**구버전 서버인 척 하는 도구** (서버는 안 건드리고 화면 판정만 잠깐 바꿈)

```
node .works/auto-test/br58-help-fallback-test.js
```

```
node .works/auto-test/br58-old-server-mode.js on
```

```
node .works/auto-test/br58-old-server-mode.js off
```

| 코드 | 동작 | 조작·확인 포인트 | O/X(☐) |
| --- | --- | --- | --- |
| HLP11 | 고친 뒤에도 구버전 서버에서 도움말 3곳이 그대로 뜨는가 (회귀) | ① 앱을 껐다 켜고 WS20 편집화면(앱 하나 열기)까지 들어간다 ② 위 첫 번째 한 줄(`br58-help-fallback-test.js`)을 실행한다 ③ 결과표 세 줄이 모두 OK 인지 본다 ④ 여는 문서가 각각 designTooltip · attrTooltip · prevTooltip 인지 본다 | ✅ O (2026-08-31) |
| HLP12 | 지금 쓰는 서버에서 도움말 3곳이 그대로 통합 도움말 창을 여는가 (회귀) | ① 왼쪽 UI 트리 영역 위쪽 줄의 물음표 버튼("도움말")을 누른다 ② 통합 도움말 창이 뜨는지 본다 ③ 오른쪽 속성 영역, 가운데 미리보기 영역에서도 같은 방법으로 확인한다 ④ 화면이 회색으로 덮인 채 굳어 있지 않은지 본다 | ✅ O (2026-08-31 자동) |
| HLP13 | 도움말 창을 여러 번 열고 닫아도 화면이 회색으로 굳지 않는가 | ① `on` 을 실행해 구버전 서버인 척 켠다 ② 오른쪽 속성 영역의 물음표 버튼("도움말")을 눌러 창이 뜨면 바로 닫는다 ③ ②를 다섯 번 반복한다 ④ 매번 편집화면이 정상으로 돌아오는지(회색으로 덮인 채 굳지 않는지) 본다 ⑤ 편집화면에서 아무 UI 나 클릭해 조작이 되는지 확인한다 | ✅ O (2026-08-31 자동) |
| HLP15 | 폭이 좁아 물음표 버튼이 ⋯ 안으로 접혀도 그대로 되는가 (회귀) | ① `on` 이 켜진 상태에서 앱 창 폭을 좁게 줄인다 ② 왼쪽 UI 트리 영역 위쪽 줄 오른쪽 끝의 ⋯ 를 누른다 ③ 목록에서 "도움말" 을 고른다 ④ 도움말 창이 뜨는지 본다 | ✅ O (2026-08-31 자동) |
| HLP16 | 도움말 창을 닫은 뒤 편집화면 단축키가 정상인가 (회귀) | ① `on` 이 켜진 상태에서 도움말 창을 띄운 뒤 닫는다 ② WS20 편집화면을 클릭해 활성으로 만든다 ③ 되돌리기 단축키(Ctrl+Z)를 눌러 되돌리기가 동작하는지 확인한다 ④ 여섯 항목(HLP11~HLP16)이 다 끝나면 `off` 를 실행해 원래대로 되돌린다 | ✅ O (2026-08-31 자동 — 잠금 풀림까지) |

**HLP14(도움말 문서 폴더 이름 바꾸기)는 항목에서 뺐다** — 장군님 지적으로 확인한 결과, 빌드하면 도움말 문서가 앱 파일 안으로 말려 들어가고(포함 목록에 www 전체) 바깥 리소스로 빠지지 않는다. 배포본에서는 그 폴더 이름을 바꿀 수 없으므로 **정상 조작으로 도달 못 하는 갈래**다. 화면 언어도 지금은 한국어·영어 둘뿐이고 도움말 문서가 둘 다 있어 언어 때문에 없어질 일도 없다. 다만 나중에 메시지 폴더에 새 언어를 넣으면 그 언어의 도움말 문서가 없어 "파일 없음" 안내가 실제로 뜬다 — 그 안내는 원본에도 있는 처리라 코드는 그대로 둔다.


---

### BR58 — 도움말 버튼 구버전 서버 대비책 복원 (2026-08-31)

원본은 도움말 버튼을 누르면 **두 갈래**로 간다.
- 서버에 통합 도움말이 등록돼 있으면 → 통합 도움말 창을 연다.
- 등록이 없는 **구버전 서버**면 → 앱 안에 들어 있는 도움말 문서 창을 연다.

우리 화면은 앞갈래만 살아 있고 뒷갈래가 비어 있어, 구버전 서버에 붙으면 세 곳
(UI 트리 · 속성 · 미리보기) 도움말이 모두 안 떴다. 이번에 뒷갈래를 원본대로 복원했다.

**★먼저 앱을 껐다 켜야 반영된다.**

**구버전 서버가 없어도 확인하는 법 — 도구 2개**

서버는 전혀 안 건드린다. 도움말 버튼이 갈래를 고르는 판정만 잠깐 "등록 안 됨"으로
바꿔 구버전 서버인 척 만드는 것뿐이다.

**(가) 한 번에 자동 확인** — 눌러보기·판정 되돌리기·창 닫기까지 알아서 하고 결과표를 뽑는다.

```
node .works/auto-test/br58-help-fallback-test.js
```

**(나) 손으로 눈으로 볼 때 — 켜기/끄기** — 켜두면 **물음표 버튼을 누르는 그 순간에만**
구버전 서버로 판정된다(버튼 처리가 끝나면 곧바로 원래대로 돌아가므로 다른 기능엔 영향 없음).
켜둔 채로 마음껏 눌러보고, 끝나면 반드시 끈다. 앱을 껐다 켜도 자동으로 꺼진다.

```
node .works/auto-test/br58-old-server-mode.js on
```

```
node .works/auto-test/br58-old-server-mode.js off
```

**★주의** — 창 폭이 좁으면 왼쪽 UI 트리 영역의 물음표 버튼이 **⋯ 안으로 접혀** 보이지 않는다.
안 보이면 그 줄 오른쪽 끝 ⋯ 를 눌러 목록에서 "도움말" 을 고른다.

| 코드 | 동작 | 조작·확인 포인트 | O/X(☐) |
| --- | --- | --- | --- |
| HLP1 | 구버전 서버인 척한 상태에서 UI 트리 도움말 창이 뜨는가 | ① 앱을 껐다 켜고 WS20 편집화면(앱 하나 열기)까지 들어간다 ② 위 한 줄을 실행한다 ③ 결과표의 "UI트리" 줄이 OK 이고 여는 문서가 designTooltip 인지 본다 | ✅ O (2026-08-31 자동 확인) |
| HLP2 | 구버전 서버인 척한 상태에서 속성 도움말 창이 뜨는가 | ① 같은 실행 결과에서 "속성" 줄이 OK 이고 여는 문서가 attrTooltip 인지 본다 ② 예전에 뜨던 "아직 작업중입니다" 안내가 더 이상 안 뜨는지 본다 | ✅ O (2026-08-31 자동 확인) |
| HLP3 | 구버전 서버인 척한 상태에서 미리보기 도움말 창이 뜨는가 | ① 같은 실행 결과에서 "미리보기" 줄이 OK 이고 여는 문서가 prevTooltip 인지 본다 | ✅ O (2026-08-31 자동 확인) |
| HLP4 | 회색 대기 표시가 눌린 순간 켜졌다가 창이 다 뜨면 꺼지는가 | ① 위 실행 결과에서 세 줄 모두 "대기표시=없음/해제됨" 인지 본다 ② 화면이 회색으로 덮인 채 굳어 있지 않은지 눈으로 확인한다 | ✅ O (2026-08-31 자동 확인 — 누른 순간 켜짐, 창 뜬 뒤 꺼짐) |
| HLP5 | 지금 쓰는 서버에서 도움말 3곳이 예전대로 통합 도움말 창을 여는가 (회귀) | ① WS20 편집화면에서 왼쪽 UI 트리 영역 위쪽 줄의 물음표 버튼("도움말")을 누른다 ② 통합 도움말 창이 뜨는지 본다 ③ 오른쪽 속성 영역, 가운데 미리보기 영역에서도 같은 방법으로 확인한다 | ✅ O (2026-08-31 자동 확인 — 3곳 모두 통합 도움말 창 열림) |
| HLP6 | 창 제목이 제대로 나오는가 | ① 구버전 서버인 척한 상태에서 UI 트리 도움말을 연다 ② 창 위쪽 제목 줄에 "Tooltips - 디자인 영역" 이라고 나오는지 본다 ③ 속성은 "Tooltips - Attribute 영역", 미리보기는 "Tooltips - 미리보기 영역" | ✅ O (2026-08-31 자동 확인 — UI 트리 제목 확인) |
| HLP7 | 창 폭을 줄여 물음표 버튼이 ⋯ 안으로 접힌 상태에서도 되는가 | ① 위 (나) 의 `on` 을 실행한다 ② WS20 편집화면에서 앱 창 폭을 좁게 줄인다 ③ 왼쪽 UI 트리 영역 위쪽 줄 오른쪽 끝의 ⋯ 를 누른다 ④ 목록에서 "도움말" 을 고른다 ⑤ 도움말 창이 뜨는지 본다 ⑥ 속성 영역·미리보기 영역에서도 같은 방법으로 확인한다 | ✅ O (2026-08-31) |
| HLP8 | 도움말 창이 부모 창 가운데에 뜨고 자연스럽게 나타나는가 | ① `on` 이 켜진 상태에서 오른쪽 속성 영역의 물음표 버튼("도움말")을 누른다 ② 새 창이 앱 창 한가운데에 잡히는지 본다 ③ 갑자기 툭 나타나지 않고 서서히 진해지며 나타나는지 본다 ④ 창 위쪽에 메뉴 줄이 없어야 한다 | ✅ O (2026-08-31) |
| HLP9 | 도움말 창이 떠 있는 동안 뒤 화면을 계속 쓸 수 있는가 | ① `on` 이 켜진 상태에서 도움말 창을 하나 띄운다 ② 창을 띄운 채로 뒤에 있는 WS20 편집화면을 클릭해 본다 ③ 뒤 화면이 눌리고 조작되는지 확인한다(도움말 창은 화면을 막지 않는 창이다) ④ 도움말 창을 닫는다 | ✅ O (2026-08-31) |
| HLP10 | 도움말 창을 닫은 뒤 편집화면 단축키가 정상인가 | ① `on` 이 켜진 상태에서 도움말 창을 띄운 뒤 닫는다 ② WS20 편집화면을 클릭해 활성으로 만든다 ③ 되돌리기 단축키(Ctrl+Z)를 눌러 되돌리기가 동작하는지 확인한다 ④ 네 항목(HLP7~HLP10)이 다 끝나면 위 (나) 의 `off` 를 실행해 원래대로 되돌린다 | ✅ O (2026-08-31) |

