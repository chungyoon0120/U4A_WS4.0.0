# 📚 대형 바인딩 팝업 — 작업 히스토리 (완료 이관)

> 현황판(`00_현황판.md`)에서 **완료·통과된 항목**을 이관해 보존한다. **최신이 맨 위.**
> 현황판은 "지금 할 것 + 다음 예정"만 유지. 상태 SSOT 는 현황판.

---

## 2026-08-13 · MSG — 바인드 버튼 "고른 줄 없음" 메시지 원본 복원 — 전 항목 통과

> 증상: 소형 인앱 팝업에서 아무 필드도 안 고르고 바인드 버튼을 누르면, 원본 문구("선택한 행이 존재하지 않습니다")가 아닌 엉뚱한 문구("바인딩에 대한 필드 정보를 선택합니다")가 떴다.
> 원인: 컨버전 때 원본과 다른 메시지 키로 잘못 연결됨.
> 수정: `www/ws30/ws10_20/js/fnBindPopupOpen.js` — 바인드 버튼 "고른 줄 없음" 분기를 원본 메시지 키로 바로잡음. `node --check` 통과.

| 코드 | 동작 | 조작·확인 포인트 | O/X |
|---|---|---|---|
| MSG1 | 고른 줄 없이 바인드 → 원본 문구 | 필드 안 고르고 바인드 버튼 클릭 → "선택한 행이 존재하지 않습니다" 표시 | ✅ O |

---

## 2026-08-13 · BC2 — 참조필드(Reference Field name) 칸 오류표시 원본 복원 — 전 항목 통과

> 증상: 소형 인앱 팝업 추가 속성의 **Reference Field name** 드롭다운 칸. Bind type만 고르고 이 칸을 비운 채 바인드하면, 원본은 **그 칸 자체에 빨간 표시 + 펼침목록 맨 위 오류 문구**가 뜨는데 HTML5는 그 칸엔 안 뜨고 가운데 안내만 떴다.
> 원인: 공통 드롭다운엔 오류표시 기능이 완비돼 있는데, 공통 입력칸 만들기의 드롭다운 갈래가 오류표시 함수를 빈 껍데기로 덮어 연결이 끊겨 있었다.
> 수정: `www/ws30/ws10_20/theme/u4a-ui.js` — 입력칸 만들기의 드롭다운 갈래가 공통 드롭다운의 실제 오류표시로 연결(생성 시 오류상태도 전달). `www/ws30/ws10_20/js/fnBindPopupOpen.js` — 값 채우면 즉시 빨강 해제 + 편집 가능한 칸에만 빨강 표시(비활성 칸은 원본처럼 표시 안 함) + 초기화 시 칸의 빨강도 함께 지움. `node --check` 통과.

| 코드 | 동작 | 조작·확인 포인트 | O/X |
|---|---|---|---|
| BC2-1 | 참조필드 비운 채 바인드 → 그 칸에 빨간 표시 | Bind type만 고르고 Reference Field name 비운 채 바인드 → 그 드롭다운 칸에 빨간 테두리 | ✅ O |
| BC2-2 | 그 칸 펼치면 맨 위에 오류 문구 | 빨개진 드롭다운 펼침 → 목록 맨 위에 오류 안내 문구 | ✅ O |
| BC2-3 | 참조필드 채우면 빨간 표시 사라짐(회귀) | 빨개진 칸에 값 고르고 다시 바인드 → 빨강 사라지고 정상 바인딩 | ✅ O |

---

## 2026-08-13 · FIL — 바인딩 트리 걸러보기(필터) 원본 복원 — 전 항목 통과

> 증상: 소형 인앱 팝업(사슬 아이콘 → "데이터 바인딩/바인딩 해제") 왼쪽 필드 목록에, 원본엔 있던 **오브젝트 이름·유형·설명 3개 컬럼 헤더로 걸러 찾기**가 HTML5엔 빠져 있었다.
> 수정: `www/ws30/ws10_20/js/fnBindPopupOpen.js` — 3개 컬럼 헤더에 공통 컬럼메뉴(걸러보기 전용, 정렬 없음) 배선. 필터 걸면 전체 펼침, 팝업 다시 열면 필터 해제. 걸린 컬럼 헤더엔 공통 깔때기 표시(다른 화면 필터와 동일).
> 후속 다듬기(장군님 지적 반영): ① 깔때기가 헤더 글자에 너무 붙어 보여 살짝 띄움(글자 옆 약간 간격). ② 걸러보기 입력칸이 커서 빠짐(탭·딴곳 클릭)만으로도 걸리던 것을 **엔터로만 걸리게** 통일 — 공통 컬럼메뉴 전체 적용(`www/ws30/ws10_20/theme/u4a-ui.js`). `node --check` 통과.

| 코드 | 동작 | 조작·확인 포인트 | O/X |
|---|---|---|---|
| FIL1 | 이름으로 걸러보기 + 깔때기 표시 | 오브젝트 이름 헤더 클릭 → 이름 일부 입력 Enter → 그 글자 든 줄만(부모 포함) 남고 전체 펼침 + 헤더에 깔때기 | ✅ O |
| FIL2 | 유형/설명으로도 걸러보기 | 유형·설명 헤더도 같은 방식으로 걸러짐 | ✅ O |
| FIL3 | 필터 초기화 | 필터 건 헤더 다시 클릭 → 필터 초기화 → 전체 복원, 깔때기 사라짐 | ✅ O |
| FIL4 | 팝업 다시 열면 필터 풀림(회귀) | 필터 건 채 닫고 재오픈 → 필터 없이 전체 목록 | ✅ O |

---

## 2026-08-13 · BR27 — 바인딩 필드 목록 가상 스크롤 미적용 — 전 항목 통과

> 증상: WS20 속성 영역 바인딩 팝업(사슬 아이콘 → "데이터 바인딩/바인딩 해제")의 왼쪽 필드 목록이 화면 밖 줄까지 전부 그려, 데이터 많으면 팝업 표시·스크롤이 느림.
> 원인: 원본 sap.ui.table.TreeTable 은 본래 보이는 행만 렌더(가상)인데, HTML5 변환 때 전 행을 그리도록 바뀜(원본 성질 유실).
> 수정: `www/ws30/ws10_20/js/fnBindPopupOpen.js` — 필드 목록에 "보이는 만큼만 그리기"(가상) 켬 + 재오픈 세션경계에서 이전 선택 초기화(유령 강조 방지) + 선택 불가 줄 클릭 시 선택 해제 통일. `www/ws30/ws10_20/theme/u4a-ui.js` — 공통 트리 "줄 선택"이 화면 밖 왕복 후에도 강조 유지(가상일 때만, 가드). `node --check` 통과.
> 검수: `.audit/br27/` — 코덱스·안티 P1(재오픈 유령 선택) 반영(03). 활성일 때만 선택하도록 좁힌 건 원본 스크롤(setFirstVisibleRow) 회귀라 되돌림(반론). 노션=수정완료.

| 코드 | 동작 | 조작·확인 포인트 | O/X |
|---|---|---|---|
| BR27-1 | 많은 필드 목록 정상 표시 + 스크롤 부드러움 | 팝업 왼쪽 필드 목록 위아래 끝까지 스크롤 → 줄 빠짐·겹침 없이 부드러움 | ✅ O |
| BR27-2 | 선택 줄 화면 밖 왕복 후 강조 유지 | 값 필드 클릭 강조 → 아래로 스크롤 후 복귀 → 여전히 강조 | ✅ O |
| BR27-3 | 다른 속성 재오픈 시 이전 선택 안 남음 | A에서 필드 클릭 후 닫고 B로 재오픈 → 강조 없음(유령 없음) | ✅ O |
| BR27-4 | 바인딩된 속성=자동선택+스크롤 / 미바인딩=백지 | 바인딩 속성 열면 그 줄 자동 강조+스크롤 / 미바인딩은 백지 | ✅ O |
| BR27-5 | 선택 불가 줄 클릭 시 이전 선택 해제 | 회색(선택 불가) 줄 클릭 → 이전 강조 사라짐 | ✅ O |
| BR27-6 | 펼침·접기·새로고침·바인드 정상(회귀) | 툴바 버튼(펼침/접기/새로고침)·바인드/해제 정상 | ✅ O |

---

## 2026-08-13 · BR28 — 이미 바인딩된 속성으로 다시 열 때 그 필드 위치로 목록 스크롤 미이동 — 전 항목 통과

> 증상: 소형 팝업(사슬 아이콘으로 여는 "데이터 바인딩/바인딩 해제") 왼쪽 필드 목록에서, 이미 바인딩된 속성으로 다시 열면 그 바인딩 필드가 목록에 있어도 그 위치로 목록이 굴러가지 않아 사용자가 직접 찾아야 했다(값·상세는 정상 복원). Property·Aggregation 동일.
> 원인: 원본(`design/js/callBindPopup.js:506~617` lf_setSelectTreeItem)은 재오픈 시 기존 바인딩 줄로 목록을 이동(setFirstVisibleRow)한다. HTML5는 그 자리에 공통 트리 "줄 선택"만 호출했는데, 공통 트리는 일반(비가상) 목록에선 선택만 하고 스크롤은 하지 않도록 되어 있고, 바인딩된 프로퍼티면 그 직후 추가속성 패널이 펼쳐지며 목록이 재배치돼 위치가 흐트러졌다.
> 수정: `www/ws30/ws10_20/js/fnBindPopupOpen.js` — 재오픈 시 바인딩 줄 선택 후, 화면 배치가 끝난 다음(rAF) 그 줄 위치로 목록을 한 번 더 확정 이동(scrollToKey, 가상·비가상 모두 대응). 바인딩 없는 속성으로 열면 목록을 맨 위로 되돌림(옛 위치 잔상 제거). 닫을 때 예약된 이동을 취소. 공통 자산 무수정(화면 스코프), `node --check` 통과.
> 검수: `.audit/br28/` — 코덱스·안티 취합. 과잉방어(N건 스크롤 무력화)=해소확인(스크롤이 활성조건 밖·안티 거짓반박), 미바인딩 옛 스크롤 잔존(P1)·닫힘 후 예약 정리=반영, 완전 비동기 직렬화(요청 세대표 P2)=반론(원본에 없는 방어·BR28 직접원인 아님, 별도 이슈 권고).

| 코드 | 동작 | 조작·확인 포인트 | O/X |
|---|---|---|---|
| BR28-1 | 바인딩된 프로퍼티 재오픈 → 그 필드로 목록 이동·강조 | 이미 바인딩된 프로퍼티(값 경로+사슬 켜짐, 필드가 목록 아래쪽)의 바인딩 아이콘 클릭 → 열자마자 그 필드로 목록 스크롤 + 파란 강조 | ✅ O |
| BR28-2 | 바인딩된 집합(Aggregation) 재오픈 → 그 항목으로 목록 이동 | 이미 테이블/구조로 바인딩된 items·content의 바인딩 아이콘 클릭 → 그 항목 위치로 목록 스크롤 | ✅ O |
| BR28-3 | 미바인딩 속성 재오픈 → 목록 맨 위(옛 위치 안 남음) | 바인딩된 속성으로 열어 목록 내려둔 뒤 닫고, 바인딩 안 된 다른 속성으로 다시 열면 목록이 맨 위에서 시작 | ✅ O |
| BR28-4 | 여러 번 반복해도 매번 정상(회귀) | 바인딩된 속성 열고 닫기 반복 → 매번 그 필드로 이동·강조, 오류·멈춤 없음 | ✅ O |

---

## 2026-08-13 · SEL — 바인딩 모달 트리 줄 선택 표시가 사라지던 문제 (재렌더 강조 소실) — 전 항목 통과

> 증상: 소형 팝업(사슬 아이콘으로 여는 "데이터 바인딩/바인딩 해제") 왼쪽 트리에서 ①줄을 처음 누르면 선택 표시(파란 강조)가 안 뜨고 오른쪽 추가속성 화면이 뜬 뒤 두 번째 눌러야 표시됨, ②이미 바인딩된 사슬로 열면 그 줄로 스크롤은 되나 선택 표시가 빠짐.
> 원인: 화면에 보이는 만큼만 그리는(가상) 공통 트리 부품이 다시 그릴 때 선택 표시를 실제 줄(.u4a-tree__row)이 아니라 바깥 껍데기(li)에 붙였다. 강조 색은 실제 줄에만 걸려 있어 다시 그리는 순간(패널 표시·트리 폭 변화) 강조가 사라짐. 직접 선택(findRow)은 실제 줄에 붙어 정상이라, 다시그리기에서만 소실.
> 수정: `www/ws30/ws10_20/theme/u4a-ui.js` — 가상 스크롤러 재렌더의 선택 표시 부착 대상을 옵션(selEl)으로 분리해, 트리는 안쪽 실제 줄에 붙이도록 함. 표(table)는 기본값(그대로)이라 무영향(하위호환). 부수효과로 다른 가상 트리(USP·MIME 등) 선택 표시도 안정화. 백업 `_u4a-ui.js.treeselbak`, `node --check` 통과.

| 코드 | 동작 | 조작·확인 포인트 | O/X |
|---|---|---|---|
| SEL1 | 줄 한 번 클릭에 바로 선택 표시 | ① 값칸 사슬 아이콘으로 소형 팝업 ② 왼쪽 트리 줄 한 번 클릭 → 곧바로 파란 선택 표시 + 오른쪽 추가속성 화면 | ✅ O |
| SEL2 | 이미 바인딩된 사슬로 열면 그 줄 선택 표시 | ① 바인딩된 값칸 사슬 아이콘 클릭 ② 그 모델 줄로 스크롤 + 파란 선택 표시 | ✅ O |
| SEL3 | 패널 뜬 뒤 다른 줄도 한 번에 선택(회귀) | ① 줄 선택해 패널 뜬 상태 ② 다른 줄 클릭 → 매번 한 번에 정상 표시 | ✅ O |

---

## 2026-08-13 · BR25 — 집합(items) 바인딩 해제 확인창 버튼이 안 눌리던 문제 (BUSY 미해제) — 전 항목 통과

> 증상: 부모 컨테이너의 items(집합)와 그 안 자식 입력칸 값이 둘 다 바인딩된 상태에서 소형 팝업의 빨간 "바인딩 해제"를 누르면 "…자식 오브젝트 프로퍼티에 지정한 바인딩이 초기화됩니다. 계속하시겠습니까?" 확인창은 뜨는데 화면잠금(로딩표시)이 안 꺼져 확인/취소 버튼이 안 눌림(버튼 눌러도 확인창 판이 클릭을 먹음).
> 원인: 이 처리는 시작할 때 화면을 잠그는데, 원본은 확인창을 띄운 직후 잠금을 꺼서 버튼이 눌리게 한다(uiAttributeArea.js:4753·4831). HTML5 포팅본은 이 "확인창 직후 잠금 끄기"가 빠져 있었다.
> 수정: `www/ws30/ws10_20/js/fnBindPopupOpen.js` 공유 확인창 함수 말미에 잠금 해제 1줄 추가(원본 1:1). "예"=다시 잠그고 해제 작업, "아니오"=잠금만 풀고 기존 유지. 백업 `_fnBindPopupOpen.js.br25bak`, `node --check` 통과. 코덱스·안티 검수 둘 다 통과(지적 0).

| 코드 | 동작 | 조작·확인 포인트 | O/X |
|---|---|---|---|
| BR25-1 | 확인창 취소 버튼이 눌린다(기존 유지) | items·자식 바인딩된 상태에서 "바인딩 해제" → 확인창 "취소" → 눌리고 닫힘, 화면 안 굳음, 바인딩 유지 | ✅ O |
| BR25-2 | 확인창 확인 버튼이 눌린다(해제 반영) | 확인창 "확인" → 눌리고 닫힘, items·자식 value 바인딩 둘 다 풀림, 자식 입력칸은 남음 | ✅ O |
| BR25-3 | 여러 번 눌러도 안 굳음(회귀) | 해제→취소 3~4번 반복 → 매번 버튼 눌리고 로딩표시 잔상 없음 | ✅ O |

### 노션=수정완료 (별도 처리)

---

## 2026-08-11 · 테스터 BR4 — 표·구조 밖 단독 변수 바인딩 차단 (원본 거절 복원) — 전 항목 통과

> 증상: 표(TABLE)·구조(Structure)에 안 든 최상위 단독 변수(예: 유형 STRING)가 바인딩돼버림.
> 원본은 첫 목록 만들 때 이런 변수엔 "표/구조에서 시작한 필드" 표식(KIND_PATH)을 안 붙여, 드롭 시 checkValidBind(bindData.js:269)가 메시지 115("Structure, Table로 시작하는 필드만 바인딩이 가능합니다")로 거절.
> HTML5는 루트부터 순회하며 이 변수에도 표식을 붙여 거절을 우회 → 바인딩됨. 수정: modelFieldArea.js `_applyBindEnable._walk` case "E"에서 최상위(sKindPath==="")면 `delete n.KIND_PATH; break;`로 표식 미부여 복원(딱 1곳). 원본 진입 필터 `ZLEVEL===2 && KIND!=="E"`(index.js:8131)와 동치.
> ★검수 3중 통과: 서브에이전트 원본대조(index.js·bindPopup-origin 직접 대조) + 코덱스 + 안티, 전부 결함 0.
> 참고: 처음에 넣었던 "선택 시 재계산에서도 초록 빼기" 2곳은 원본이 선택 시 단독 변수도 초록으로 칠하므로 과잉 → 되돌림. 놓을 곳이 속성 줄이면 사유 토스트가 뜨고, 폴더/빈 곳이면 조용히 무시(둘 다 원본 동작·안 걸림).

- **SV1** 단독 변수 끌어놓기 → 바인딩 안 걸림(속성 줄에 놓으면 사유 토스트) ✅ O(08-11).
- **SV2** 표/구조 안 필드는 그대로 정상 바인딩(회귀 없음) ✅ O(08-11).

---

## 2026-08-10~11 · 동일속성 적용 팝업(S5) 마무리 + 좌측 빈 문구 — 전 항목 통과

> 장군님 실물 테스트 통과분(그룹째 이관). 코드 근거 병기.

- **WP1** 화면 넘어가는 중(슬라이드 0.26s) "동일속성 적용 팝업 호출" 연타 시 가운데 백지 → ① busy off 를 designSwapToPage 완료 콜백(애니 종료 후)으로 이동(원본 afterNavigate) ② designSwapToPage/moveDesignPage 의 뒷정리 setTimeout 을 `oD._swapTimer` 로 저장+다음 동작 시작 시 clearTimeout(직렬화, 되살린 트리 재감춤 방지) ✅ O(08-10). ★이 방식=code.md "WP1 방식" 규칙으로 승격.
- **HV1** 초록 Accept("동일속성 바인딩") 버튼 마우스 올림 색을 파랑 강조 버튼과 동일한 **테마별 진한짝** 체계로 편입 — tokens `--c-success-strong`(#0d6b35 진한 초록) + theme-api SKIN_MAP `successStrong`(어두운 테마 #3aa564 밝은 초록) + `--state-success-strong`. 밝은 테마=진하게/어두운 테마=밝게(파랑과 방향 일치) ✅ O(08-11). .analy 16 §1.1 기록.
- **SS4** "Property 모두 바꾸기" 창 열 때 후보 표의 개별 체크 + 맨 위 전체선택 체크박스 함께 초기화(_clearSyncListSelection 에 _syncSetAllChkState 추가) ✅ O(08-11).
- **BK1** 동일속성 화면 "뒤로" 버튼 화살표를 WS20 본화면 뒤로가기와 동일(nav-back→chevron-left) ✅ O(08-11).
- **SS2** X/취소로 창 닫으면 뒤 화면 잠금 해제 + 창 제거 + WS20 로딩 해제(_closeSyncDialog) ✅ O(08-11).
- **SS3** "동일속성 적용 팝업 호출" 버튼=한 번 열면 계속 회색, 동일속성 화면 재진입 때만 재활성(원본 666) ✅ O(08-11).
- **SS5** "일괄적용" 성공으로 창 닫힐 때도 X/취소와 같은 닫기 절차(closeSyncBindDialog) 호출 → 뒤 화면 정상 복원(원본 UI5 close 의 beforeClose/afterClose 자동동작 HTML5 수동 재현) ✅ O(08-10).
- **EM1** 좌측 트리 데이터 없을 때 "데이터를 찾을 수 없습니다" 빈 문구가 host 가시폭 가운데 표시(가로 스크롤·밀림 없음, _fixEmpty) ✅ O(08-11).

---

## 2026-08-09~10 · 별창 실화면 테스트 통과 (DB / CB / SY)

> 장군님 실물 테스트 통과분. 상세 조작·확인 절차는 이관 전 현황판 참조(git 이력).

- **DB1~DB4** 추가속성 적용·드롭 시작 시 WS20 로딩(219/220/221) 표시·해제 ✅ O(08-09). DB1 중앙하단 "추가속성적용"/DB2 좌측필드·WS20캔버스 드롭/DB3 트리행 ✓/DB4 우측 "추가 속성 바인딩". 편집불가·팝업열림 시 좌→가운데 드롭 커서 🚫(08-10).
- **CB1 ①~⑥** 각 동작(바인딩해제/멀티바인딩/멀티해제/동일속성일괄적용/트리행✓/우측 추가속성바인딩) 성공 후 우상단 X 닫힘 ✅ O(08-09~10). 성공 시 X 죽던 결함 수정 검증.
- **CB2 ①~③** 취소 후 X 눌림 / 로딩 중 X 막힘(정상) / 화면 이동 중 X 정상 ✅ O(08-10).
- **SY1~SY4** 동일속성 화면 진입(슬라이드·버튼4개 잠금·WS20 로딩 225·X정상) / 상단 패널(4줄+추가속성 가로배치) / 하단 후보표(7컬럼+전체선택 체크박스) / 일괄적용(확인창·트리복귀·반영) ✅ O(08-09).
- **SS1 / SS1b** "Property 모두 바꾸기" 창(비모달) 모양(뒤 안 비침·패널/표 다 보임·크기키우면 표 커짐·제목더블클릭 중앙복귀·가운데 트리복귀) / 뒤 화면 잠금(트리 줄선택·링크·왼쪽필드 드롭커서🚫·위버튼3개·줄 체크박스·줄 아이콘·톱니바퀴·값 입력칸·바인딩 경로 링크 회색) ✅ O(08-10).
- **037 서버 오류 한글화** — 접속언어(KO) 대신 글로벌언어(EN)를 넘겨 서버 영문이 그대로 뜨던 버그 → frame.js relocalizeServerMsg 가 LANGU(접속언어) 우선 사용하도록 수정 ✅ O(08-10).

---

## 2026-08-06 · 동일속성 바인딩 화면 (S1~S4) 코드완료

> 진행 로그(코드완료 근거). 실화면 통합 테스트는 현황판 SY1~SY4 로 진행.

- **S4** 일괄적용(onSetSyncAttr/_setSyncAttr, designArea.js) — 커밋 `395f9c0`. 141 버튼 활성. 미선택 183 / 166+159 confirm(_confirmAdditApply=WS20 busy 왕복, 취소 busy off) / 150 소스필드없음=all-or-nothing 중단 / UIATY 1=attrSetBindProp, 3=unbind후 재바인딩 / ★MPROP 전파=이 경로만(§5.4) / 자기화면 갱신(refreshDesignTree+후보테이블) / 복원4종 / ★busy 자기해제 금지=WS20 왕복 BUSY_OFF(onMultiBind 동일) / oDialog close=S5 훅. 적용경로 하네스 17/17.
- **S3** 하단 후보 테이블(공통 makeDataTable 7컬럼: 190 OBJID/191 UIATT/178 Value/194 UILIB/195 UIOBK/196 POBID/197 PUIOK) + 체크박스 멀티선택 + 제목 061 + 툴바([141 일괄적용][161 컬럼최적화]) — 커밋 `7159bab`. 데이터=getSameAttrList(oSync.aList). getSyncSelectedRows 하네스 5/5. 161=table-layout:auto 스코프. 이미 바인딩된 후보도 포함.
- **S2** 상단 패널(공통 createPanel + 4줄: 190/191/192/193) + 추가속성(T_MPROP) 값 — 커밋 `f8e6f7b`. ★UA028(FLD02≠X, ITMCD 정렬) 인덱스 짝 = MPROP 짝 하네스 10/10. MPROP 없으면 미표시.
- **S1b 정정** 진입 시 WS20 busy 유지(장군님 실물 발견) — 커밋 `f20d916`. 진입 busy off 를 `setBusy(false,{ISBROAD:true})`(로컬만)로 → WS20 는 뒤로/적용완료 시 해제. 원본 sOption 없으면 WS20 미방송(index.js:3550). "코드 존재≠동작" 실사례.
- **S1a+S1b 실화면 = CDP 15/15 통과** — `.works/auto-test/sync-cdp-test.js`. 진입: 슬라이드 스왑+잠금5개 비활성+bSync=true+트리숨김+busy해제 / 뒤로: 페이지 teardown+잠금5개 복원+트리복귀. 스크린샷 2장 육안 확인.
- **S1b** 화면 껍데기(툴바 3버튼: 뒤로189/팝업140/도움말198) + 가운데 슬라이드 스왑(designSwapToPage/moveDesignPage, .analy 16 §9.2) + 진입/뒤로 부수효과 — 커밋 `703a2c5`. 하네스 21/21.
- **S1a** 잠금/복원 4종(setViewEditable=중앙하단적용+좌측갱신+좌측기어 / setAdditBindButtonEnable / setLayoutCustomizingEditable / bSyncEqualityScreenActive) — 커밋 `400ec67`. frame.js+data-bwp-lock 5버튼, 하네스 12/12.

---

## 2026-08-06 · ③ 트리 가상 스크롤 · ④ 드롭 회귀 — 전부 통과

- **VS1** UI 많은 앱 열기/스크롤/드래그 버벅임 없음 ✅
- **VS2** 행 선택 후 스크롤·스플리터 움직여도 파랑 강조 유지 ✅
- **VS3** 캔버스에서 UI 선택 → 화면 밖 행도 그 행으로 스크롤 이동 ✅
- **VS4** 오류 팝오버 "오류 위치 확인" → 그 오류행으로 스크롤 이동 ✅
- **VS5** 전체펼침·컬럼최적화·이름검색·드래그드롭·체크박스 정상 ✅ (부수: 공통 가상토글 자손 접힘 WS20식 통일 u4a-ui.js · 필터 후 드롭 복사본→원본 매핑 _srcNode)
- **VS6** 초기 컬럼폭 너무 좁지 않음 ✅
- **G4** 좌측 필드를 디자인 트리 속성에 정상 드롭 → 바인딩 ✅

---

## 2026-08-05 · ① 확인창 WS20 busy (5개) — 통과

- **BU1** 단건해제 · **MB1** 멀티바인딩 · **MU1** 멀티해제 · **AB1** 추가속성적용 · **MA1** 멀티추가속성 ✅
- 근거: 원본은 확인창 뜨는 즉시 WS20 busy 다이얼로그(진행 메시지) 유지→취소/성공 해제. HTML5 1:1 복원(BU1=222·MB1=224·MU1=223·AB1/MA1=219). MU1엔 `bindPossibleRecompute`(원본 2244) 누락 복원. 메모리 `ws20-busy-dialog-during-popup-confirm`.

---

## 2026-08-05 · ② 원본 누락 복원 + 오류표시 (커밋 8e0a0e2) — 통과

- **B4** 영역별 도움말 버튼이 서로 다른 문서를 열도록 ✅ — 공용 `onHelp`가 무조건 `000276` 방송하던 것 → `onHelp(sStartMenuId)` 파라미터화 + 호출부 4곳 자기 ID 전달(모델필드 276/디자인트리 275/추가속성 274/레이아웃 281/동기화 277). WLO UHAK901369 등록 시스템에서만 발현.
- **GAP2** 오류로 빨개진 행에 마우스 올리면 오류 툴팁 ✅ — 공통 data-tip-trunc-sel 조건, rowHook 서 해제.
- **GAP3** WS20 디자인 바뀌어 트리 재구성 시 열린 오류 팝오버 자동 닫힘 ✅
- **오류행 세로줄** 오류 행 선택 시 앞 세로줄도 빨강(오류>선택 우선) ✅
- ~~GAP1~~ ❌ 취소 (checkAdditData: 원본 코드엔 있으나 런타임에 이 케이스 안 막음=동작 파리티 아님)
- ~~F2/PC1~~ 팝오버 자동닫힘 UX 유지(원본 결함 보정)로 정리, 미적용. 원본 sap.m.MessagePopover 는 성공/입력 시 안 닫음(결함) → HTML5 는 바깥클릭 자동닫힘+_clearError 로 보정(장군님 결정: "자동으로 닫아주며 오류도 사라지는 게 낫다"). ★교훈: 취소/추가 판정 전 현행·원본 동작을 실물+소스로 먼저 확정.

---

## 2026-07-30 · 동작원리 관통조사 발견 4건 + 완료

| # | 내용 | 상태 |
|---|---|---|
| G-1 | 동일속성 바인딩 화면 미구현 | → S1~S4 코드완료(위 08-06) |
| G-2 | 드래그 시작 시 좌측 자동선택 누락 | ✅ 통과 07-30 (dragstart 말미 `oM.ctrl.select` = 원본 setSelectedIndex index.js:8544) |
| G-3 | 행 버튼 툴팁 키 132→139(원본값) | ✅ 통과 07-30 (원본 _txt6 designTree.js:3914) |
| G-4 | 드롭 실패 시 안내(원본 099~102) | ✅ 통과 08-06 |
| E-1 | 하단 추가속성 오류 라인 빨강 | ✅ 통과 07-30 (원본 oModel.refresh 재현, 재렌더 없이 클래스 토글) |
| E-2 | 가운데 트리 오류 행 배경 빨강(+hover+적용 시 해제) | ✅ 통과 07-30 (원본 _style 배경, _bind_error 일원화, resetErrorField 8668 이식) |

---

## 이전 완료 (감사·기능·경계·리소스)

- **감사·잔결함** — A0 전수감사 · N1 attrChgAttrVal · N2 대문자 · N3 초기펼침 · N4 필드선택 · N5 P05클로저 · N6 선택튕김 · D1 펼침전체 · D2 _style (전부 ✅ 07-23)
- **기능(R)** — R0 커밋 · R1 검증팝오버+에러배경 · R3 트리↔캔버스 선택동기 · R3b 접힌노드 펼침 · R4 이름필터 (전부 ✅). R2 = N/A(별창 구조상 재현불가, 무해).
- **WS20↔팝업 경계(F)** — F-1 반영 스텁→실제 방송 · F-2 삭제 2경로 · F-3 undo/redo · F-4 삭제 확인창 busy · F-5 속성변경 허브 전체반영 · F-6 삽입추가 · F-7 이동/위치 (전부 ✅ 07-29)
- **B-1/2** ✅ 진단 07-30 — 적용 중 busy 왕복이 이미 원본대로 완결(designBroadcastUpdate→updateBindPopupDesignData busy ON→WS20 왕복→BUSY_OFF, 자기해제 없음, rAF 1회). 코드 변경 불필요.
- **아이콘 I-1** ✅ — 큐브→실제 컨트롤 이미지(fnGetSapIconPath frame.js 이식).
- **RANGE_TAB L-1** ✅ — SelectOptions value에 range 필드만 드롭(chkRangeTable 이식).
- **R-3 하단 "추가속성 적용" 갱신** ✅ 07-30 — 드롭·버튼·행 체크 3경로 모두 갱신.
- **C-13~C-16 · DA(busy 페이드)** ✅ 07-30 — Conversion Routine 오류 즉시표시+재포커스 · 경로클릭/드래그→좌측자동선택 · 새로고침 하단청소 · 적용실패 행빨강 · 삼킴제거 회귀 · `.u4a-busy` 페이드 제거로 짧은 busy 즉시표시.
- **BZ1·DA1·DA2·LK1·LK2** ✅ + 07-30분.

---

## 종결된 판단

- **F1 이름검색 = 전역 유지(08-06)** — 원본 `onFilterDesignTree`(designTree.js:2020)는 UI 오브젝트 선택 스코프(그 UI 속성행 DATYP=02만, UI 미선택 시 무동작)+3컬럼(DESCR/UIATV/MPROP) 필터가 실재. 07-28 "원본에 스코프 없음"은 오독. **장군님 결정: 사용성 위해 전역 검색 유지**(원본과 의도적 상이, 코드 무변경, 주석 designArea.js:1291 정정).
- **R2** = N/A(별창 구조상 트리거 불가). 07-28 승인.
