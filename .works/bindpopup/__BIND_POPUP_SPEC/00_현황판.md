# 📊 대형 바인딩 팝업 — 현황판

> ## 👉 장군님은 아래 **"🧪 지금 할 것"** 만 보시면 됩니다.
> 각 줄 끝 **☐ 에 O/X** 만 주세요. 나머지(구현·근거·상태갱신·다른 md)는 전부 제가 관리합니다.
> · 최종 갱신 **2026-07-30** · 대상 `Popups/bindPopup/` (WS20 헤더 "바인딩 팝업" 별창)

---

## 🧪 지금 할 것 — 앱 재시작 후 확인 · **1건**

### G-4 · 드롭 실패 시 안내 메시지 (원본 4종 이식)
> 원본은 잘못된 드래그를 디자인 트리에 놓으면 안내(099~102)를 띄움. HTML5는 무음이던 것을 이식.

- 좌측 필드를 디자인 트리 속성에 **정상 드롭** → **평소대로 바인딩**되면 통과(회귀 확인) ☐
- (099~102는 비정상 드래그 방어 케이스라 일반 재현이 어려움 — 원본 1:1 코드근거로 통과)

### VS · 트리 가상 스크롤 전환 (성능 — 3만 UI 대비)
> 좌측·가운데 트리를 가상 스크롤로 전환(원본 UI5 TreeTable·WS20 트리와 동일). B1(선택강조)·B3(스크롤이동)·A3(옵저버) 완화 동반. 근거 = `참고자료/트리_가상화_계획_2026-07-30.md`.

**성능**
- UI 많은 앱에서 팝업 열기 / 좌·가운데 트리 스크롤 / 드래그드롭 — **버벅임 없이 부드러우면** 통과 ☐

**회귀(중요)**
- 좌측/가운데 행 선택 후 **스크롤하거나 스플리터를 움직여도 선택 강조(파란색)가 유지**되면 통과 ☐ ← B1(비가상엔 없던 리스크)
- 캔버스에서 UI 선택 → 팝업 트리가 **화면 밖 행이어도 그 행으로 스크롤 이동** ☐ ← B3
- 적용 실패 → 그 **오류행으로 스크롤 이동** ☐
- 전체펼침 / 컬럼최적화 / 이름검색 / 드래그드롭 / 체크박스 멀티선택 — 다 평소대로 ☐
- 초기 **컬럼폭이 너무 좁지 않은지**(가상은 보이는 행만 측정 — 이상하면 보고) ☐

> ✅ 07-30 통과분(C-13·C-14·C-15·C-16·G-2·G-3·E-1·E-2)은 완료로 이동.

---

## 🔎 이번 동작원리 관통조사 발견 4건 (07-30) — 처리 상태

| # | 내용 | 상태 |
|---|---|---|
| G-1 | **동일속성 바인딩 화면 미구현** (3경로 중 이것만 쓰기 불가) | → 아래 **S1~S5**(예정, 큼) |
| G-2 | 드래그 시작 시 좌측 자동선택 누락 | ✅ 통과 07-30 |
| G-3 | 행 버튼 툴팁 키 132→139(원본값) | ✅ 통과 07-30 |
| G-4 | 드롭 실패 시 안내 메시지(원본 099~102 이식, 무음→안내) | ✅ 코드완료 → 위 테스트 |
| E-1 | 하단 추가속성 오류 라인 빨강(원본 refresh→라인 빨강, 입력칸만 되던 것) | ✅ 통과 07-30 |
| E-2 | 가운데 트리 오류 행 **배경** 빨강(+hover 유지+하단 적용 시 해제) ★요청 | ✅ 통과 07-30 |

> 근거·상세 = `참고자료/동작원리_전체파악.md` (동결 스냅샷).

---

## ▶️ 다음 예정 (테스트 통과 후 내가 진행)

1. **S1a~S5** — "동일속성 바인딩" 화면 통째 신설 (=G-1, 마지막 큰 작업). 로드맵 Part 3 / 설계 `참고자료/S1_S5_설계.md`.

> 로드맵·실행계획 = `참고자료/잔여_로드맵_실행계획_2026-07-30.md`.
> B-1/2(busy 왕복) = 진단 결과 **이미 원본대로 완결**(코드근거 통과). V-1 = E-1/E-2로 원본 복원 확정, 종결.

---

## ✅ 완료 (참고용 — 접어둠)

<details>
<summary><b>완료 항목 전체 펼치기</b></summary>

**C-13** ✅ 통과 07-30 — Conversion Routine 잘못 입력 시 오류 메시지 즉시 표시 + 다른 데 클릭해도 그 칸으로 재포커스·메시지 유지.

**C-14 · C-15 · C-16 · G-2 · G-3** ✅ 전수 통과 07-30 — 경로클릭→좌측자동선택(C-15) · 드래그→좌측자동선택(G-2) · 새로고침 하단청소(C-14a) · 적용실패 행빨강(C-14b) · 삼킴제거 회귀(C-16) · ✓툴팁 139(G-3). 코드근거는 아래 "재시작 대기분 코드 근거" 참조.

**E-1** ✅ 통과 07-30 — 하단 Conversion Routine 오류 시 그 라인 전체 빨강(원본 `oModel.refresh()` 재현, 재렌더 없이 클래스 토글).

**E-2** ✅ 통과 07-30 — 가운데 디자인 트리 오류 행 **배경 전체 빨강**(원본 `_style` 배경 대응). 부수 픽스: 오류=`_bind_error` 일원화로 reset 해제(원본 `_style` 구조) · hover `!important`(원본 동일) · 하단 적용 시 `resetErrorField`(원본 8668 누락 이식).

**B-1/2** ✅ 진단 07-30 — 적용(드롭/멀티/단건/하단) 중 busy 왕복이 이미 원본대로 완결(`designBroadcastUpdate`→`updateBindPopupDesignData` busy ON→WS20 왕복→`BUSY_OFF`→OFF, 자기해제 없음, rAF 1회 합침). **코드 변경 불필요**.

**감사·잔결함** — A0 전수감사 · N1 attrChgAttrVal · N2 대문자 · N3 초기펼침 · N4 필드선택 · N5 P05클로저 · N6 선택튕김 · D1 펼침전체 · D2 _style (전부 ✅ 07-23)

**기능(R)** — R0 커밋 · R1 검증팝오버+에러배경 · R3 트리↔캔버스 선택동기 · R3b 접힌노드 펼침 · R4 이름필터 (전부 ✅). R2 = ⛔ N/A(별창 구조상 재현불가, 무해).

**WS20↔팝업 경계(F)** — 전부 ✅ 장군님 확인 07-29
- F-1 반영 스텁 → 실제 방송 연결 · F-2 삭제 2경로 반영 · F-3 undo/redo 반영 · F-4 삭제 확인창 busy · F-5 속성변경 허브 전체반영 · F-6 삽입추가 반영 · F-7 이동/위치이동 반영

**아이콘 I-1** ✅ — 큐브 → 실제 컨트롤 이미지. 원인=팝업에 `fnGetSapIconPath` 부재 → frame.js에 이식.

**RANGE_TAB L-1** ✅ — SelectOptions value에 range 필드만 드롭 허용. `chkRangeTable` 각인 이식.

**R-3 하단 "추가속성 적용" 영역 갱신** ✅ 장군님 확인 07-30 (C-12) — 드롭·"추가 속성 바인딩" 버튼·행 체크(✓) 3경로 모두 갱신.

</details>

<details>
<summary><b>재시작 대기분 코드 근거</b> — 원본 대응 라인</summary>

- **C-15**: 링크클릭 `onShowBindAdditInfo`(designArea.js:286)에서 `selectModelFieldByPath`(modelFieldArea.js:289)로 좌측 자동선택 → 참조필드 자동. 링크 경로에서만(부작용 차단).
- **C-16**: 삼킴 39곳 직접호출화 + 죽은 네임스페이스(`oAPP.attr.oAddit`) 3곳 `oAPP.fn`으로 복구.
- **C-14 화면갱신 4건 근거**: C14① `onMultiUnbind`에 `setRefFieldList()`(원본 2232) · C14② `_clearChecks`에 `setRefFieldList()`(1619) · C14③ `loadBindData` 진입 `clearSelectAdditBind`+`setAdditLayout("")`(8058·8062) · C14④ `onAdditionalBind` 진입 오류초기화+실패 시 행 각인(1679·1713).
- **G-2**: dragstart 말미 `oM.ctrl.select(oNode)` = 원본 `setSelectedIndex`(index.js:8544) 이식.
- **G-3**: 툴팁 `H.z("132")`→`H.z("139")` = 원본 `_txt6` 실제값(designTree.js:3914).

</details>

---

## 📎 참고 (근거·규칙 — 접어둠)

<details>
<summary><b>절대 불변 · 판단 이력 · 문서 지도</b></summary>

**🔒 절대 불변 (회귀=사양 위반)**
1. `attrSetBindProp` = 드롭·멀티·동일속성 공유 단일 진입점 (§12)
2. MPROP 전파 비대칭 — 일반/멀티 미전파 / 동일속성 일괄적용만 전파 (§5.4)
3. all-or-nothing — 부분 성공 없음 (§6)
4. PRCCD 철자 — 송신 `UPDATE-DESIGN-DATA`(하이픈)/수신 `UPDATE_DESIGN_DATA`(밑줄), 통일 금지 (§11-6)
5. busy = WS20 왕복 완료 후 해제, 자기해제 금지 (§3.11)
6. 오류 삼킴·회피 금지 (의도된 안전장치)

**📌 판단 이력**
- R2 = N/A(별창 구조상 트리거 불가). 07-28 승인.
- R4 = 전체 트리 이름검색만. "UI 안에서만 검색" 스코프는 원본에 없어 제거(장군님 지적 07-28).
- 공통 변경 승인: `openColumnMenu` filter/sort 독립 토글(하위호환).
- 참조필드(P05) 목록 기준 = **가운데 체크 + 좌측 선택 조합**(좌측만 아님). 통화·단위 필드 있고 한 구조로 모일 때만 채움 (07-30 확인).

**🗺️ 문서 (상태는 이 현황판만 소유)**
- `BINDING_POPUP_SPEC.md` = 원본 동작 근거(§조항)
- `참고자료/동작원리_전체파악.md` = **동작 원리 관통 파악**(4영역·데이터흐름·참조필드 규칙·발견 4건), 동결.
- `참고자료/` = 계획·분석·감사 스냅샷(동결, 상태 안 씀). S1~S5 설계는 `스텝계획서_바인딩팝업_잔여.md`.

**✅ 큰 그림(P0~P7)**: P0~P3 ✅ · P4·P5(=S1~S5) ⛔미착수 · P6 🔶거의완료(busy·오류표시 잔여) · P7 후순위.

</details>
