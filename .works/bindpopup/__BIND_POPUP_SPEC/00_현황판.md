# 📊 대형 바인딩 팝업 — 현황판

> ## 👉 장군님은 아래 **"🧪 지금 할 것"** 만 보시면 됩니다.
> 각 줄 끝 **☐ 에 O/X** 만 주세요. 나머지(구현·근거·상태갱신·다른 md)는 전부 제가 관리합니다.
> · 최종 갱신 **2026-08-05** · 대상 `Popups/bindPopup/` (WS20 헤더 "바인딩 팝업" 별창)

---

## 🧪 지금 할 것 — 재시작 후 한 번에 (코드로 O/X, 예: "BU1 O")

> 전부 코드완료·검사통과. **왜/근거는 맨 아래 [자세히]에 접어둠.**

### ✅ ① 확인창 WS20 busy (5개) — 통과 08-05
- [x] BU1 단건해제 · [x] MB1 멀티바인딩 · [x] MU1 멀티해제 · [x] AB1 추가속성적용 · [x] MA1 멀티추가속성

### ② 원본 누락 복원 + 오류표시 — ✅ 전부 통과 08-05, 커밋 8e0a0e2
- [x] **B4** — **영역별 도움말 버튼이 서로 다른 문서**를 열도록 (모델필드/디자인트리/추가속성/화면커스터마이징 창) ✅ 통과 08-05
- [x] **GAP2** — 오류로 빨개진 행에 **마우스 올리면 오류 메시지 툴팁** ✅ 통과 08-05 (원인=공통 data-tip-trunc-sel 조건, rowHook 서 해제)
- [x] **GAP3** — WS20에서 디자인 바뀌어 트리 재구성될 때 **열린 오류 팝오버 자동 닫힘** ✅ 통과 08-05
- [x] **오류행 세로줄** — 오류 행 선택 시 앞 세로줄도 **빨강**(오류>선택 우선) ✅ 통과 08-05
- ~~GAP1~~ — ❌ **취소 08-05** (checkAdditData: 원본 코드엔 있으나 런타임에 이 케이스 안 막음=동작 파리티 아님)
- ~~F2/PC1~~ — 팝오버 자동닫힘 UX 유지(원본 결함 보정)로 정리, 미적용

### ③ 트리 가상 스크롤 (6개)
- [x] **VS1** — UI 많은 앱에서 열기/스크롤/드래그 **버벅임 없음** ✅ 통과 08-05
- [x] **VS2** — 행 선택 후 스크롤·스플리터 움직여도 **파랑 강조 유지** ✅ 통과 08-05
- [x] **VS3** — 캔버스에서 UI 선택 → 화면 밖 행이어도 **그 행으로 스크롤 이동** ✅ 통과 08-05
- [x] **VS4** — 오류 팝오버 **"오류 위치 확인" 클릭** → 그 오류행으로 스크롤 이동 ✅ 통과 08-05
- [x] **VS5** — 전체펼침·컬럼최적화·이름검색·드래그드롭·체크박스 정상 ✅ 통과 08-06
  - 부수 수정: 공통 가상토글 자손 접힘(WS20식 표준 통일, u4a-ui.js) · 필터 후 드롭 복사본→원본 매핑(_srcNode)
- [x] **VS6** — 초기 컬럼폭 너무 좁지 않은지 ✅ 통과 08-06

### ④ 드롭 회귀 (1개)
- [x] **G4** — 좌측 필드를 디자인 트리 속성에 **정상 드롭** → 평소대로 바인딩 ✅ 통과 08-06

---

**✅ 이미 통과:** BZ1·DA1·DA2·LK1·LK2 + **확인창 busy 5개(BU1·MB1·MU1·AB1·MA1)** + 07-30분
**⏳ 유보:** AG1(표 재바인딩 확인창 busy — 드롭경로와 결합, 별도)

<details>
<summary>ⓘ 자세히 — 위 항목별 근거·구현·감사 (내가 관리, 안 보셔도 됨)</summary>

**확인창 WS20 busy(①):** 원본은 확인창 뜨는 즉시 WS20에도 busy 다이얼로그(진행 메시지) 띄우고 확인창 동안 유지→취소/성공 해제. HTML5가 다 빠뜨려 원본 메시지로 1:1 복원(BU1=222·MB1=224·MU1=223·AB1/MA1=219). MU1엔 `bindPossibleRecompute`(원본 2244) 누락도 복원. 근거=메모리 `ws20-busy-dialog-during-popup-confirm`.

**복원 로직(②):** 서브에이전트 4개 감사+직접 재검증. 쓰기코어·bindPossible·참조필드·CARDI·입력검증=완비(누락0). 진짜 누락 3건=GAP1 checkAdditData(146 차단) / GAP2 _error_tooltip / GAP3 closeMessagePopover. 감사판정: resetUiTableFilterSort=흡수 · setAdditBindButtonEnable=P4용 · 이름필터=원본은 UI스코프이나 장군님 결정으로 전역 유지(08-06 정정).

**08-05 원본 재감사 (서브에이전트 4개 병렬 + 직접 소스 재검증):** "코드존재≠동작" 규칙으로 실동작 확인된 것만 추림.
- **B4(코드완료)** — 도움말 문서 라우팅 붕괴. 원본은 영역별 문서(모델필드 `000276`/디자인트리 `000275`/추가속성 `000274`/레이아웃 `000281`/동기화 `000277`), 현행은 공용 `onHelp`가 무조건 `000276` 방송 → 모델필드만 맞고 나머지 엉뚱한 문서. 픽스=`onHelp(sStartMenuId)` 파라미터화(frame.js) + 호출부 4곳 자기 ID 전달. (WLO UHAK901369 등록된 시스템에서만 발현.)
- **팝오버 자동닫힘 = 원본과 의도적 상이(장군님 결정 08-05)** — 원본 sap.m.MessagePopover 는 적용 성공/입력 시 팝오버를 **안 닫는다**(성공경로 designTree.js:1806~1842·convChangeInput 둘 다 닫기 없음, 닫는 곳은 새오류/드래그시작/재구성/동기화 4곳뿐). → 원본은 "재적용 성공해도 오류목록 팝오버가 남는" 결함. HTML5 는 **바깥클릭 자동닫힘 + 닫힘 시 `_clearError`(빨강 해제)** 를 유지해 이 결함을 보정한다(showMessagePopover.js fnOnDocDown). 장군님 판단: "자동으로 닫아주면서 오류도 사라지는 이전이 낫다." F2(onAdditionalBind 진입 clearAdditErrorMark) 는 이 자동닫힘이 이미 처리하므로 **불필요 → 미적용**. ★왕복 교훈: F2/PC1 을 여러 번 넣었다 뺐다 함 — 근본은 "취소/추가 판정의 전제가 되는 현행·원본 동작을 실물+소스로 먼저 확정"하지 않은 것.
- **P6로 이미 추적중(신규 아님)** — B1(드롭 busy 220/221)·B2(중앙하단139 적용 busy 219)·B3(새로고침 busy 229): 각 진입 WS20 busy DIALOG. 현행이 코드에 P6 명시 주석. B2는 우측 멀티적용만 이식된 불일치.
- **판단 필요(미착수)** — BW-1 Expand/Collapse 후 autofit 재실행(원본 3999/4025 직후). "autofit 후속제거=수동폭 보존, 07-14 승인" 결정과 충돌.
- **F1 이름검색 = 전역 유지로 종결(08-06)** — 원본 `onFilterDesignTree`(designTree.js:2020, attachFilter 실배선)는 **UI 오브젝트 선택 스코프**(그 UI 속성행 DATYP=02만, UI 미선택 시 무동작)+3컬럼(DESCR/UIATV/MPROP) 필터가 실재·실행 확인. 07-28 "원본에 스코프 없음"은 오독. **장군님 결정: 사용성 위해 전역 검색 유지**(원본과 의도적 상이). 코드 무변경, 주석(designArea.js:1291)만 사실대로 정정. → 계획 C 종료.
- **오판 걸러냄** — checkAdditData(GAP1 재확인, 원본도 안 막음) · 참조필드 for-var(현행 IIFE 정상) · 컨버전루틴 1:1 · autofit후속제거(의도).

**busy 안 뜨던 근본원인(DA):** 공통 `.u4a-busy` opacity 페이드(~0.25s)가 수십ms 짧은 busy를 삼킴 → `#bwpBusy` 페이드 제거로 즉시표시. (위치 문제 아니었음.)

**VS 가상화:** 좌·가운데 트리 virtual 전환. B1(선택강조 rowHook 재적용)·B3(selectKey reveal)·A3(옵저버 rAF) 완화 동반.

**G4:** 드롭 실패 안내 099~102는 방어 케이스라 원본 1:1 코드근거로 통과.
</details>

---

## 🔎 이번 동작원리 관통조사 발견 4건 (07-30) — 처리 상태

| # | 내용 | 상태 |
|---|---|---|
| G-1 | **동일속성 바인딩 화면 미구현** (3경로 중 이것만 쓰기 불가) | → 아래 **S1~S5**(예정, 큼) |
| G-2 | 드래그 시작 시 좌측 자동선택 누락 | ✅ 통과 07-30 |
| G-3 | 행 버튼 툴팁 키 132→139(원본값) | ✅ 통과 07-30 |
| G-4 | 드롭 실패 시 안내 메시지(원본 099~102 이식, 무음→안내) | ✅ 코드완료 → 위 테스트 |
| E-1 | 하단 추가속성 오류 라인 빨강(원본 refresh→라인 빨강, 입력칸만 되던 것) | ✅ 통과 07-30 |
| E-2 | 가운데 트리 오류 행 **배경** 빨강(+hover 유지+하단 적용 시 해제) ★요청 | ✅ 통과 07-30 |

> 근거·상세 = `참고자료/동작원리_전체파악.md` (동결 스냅샷).

---

## ▶️ 다음 예정 (테스트 통과 후 내가 진행)

1. **busy 감사 (B)군** — 원본은 걸고 HTML5는 빠진 핸들러 진입 busy 이식(하나씩 원본 1:1 + 테스트 게이트):
   `onUnbind` · `onMultiBind` · `onMultiUnbind` · `onAdditionalBind` · `applyDesignAdditBind` · `onMultiAdditionalBind` · 좌필드드롭 `_onDesignDrop`.
   패턴 = 진입 ON → 가드/취소마다 OFF → 성공은 WS20 왕복 OFF 위임(§3.11 자기해제 금지). 단독실행(채널 없음) 시 busy 무발생도 점검.
2. **S1a~S5** — "동일속성 바인딩" 화면 통째 신설 (=G-1, 마지막 큰 작업). 실행계획 `참고자료/실행계획_S1_S5_2026-08-06.md`(원본 3중 재감사 반영) / 설계 `참고자료/S1_S5_설계.md`.
   - **S1a** 잠금/복원 4종(setViewEditable=중앙하단적용+좌측갱신+좌측기어 / setAdditBindButtonEnable / setLayoutCustomizingEditable / bSyncEqualityScreenActive) = **완료**(2026-08-06, frame.js+data-bwp-lock 5버튼, 하네스 12/12).
   - **S1b** 화면 껍데기(툴바 3버튼: 뒤로189/팝업140비활성/도움말198) + 가운데 슬라이드 스왑(designSwapToPage/moveDesignPage, .analy 16 §9.2) + 진입/뒤로 부수효과 배선 = **완료**(2026-08-06, syncBindScreen.js 신규, 하네스 21/21). 진입 busy(onSynchronizionBind on→렌더후 off).
   - **S1a+S1b 실화면 검증 = CDP 15/15 통과**(2026-08-06, .works/auto-test/sync-cdp-test.js). 진입: 슬라이드 스왑+잠금5개 비활성+bSync=true+트리숨김+busy해제 / 뒤로: 페이지 teardown+잠금5개 복원+트리복귀. 스크린샷 2장 육안 확인.
   - **[S1b 정정 2026-08-06] WS20 busy 유지** — 장군님 실물 발견: 동일속성 화면 동안 WS20(뒤 캔버스) busy 가 유지돼야 하는데 진입 직후 꺼졌다. 원인=진입 busy off 에 `setBusyWS20Interaction(false)`(인자 없어도 WS20 BUSY_OFF 방송)를 써 WS20 이 즉시 풀림. 원본은 sOption 없으면 WS20 미방송(index.js:3550). 관례(designArea.js:312)대로 `setBusy(false,{ISBROAD:true})`로 로컬만 해제 → WS20 는 뒤로/적용완료 시 해제. "코드 존재≠동작" 실사례.
   - **S2** 상단 패널(공통 createPanel + 4줄: 190 UI Object ID/191 Attribute ID/192 Attribute Type/193 Binding Field) + 추가속성(T_MPROP) 값 목록 = **코드완료·테스트대기**(2026-08-06, syncBindScreen.js _renderSyncPanel/_buildTMprop). ★UA028(FLD02≠X, ITMCD 정렬) 인덱스 짝 = MPROP 짝 하네스 10/10. MPROP 없으면 미표시.
   - **S3** 하단 후보 테이블(공통 makeDataTable 7컬럼: 190 OBJID/191 UIATT/178 Value/194 UILIB/195 UIOBK/196 POBID/197 PUIOK) + 체크박스 멀티선택 + 제목 061 + 툴바([141 일괄적용=S4까지 비활성][161 컬럼최적화]) = **코드완료·테스트대기**(2026-08-06). 데이터=getSameAttrList(oSync.aList). getSyncSelectedRows(원본 _getSelectedData) 하네스 5/5. 161=table-layout:auto 스코프(공통 fixed override). 이미 바인딩된 후보도 포함.

> **busy 감사 진행:** BZ1 초기로드 ✅ · (A)재구성 DA1·DA2 ✅ · (B)군 **BU1(onUnbind, 행 단건해제) 코드완료·테스트중** → BU2(onMultiUnbind 상단 멀티)~ 예정. 근거 = 원본↔HTML5 busy 전수매핑.
> 로드맵·실행계획 = `참고자료/잔여_로드맵_실행계획_2026-07-30.md`.
> B-1/2(busy 왕복) = 진단 결과 **이미 원본대로 완결**(코드근거 통과). V-1 = E-1/E-2로 원본 복원 확정, 종결.

---

## ✅ 완료 (참고용 — 접어둠)

<details>
<summary><b>완료 항목 전체 펼치기</b></summary>

**C-13** ✅ 통과 07-30 — Conversion Routine 잘못 입력 시 오류 메시지 즉시 표시 + 다른 데 클릭해도 그 칸으로 재포커스·메시지 유지.

**C-14 · C-15 · C-16 · G-2 · G-3** ✅ 전수 통과 07-30 — 경로클릭→좌측자동선택(C-15) · 드래그→좌측자동선택(G-2) · 새로고침 하단청소(C-14a) · 적용실패 행빨강(C-14b) · 삼킴제거 회귀(C-16) · ✓툴팁 139(G-3). 코드근거는 아래 "재시작 대기분 코드 근거" 참조.

**E-1** ✅ 통과 07-30 — 하단 Conversion Routine 오류 시 그 라인 전체 빨강(원본 `oModel.refresh()` 재현, 재렌더 없이 클래스 토글).

**E-2** ✅ 통과 07-30 — 가운데 디자인 트리 오류 행 **배경 전체 빨강**(원본 `_style` 배경 대응). 부수 픽스: 오류=`_bind_error` 일원화로 reset 해제(원본 `_style` 구조) · hover `!important`(원본 동일) · 하단 적용 시 `resetErrorField`(원본 8668 누락 이식).

**B-1/2** ✅ 진단 07-30 — 적용(드롭/멀티/단건/하단) 중 busy 왕복이 이미 원본대로 완결(`designBroadcastUpdate`→`updateBindPopupDesignData` busy ON→WS20 왕복→`BUSY_OFF`→OFF, 자기해제 없음, rAF 1회 합침). **코드 변경 불필요**.

**감사·잔결함** — A0 전수감사 · N1 attrChgAttrVal · N2 대문자 · N3 초기펼침 · N4 필드선택 · N5 P05클로저 · N6 선택튕김 · D1 펼침전체 · D2 _style (전부 ✅ 07-23)

**기능(R)** — R0 커밋 · R1 검증팝오버+에러배경 · R3 트리↔캔버스 선택동기 · R3b 접힌노드 펼침 · R4 이름필터 (전부 ✅). R2 = ⛔ N/A(별창 구조상 재현불가, 무해).

**WS20↔팝업 경계(F)** — 전부 ✅ 장군님 확인 07-29
- F-1 반영 스텁 → 실제 방송 연결 · F-2 삭제 2경로 반영 · F-3 undo/redo 반영 · F-4 삭제 확인창 busy · F-5 속성변경 허브 전체반영 · F-6 삽입추가 반영 · F-7 이동/위치이동 반영

**아이콘 I-1** ✅ — 큐브 → 실제 컨트롤 이미지. 원인=팝업에 `fnGetSapIconPath` 부재 → frame.js에 이식.

**RANGE_TAB L-1** ✅ — SelectOptions value에 range 필드만 드롭 허용. `chkRangeTable` 각인 이식.

**R-3 하단 "추가속성 적용" 영역 갱신** ✅ 장군님 확인 07-30 (C-12) — 드롭·"추가 속성 바인딩" 버튼·행 체크(✓) 3경로 모두 갱신.

</details>

<details>
<summary><b>재시작 대기분 코드 근거</b> — 원본 대응 라인</summary>

- **C-15**: 링크클릭 `onShowBindAdditInfo`(designArea.js:286)에서 `selectModelFieldByPath`(modelFieldArea.js:289)로 좌측 자동선택 → 참조필드 자동. 링크 경로에서만(부작용 차단).
- **C-16**: 삼킴 39곳 직접호출화 + 죽은 네임스페이스(`oAPP.attr.oAddit`) 3곳 `oAPP.fn`으로 복구.
- **C-14 화면갱신 4건 근거**: C14① `onMultiUnbind`에 `setRefFieldList()`(원본 2232) · C14② `_clearChecks`에 `setRefFieldList()`(1619) · C14③ `loadBindData` 진입 `clearSelectAdditBind`+`setAdditLayout("")`(8058·8062) · C14④ `onAdditionalBind` 진입 오류초기화+실패 시 행 각인(1679·1713).
- **G-2**: dragstart 말미 `oM.ctrl.select(oNode)` = 원본 `setSelectedIndex`(index.js:8544) 이식.
- **G-3**: 툴팁 `H.z("132")`→`H.z("139")` = 원본 `_txt6` 실제값(designTree.js:3914).

</details>

---

## 📎 참고 (근거·규칙 — 접어둠)

<details>
<summary><b>절대 불변 · 판단 이력 · 문서 지도</b></summary>

**🔒 절대 불변 (회귀=사양 위반)**
1. `attrSetBindProp` = 드롭·멀티·동일속성 공유 단일 진입점 (§12)
2. MPROP 전파 비대칭 — 일반/멀티 미전파 / 동일속성 일괄적용만 전파 (§5.4)
3. all-or-nothing — 부분 성공 없음 (§6)
4. PRCCD 철자 — 송신 `UPDATE-DESIGN-DATA`(하이픈)/수신 `UPDATE_DESIGN_DATA`(밑줄), 통일 금지 (§11-6)
5. busy = WS20 왕복 완료 후 해제, 자기해제 금지 (§3.11)
6. 오류 삼킴·회피 금지 (의도된 안전장치)

**📌 판단 이력**
- R2 = N/A(별창 구조상 트리거 불가). 07-28 승인.
- R4 이름검색 = UI 무관 전역(현행). ★원본(onFilterDesignTree, designTree.js:2020)은 실제로 "선택한 UI 오브젝트(DATYP=01)의 속성행만" 스코프 검색(UI 미선택 시 무동작)이었음 — 08-06 코드 재확인. 07-28 "원본에 스코프 없음"은 오독. **장군님 결정(08-06): 사용성 위해 원본과 다르게 전역 검색 유지**(코드 무변경).
- 공통 변경 승인: `openColumnMenu` filter/sort 독립 토글(하위호환).
- 참조필드(P05) 목록 기준 = **가운데 체크 + 좌측 선택 조합**(좌측만 아님). 통화·단위 필드 있고 한 구조로 모일 때만 채움 (07-30 확인).

**🗺️ 문서 (상태는 이 현황판만 소유)**
- `BINDING_POPUP_SPEC.md` = 원본 동작 근거(§조항)
- `참고자료/동작원리_전체파악.md` = **동작 원리 관통 파악**(4영역·데이터흐름·참조필드 규칙·발견 4건), 동결.
- `참고자료/` = 계획·분석·감사 스냅샷(동결, 상태 안 씀). S1~S5 설계는 `스텝계획서_바인딩팝업_잔여.md`.

**✅ 큰 그림(P0~P7)**: P0~P3 ✅ · P4·P5(=S1~S5) ⛔미착수 · P6 🔶거의완료(busy·오류표시 잔여) · P7 후순위.

</details>
