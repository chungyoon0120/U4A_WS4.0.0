# 📊 대형 바인딩 팝업 — 현황판

> ## 👉 장군님은 **"🧪 지금 할 것"** 만 보시면 됩니다. 각 줄 끝 **☐ 에 O/X**.
> · 최종 갱신 **2026-08-06** · 대상 `Popups/bindPopup/` (WS20 헤더 "바인딩 팝업" 별창)
> · **완료·통과 이력 = `00_히스토리.md`** (현황판은 현재 것만 유지)

---

## 🧪 지금 할 것 — 재시작 후 테스트

> **하는 법**: 앱 **재시작** 후 아래 순서대로. 결과는 코드로 회신 (예: `CB1 O`, `SY2 X`). X면 증상 한 줄.
> **순서**: 🟣 SS(동일속성 적용 팝업) → 🟢 EM(빈 문구). 통과분(DB1~4 · CB1 · CB2 · SY1~4 · 037)은 `00_히스토리.md`로 이관했다. ★새 미테스트는 항상 이 섹션 **맨 위**.

### 🟣 1순위 — S5 동일속성 적용 팝업 (비모달 창)  ·  08-10 레이아웃·뒤화면 잠금 보강

> 원본은 이 창을 **비모달**(뒤 조작 가능)로 띄우고, 열면 가운데는 트리로 돌아간다.

| 코드 | 확인 포인트 | O/X |
|---|---|:---:|
| **SS1** 창 모양 | "동일속성 적용 팝업 호출"(140) 누름 → ① 창이 **뒤 화면 위에** 뜨고 **뒤가 비치지 않음** · ② 위 패널(선택 정보)·아래 표(후보)가 **다 보임**(겹침·잘림 없음) · ③ 창을 **크게 키우면 아래 표 영역도 같이 커짐** · ④ 창을 옮긴 뒤 **제목 부분 더블클릭 → 화면 가운데로 복귀** · ⑤ 창이 뜨면서 가운데는 **다시 트리로 돌아옴** | ✅ **O**(08-10) |
| **SS1b** 뒤 화면 잠금 | 창이 떠 있는 동안 뒤 화면(왼쪽·가운데·오른쪽)이 다음처럼 **못 만지게** 되는지: ① 가운데 트리 **줄 선택·파란 바인딩 경로 링크** 클릭 안 됨 · ② **왼쪽 필드를 가운데로 끌면 커서 🚫**(안 놓아짐) · ③ 가운데 위 버튼 **동일속성 바인딩·멀티 바인딩·바인딩 해제** 회색 · ④ 가운데 각 줄 앞 **체크박스** 회색(못 누름) · ⑤ 줄 오른쪽 **아이콘(✓ 추가속성적용 · 🔗 해제)** 회색 · ⑥ 가운데 위 **톱니바퀴** 회색 · ⑦ 중앙 하단·오른쪽 **값 입력칸/드롭다운** 회색(못 씀) · ⑧ 가운데 **바인딩 경로 링크**(파란 글씨, 예 GS_TT1-F01·GT_TT2)가 **회색**으로 바뀌고 클릭 안 됨 | ☐ |
| **SS2** 닫기 | X 또는 취소로 닫으면 ① 창 사라짐 · ② **뒤 화면 잠금 풀림**(트리·버튼 다시 됨) · ③ 뒤 WS20 로딩 해제 | ☐ |
| **SS3** 140 버튼 | 한 번 열면 **계속 회색**, 동일속성 화면 **재진입 때만** 다시 켜짐 | ☐ |

---

### 🟢 좌측 빈 데이터 문구  ·  08-10

| 코드 | 확인 포인트 | O/X |
|---|---|:---:|
| **EM1** | 왼쪽 트리(오브젝트 이름)에 **데이터가 없을 때** "데이터를 찾을 수 없습니다"가 **가운데에 깔끔히** 뜸 — 오른쪽으로 밀리거나 잘리거나 가로 스크롤이 생기지 않음 | ☐ |

---

## ▶️ 다음 예정 (테스트 통과 후 내가 진행)

1. **판단 필요** — BW-1 Expand/Collapse 후 autofit 재실행(원본 3999/4025 직후)이 "autofit 후속제거=수동폭 보존(07-14 승인)"과 충돌. 착수 전 장군님 확인.
3. **보류** — onHelp 구버전 로컬 도움말(키 203). WLO 미등록 구버전 서버에서만 발현.

> ✅ **busy 감사 (B)군 = 코드 이식 완료(08-09)** — onUnbind/onMultiBind/onMultiUnbind/onAdditionalBind(확인창 5개, 08-05) + applyDesignAdditBind(219)·좌필드드롭(220/221, 08-09). 실화면 테스트 = 위 DB1/DB2 및 CB1.

> 로드맵 = `참고자료/잔여_로드맵_실행계획_2026-07-30.md`.

---

## 🔒 절대 불변 (회귀 = 사양 위반 — 작업 시 항상 준수)

1. `attrSetBindProp` = 드롭·멀티·동일속성 공유 단일 진입점 (§12)
2. MPROP 전파 비대칭 — 일반/멀티 미전파 / 동일속성 일괄적용만 전파 (§5.4)
3. all-or-nothing — 부분 성공 없음 (§6)
4. PRCCD 철자 — 송신 `UPDATE-DESIGN-DATA`(하이픈) / 수신 `UPDATE_DESIGN_DATA`(밑줄), 통일 금지 (§11-6)
5. busy = WS20 왕복 완료 후 해제, 자기해제 금지 (§3.11)
6. 오류 삼킴·회피 금지 (의도된 안전장치)

---

## 🗺️ 문서 지도

- `00_히스토리.md` = **완료·통과 이력**(현황판에서 이관).
- `BINDING_POPUP_SPEC.md` = 원본 동작 근거(§조항).
- `참고자료/실행계획_S1_S5_2026-08-06.md` = 동일속성(S1~S5) 실행계획(원본 3중 재감사 반영).
- `참고자료/동작원리_전체파악.md` = 동작 원리 관통 파악(동결).
- `참고자료/` = 계획·분석·감사 스냅샷(동결, 상태 안 씀).

> **큰 그림(P0~P7):** P0~P3 ✅ · P4·P5(=S1~S5) 🔶진행중(S1~S4 코드완료, S5 남음) · P6 🔶거의완료(busy 잔여) · P7 후순위.
