# 📊 대형 바인딩 팝업 — 현황판

> ## 👉 장군님은 **"🧪 지금 할 것"** 만 보시면 됩니다. 각 줄 끝 **☐ 에 O/X**.
> · 최종 갱신 **2026-08-06** · 대상 `Popups/bindPopup/` (WS20 헤더 "바인딩 팝업" 별창)
> · **완료·통과 이력 = `00_히스토리.md`** (현황판은 현재 것만 유지)

---

## 🧪 지금 할 것 — 재시작 후 테스트 (코드로 O/X, 예: "SY1 O")

> ★새 스텝 테스트 항목은 **이 섹션 맨 위에** 추가(최신이 위). 통과하면 히스토리로 이관·현황판에서 제거.

### 🔵 동일속성 바인딩 화면 (S1~S4) · 08-06 코드완료
- [ ] **SY1 진입** — 바인딩된 속성 **1개** 체크 → "동일속성 바인딩" → ① 가운데가 **슬라이드**로 동일속성 화면 전환 ② 좌측 새로고침·기어 / 우측 추가속성 바인딩·기어 / 가운데하단 적용 = **회색(잠김)** ③ 뒤 **WS20 캔버스 로딩(busy) 유지**
- [ ] **SY2 상단 패널** — 접이식 패널 4줄(UI Object ID / Attribute ID / Attribute Type / Binding Field). 추가속성 걸린 속성(예 `PAGE1` title)이면 그 아래 **이름:값**(Conversion Routine=ALPHA…), 없으면 4줄만. 화살표로 접힘/펼침
- [ ] **SY3 하단 후보 테이블** — 제목 "Target Replace Properties" + 7컬럼 + 후보 행들. **체크박스 여러 개 선택** + **컬럼최적화** 폭 맞춤
- [ ] **SY4 일괄적용** — 후보 여러 개 체크 → "일괄적용" → 확인창(N건) → 확인 → **실제 바인딩 + 트리 복귀 + 가운데/WS20 캔버스 반영**(추가속성 있으면 후보에도 따라감) → 로딩 **WS20 반영 후 꺼짐**. / **뒤로**=복귀+버튼 복원+busy 꺼짐 / **미선택**=차단 / **취소**=로딩 안 남음

---

## ▶️ 다음 예정 (테스트 통과 후 내가 진행)

1. **S5** — 비모달 플로팅 다이얼로그(140 "동일속성 적용 팝업 호출" 버튼 활성). 동일속성 화면 **마지막 스텝**. 설계 = `참고자료/실행계획_S1_S5_2026-08-06.md`.
2. **busy 감사 (B)군** — 원본은 걸고 HTML5는 빠진 핸들러 진입 busy 이식(하나씩 원본 1:1 + 테스트 게이트): `onUnbind`·`onMultiBind`·`onMultiUnbind`·`onAdditionalBind`·`applyDesignAdditBind`·`onMultiAdditionalBind`·좌필드드롭 `_onDesignDrop`. 패턴 = 진입 ON → 가드/취소마다 OFF → 성공은 WS20 왕복 OFF 위임(§3.11 자기해제 금지). **BU1(onUnbind) 코드완료·테스트중** → 이후 예정.
3. **판단 필요** — BW-1 Expand/Collapse 후 autofit 재실행(원본 3999/4025 직후)이 "autofit 후속제거=수동폭 보존(07-14 승인)"과 충돌. 착수 전 장군님 확인.

> 로드맵 = `참고자료/잔여_로드맵_실행계획_2026-07-30.md`.

---

## 🔒 절대 불변 (회귀 = 사양 위반 — 작업 시 항상 준수)

1. `attrSetBindProp` = 드롭·멀티·동일속성 공유 단일 진입점 (§12)
2. MPROP 전파 비대칭 — 일반/멀티 미전파 / 동일속성 일괄적용만 전파 (§5.4)
3. all-or-nothing — 부분 성공 없음 (§6)
4. PRCCD 철자 — 송신 `UPDATE-DESIGN-DATA`(하이픈) / 수신 `UPDATE_DESIGN_DATA`(밑줄), 통일 금지 (§11-6)
5. busy = WS20 왕복 완료 후 해제, 자기해제 금지 (§3.11)
6. 오류 삼킴·회피 금지 (의도된 안전장치)

---

## 🗺️ 문서 지도

- `00_히스토리.md` = **완료·통과 이력**(현황판에서 이관).
- `BINDING_POPUP_SPEC.md` = 원본 동작 근거(§조항).
- `참고자료/실행계획_S1_S5_2026-08-06.md` = 동일속성(S1~S5) 실행계획(원본 3중 재감사 반영).
- `참고자료/동작원리_전체파악.md` = 동작 원리 관통 파악(동결).
- `참고자료/` = 계획·분석·감사 스냅샷(동결, 상태 안 씀).

> **큰 그림(P0~P7):** P0~P3 ✅ · P4·P5(=S1~S5) 🔶진행중(S1~S4 코드완료, S5 남음) · P6 🔶거의완료(busy 잔여) · P7 후순위.
