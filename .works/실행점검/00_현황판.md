# 현황판 — Activate(활성화)·Save(저장) 오류 처리 (WS20 편집화면)

> 화면: WS20 편집화면 → 위쪽 도구모음
> 상태 SSOT. 통과하면 그 자리 O/X 칸에 **✅ O** 만 표시. 그룹 전부 통과 시 `00_히스토리.md`로 이관.
> 코드반영은 **앱을 껐다 켜야** 보인다(새로고침 아님).
> 버튼 이름은 화면 툴팁 그대로 쓴다 — **Save(저장) / Activate(활성화) / Syntax Check(구문 검사) / Undo(실행취소) / Redo(재실행)**.

---

## 지금 할 것

### 1. 테스트할 것 (미테스트)

> 미테스트 항목 없음. **CE 그룹(CE1~CE10) 전 항목 통과 → `00_히스토리.md` 이관 완료(2026-09-01).**
> BR55 그룹(BR55-1~6)도 앞서 이관 완료.

### 2. 정해 주실 것 — **없음**

지금 막혀서 기다리는 결정은 없다. 위 테스트만 봐 주시면 된다.
(테스트가 다 통과하면 `br55 수정 완료해라` 지시 → 노션 이슈 상태를 「수정완료」로 바꾸고 히스토리로 이관.)

### 3. 나중에 하면 좋은 것 (지시 주실 때만 — 지금 안 해도 지장 없음)

| 무엇 | 왜 |
|---|---|
| **다른 별도 창들에도** 「뜨다가 실패하면 잠금 풀기」 장치 넣기 | 이번엔 오류 목록 창 하나만 넣었다. 같은 방식으로 창을 여는 곳이 여럿이고 같은 문제가 있다 |
| 오류 코드 방식을 **다른 화면·버튼으로** 넓히기 | 지금은 Activate·Save·Syntax Check 3개 버튼만. 표준(`.analy/19` 부록 B)은 "만지는 김에 점진" |
| 원본 결함 3건을 노션 「UI5 레거시 잠재 버그」에 기록 | 우리 쪽은 고쳤지만 **진짜 원본엔 그대로 남아 있어** 기록 가치가 있다 |

### 4. 이미 정해진 것 (다시 안 물음)

| 무엇 | 결정 | 근거 |
|---|---|---|
| 크리티컬 오류 때 세션 창을 전부 닫을지 | **안 닫는다** | 표준 SSOT `.analy/19` 부록 A 에 확정 — "저장 안 한 앱이 사라지지 않도록 세션 강제 종료 콜백은 붙이지 않는다" |
| 크리티컬 오류를 어떻게 드러낼지 | WS20 같은 메인 창은 **던지기가 아니라 명시 호출**(콘솔 + 잠금 해제 + 화면 오류 팝업 + 중단) | 같은 문서 부록 A. **이 창에도 전역 오류 감시는 걸려 있다**(실측 2026-09-01) → 던지면 치명적 오류 창이 뜨고 **닫는 순간 앱이 종료**돼 저장 안 한 앱 설계가 사라진다. 그래서 앱은 살리고 그 동작만 막는다 |
| 작업 폴더 안 사본 폴더를 고쳐도 되는지 | **고쳐도 된다** | 장군님 지시 2026-09-01. 진짜 원본은 다른 깃, 통째 이동 안 함, 기능 같으면 성공 |
| 원본 결함 3건을 고칠지 | **고친다 → 완료** | 장군님 지시 2026-09-01 |

---

## 원본 결함 3건 상세 — ✅ **2026-09-01 전부 수정 완료**(장군님 지시)

> 셋 다 **원본(as-is) UI5 소스에 그대로 있던 결함**이다. 변환하며 생긴 게 아니다.
> 2026-09-01 룰 변경(작업 폴더 안은 사본 폴더 포함 수정 가능)에 따라 **그 자리에서 고쳤다.**
> 근거 위치는 원본 폴더 `C:\Users\socce\Documents\Github\U4A_WS3.0.0` 기준.

### 수정 결과 요약

| # | 고친 파일 | 새 오류 코드 | 고친 내용 |
|---|---|---|---|
| 원본-1 | `js/fnDialogPopupOpener.js` (오류 목록 창 열기) | `FDPO-001`~`004` | 창이 **뜨다가** 실패하는 4가지 경우(주소 읽기 실패 / 내용 못 읽음 / 화면 프로세스 죽음 / 다 뜨기 전 닫힘)에 **화면·자식 윈도우 잠금을 풀고** 코드가 찍힌 안내를 띄운 뒤 반쯤 뜬 창을 정리한다. 정상으로 다 뜨면 이 장치는 **동작하지 않는다**(원래대로 창이 보내는 신호가 잠금을 푼다) |
| 원본-2 | `design/js/checkAppData/designTreeData.js` (값 검사) | `DCHK-001`~`003` | 값 확인 없이 파고들던 자리를 정리. **코드마스터 자체가 없으면** 코드로 중단(001) / 코드마스터는 있고 해당 항목만 없으면 **점검 대상 없음으로 정상 진행** / **자식 목록이 없으면** 「자식 없음」으로 보고 정상 점검(오류로 수집) / **미리보기 정보·속성 목록이 없으면** 코드로 중단(002·003) — 검사 못 한 값이 저장되지 않게 |
| 원본-3 | 원본-2 와 같은 파일 | `DCHK-004` | 값 검사 스크립트가 터질 때 **조용히 삼키던 빈 catch** 를 코드로 드러냄 → 틀린 값이 「이상 없음」으로 통과되지 않는다 |

> 안쪽에서 붙인 코드(`DCHK-…`·`FDPO-…`)는 **화면 안내에 그대로 표시**된다
> (`ws_events.js` 공통 처리기가 바깥 코드로 덮어쓰지 않고 안쪽 코드를 우선한다 — 실행 검증 완료).
> 백업 = `_designTreeData.js.br55bak` · `_fnDialogPopupOpener.js.br55bak`.

### (아래는 고치기 전 조사 내용 — 근거 보관)

| # | 무슨 일이 벌어지나 | 왜 그런가 (원본 근거) | 지금 위험도 |
|---|---|---|---|
| **원본-1** | Activate·Syntax Check 를 눌러 오류가 나왔을 때, **오류 목록 창이 뜨다가 실패하면** 편집화면이 흐려진 채 영영 안 풀린다(아무 안내도 없음). 앱을 껐다 켜야 한다 | 오류 목록 창을 여는 함수는 들어가자마자 편집화면과 자식 윈도우를 **먼저 잠근다**. 잠금을 푸는 길은 **오직 하나** — 그 창 내용이 다 뜬 뒤 보내오는 신호뿐이다. 그런데 원본에는 **창 띄우기가 실패했을 때(파일 없음·화면 프로세스 죽음·다 뜨기 전에 닫힘) 잠금을 푸는 장치가 하나도 없다.** 창이 닫힐 때도 안 푼다. `fnDialogPopupOpener.js` 2968~3082행 실측 — 현행 HTML5 와 **실패 처리 코드 차이 0** | 중간. 정상 설치·정상 파일이면 안 터진다. 터지면 작업 중이던 앱 설계를 잃는다 |
| **원본-2** | 실행 전 오류 점검이 **도중에 터질 수 있는 자리가 7곳** 있다. 터지면(이제는) 오류 코드가 뜨고 저장이 막히지만, 애초에 점검 자체가 완주하지 못한다 | 점검하는 코드가 값이 없을 수도 있는 곳을 **확인 없이 그대로 파고든다**. ① 트리에는 UI 가 있는데 그 UI 의 미리보기 정보가 없을 때(3곳) ② 라이브러리에 「자식 필수」 코드가 한 건도 없을 때 ③ 자식 필수 UI 에 자식 목록 자체가 없을 때(2곳) ④ 점검 파일을 못 읽을 때 ⑤ 오류표시 지우기·새로고침 함수가 없을 때. 근거 = `design/js/checkAppData/designTreeData.js` 143·157·165·281·283행, `design/js/main.js` 1306행 `chkExcepionAttr`·1371행 `chkExcepUiTable`. 상세 = `.audit/br55/04_final.md` 1장 표 | ①은 **도달 가능**(미리보기가 안 떠 있거나 다시 뜨는 중일 때 UI 를 넣거나, 속성 한 번도 안 건드린 UI 를 지웠다가 UNDO 하면 미리보기 정보가 안 되살아남). ②는 서버 데이터라 미확인. ③은 재현 경로 못 찾음 |
| **원본-3** | 속성 값이 규칙에 안 맞는지 **검사하는 스크립트가 오류를 내면 그냥 통과**된다. 즉 잘못된 값인데 「이상 없음」으로 넘어갈 수 있다 | 그 검사 스크립트를 실행하는 자리가 **빈 catch** 로 감싸져 있어 오류를 조용히 삼킨다. 근거 = `design/js/checkAppData/designTreeData.js` 110~114행 | 낮음~중간. 검사 규칙 데이터가 깨졌을 때만. 다만 **틀린 값이 저장·활성화된다**는 점에서 성격이 나쁨 |

> ⤴ 이 조사를 바탕으로 3건 모두 2026-09-01 수정 완료(위 「수정 결과 요약」 표).
