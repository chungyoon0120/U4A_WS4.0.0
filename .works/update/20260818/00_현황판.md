# 2026-08-16 원본 갱신분 — 항목별 적용 여부

> 원본 문서 = `WS 3.0 변경사항.md`(35개 항목). **2026-09-01 우리 소스 실측** 기준.
> **판정 기준 = 화면에서 같은 동작이 나오는가**(소스가 같은지가 아님, 장군님 지시 2026-09-01).
> 상태 = ✅같게 동작함 / ❌아직 안 됨 / ⏸보류(그 기능 자체가 없음) / ➖우리와 무관

# ★ 지금 할 것

## 1) 아직 안 된 것 — 없음 ✅

## 2) 테스트 대기 — 2건

| 코드 | 무엇 | 테스트 |
|---|---|---|
| D03 | Ctrl 누른 채 복사할 때 못 쓰는 바인딩 경로 지우기 | 아래 체크리스트 5건 |
| D01 | 파일 올리기 UI의 그림 압축 설정을 바인딩 대상에서 빼기 | 그 속성이 **원작 개발자 개발 중**이라 지금 확인 불가 |

## 3) 원작 개발자께 확인 요청 — 1건

**증상** — 데이터 여러 건을 뿌리는 `sap.ui.table.Table` 안에서 `sap.ui.table.Column` 을
**Ctrl 누른 채 같은 Table 에 복사**하면, 복사본 안 UI 의 바인딩이 **풀린다**.

**원본도 똑같이 풀린다 — 우리 동작은 원본과 일치.** 원본 소스를 끝까지 따라가 확인함
(2026-08-31 최신본, `U4A_WS_DESIGN` 커밋 `323076d`).

| 순서 | 원본 파일·행 | 무슨 일이 일어나나 |
|---|---|---|
| 1 | `design/js/uiDesignArea.js` 4477~4520 | 형제 Column 옆에 놓으면 놓는 대상이 **그 Column 이 아니라 부모 Table** 로 바뀐다 |
| 2 | 같은 파일 3314~3320 | Ctrl 이면 복사 함수로 넘어간다 (놓는 곳 = Table, aggregation = `columns`) |
| 3 | 같은 파일 2894 | 끌어온 것이 Column 이고 `template` 안에 UI 가 있으면 판정 기준을 `template` 로 잡는다 |
| 4 | 같은 파일 2905 | **끌어온 쪽** 경로 = `template` 특례를 타서 **그 Table 의 `rows` 바인딩 경로**를 가져온다 |
| 5 | 같은 파일 2909 | **놓는 쪽** 경로 = `columns` 로 조회. `columns` 엔 바인딩이 없어 **그 Table 의 `rows` 는 건너뛰고 Table 의 부모로 올라간다** → 값 없음 |
| 6 | 같은 파일 2915·2921 | 두 경로가 다르므로 복사본의 바인딩을 지운다 |

근거 함수 = `design/js/uiAttributeArea.js` 7735~7791(경로 찾기), 7708~7728(바인딩된 aggregation 만 기록됨).

**언제 생겼나** — 2026-08-16 갱신(D03)으로 **새로 생긴 동작**이다. 그 전(6/26 본)엔 복사할 때
바인딩을 푸는 처리가 아예 없어 이 증상이 없었다.

**변경사항 문서에는 이 경우가 안 적혀 있다.** 문서에 적힌 건 "허용 여부를 판단하여 필요한 경로
정보를 초기화" 한 줄뿐이라, **같은 Table 안 복사도 푸는 게 의도인지 아닌지는 문서로 판단 불가.**

**여쭐 것** — 원작 개발자께 "같은 Table 안에서 Column 을 Ctrl 복사할 때도 바인딩을 푸는 것이
의도한 동작이 맞는지" 확인 요청. **우리 쪽은 원본과 같으므로 손대지 않는다.**

---

## 4) 정해 주실 것 — 1건

| 무엇 | 걸린 항목 |
|---|---|
| UI 추가 팝업 **안내 영역·예제 실행**을 만들지 | 10건이 여기 걸려 있음(D24·D26~D31·D33~D35) |

---

# 테스트 체크리스트

### D03 — Ctrl 누른 채 복사할 때 못 쓰는 바인딩 경로 지우기

> **원본 문서 근거** — `WS 3.0 변경사항.md` › 2. design › [Aggregation 및 Drag & Drop]
> "**Ctrl+D&D로 UI를 복사할 때 모델 바인딩 경로의 허용 여부를 판단하여 필요한 경로 정보를 초기화하도록 변경**"
>
> 고친 파일: `js/ws_html5_ws20_dnd.js` 1590~1614행(복사본 만든 뒤 · 되돌리기 저장 앞).
> 원본 근거 코드: `U4A_WS_DESIGN\design\js\uiDesignArea.js` 2873~2907행.

| 코드 | 동작 | 조작·확인 포인트 | O/X(☐) |
|---|---|---|---|
| D03-T1 | 다른 부모로 Ctrl 복사하면 복사본의 바인딩이 풀린다 | **[준비]** ① WS20 디자인 화면에서 `sap.ui.table.Table` 을 하나 넣는다 ② UI 트리에서 그 Table 을 고른 뒤, 가운데 속성 목록에서 `rows` 줄을 찾아 **줄 왼쪽 사슬 모양 아이콘**을 누른다 ③ 바인딩 팝업(제목 `Data Binding / Unbinding - Aggregation : rows`)에서 데이터 여러 건이 들어오는 항목을 고르고 **Confirm** ④ 그 Table 밑에 `sap.ui.table.Column` 을 하나 넣고, 그 Column 의 `template` 자리에 `sap.m.Text` 를 하나 넣는다 ⑤ UI 트리에서 그 Text 를 고른 뒤 속성 목록의 `text` 줄에서 **사슬 모양 아이콘**을 누른다 ⑥ 바인딩 팝업(제목 `Data Binding / Unbinding - Property : text`)에서 ③에서 고른 항목 **아래에 딸린 필드**를 고르고 **Confirm** ⑦ `text` 줄 아이콘이 **초록 체크 표시**로 바뀌었는지 확인(= 바인딩 걸림) ⑧ 그 Table **밖**에 `sap.m.Panel` 을 하나 넣는다 **[조작]** ⑨ UI 트리에서 ⑤의 Text 를 **Ctrl 키를 누른 채** 끌어서 ⑧의 Panel 위에 놓는다 ⑩ 놓을 자리 후보가 2개 이상이면 `Aggregation List - <UI 이름>` 창이 뜬다 → 자리를 고르고 **Confirm**(후보가 하나면 이 창은 안 뜨고 바로 넘어감) **[확인]** ⑪ 화면 아래에 **복사됐다는 안내**가 뜨고, UI 트리 선택이 **새로 생긴 복사본**으로 옮겨가는지 확인 ⑫ 그 복사본을 고른 채 속성 목록의 `text` 줄을 본다 → 아이콘이 **초록 체크가 아니라 다시 사슬 모양**이고 값 칸이 비어 있어야 한다(= 바인딩 풀림) ⑬ 원래 있던 Text(Column 안)를 다시 골라 `text` 줄이 **초록 체크 그대로**인지 확인(원본은 안 건드림) |✅ O |
| D03-T2 | 같은 Table 안에서 Ctrl 복사하면 바인딩이 풀린다(원본과 동일) | ① D03-T1 의 준비 ①~⑦ 상태를 만든다 ② UI 트리에서 그 **Column** 을 **Ctrl 키를 누른 채** 끌어서 **같은 Table** 위에 놓는다 ③ `Aggregation List` 창이 뜨면 `columns` 를 고르고 **Confirm** ④ 복사본 Column 안의 Text 를 골라 `text` 줄을 본다 → 초록 체크 표시가 사라지고 사슬 모양으로 되돌아온다 ⑤ ★주의 — 펼쳐진 형제 Column 줄 위에 그대로 놓으면 복사가 아니라 **자리바꿈**이 된다(원본도 동일) | ✅ O (원본과 같음 — 단, 위 3번 항목으로 원작 개발자 확인 필요) |
| D03-T3 | 하위 UI까지 같이 풀린다 | ① 바인딩된 UI 안에 자식 UI가 있고 그 자식에도 바인딩이 걸린 상태를 만든다 ② 그 UI를 다른 부모로 **Ctrl 복사**한다 ③ 복사된 **자식 UI의 바인딩도 풀려 있는지** 확인 | ☐ |
| D03-T4 | 되돌리기가 정상이다 | ① D03-T1 을 한 뒤 **되돌리기**를 누른다 ② 복사 전 상태로 돌아가는지 확인 ③ 다시 실행했을 때도 어긋나지 않는지 확인 | ☐ |
| D03-T5 | (회귀) Ctrl 없이 그냥 옮기기는 영향 없다 | ① 바인딩이 걸린 UI를 **Ctrl 없이** 다른 부모(예: Panel)로 끌어다 놓는다(복사 아님, 이동) ② 복사본이 생기지 않고 **그 UI 자체가 옮겨졌는지** 확인 ③ 옮긴 UI를 클릭해 **바인딩이 풀려 있는지** 확인(D04-T4 와 같은 결과여야 함) | ☐ |

---

# 전체 35개 항목 (실측)

| 코드 | 변경 내용 | 상태 | 어디서 도는가 · 확인 근거 |
|---|---|---|---|
| D01 | 파일 올리기 UI의 그림 압축 설정을 바인딩 대상에서 빼기 | ✅ | 우리 바인딩 팝업 목록에 3줄 넣음. 단 그 속성이 서버에 아직 없어 테스트 불가 |
| D02 | 넣을 자리를 못 만들면 오류 내용과 안내문을 같이 반환 | ✅ | 우리 끌어놓기 파일이 새 방식(결과+안내문+목록)으로 바뀌어 있음. 자리 고르기·마법사 호출부도 새 방식 |
| D03 | Ctrl 복사 때 못 쓰는 바인딩 경로 지우기 | ✅ | 2026-08-31 우리 끌어놓기 파일에 반영. **테스트 대기** |
| D04 | sap.ui.table.Table / TreeTable 의 columns 에 Drop 시 template 기준 판정 | ✅ | 2026-09-01 반영 · **테스트 전 항목 통과**(히스토리 이관) |
| D05 | 바인딩된 자리에 2건 이상 넣을 때 안내문 구체화 | ✅ | 우리 파일이 새 문구로 바꿔 놨고, 원본이 덮어쓰는 문제도 다시 설치로 처리돼 있음 |
| D06 | 서버 이벤트 목록에 지금 값이 없으면 항목으로 추가 | ✅ | 우리 속성 파일에 같은 처리 있음 |
| D07 | 다른 항목 골라도 이전 값이 남던 문제 | ✅ | 우리는 목록을 직접 만들어 그 증상 구조가 없음 |
| D08 | 여러 팝업 같이 썼을 때 변경이 속성에 안 나타남 | ✅ | 우리 속성 파일에 같은 처리 있음 |
| D09 | 이름 바꿀 때 UI 안 식별값도 같이 변경 | ✅ | 우리 속성 파일에 있음 |
| D10 | 동일 속성 맞추기 값이 기본값이어도 미리보기 반영 | ✅ | 우리 동일 속성 팝업 파일에 있음 |
| D11 | 꾸밈 이름 변경을 공통 처리로 | ✅ | 우리 꾸밈 이름 팝업이 공통 변경 경로 사용 |
| D12 | 꾸밈 이름·직접 스타일 바뀜 감지 | ✅ | 미리보기 화면 파일이 최신 원본 |
| D13 | 고른 요소뿐 아니라 UI 뿌리까지 감시 | ✅ | 〃 |
| D14 | 여백 등으로 위치 바뀌면 선택 표시 다시 계산 | ✅ | 〃 |
| D15 | 선택 바뀌거나 풀리면 감시 정리 | ✅ | 〃 |
| D16 | 고른 UI 위치로 화면 이동 | ✅ | 〃 (원본 방식으로 동작) |
| D17 | 보기→변경 전환 때 이전 수집 정보 비우기 | ✅ | 우리 자료 처리 파일에 있음(비우는 시점만 원본과 다름) |
| D18 | 서버 이벤트 만들기 팝업 첫 칸 커서를 열기 전으로 | ✅ | **손대지 않음(장군님 지시 2026-09-01).** 우리 팝업은 이미 첫 칸에 커서가 들어감 — 방식만 다름 |
| D19 | 데이터셋 필드 목록 팝업 — 현재 창의 것 사용 | ✅ | 우리 팝업엔 그 의존이 애초에 없음 |
| D20 | 데이터셋 필드 목록 팝업 — 서버 호출도 현재 창의 것 | ✅ | 〃 |
| D21 | 개인화 설정 팝업을 닫기·Esc로 닫으면 취소 처리 | ✅ | 우리 팝업에서 닫기·취소·Esc 모두 취소 처리 |
| D22 | 취소 시 미리보기 값 되돌리기 | ✅ | 우리 팝업 취소 처리에서 원래 값 복원 |
| D23 | UI 추가 팝업 스크립트 위치 변경 | ➖ | 우리는 자체 팝업을 씀 |
| D24 | UI 추가 팝업 공통 안내 문구 구성 | ⏸ | 문구 20개 중 8개가 **안내 영역·복사 단추 전용** — 그 기능이 우리에게 없음 |
| D25 | 검색어 지우면 결과 목록 정렬·컬럼 필터까지 초기화 | ✅ | 2026-09-01 반영 · **테스트 전 항목 통과**(히스토리 이관) |
| D26 | 결과 목록 안내 단추 열 최소 너비 | ⏸ | 안내 영역 없음 |
| D27 | 물려받은 자리 설명의 원래 묶음 이름 조회 | ⏸ | 자리 설명 말풍선 기능 없음 |
| D28 | 접속 언어의 자리 설명만 말풍선 자료로 | ⏸ | 〃 |
| D29 | 창 크기 바꾼 뒤 나누는 선 다시 계산 | ⏸ | 안내 영역 없음 |
| D30 | 줄이다 0이 된 안내 영역 되살리기 | ⏸ | 〃 |
| D31 | 안내 그리는 동안 공통 대기 표시 사용 | ⏸ | 〃 |
| D32 | 안내 단추 누른 줄을 표 선택 상태로 | ✅ | 원본도 그림 아이콘엔 이 처리를 안 붙임 → **지금 우리 동작이 원본과 같음** |
| D33 | 예제를 자식 아닌 별도 창으로 실행 | ⏸ | 예제 실행 기능 없음 + **원본끼리도 동작 안 함**(받는 쪽이 옛 상태) |
| D34 | 예제 검색값 이중 인코딩 풀기 | ⏸ | 〃 |
| D35 | 안내 영역 "그림 없음" 알림을 같은 문구로 | ⏸ | 안내 영역 없음 |

## 집계

| 상태 | 개수 |
|---|---|
| ✅ 같게 동작함 | 24 (그중 2건은 테스트 대기) |
| ❌ 아직 안 됨 | 0 |
| ⏸ 보류 | 10 |
| ➖ 무관 | 1 |
| 합계 | 35 |

## 참고 — 이번에 원본 사본을 최신으로 갈아 끼운 효과

미리보기 감시·화면 이동 5건(D12~D16)은 **미리보기 화면 파일을 최신 원본으로 갈아 끼우면서 함께 반영**됐다.
우리가 코드를 쓴 게 아니라, 앱이 그 파일을 그대로 읽어 돌기 때문이다.
