# 원본 이식 전체 현황판

# ★ 지금 테스트할 것 (앱 껐다 켠 뒤)

### 1. 꾸밈(CSS) 여러 UI에 적용 — 6건 ✅ 전 항목 통과 (2026-08-31)
> 2026-08-31 우리 파일로 옮긴 처리. 원본 사본은 원본과 같게 되돌림.

| 코드 | 동작 | 조작·확인 포인트 | O/X(☐) |
|---|---|---|---|
| A7-T1 | 꾸밈을 여러 UI에 미리 보기 하면 그 UI들에 적용된다 | ① WS20 디자인 화면에서 왼쪽 UI 트리의 UI 몇 개에 체크를 켠다 ② 위쪽 툴바에서 **UI5 미리 정의된 CSS** 팝업을 연다 ③ 꾸밈을 하나 고르고 **미리 보기**를 누른다 ④ 오른쪽 미리보기에서 그 UI들의 모양이 바뀌는지 확인 |✅ O |
| A7-T2 | 확정하면 꾸밈이 속성에 저장된다 | ① A7-T1 상태에서 **적용**을 누른다 ② 팝업이 닫히고 안내가 뜨는지 확인 ③ 그 UI를 골라 가운데 속성 목록의 **styleClass** 값에 고른 꾸밈이 들어갔는지 확인 |✅ O |
| A7-T3 | 꾸밈 이름이 바인딩된 UI가 섞이면 전부 중단되고 어떤 UI인지 알려 준다 | ① 어떤 UI의 **styleClass** 속성에 바인딩을 건다 ② 그 UI를 포함해 여러 UI에 체크를 켠다 ③ **UI5 미리 정의된 CSS** 팝업에서 꾸밈을 고르고 **적용** ④ 안내가 뜨고 **아무 UI에도 적용되지 않았는지** 확인 ⑤ 안내에 그 UI 이름이 나오는지 확인 |✅ O |
| A7-T4 | 꾸밈을 못 붙이는 UI가 섞이면 전부 중단되고 어떤 UI인지 알려 준다 | ① 꾸밈 이름 속성이 없는 UI를 포함해 여러 UI에 체크를 켠다 ② 같은 팝업에서 꾸밈을 고르고 **적용** ③ 안내가 뜨고 아무 UI에도 적용되지 않았는지 확인 |✅ O |
| A7-T5 | 아무 UI도 안 골랐을 때 안내가 뜬다 | ① 체크를 모두 끈다 ② 팝업에서 꾸밈을 고르고 **적용** ③ 안내가 뜨는지 확인 |✅ O |
| A7-T6 | (회귀) 미리보기가 평소대로 뜬다 | ① 앱을 껐다 켠다 ② 앱을 하나 열어 WS20 디자인 화면에 들어간다 ③ 오른쪽 미리보기가 정상 표시되는지 확인 |✅ O |

### 2. Ctrl 누른 채 복사할 때 못 쓰는 바인딩 경로 지우기 — 5건
> 2026-08-31 반영. 원본 8/16 변경분(D03).

| 코드 | 동작 | 조작·확인 포인트 | O/X(☐) |
|---|---|---|---|
| D03-T1 | 다른 부모로 Ctrl 복사하면 복사본의 바인딩이 풀린다 | ① 화면에 **Table**을 넣고 데이터 여러 건 바인딩을 건다 ② 그 표 안 **Column** 의 UI에 속성 바인딩을 하나 건다 ③ 화면 다른 곳에 **Panel** 같은 부모 UI를 하나 더 넣는다 ④ 왼쪽 UI 트리에서 그 UI를 **Ctrl 누른 채** Panel 위로 끌어다 놓는다 ⑤ 자리 고르는 목록이 뜨면 자리를 고른다 ⑥ 복사돼 들어간 UI를 클릭해 **속성 영역의 바인딩 표시가 풀려 있는지** 확인 | ☐ |
| D03-T2 | 같은 부모 안에서 복사하면 바인딩이 그대로다 | ① 같은 표 안에서 Column 하나를 **Ctrl 누른 채** 같은 표의 열 자리에 끌어다 놓는다 ② 복사된 UI의 **바인딩이 그대로 남아 있는지** 확인 | ☐ |
| D03-T3 | 하위 UI까지 같이 풀린다 | ① 바인딩된 UI 안에 자식 UI가 있고 그 자식에도 바인딩이 걸린 상태를 만든다 ② 그 UI를 다른 부모로 **Ctrl 복사**한다 ③ 복사된 **자식 UI의 바인딩도 풀려 있는지** 확인 | ☐ |
| D03-T4 | 되돌리기가 정상이다 | ① D03-T1 을 한 뒤 **되돌리기**를 누른다 ② 복사 전 상태로 돌아가는지 확인 ③ 다시 실행했을 때도 어긋나지 않는지 확인 | ☐ |
| D03-T5 | (회귀) 그냥 옮기기는 영향 없다 | ① Ctrl 없이 UI를 다른 부모로 끌어다 놓는다(이동) ② 평소대로 이동되고 바인딩 처리도 전과 같은지 확인 | ☐ |

### 3. 미리보기 오류 드러내기 — 회귀 2건
> 2026-08-31 우리 파일에서 덧댐.

| 코드 | 동작 | 조작·확인 포인트 | O/X(☐) |
|---|---|---|---|
| ES1 | 미리보기가 평소대로 뜬다 | ① 앱 실행 ② 앱 하나 열어 WS20 디자인 화면 진입 ③ 오른쪽 미리보기에 화면이 정상 표시되는지 확인 | ☐ |
| ES2 | 미리보기에서 UI 고르기·끌어놓기가 평소대로 된다 | ① 왼쪽 UI 트리에서 UI 하나 클릭 ② 미리보기에 선택 테두리 표시 확인 ③ 팔레트에서 UI 하나를 끌어다 놓아 정상 추가되는지 확인 | ☐ |

# ★ 정해 주셔야 다음 작업 가능

| 무엇을 정하나 | 걸린 작업 |
|---|---|
| **나머지 원본 사본 7개를 새 원본으로 갈아 끼울까요** | 아래 A1~A6·A8 (지금 바로 가능) |
| 넣을 수 없는 부모 자리 **새 안내 문구 등록**(문구 저장소에 없음) | 8/31 보완 E04 |
| 미리보기 화면 파일 교체 뒤 우리 "오류 드러내기"를 유지할지 | A4 · E05 |
| 넣을 자리 판정 교체 관련 4가지(AI 화면 만들기 파일 교체 / 방어 한 줄 / 마법사 막기 / 하나만 들어가는 자리) | 8/16 변경분 |
| 미리보기에서 UI 고를 때 화면 이동 방식(원본 방식 / 우리 방식) | 8/16 변경분 |
| UI 추가 팝업 안내 영역·예제를 만들지 | 8/16 변경분 12건 |

## 남은 개수 (2026-08-31 기준)

| 묶음 | 전체 | 끝남 | 남음 | 지금 바로 가능 |
|---|---|---|---|---|
| A. 원본 사본을 새 원본으로 맞추기 | 8 | 1 | **7** | 7 |
| B. 8/31 보완 반영 | 6 | 0 | **6** | 2 |
| C. 8/16 변경분 반영 | 35 | 18 | **17** | 2 |
| **합계** | **49** | **19** | **30** | **11** |

---
# A. 원본 사본을 새 원본으로 맞추기 — 8건

**왜 하나** — 원작 개발자가 2026-08-31에 원본을 고쳤다(원본 폴더 최신 저장분 확인함). 그런데 우리 작업 폴더 안 사본은 옛 상태라, 앱이 도는 코드에는 그 수정이 안 들어가 있다.
**어떻게 하나** — 우리가 고치는 게 아니라 **파일을 새 원본으로 갈아 끼운다.**
**무엇을 조심하나** — 사본 중 일부는 우리가 예전에 손댄 적이 있어, 그냥 덮으면 그 수정이 사라진다.

| 코드 | 파일(하는 일) | 새 원본에서 뭐가 바뀌었나 | 우리 사본 상태 | 조치 | 상태 |
|---|---|---|---|---|---|
| A1 | 값 검사(`design\js\checkAppData\designTreeData.js`) — 속성값을 바꿀 때 값이 올바른지 검사 | 화면 나눔 배치 UI의 칸 수·라벨 폭 값 검사가 새로 들어옴(89줄) | **우리가 손댄 적 없음**(옛 원본 그대로) | 그냥 교체 | ⬜ |
| A2 | 미리보기 화면 스타일(`design\preview\index.css`) | 323줄 정리·20줄 추가 | 손댄 적 없음 | 그냥 교체 | ⬜ |
| A3 | 미리보기 화면 틀(`design\preview\index.html`) | 4줄 삭제 | 손댄 적 없음 | 그냥 교체 | ⬜ |
| A4 | 미리보기 화면(`design\preview\index.js`) — 오른쪽 미리보기 안에서 UI를 그리고 선택 표시를 하는 화면 | 주석 4,400여 줄 정리 + **미리보기에서 난 오류를 앱 공통 오류 처리로 넘김** | 손댄 적 없음(8/19에 8/16 원본과 같게 맞춤) | 교체 + **우리가 8/31에 바깥에서 덧댄 "오류 드러내기"와 겹치는지 확인** | ⬜ |
| A5 | 속성 영역(`design\js\uiAttributeArea.js`) — 가운데 속성 목록을 만들고 값 변경을 처리 | ① 서버 이벤트 목록에 지금 값 넣기 ② UI 이름 바꿀 때 UI 안 식별값도 바꾸기 ③ 값이 틀려 되돌릴 때 드롭다운 글자도 되돌리기 | **옛 버전**(위 ①②③ 없음). 우리 추가 수정은 확인된 것 없음 | 교체 | ⬜ |
| A6 | 디자인 영역(`design\js\uiDesignArea.js`) — 왼쪽 UI 트리와 끌어놓기 처리 | ① 넣을 자리 판정 새 방식 ② 넣을 수 없는 부모 자리 안내를 확인창으로 ③ 트리에서 끌기 시작 전 바인딩 팝업 표시 | **옛 버전**. 우리 사본엔 트리 줄 선택 시 잠금 해제·대기표시 끄기 등 옛 코드가 남아 있음 | 교체 | ⬜ |
| A7 | 미리보기 영역(`design\js\uiPreviewArea.js`) — 미리보기 영역의 틀·도구줄 | **미리보기 선택 표시 켜기/끄기 스위치**가 새로 생김 | ✅ 원본과 같게 되돌림(우리 것 3가지는 우리 파일로 이관) | 끝남 | 🔧 |
| A8 | 도움말 공통(`design\util\helpViewer.js`) — 도움말 창·말풍선 공통 | 닫기·Esc를 취소로 넘기는 처리(8/16분) | **옛 버전**(우리가 손댄 적 없음) | 그냥 교체 | ⬜ |

> **참고** — A1(값 검사)과 A8(도움말 공통)은 우리 파일에서 직접 부르는 곳이 있다(속성 개인화 팝업·사전화면 팝업). A6·A7은 우리 화면이 대부분 다시 만들어 쓰지만 일부 함수는 원본 것을 그대로 부른다.

---
# B. 8/31 보완 반영 — 6건

원작 개발자가 2026-08-31에 손본 내용이다. **원본에는 이미 들어있고**, 우리 화면에 같은 동작이 나게 하는 것이 우리 일이다.

| 코드 | 무엇이 달라지나 (화면 기준) | 어디서 확인되나 | 우리가 할 일 | 막힌 것 | 상태 |
|---|---|---|---|---|---|
| E01 | 화면 나눔 배치 UI(폼을 여러 칸으로 나누는 UI)의 **칸 수·라벨 폭 속성**에 1~12를 벗어난 값·빈 값·소수를 넣으면 막고 안내가 뜬다(일부 속성은 -1도 허용) | WS20 가운데 속성 목록에서 그 속성 값을 바꿀 때 | A1 교체로 반영. 우리 코드 수정은 필요 없어 보임(확인 필요) | 검사 대상·방법이 **서버 공통코드 설정**에 들어 있어야 함 | ⬜ |
| E02 | 값이 틀려 **기본값으로 되돌릴 때**, 드롭다운에 **보이는 글자**도 같이 되돌아간다(지금은 옛 글자가 남음) | 속성 목록에서 드롭다운으로 고르는 속성에 잘못된 값이 들어갔을 때 | 우리 속성 파일에 한 줄 추가 | 없음 | ⬜ |
| E03 | 파일 올리기 UI(FileUploader·UploadCollection)의 **올릴 주소**에 기본 주소가 실제로 들어간다(지금은 엉뚱한 칸에 넣어 값이 안 들어감) | 그 UI를 화면에 넣었을 때 / 앱 번호가 바뀌었을 때 | 우리 속성 파일 3곳 수정 | 없음 | ⬜ |
| E04 | 넣을 수 없는 부모 자리에 UI를 넣으려 하면 **잠깐 뜨는 알림 대신 확인창**이 뜨고, 넣을 수 있는 자리 목록이 **줄바꿈으로** 나온다 | 끌어놓기 확정 / UI 추가 확정 / 자리 고르기 확정 | 우리 끌어놓기 파일 수정 | **그 안내 문구가 문구 저장소에 아직 없음**(확인함) | ⏸ |
| E05 | 미리보기 안에서 난 오류를 미리보기 자체 처리 대신 **앱 공통 오류 처리**로 넘긴다 | 미리보기에서 UI를 그리다 오류가 났을 때 | A4 교체로 반영 | ① 8/31에 우리가 덧댄 "오류 드러내기"와 역할 겹침 ② 공통 처리에는 **앱을 끄는 경로**가 있음 | ⏸ |
| E06 | 파일 올리기 UI의 **그림 압축 설정** 속성을 단추로 눌러 설정 창에서 사용 여부·품질을 바꾼다 | 속성 목록에서 그 속성 줄의 단추 | **설정 창(모듈)이 우리 쪽에 없음** → 이식 필요. 단추 표시·모듈 찾기 기준은 이미 이식돼 있음 | **그 속성이 서버 속성 목록에 아직 없음**(원작 개발자 개발 중) | ⏸ |

---
# C. 8/16 변경분 반영 — 35건

항목별 표는 [8/16 현황판](20260818/00_현황판.md).

| 상태 | 건수 | 무엇 |
|---|---|---|
| ✅ 이미 됨 | 16 | 서버 이벤트 목록 값 유지, 이름 바꿀 때 식별값, 동일 속성 기본값, 데이터셋 팝업 범위, 개인화 팝업 취소 복원, 미리보기 감시 5건 등 |
| 🔧 반영완료·테스트 대기 | 1 | **Ctrl 누른 채 복사할 때 못 쓰는 바인딩 경로 지우기** ← 지금 테스트 가능(5건) |
| 🔧 반영완료·테스트 보류 | 1 | 그림 압축 설정을 바인딩 대상에서 빼기(속성이 서버 개발 중) |
| ⬜ 지금 바로 가능 | 2 | ① 표 열 자리에 놓을 때 바인딩 유지 판정 ② UI 추가 팝업에서 검색어 지우면 정렬·열 거르기 초기화 |
| ⏸ 결정 대기 | 3 | 넣을 자리 판정 교체 / 2건 이상 안내문 / 미리보기 화면 이동 방식 |
| ⏸ 보류 | 12 | UI 추가 팝업의 안내 영역·예제 실행 관련 |
| ➖ 무관 | 1 | 팝업 스크립트 위치 변경 |

---
# 원작 개발자께 요청할 것

| 무엇 | 왜 |
|---|---|
| 넣을 수 없는 부모 자리 새 안내 문구 등록 | 코드가 그 문구를 쓰는데 저장소에 없음 |
| 그림 압축 설정 속성 개발 완료 시점 | 속성 목록에 아직 없어 화면에 안 뜸(단추·모듈 설정은 이미 등록됨) |
| 화면 나눔 배치 값 검사용 공통코드 설정이 우리 개발서버에도 들어갔는지 | 안 들어가면 검사가 안 돎 |
| UI 추가 팝업 새 폴더 전달 | 없으면 그 팝업이 안 열림 |
| 예제를 별도 창으로 여는 부분 수정 | 넘기는 값을 받는 쪽이 옛 상태라 원본끼리도 안 됨 |
| 화면 만들기 마법사의 "넣을 자리 없음" 막기 | 결과를 담는 칸과 읽는 칸이 어긋나 막기가 안 됨 |
# 진행 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-31 | 미리보기 영역 사본을 원본과 같게 되돌리고 우리 것 3가지를 우리 파일로 이관 — **테스트 6건 전 항목 통과 ✅** |
| 2026-08-31 | 원본 폴더가 8/31 갱신본인 것 확인. 우리 사본 8개가 뒤처진 것 확인 |
| 2026-08-31 | 8/31 보완 문서 분석 완료 |
| 2026-08-31 | Ctrl 복사 시 바인딩 경로 정리 반영(테스트 5건 등록) |
| 2026-08-31 | 그림 압축 설정 바인딩 제외 반영(속성 개발 중이라 테스트 보류) |
| 2026-08-31 | 미리보기 오류 드러내기 복원(원본 자리 밖 우리 파일에서 덧댐) |
