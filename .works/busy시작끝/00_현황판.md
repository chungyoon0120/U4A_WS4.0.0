# busy 시작·끝 — 기존 코드 전수 / 현황판

> 시작 2026-09-15 · 장군님 지시: **"모든 로직에서는 어떤 처리 과정 중에 조작을 하지 못하게 우선 막고 시작해"** → 기존 코드도 거슬러 적용, **목록부터**.
> 기준 = [.works/DEV_STANDARD_busy.md](../DEV_STANDARD_busy.md) · **아직 코드 한 줄도 안 고쳤다.**

---

## 0. 지금 할 것 — 테스트 (메인 창 busy 를 공통 파일 `theme/u4a-busy.*` 로 옮김, 2026-09-15)

> 서버 목록 창 busy(SB 그룹)는 2026-09-16 통과 → [00_히스토리.md](00_히스토리.md). UB2 는 「새창이 뜨는 순간부터 busy」로 이 대화창의 중점과 같은 항목이다.
> 무엇을 바꿨나 = [05_busy_theme이동_계획.md](05_busy_theme이동_계획.md) §2. **코드 반영은 앱을 완전히 껐다 켜야 된다.**
> 모양 CSS 는 규칙 20개를 순서·값 그대로 옮겼다(소스 대조 일치). 앱 화면으로 본 것은 아직 없다.

| 코드 | 동작 | 조작·확인 포인트 | O/X(☐) |
|---|---|---|---|
| UB1 | 로그인할 때 busy 가 뜨고, 메인이 나오면 사라진다 | ① 앱을 완전히 껐다 켠다 ② 서버 목록에서 서버를 골라 로그인 화면에서 로그인한다 ③ 로그인 처리 중 화면 가운데에 스피너 카드가 보이는지 본다 ④ 메인 화면이 나온 뒤 스피너가 사라지는지 본다 — 스피너가 안 보이거나 안 사라지면 ❌ | ☐ |
| UB2 | 로그인 후 새창이 **뜨는 순간부터** busy 가 돈다 | ① UB1 에서 로그인한 메인 화면에서 `Ctrl + N` 을 누른다(또는 `새 창` 버튼) ② 새창이 나타나는 순간부터 스피너가 있는지 본다 ③ 새창 첫 화면이 다 그려진 뒤 스피너가 사라지는지 본다 — 검은 화면만 보이거나 스피너가 안 사라지면 ❌ | ☐ |
| UB3 | busy 모양이 **옮기기 전과 같다** | ① UB1·UB2 에서 본 스피너 카드를 떠올린다 ② 카드 크기·가운데 위치·링 두 개(바깥 굵은 링, 안쪽 반대로 도는 링)·색이 전과 같은지 본다 ③ 카드 뒤 배경이 어두워지지 않고 그대로인지 본다 — 달라 보이면 어디가 다른지 적어 주신다 | ☐ |

---

## 1. 정해 주실 것 (앞서 여쭌 것 — 기존 코드 전수 판정)

| 정해 주실 것 | 선택지 |
|---|---|
| 목록(265개 파일) 중 **어디부터** 로직 단위로 판정·수정할지 | ⓐ **표시 A(서버 부르는데 busy 없음, 12개) → B(타이머로 끔, 11개) → C → D 순서** ⓑ 화면 영역별(예: 팝업 폴더부터) ⓒ 장군님이 파일을 지정 |
| 판정 결과를 **몇 개씩 끊어서** 보고할지 | ⓐ **파일 1개씩** — 한 파일의 시작점·끝점·중간 해제를 다 따라간 뒤 수정안까지 한 번에 ⓑ 표시 그룹(A·B…) 단위로 판정만 먼저 모아서 |

---

## 2. 목록

**[01_대상목록.md](01_대상목록.md)** — 파일 단위 1차. 맨 위 「이 목록의 한계」부터 읽는다.

| 표시 | 뜻 | 파일 수 |
|---|---|---|
| A | 서버를 부르는데 busy 켜기가 한 번도 없다 | 12 |
| B | 타이머 안에서 busy 를 끈다 | 11 |
| C | 처리 시작점이 있는데 busy 켜기가 한 번도 없다 | 95 |
| D | 끄기가 켜기의 2배를 넘는다 — 중간 해제 의심 | 56 |

---

## 3. 이미 이 기준으로 고친 것

| 흐름 | 날짜 | 결과 |
|---|---|---|
| 로그인 후 새창 뜨기 | 2026-09-15 | 시작점에서 켬 · 중간 해제 제거 · 끝점(성공·조기 return·실패)에서만 끔 — **장군님 확인 「새창 실행시 busy 잘된다」** |
