# 자동 시험 오류 기록에 「확인 및 조치 내용」 작성 (노션) + 동시 실행 중인 일괄 치환 사고 발견

## 요청
장군님: "노션에 확인 및 조치 내용 이라는 제목으로, 기존에 있는 내용과 구분 짓기위해 경계선 같은 표시나
칸을 띄어쓰기 등을 통해 원본 내용과 좀 구분되게 해서 원인 및 조치결과를 작성해"

## 변경 내용
### 1) 노션 (소스 변경 없음)
개인 노션(`me`) "cdp 자동화 > WS10 <--> WS20 테스트 > 오류 기록" 의 2026-09-11 09:50 건
(`PREV-ERR-00`) 페이지 본문 끝에 블록 28개 추가.
- 원본 내용과의 구분 = 빈 줄 → divider → 빈 줄 → 제목
- 제목 "확인 및 조치 내용" = **빨간색 굵게**(heading_2), 하위 소제목 5개도 빨간색 굵게
- 내용: 이 로그의 뜻 / 원인 3가지 / 조치 4가지 / 다음 회차 로그 읽는 법 / 고친 소스·검증
- `raw PATCH /v1/blocks/<id>/children` 으로 직접 넣었다 — 마크다운 append 로는 글자색을 못 준다.
  색은 rich_text 의 `annotations.color = "red"` 로만 지정된다.

### 2) 시간 못 박은 곳 전수조사 — 착수했다가 중단
장군님이 노션을 먼저 지시하셔서 멈췄다. 후보 164건까지 뽑아 둔 상태.
집계: 내 파일 48 · 팝업 92 · 사본(design) 13 · 자동 시험 8 · Electron 3.
검색 결과 파일 = 스크래치패드의 `scan_timehardcode.tsv` (세션 전용이라 곧 사라진다. 다시 돌리면 된다)
※ 처음에 이 파일을 **프로젝트 루트에 만들었다가** 스크래치패드로 옮겼다. 임시 파일을 repo 에 만들지 말 것.

## 변경 파일
- 추가: 없음
- 변경: 없음 (이 세션은 소스를 한 줄도 고치지 않았다)
- 삭제: 없음 (루트에 잘못 만든 `scan_timehardcode.tsv` 를 스크래치패드로 이동)

## 변경 이유
노션 기록은 장군님 지시. 소스 변경은 지시 없었음.

## 영향 범위
없음 (노션 페이지 1건에 본문 추가).

## 검증
- 노션 페이지를 다시 `get` 해서 추가된 내용 전체를 눈으로 확인 — 구분선·제목·5개 절 모두 정상.
- 소스 변경이 없으므로 `node --check` 대상 없음.

## 참고 사항 — ★★다음 agent 는 이것부터 볼 것

★★ **이 세션이 아닌 다른 세션이 지금 "로그 문구 영문화" 를 전 파일에 일괄 실행 중이다.**
stop hook 이 "117개 파일을 고쳤다"고 잡은 것은 전부 그쪽 작업이다. 이 세션은 노션만 만졌다.

그 일괄 치환에서 **실제 사고 2건을 발견했다 (아직 안 고침 — 지시 대기)**:

1. **단어 안쪽까지 치환됐다** — 통째 문자열 바꾸기를 무차별로 돌린 흔적
   - `www/ServerList_v2/ServerList.js:679` → `"레지스tree save failed"` (원래 = 레지스트리)
   - `www/ws30/ws10_20/Popups/bindPopup/designArea/designArea.js:1068` → `"디자인tree drop"`
2. **반쪽만 번역된 로그가 대량으로 남았다** — 한글 + 영어가 한 줄에 섞였다. 표본:
   - `console.log("**메모리 info: ", ...)` / `"**배터리 info: "` (www/intro.js)
   - `"[ServerList] 뷰 모드 save failed:"` · `"접속 history save failed:"`
   - `"[fnOnInitModeling] i18n 모델 build failed, 폴백 use:"`
   - `"[fnShowMainWindow] 활성 window move/show failed:"`
   - `oOpt.지금라벨` ← **코드 식별자가 한글**인데 그대로 남았다(치환 대상에서 빠짐)

→ 장군님 지시 "로그 글은 전부 영어" 를 **통째 문자열 바꾸기로 처리하면 이렇게 깨진다.**
   같은 파일에서 과거에도 같은 사고가 있었다(주석 한가운데에 `try/catch (_u4aErr) { ... }` 가 박힌 건).
   **여러 파일 일괄 수정은 대상 목록을 먼저 보고한 뒤 실행하라**는 규칙(2026-09-08)이 이걸 막으라고 있는 것이다.

★ 같은 파일을 두 세션이 동시에 만지고 있다. `ws_html5_ws20_prev.js` 편집 중
  "file had been modified on disk" 를 한 번 받았다. **이 시기 파일은 편집 직전에 반드시 다시 읽을 것.**
