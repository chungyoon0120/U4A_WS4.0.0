# 3.0 ↔ 4.0 내부 데이터 비교 — 1단계 (8888 받기 + 좌우 JSON viewer)

## 요청

장군님 지시(2026-09-11, 5차에 걸쳐 나눠 받음).

- 두 프로그램이 각각 관리하는 **데이터 구조가 같은지 다른지만** 본다. 값이 같든 다르든 비교는 하되
  다른 자리는 **filter 로 골라 볼 수 있게** 한다.
- **3.0 소스는 고치지 않고 파일도 만들지 않는다.** 실행 중인 3.0 화면에서 감지 코드를 그 자리에 돌려
  수집한 뒤, **보내기 단추**를 만들어 HTTP 로 4.0 에 보내기만 한다.
- **4.0** 은 데이터 모니터 창을 열 때 **express 로 8888 을 열고**, 받은 것을 비교해 **화면에 보여주기만** 한다.
- 1단계는 **좌우로 나눠 JSON viewer 로 출력**까지. 나머지는 장군님이 화면을 직접 보고 정한다
  ("한번에 요구사항을 다 적어서 하는거보단 난 직접 하나씩 눈으로 보고 판단하고 싶다").

## 변경 내용

### `js/ws_html5_datamon.js` — 데이터 구조를 한 벌 돌려주는 문 하나 추가

- `oAPP.datamon.snapshot()` 추가. 안에서 이미 쓰던 snapshot 뜨는 함수를 그대로 부른다.
- 왜 필요한가: 3.0 에서 이 파일을 실행 중인 화면에 붙여 돌린 뒤, 그 결과를 4.0 의 8888 로 보내야 한다.
  비교하려는 것이 **구조**라서 달라진 것만이 아니라 **전체**가 필요하다. 감시를 켜지 않아도 부를 수 있다.
- 실패하면 `DMON-009` 로 드러내고 `null` 을 준다(보내는 쪽이 "빈 것"으로 착각하지 않게 로그를 남긴다).
- 그 외 로직은 한 줄도 안 건드렸다.

### `Popups/dataMonitor/Popup/index.html` — 비교 뷰 자리

- 뷰 전환 줄에 **비교 뷰** 단추 추가(리스트 뷰 / 트리 뷰 옆).
- 비교 뷰 전용 조작 묶음 추가 — **4.0 지금 담기** 단추, **다른 것만** 체크, 상태 글.
- 좌우 두 쪽 + 가운데 분할 바 추가. 분할 바는 공통(`U4AUI.wireSplitter`)이 맡는다.

### `Popups/dataMonitor/Popup/js/index.js` — 8888 열기 + 비교 그리기

- 창이 뜰 때 **127.0.0.1:8888** 을 연다. 창을 닫을 때 닫는다.
  - `GET /ping` = 살아 있는지 확인, `POST /snapshot` = 3.0 이 보낸 데이터 구조 받기.
  - 받는 한도를 크게 잡았다(데이터 구조 한 벌이 통째로 온다 — 실측 48만자).
  - 다른 프로그램이 보내므로 출처 제한을 열어 두었다(받기 전용).
- 3.0 것이 들어오면 **4.0 것도 그 자리에서 같이 담아** 나란히 놓는다.
- **어긋난 자리 찾기** — 두 구조를 함께 훑어 `다름` / `3.0만` / `4.0만` 로 표시하고,
  거기까지 가는 윗자리도 기억해 둔다(걸러 볼 때 길이 막히지 않게).
- **좌우 트리** — 공통 컬럼 트리를 좌우 각각 하나씩. 칸은 [이름] [값].
  - 펼침 상태는 **좌우가 같은 것**을 본다 — 같은 자리를 함께 펼쳐야 맞대 볼 수 있다.
  - 어긋난 줄은 색으로 구분(값 다름 / 한쪽에만 있음).
- **다른 것만** 을 켜면 어긋난 자리와 거기까지 가는 길만 남는다.

### `Popups/dataMonitor/Popup/css/index.css` — 비교 뷰 모양

- 좌우 두 쪽·머리줄·어긋난 줄 색. 색은 전부 의미 토큰만 썼다(하드코딩 없음, 고정 px 폭 없음).
- **원래 있던 결함 하나를 같이 고쳤다** — 아래 「참고 사항」 참조.

### `.works/데이터모니터/02_3.0에서_보내기.md` (신규)

3.0 에서 그 자리에 붙여 돌릴 코드 두 덩이(감지 코드 싣기 / 보내기 단추 만들기)와 하는 순서.
**3.0 폴더에는 아무것도 안 만든다.**

## 변경 파일

- 추가:
  - `.works/데이터모니터/01_버전간_데이터비교_설계.md` (설계 받아쓰기 — 이 세션에서 계속 갱신)
  - `.works/데이터모니터/02_3.0에서_보내기.md`
- 변경:
  - `www/ws30/ws10_20/js/ws_html5_datamon.js`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/index.html`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/css/index.css`
  - `.works/데이터모니터/00_현황판.md` (테스트 그룹 DMC 10건 추가)
- 삭제: 없음
- 백업:
  - `www/ws30/ws10_20/js/_ws_html5_datamon.js.20260911.cmpapi.bak`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/_index.html.20260911.cmp.bak`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/_index.js.20260911.cmp.bak`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/css/_index.css.20260911.cmp.bak`

## 변경 이유

3.0 과 4.0 이 같은 조작에서 같은 내부 구조를 만드는지 눈으로 확인할 방법이 없었다.
두 앱은 서로 다른 process 라 지금 쓰는 창끼리 주고받는 길로는 못 잇는다 —
그래서 4.0 이 자리를 하나 열고 3.0 이 보내는 쪽이 된다(장군님 지정: 8888).

## 영향 범위

- **4.0 데이터 모니터 창 안에서만.** 리스트 뷰·트리 뷰 동작은 건드리지 않았다.
- 감지 파일에는 **읽기만 하는 문 하나**를 더했을 뿐, 감시 로직은 그대로다.
- 창이 떠 있는 동안 **8888 을 붙잡는다.** 창을 닫으면 놓는다.

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` (감지 파일 · 별창 화면 파일) | 통과 |
| 화면에 적은 id 와 코드가 찾는 id 가 맞는지 (11개) | 전부 1:1 일치 |
| express 로 받는 방식이 실제로 도는지 (따로 띄워 보냄 → 받음) | **통과** (200, 뿌리 2개 인식) |
| 쓴 색 이름이 실제로 정의돼 있는지 | 확인함 (정의 안 된 이름 2개는 피해서 씀 — 아래 참고) |
| **실제 화면에서 비교 뷰가 뜨는지** | **미검증** — 앱을 다시 띄워야 확인된다 |
| **3.0 에서 감지 코드가 도는지 · 보내기가 닿는지** | **미검증** |

**CDP 실측은 하지 않았다**(장군님 지시 2026-09-10 — 지시가 있을 때만).

## 참고 사항

- **원래 있던 결함 하나를 같이 고쳤다** — 트리 뷰 전용 조작 줄이 `display:flex` 만 걸려 있어
  **숨김 표시가 안 먹었다**(브라우저 기본 숨김 규칙을 화면 규칙이 이겨 버린다). 리스트 뷰에서도
  트리 뷰 조작 줄이 그대로 보이고 있었을 것이다. 비교 뷰가 생기면서 세 뷰의 조작 줄이 한꺼번에
  보이게 되므로 같이 막았다. 같은 처리는 공통 자산에도 이미 두 곳 쓰여 있다.
- **보고만 하고 안 고친 것** — 이 창의 스타일 파일이 **정의돼 있지 않은 색 이름 두 개**를 쓰고 있다.
  그 자리는 색이 아예 안 먹는다. 내가 새로 쓴 부분은 정의된 이름만 골라 썼고, 기존 자리는 손대지 않았다.
  **장군님 판단이 필요하다.**
- 1단계에 **일부러 안 넣은 것** — 차이 강조 손질, 양쪽 스크롤 맞물리기, 3.0 것과 4.0 것 짝짓기 규칙,
  값 감추기. 장군님이 화면을 보고 정하신 뒤에 얹는다(장군님 지시 2026-09-11).


---

# [이어서 · 2026-09-11 11:40] express 로는 못 연다 — 원래 들어 있는 것으로 바꿈

## 무슨 일이 있었나

장군님이 앱을 띄우자 창을 열자마자 터졌다.

```
[DMWN-012] ERROR EXPRESS_LOAD_FAILED port=8888
Cannot find module 'node:events'
```

## 원인 (소스로 확인)

**설치 문제가 아니다. 버전이 안 맞는다.**

| 확인한 것 | 값 |
|---|---|
| 프로젝트에 깔린 express | **5.2.1** |
| 그 express 가 요구하는 Node | **18 이상** (자기 package.json 에 적혀 있음) |
| 이 앱의 Electron | **14.2.9** → 들고 있는 Node 는 **14 대** |
| 터진 자리 | express 안쪽에서 기본 기능을 **새 방식 이름**으로 부르는 줄 |

Node 14 는 그 새 방식 이름을 모른다. 그래서 express 를 부르는 순간 즉시 터진다.
**설치를 다시 해도 똑같이 터진다.**

## 어떻게 고쳤나

**express 를 안 쓴다.** 받을 것이 확인용 하나 · 데이터 받기 하나뿐이라
**Node 에 원래 들어 있는 것으로 8888 을 그대로 열었다.**

- 설치할 것이 없다. 버전에 매이지 않는다.
- 받는 크기 상한을 두고, 넘거나 글자로 못 바꾸면 각각 다른 자리에 흔적을 남긴다.
- 주소를 잘못 적고 보내면 그것도 흔적을 남긴다(조용히 삼키면 원인을 못 찾는다).

express 를 그대로 쓰려면 **express 4 를 설치**해야 하는데, 설치는 장군님 승인이 필요해서 하지 않았다.

## 변경 파일

- 변경: `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` (8888 여는 부분만 통째로 교체)
- 백업: 앞서 뜬 `_index.js.20260911.cmp.bak` 그대로 사용

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 코드에 express 가 남아 있는지 | 설명 글 두 줄만 남고 **부르는 자리는 없음** |
| 바꾼 방식이 실제로 받는지 (따로 띄워 24만자 보냄) | **통과** — 200, 뿌리 수 인식, 받은 크기 일치 |
| 확인용 주소 | **통과** — 200 |
| **실제 화면** | **미검증** — 앱을 다시 띄워야 확인된다 |

---

# [이어서 · 2026-09-11] express 를 지웠더니 앱이 안 뜬 건 — 원래 있던 구멍

## 무슨 일이 있었나

장군님이 express 를 설치했다 지우자 **앱이 아예 안 떴다.**

```
[Critical Error] Cannot find module 'qs'
Require stack: ... www/ws30/ws10_20/js/ws_util.js
```

## 원인 (소스로 확인)

**이번 작업과 무관한, 원래부터 있던 구멍이다.**

- 앱의 공용 도구 파일이 **`qs` 를 직접 부른다**(`www/ws30/ws10_20/js/ws_util.js:39`).
- 그런데 `qs` 는 `package.json` 에 **한 번도 적혀 있지 않았다.**
- express 가 자기 필요로 끌고 온 덕에 **우연히 깔려 있던 것**뿐이다.
- 그래서 express 를 지우자 `qs` 도 같이 지워졌고, 앱이 뜨자마자 터졌다.

## 어떻게 풀렸나

장군님이 `qs` 를 설치해 해결. **이제는 `package.json` 에 제대로 적혔다(6.16.0)** — 다른 것을 지워도 같이 안 지워진다.

- `express` 는 이제 목록에서 빠졌다. **우리는 안 쓰므로 그대로 두면 된다.**

## 남은 위험 (보고만 — 조치 안 함)

같은 방식으로 **적어 두지 않은 채 우연히 깔려 있던 것**이 또 있을 수 있다.
`npm` 으로 무언가를 지울 때마다 같은 일이 날 수 있다. 전수 점검은 지시가 있을 때 하겠다.

## 검증

| 항목 | 결과 |
|---|---|
| `qs` 가 목록에 적혔는지 | **적힘** (6.16.0) |
| 앱이 뜨는지 | **장군님 확인 — 정상** |
| 목록에 적힌 것 중 안 깔린 것이 있는지 | **없음** |

---

# [이어서 · 2026-09-11] 비교 뷰가 옆으로 잘려 보인 것

## 무슨 일이 있었나

장군님 화면: 오른쪽 칸이 자리를 다 못 채우고 중간에서 끊겼고,
가운데 안내 문구(**4.0 지금 담기를 누르십시오.**)도 **글자가 잘렸다.** 아래에 가로 밀대도 생겼다.

## 원인 (내 잘못)

좌우로 **반씩 나눠 쓰는 좁은 자리**인데, 두 칸의 폭을 **둘 다 못 박아** 두었다.
두 칸 폭의 합이 한쪽 자리보다 넓어서 옆으로 밀렸고, 가운데 안내 문구는
**밀려난 넓이의 한가운데**에 놓이는 바람에 보이는 자리 밖으로 나갔다.

이미 같은 일로 지적받고 기억해 둔 것이 있었는데(좁은 자리에서는 칸 폭을 다 못 박지 말 것),
**이번에 그대로 되풀이했다.**

## 어떻게 고쳤나

공통이 이미 주고 있는 **마지막 칸 채우기** 방식으로 바꿨다.

- [이름] 칸만 폭을 두고, [값] 칸은 **남는 자리를 채운다.**
- 이렇게 하면 폭이 자리에 맞춰져 **옆으로 안 밀리고**, 안내 문구도 제자리에 온다.
- 같은 화면의 다른 곳(소스 목록·MIME 목록)이 이미 쓰는 방식이라 새로 만든 것이 아니다.

## 변경 파일

- 변경: `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` (좌우 칸 설정 3줄)

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 공통이 그 방식을 실제로 지원하는지 | 확인함 (공통 코드·스타일·쓰고 있는 화면 2곳) |
| **실제 화면** | **미검증** — 앱을 다시 띄워야 확인된다 |

---

# [이어서 · 2026-09-11] 비교 뷰에 트리 동작이 빠져 있던 것 + 오른쪽 칸 · 자리 번호

## 지적받은 것 (장군님, 같은 지적 2회)

1. **트리 뷰에서 이미 만든 동작을 비교 뷰에 안 넣었다.** 접으면 그 아래도 접히기 · 이름 칸 아무 데나 눌러 접었다 폈다 · 잘린 글자 보여주기 — 셋 다 빠져 있었다.
2. **"접으면 하위도 접힌다"는 공통 아니냐. 아니면 공통에 명확히 명시해라. 두 번 말하게 하지 마라.**
3. **오른쪽 칸이 여전히 잘려 보인다.**
4. 자리 번호를 **8888 → 9999** 로 바꿔라.

## 원인

### 1·2 — 왜 빠졌나 (소스로 확인)

공통 트리의 표준은 **"접기 = 자손까지 재귀 접힘"** 이 맞다(`.analy/16` §3.2).
그런데 **펼침 상태를 바깥에서 들고 있는 방식**으로 쓰면 공통이 그 처리를 쓰는 쪽에 넘긴다
(공통 소스의 가상 트리 토글 주석에 그렇게 적혀 있다 — 자손 접기는 `onToggle` 을 **안 준** 갈래에만 있다).

이 화면은 데이터가 커서 그 방식을 쓴다. 트리 뷰에는 이미 구현해 뒀는데,
**비교 뷰를 새로 만들면서 그 동작을 옮기지 않았다.** 같은 지적을 두 번 받은 이유다.

### 3 — 오른쪽 칸

화면에 `u4a-splitter__pane--flex` 라고 적어 뒀는데 **그런 이름은 어디에도 정의돼 있지 않다.**
다른 화면 markup 을 확인 없이 베껴 왔다. 그래서 오른쪽 칸이 남는 자리를 안 가져가고
안쪽 내용 폭만큼만 차지해, 오른쪽이 텅 빈 채 잘려 보였다.

## 어떻게 고쳤나

### 트리 동작 — 한 곳으로 모았다

화면 안에 공용 함수 넷을 두고 **트리 뷰와 비교 뷰가 같은 것을 쓴다.**

| 공용 함수 | 하는 일 |
|---|---|
| 접힘 기록 | 접으면 그 아래도 접힌다. 대용량이라 자손 전부에 쓰지 않고 "한 번이라도 접은 자리"만 기록 |
| 조상 확인 | 윗자리를 접은 적 있으면 그 아래는 저절로 안 펼친다 |
| 툴팁 | 잘린 칸에 마우스를 올리면 다 보여준다 |
| 이름 칸 배선 | 이름 칸 아무 데나 눌러 접었다 폈다. **요소당 한 번만** 배선(가상 스크롤이 행 요소를 다시 쓰므로) |

트리 뷰도 이 공용 함수를 쓰도록 바꿨다(동작은 그대로, 구현이 한 곳으로 모였다).

### 공통에 명시 — 두 번 말하지 않게

- `.analy/16_공통_화면UX_표준.md` §3.4.1 ① 에 **별표 두 개로 명시**했다:
  "외부 펼침맵을 쓰면 자손 접힘도 화면이 책임진다. 트리를 하나 더 얹을 때는 그 화면의 기존 구현을 재사용하고 새로 짜지 않는다."
  대용량에서 자손 전부에 쓰면 안 되는 이유와, 같이 딸려야 할 3종(자손 접힘·이름 칸 토글·툴팁)도 적었다.
- 기억에도 같은 내용을 남겼다.

★ **공통 자산(`theme/u4a-ui.js`)은 고치지 않았다.** 자손을 공통에서 훑게 하면
대용량(2만 자리 이상)에서 토글 한 번마다 그만큼 순회하게 되어 느린 PC 가 멎는다.
장군님이 주신 두 갈래 중 **"공통에 명확히 명시"** 쪽을 택했다.

### 오른쪽 칸 · 자리 번호

- 없는 이름을 화면에서 걷어내고, **오른쪽 칸이 남는 자리를 전부 가져가도록** 스타일에 명시했다.
- 자리 번호를 **9999** 로 바꿨다 — 코드·3.0 에서 돌릴 코드·설계 문서·현황판 전부.

## 변경 파일

- 변경:
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/index.html`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/css/index.css`
  - `.analy/16_공통_화면UX_표준.md` (§3.4.1 ①)
  - `.works/데이터모니터/01_버전간_데이터비교_설계.md` · `02_3.0에서_보내기.md` · `00_현황판.md` (자리 번호)

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 공용 함수를 두 트리가 실제로 쓰는지 | 확인 — 트리 뷰 2곳 · 비교 뷰 3곳에서 호출 |
| 코드·문서에 옛 자리 번호가 남았는지 | **0건** |
| 정의 없는 이름이 화면에 남았는지 | **0건** |
| **실제 화면** | **미검증** — 앱을 다시 띄워야 확인된다 |

## 참고 사항

- 3.0 에서 돌릴 코드의 보내는 주소도 새 번호로 바뀌었다. **이미 붙여 넣어 둔 단추가 있으면 다시 붙여 넣어야 한다.**

---

# [이어서 · 2026-09-11] 3.0 에 심을 수집 코드 — 한 덩이로 뽑음

## 요청

장군님: "우선 변경된 데이터를 수집하는 로직을 3.0에다가 심어야 되는데 그 로직좀 줘"

## 만든 것

`.works/데이터모니터/03_3.0에_심을_수집코드.js` (신규, 1,051줄)

| 부분 | 내용 |
|---|---|
| 앞 | **4.0 의 수집 코드 그대로** — 글자 하나 안 고쳤다 |
| 뒤 | 보내는 부분(3.0 전용) — 보내기 단추 · 감시 자동 켜기 · 콘솔에서 부를 수 있는 문 |

**왜 원본 그대로인가**: 수집 규칙이 조금이라도 다르면 두 프로그램을 비교하는 뜻이 없어진다.
그래서 4.0 엔진 파일을 **통째로 앞에 두고** 뒤에만 덧붙였다. 같은 내용인지 대조해 확인했다.

**보내는 것** = 데이터 구조 한 벌 전체 + 그동안 쌓인 변경 내용.
비교 화면이 그리는 것은 데이터 구조 쪽이고, 변경 내용은 4.0 이 나중에 쓸 수 있게 같이 보낸다.

**심는 방법 두 가지** — ① 개발자 도구에 붙여 넣기 ② 3.0 의 파일 로드 목록 맨 뒤에 얹기.
②는 3.0 폴더에 파일을 두는 것이라 **장군님이 쓰기 가능한 3.0 폴더를 정해 주셔야 한다**(안 정해졌으면 ①로 쓴다).

**로그**: 준비됨 / 변경 잡힘 / 보냄(크기·걸린 시간) / 실패(주소·크기·4.0 창 열렸는지) 전부 남긴다. 접두 DM30.

## 변경 파일

- 추가: `.works/데이터모니터/03_3.0에_심을_수집코드.js`
- 변경: `.works/데이터모니터/02_3.0에서_보내기.md` (붙여 넣을 코드 두 덩이 → 파일 하나 안내로 교체)

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 수집 코드가 4.0 원본과 같은지 | **같음** (글자 단위 대조) |
| **3.0 에서 실제로 도는지** | **미검증** |

---

# [이어서 · 2026-09-11] 비교 뷰 위에 리스트 뷰 표가 겹쳐 나온 것

## 지적받은 것

장군님: "비교뷰에 이딴건 필요 없다고 이건 다른 뷰에 있는 화면이잖아"
비교 뷰를 보고 있는데 위쪽에 **리스트 뷰의 표(시각/종류/path/이전/새 값 + 걸러내기 줄)** 가 같이 떠 있었다.

## 원인 (내 잘못)

뷰가 **둘(리스트·트리)뿐이던 때** 쓴 판단이 그대로 남아 있었다 —
변경 묶음이 들어올 때마다 "**트리 뷰가 아니면** 표를 보인다"로 되어 있어,
비교 뷰는 트리 뷰가 아니므로 **표가 도로 나왔다.**

뷰를 바꿀 때는 제대로 숨기고 있었는데, **감시가 켜져 있으면** 변경이 들어올 때마다 되살아났다.
그래서 바꾼 직후에는 멀쩡해 보이다가 조작하면 나타났다.

## 어떻게 고쳤나

**"리스트 뷰일 때만 표를 보인다"** 로 바꿨다. 뷰가 더 늘어나도 같은 일이 안 생긴다.

## 변경 파일

- 변경: `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` (한 줄 + 주석)

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 표를 보이거나 숨기는 자리 전부 확인 | 3곳 — 전부 리스트 뷰 기준으로 맞음 |
| **실제 화면** | **미검증** — 앱을 다시 띄워야 확인된다 |

---

# [이어서 · 2026-09-11] 「4.0 지금 담기」가 지금을 안 뜨던 것 + 쓰는 법 정리

## 장군님 질문

"화면 어떻게 쓰라는거야? 용도가?"
"비교뷰에서도 난 우선 4.0에서 감지할때 감시 시작 버튼 누르고 해야되는거야?"
"4.0 지금 담기로 해야되는거야?"

## 답 (소스로 확인)

- **비교 뷰는 감시 시작이 필요 없다.** 감시는 "조작할 때마다 뭐가 바뀌었나"를 쌓는 것이라
  리스트 뷰·트리 뷰용이다. 비교 뷰는 **누른 그 순간의 데이터 구조**를 통째로 떠서 맞대 본다.
- **[4.0 지금 담기]** 를 쓰는 것이 맞다. 3.0 쪽은 **[3.0 → 4.0 보내기]**.
  3.0 것이 도착하면 4.0 것도 자동으로 같이 담기므로, 보통은 보내기만 눌러도 된다.

## 이 질문 덕에 드러난 결함 (고침)

**[4.0 지금 담기] 가 "지금"을 안 뜨고 있었다.**

- 4.0 쪽 담기는 트리 뷰가 쓰던 길을 그대로 쓰고 있었는데, 그 길은 **마지막으로 비교할 때 뜬 것**을 준다
  (트리 뷰는 화면에 뿌린 변경 목록과 시점이 같아야 하므로 일부러 그렇게 돼 있다).
- 그래서 **감시가 켜져 있으면** 조작 직후에 담기를 눌러도 **최대 1초 전 것**이 담겼다.
- 3.0 쪽은 누를 때마다 지금을 뜬다 → **양쪽 시점이 어긋나** 비교가 틀어진다.

**고침**: 비교 뷰 전용으로 "지금 이 순간을 새로 떠 달라"는 길을 따로 만들었다.
이제 담기는 감시가 켜져 있든 꺼져 있든 **항상 지금**을 뜬다. 트리 뷰가 쓰던 길은 그대로 두었다
(그쪽은 변경 목록과 시점이 같아야 맞다).

## 변경 파일

- 변경:
  - `www/ws30/ws10_20/js/ws_html5_datamon.js` (비교 뷰 전용 길 추가)
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` (담기가 그 길을 쓰도록)
  - `.works/데이터모니터/02_3.0에서_보내기.md` (쓰는 법·용도 표 추가)
  - `.works/데이터모니터/03_3.0에_심을_수집코드.js` (수집 코드가 바뀌었으므로 다시 뽑음)

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` (수집 코드 · 별창 · 3.0 파일) | 전부 통과 |
| 3.0 파일의 수집 코드가 4.0 것과 같은지 | 다시 뽑아 동일 |
| **실제 화면** | **미검증** |

---

# [이어서 · 2026-09-11] 비교 뷰에서 안 쓰는 조작을 숨김

## 지적받은 것

장군님: "니말대로라면 비교뷰일때 감시시작 버튼이 나오지 말게 하던지 막든지 숨기던지.
프로그램 좀 좃같이 짜지마 사용자가 헷갈려서 쓰겠냐?"

맞는 지적이다. 앞 답에서 "비교 뷰는 감시가 필요 없다"고 해 놓고 **버튼은 그대로 두었다.**
쓰지도 않는 버튼이 눌리는 채로 있으면 무엇을 눌러야 하는지 알 수 없다.

## 어떻게 고쳤나

비교 뷰로 가면 **그 뷰에서 안 쓰는 것을 통째로 숨긴다.**

| 비교 뷰에서 숨기는 것 | 왜 |
|---|---|
| **감시 시작 · 지금 비교 · 기록 지우기** | 비교 뷰는 감시를 쓰지 않는다(누른 순간을 통째로 뜬다) |
| **감시 꺼짐/켜짐 글자** | 위와 같은 이유 |
| **공통코드 포함 · 라이브러리 포함** | 여기서 범위를 넓히면 **4.0 쪽만 커져** 3.0 에 없는 자리가 전부 다름으로 나와 비교가 못 쓰게 된다 |

**비교 뷰에 남는 것** = 뷰 전환 3개 · **4.0 지금 담기** · **다른 것만** · 받기 상태 글자. 그게 전부다.

구조상으로도 **묶음 두 개(감시 관련 / 감시 범위)로 감싸** 통째로 보이고 숨게 했다.
앞으로 뷰가 늘어도 이 묶음만 껐다 켜면 된다.

**받기 상태 글자를 오른쪽 끝으로 옮겼다** — 감시 상태 글자가 있던 자리와 **같은 자리**다.
리스트/트리 뷰는 감시 상태, 비교 뷰는 받기 상태가 늘 같은 곳에 보인다.

## 변경 파일

- 변경:
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/index.html` (묶음 두 개로 감싸기 + 상태 글자 자리)
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` (뷰 바뀔 때 묶음 보이기/숨기기)
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/css/index.css` (묶음 모양 + 숨김 존중)
  - `.works/데이터모니터/02_3.0에서_보내기.md` (설명 갱신)

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 묶음이 화면·코드 양쪽에 있는지 | 확인 |
| 숨김이 먹는지(가로로 늘어놓는 요소는 숨김이 안 먹는다) | 묶음·상태 글자 모두 숨김 존중 규칙을 넣음 |
| **실제 화면** | **미검증** — 앱을 다시 띄워야 확인된다 |

---

# [이어서 · 2026-09-11] 비교가 아래 레벨까지 안 내려간 것 + 「다른 것만」에서 하위가 안 나온 것

## 지적받은 것

장군님:
1. "정밀하게 비교해 하위에 데이터가 있는데 거기 레벨까지 다르게 비교해"
2. "트리에 아이콘은 하위에 더 데이터가 있는것처럼 펼침 버튼이 있는데 **다른것만** 버튼 누르면 상위 부모 노드만 나오고 하위는 안나와"

## 원인 — 둘의 뿌리가 같다 (내 잘못)

**한쪽에만 있는 자리를 만나면 그 자리 하나만 적고 아래로 안 들어갔다.**

그래서
- ① 하위 레벨이 **비교에서 통째로 빠졌다** — 어느 자리가 어떻게 다른지 알 수 없었다.
- ② **다른 것만** 을 켜면 그 자손이 걸러졌다(적어 둔 적이 없으니 걸러내기를 통과 못 한다).
  화살표는 원본 값 기준이라 그대로 보였고, 눌러도 속이 비었다.

한쪽만 덩어리인 경우(한쪽은 값, 한쪽은 묶음)도 같은 문제였다.

## 어떻게 고쳤나

| 고친 것 | 내용 |
|---|---|
| **아래까지 훑기** | 한쪽에만 있는 자리는 **그 아래 전부**를 같은 종류(3.0만 / 4.0만)로 적는다. 한쪽만 덩어리인 경우도 그 아래 전부 |
| **목록/묶음이 다를 때** | 그 자리를 다름으로 적되 **아래는 계속 견준다**(전에는 거기서 멈췄다) |
| **화살표 보정** | **다른 것만** 을 켰을 때 자식이 전부 걸러지면 **화살표를 안 그린다.** 눌러도 안 나오는 화살표가 없어진다 |
| **상한** | 어긋난 자리 20,000개에서 끊는다. 한쪽에만 있는 덩어리가 크면 그 아래가 통째로 어긋난 자리가 되어, 상한이 없으면 느린 PC 가 멎는다. 끊으면 화면 오른쪽 끝에 **(너무 많아 끊음)** 이라고 적고 콘솔에도 남긴다 |
| **로그** | 비교가 끝날 때마다 어긋난 자리 수·끊김 여부·걸린 시간을 남긴다 |

**어긋난 자리 수가 전보다 크게 늘어난다** — 전에는 윗자리 하나만 세던 것을 이제 아래까지 다 세기 때문이다. 그게 맞는 값이다.

## 변경 파일

- 변경: `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js`

## 검증

**실제 소스에서 비교 부분을 글자 그대로 잘라 내 돌려 봤다**(다시 짜지 않았다). 14가지 전부 통과.

| 확인한 것 | 결과 |
|---|---|
| 값이 다른 홑값 / 깊은 자리 값 다름 / 같은 자리는 안 걸림 | 통과 |
| 한쪽에만 있는 자리 — **그 아래 1·2·3단계까지** 전부 잡히는지 | **통과** |
| 가는 길(윗자리)이 열리는지 | 통과 |
| **다른 것만** — 어긋난 자리 통과 / 가는 길 통과 / 무관한 자리 걸러짐 | 통과 |
| `node --check` | 통과 |
| **실제 화면** | **미검증** — 앱을 다시 띄워야 확인된다 |

---

# [이어서 · 2026-09-11] 좌우가 같이 펼쳐지던 것 — 따로 움직이게

## 지적받은 것

장군님: "좌측 트리에 펼침 눌렀는데 오른쪽 트리에 같은 이름의 노드가 왜 같이 펴지는거냐?"

## 원인 — 내가 시키지 않은 동작을 넣었다

비교 뷰를 만들 때 **좌우가 펼침 상태를 하나로 같이 쓰게** 만들었다.
"같은 자리를 함께 펼쳐야 맞대 볼 수 있다"는 **내 판단**이었고 **지시받은 적이 없다.**

게다가 실제로는 도움도 안 된다 — 좌우는 줄 순서도 개수도 달라서
같이 펼쳐도 줄이 나란히 맞지 않는다. 놀랍기만 하고 얻는 것이 없었다.

## 어떻게 고쳤나

**펼침 상태를 좌우 따로 둔다.** 누른 쪽만 펼쳐지고 접히며, 다시 그리는 것도 그 쪽만 한다.

- 접으면 그 아래도 접히는 동작은 **양쪽 그대로** 유지된다(쪽마다 자기 기록을 본다).
- 좌우를 한꺼번에 다시 그리던 함수는 이제 쓰는 곳이 없어 **지웠다**(죽은 코드 안 남김).

## 변경 파일

- 변경:
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js`
  - `.works/데이터모니터/02_3.0에서_보내기.md` (좌우 펼침은 따로 움직인다는 설명 추가)

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 좌우 공용으로 쓰던 펼침 상태가 남았는지 | **0건** |
| 죽은 코드가 남았는지 | **0건** |
| **실제 화면** | **미검증** — 앱을 다시 띄워야 확인된다 |

## 참고 사항

**요청 밖 UX 를 임의로 넣지 말 것**(프로젝트 규칙)에 어긋난 사례다.
비교 화면에서 좌우를 묶을지 말지는 **장군님이 화면을 보고 정하실 것**이었는데 내가 먼저 정해 버렸다.

---

# [이어서 · 2026-09-11] 이름순 정렬 + 콘솔에 붉은 줄이 쏟아지던 것

## 지적받은 것

장군님: "이름순으로 정렬해주고, 콘솔에 이런 오류는 뭐냐?"
(콘솔에 `COMPARE_DIFF_TRUNCATED` 붉은 줄이 `COMPARE_DONE` 과 번갈아 계속 쌓여 있었다)

## 1) 콘솔 붉은 줄 — 오류가 아니었다 (내 잘못)

그 줄은 **오류가 아니라** 내가 건 상한(어긋난 자리 20,000개)에 닿아
"여기서 그만 셌다"는 표시였다. 그런데

- **오류 등급으로** 찍고 있었다 → 붉은 줄로 보인다
- **비교가 끝날 때 남기는 줄에 이미 `cut=true` 로 들어 있는데 또 찍었다** → 같은 말을 두 번
- 비교할 때마다 되풀이됐다

**고침**: 따로 찍던 줄을 **없앴다.** 끝난 줄 하나에 어긋난 자리 수·끊김 여부·걸린 시간이 다 들어 있다.

### 곁들여 고친 것 — 한 번 보내면 비교가 두 번 돌던 것

3.0 것을 받으면 ① 그 자리에서 한 번 다시 그리고 ② 4.0 것이 도착하면 또 그렸다.
그래서 한 번 보낼 때마다 비교가 **두 번** 돌고 콘솔에도 두 줄씩 쌓였다.
→ **4.0 것이 도착할 때 한 번만** 그린다. 부모 창과 못 이어진 때만 왼쪽이라도 그려 둔다.

## 2) 이름순 정렬 (지시)

좌우가 담은 순서가 서로 달라 같은 이름이 딴 높이에 있어 눈으로 맞대 보기 어려웠다.
→ **묶음은 이름순으로** 늘어놓는다.

★ **목록(Array)은 정렬하지 않는다** — 몇 번째인가가 곧 뜻이라 섞으면 안 된다.

## 변경 파일

- 변경: `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js`

## 검증

**실제 소스에서 해당 부분을 글자 그대로 잘라 내 돌렸다.**

| 확인한 것 | 결과 |
|---|---|
| 뿌리가 이름순으로 나오는지 | **통과** |
| 묶음 자식이 이름순으로 나오는지 (대문자 섞어 확인) | **통과** |
| **목록은 순서 그대로인지** | **통과** |
| 목록 자리 표기가 유지되는지 | **통과** |
| 앞서 만든 비교 시험 14가지가 그대로 통과하는지 | **전부 통과** |
| `node --check` | 통과 |
| 따로 찍던 붉은 줄이 남았는지 | **0건** |
| **실제 화면** | **미검증** |

---

# [이어서 · 2026-09-11] 3.0 에서만 UI 인스턴스 속이 통째로 딸려 나온 것

## 지적받은 것

장군님:
- "3.0 에다가 발췌하는 소스가 4.0 이랑 달라. 내부에서 제외하는 오브젝트들이 있는데 3.0 에 준건 오브젝트 전부 수집하는거 같어"
- "3.0에 준건 UI 인스턴스 내부값 전부 추출한거 같다고. 4.0에서 추출한 기준 그대로 해야된다고"
- **"3.0에 줄때는 oAPP.attr.oModel 안에는 oData 만 추출"**

## 원인 (소스로 확인 — 코드는 같은데 담긴 물건이 다르다)

수집 코드 자체는 **글자 하나까지 같다**(대조 확인). 문제는 **같은 이름 칸에 들어 있는 물건이 두 앱에서 다르다**는 것이었다.

| | `oAPP.attr.oModel` |
|---|---|
| **3.0** | `new sap.ui.model.json.JSONModel()` — **UI5 모델** |
| **4.0** | `_createModel()` — **우리가 만든 보통 객체** (`ws_html5_shell.js`) |

UI5 모델은 자기 안에 **바인딩 목록 · 이벤트 등록표 · 문맥**까지 들고 있고,
그것들이 다시 **화면 부품을 가리킨다.** 그래서 3.0 에서만 UI 인스턴스 속이 통째로 딸려 나왔다.
받은 크기가 211만자였던 것도 이 때문이다.

4.0 쪽 보통 객체는 `oData` 말고는 전부 함수라 원래도 안 담겼다.

## 어떻게 고쳤나 (지시대로)

**모델이 놓이는 칸에서는 `oData` 만 담는다.**

- 대상 칸 = `oModel` 과 UI 부품 안의 `_MODEL` 두 가지.
- 두 앱에 **똑같이** 걸린다 — 한쪽만 규칙이 다르면 비교가 뜻이 없다.
- 4.0 쪽은 담기는 내용이 **바뀌지 않는다**(원래도 oData 뿐이었다).
- `oData` 가 없는 모델이면 빈 것으로 두고 흔적을 남긴다(왜 비었는지 알 수 있게).

## 변경 파일

- 변경: `www/ws30/ws10_20/js/ws_html5_datamon.js`
- 다시 뽑음: `.works/데이터모니터/03_3.0에_심을_수집코드.js`
- 백업: `www/ws30/ws10_20/js/_ws_html5_datamon.js.20260911.model.bak`

## 검증

**실제 수집 코드를 그대로 돌려 확인했다** — 3.0 처럼 바인딩·이벤트표를 잔뜩 든 UI5 모델과
4.0 처럼 보통 객체 모델을 둘 다 넣어 봤다.

| 확인한 것 | 결과 |
|---|---|
| 3.0 쪽 모델 칸이 `oData` 하나뿐 | **통과** |
| 4.0 쪽 모델 칸이 `oData` 하나뿐 | **통과** |
| **두 쪽 결과가 완전히 같다** | **통과** |
| `oData` 속은 그대로 담긴다 | 통과 |
| 바인딩 목록 · 이벤트 등록표가 안 담긴다 | 통과 |
| UI 부품 속이 안 딸려 온다 | 통과 |
| 앞서 만든 비교 시험 14가지 | 전부 통과 |
| `node --check` (수집 코드 · 3.0 파일) | 통과 |
| 3.0 파일 안 수집 코드 == 4.0 것 | 동일 |
| **실제 화면** | **미검증** |

## 참고 사항

**3.0 에 심은 파일을 다시 심어야 한다** — 수집 규칙이 바뀌었다.

---

# [이어서 · 2026-09-11] 3.0 에서만 UI5 Core 모델 데이터를 앱 모델에 합치기

## 지시

장군님: "3.0에서 데이터 추출할때 `oAPP.attr.oModel.oData` 안에 `sap.ui.getCore().getModel().oData` 이거랑 같이 합쳐줘"

**왜 필요한가**: 4.0 은 데이터를 모델 하나에 다 들고 있는데, 3.0 은 **앱 모델**과 **UI5 Core 모델**
두 군데로 나뉘어 있다. 합쳐야 4.0 과 같은 모양이 되어 비교가 뜻이 있다.

## 어떻게 넣었나 — 수집 코드는 양쪽 같게 두고, 3.0 전용 부분에서만 건다

합치는 것은 **3.0 에서만** 해야 한다. 그렇다고 수집 코드를 3.0 용으로 고치면
"두 앱이 같은 기준으로 담는다"가 깨진다. 그래서:

| 어디 | 무엇을 |
|---|---|
| 수집 코드 (양쪽 공통) | 모델 oData 를 담기 직전에 **바깥에서 손볼 수 있는 자리**를 하나 열어 뒀다. 아무도 안 걸면 아무 일도 안 한다 — **4.0 은 안 건다** |
| 3.0 전용 부분 | 그 자리에 "앱 모델 + Core 모델을 합쳐서 돌려주는 것"을 건다 |

- **앱 모델 칸(`oModel`)에만** 합친다. 화면 부품 안의 모델은 그대로 둔다.
- **겹치는 이름은 앱 모델 값이 이긴다**(더 가까운 쪽). 몇 개가 겹쳤는지 로그에 남긴다.
- Core 가 없거나 못 읽으면 **원래 것을 그대로 담고** 왜 못 합쳤는지 남긴다(조용히 넘기지 않는다).

## 변경 파일

- 변경: `www/ws30/ws10_20/js/ws_html5_datamon.js` (손볼 자리 + 여는 문)
- 다시 뽑음: `.works/데이터모니터/03_3.0에_심을_수집코드.js` (합치는 부분 포함)
- 백업: `www/ws30/ws10_20/js/_ws_html5_datamon.js.20260911.hook.bak`

## 검증

**실제로 3.0 에 심을 파일을 그대로 돌려서** 확인했다(4.0 쪽은 수집 코드만 돌렸다).
3.0 은 앱 모델·Core 모델 두 개로 나눠 두고, 4.0 은 그 둘을 합친 하나로 두었다.

| 확인한 것 | 결과 |
|---|---|
| 3.0 에 Core 것이 합쳐지는지 (2개 이름) | **통과** |
| 3.0 에 앱 모델 것도 남는지 | **통과** |
| 겹치는 이름은 앱 모델이 이기는지 | **통과** |
| **3.0 결과 == 4.0 결과** | **통과** |
| 바인딩 목록이 안 딸려 오는지 | 통과 |
| 앞 시험(모델 7가지 · 비교 14가지) | 전부 그대로 통과 |
| `node --check` (수집 코드 · 3.0 파일) | 통과 |
| 3.0 파일 안 수집 코드 == 4.0 것 | 동일 |
| **실제 화면** | **미검증** |

## 참고 사항

**3.0 에 심은 파일을 다시 심어야 한다.**

---

# [이어서 · 2026-09-11] 3.0 쪽은 send 하나만 — 부르는 그 순간을 떠서 보낸다

## 지시

장군님: "메소드를 뺄때 send만 하면 현재 시점의 오브젝트를 수집해서 전송하는걸로 해줘"

## 무엇을 바꿨나

3.0 쪽에서 할 일은 **"부르는 그 순간의 데이터 구조를 떠서 보내는 것" 하나뿐**이다.
그래서 나머지를 전부 걷어냈다.

| 뺀 것 | 왜 |
|---|---|
| **감시 자동 켜기** | 3.0 은 조작할 때마다 비교를 돌릴 필요가 없다. 켜 두면 느려지기만 한다 |
| 쌓아 두던 변경 내용 | 감시를 안 켜니 쌓일 것이 없다. 보내는 내용에서도 뺐다 |
| 감시 켜기/끄기·쌓인 것 보기·비우기 | 쓸 데가 없어졌다 |

**바깥에 여는 것은 `DM30.send()` 하나뿐이다.** 화면 오른쪽 아래 단추도 같은 것을 부른다.
누르면 그 자리에서 데이터 구조를 새로 떠서 보낸다.

**4.0 쪽 수집 코드는 한 글자도 안 건드렸다**(3.0 파일 안 수집 부분 == 4.0 것, 대조 확인).

## 변경 파일

- 다시 뽑음: `.works/데이터모니터/03_3.0에_심을_수집코드.js`
- 변경: `.works/데이터모니터/02_3.0에서_보내기.md` (설명 갱신)

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 바깥에 열린 것이 `send` 하나뿐인지 | **확인** |
| 감시 켜는 코드가 3.0 전용부에 남았는지 | **0건** |
| 앞 시험(Core 모델 합치기 6가지) | 전부 그대로 통과 |
| 3.0 파일 안 수집 코드 == 4.0 것 | 동일 |
| **실제 화면** | **미검증** |

## 참고 사항

**3.0 에 심은 파일을 다시 심어야 한다.**

---

# [이어서 · 2026-09-11] 비교 뷰 좌우에 이름 찾기 칸 추가

## 지시

장군님: "좌우 테이블 상단에 필터 기능좀 넣어줘"

## 무엇을 넣었나

각 쪽 머리줄 오른쪽에 **이름 찾기** 칸을 하나씩 뒀다. **누른 쪽만** 좁혀진다.

- **이름만 본다**(값은 안 본다) — 트리 뷰의 찾기와 **같은 방식**으로 맞췄다.
- 걸린 자리와 **거기까지 가는 길**이 남고, **걸린 자리 아래는 통째로** 보인다.
- **다른 것만** 과 같이 켜면 **둘 다 만족하는 자리**만 남는다.
- 칸 오른쪽 **X** 는 공통 지우기를 그대로 쓴다.
- 찾는 것이 없으면 **찾는 것이 없습니다.** 라고 뜬다.
- 걸러서 자식이 전부 빠지면 **펼침 화살표를 안 그린다**(눌러도 안 나오는 화살표를 없앤다).

한 쪽만 다시 그린다 — 어긋난 자리는 그대로라 다시 세지 않는다.

## 변경 파일

- 변경:
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/index.html`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/css/index.css`
  - `.works/데이터모니터/02_3.0에서_보내기.md`

## 검증

**실제 소스에서 걸러내기 부분을 글자 그대로 잘라 내 돌렸다.** 7가지 전부 통과.

| 확인한 것 | 결과 |
|---|---|
| 안 걸면 전부 나온다 | 통과 |
| 좌측만 좁혀진다 / **우측은 그대로다** | **통과** |
| 가는 길만 남는다 · 걸린 자리 아래는 보인다 | 통과 |
| 없는 글자면 빈다 | 통과 |
| **다른 것만 + 찾기 = 둘 다 만족해야 남는다** | 통과 |
| 앞 시험(비교 14가지 · 정렬 4가지) | 전부 그대로 통과 |
| `node --check` · 화면 id 2개 짝 맞음 | 통과 |
| **실제 화면** | **미검증** |

---

# [이어서 · 2026-09-11] 담을 때마다 펼침 상태가 뒤섞이던 것

## 지적받은 것

장군님: "4.0 지금 담기 버튼누르면 왜 트리가 접힌거 있고 펼친거 있고 왜 중구난방이야?"

## 원인 (내 잘못)

**새로 담아도 예전에 펼쳐 둔 기록을 그대로 들고 있었다.**

- 전에 펼쳐 둔 자리 → 그 기록대로 **펼쳐진 채**
- 그 밖의 자리 → 기본값(**뿌리만 펼침**)

두 가지가 섞여서 어떤 건 펼쳐지고 어떤 건 접힌 채로 나왔다.
담을 때마다 모양이 달라지니 예측이 안 된다.

## 어떻게 고쳤나

**그 쪽 데이터가 새로 들어오면 그 쪽 펼침 기록을 비운다.**

- 좌(3.0) = 3.0 에서 받았을 때 / 우(4.0) = 담았을 때. **각각 따로** 비운다.
- 그래서 언제 담아도 늘 **같은 모양**으로 시작한다 — **뿌리만 펼쳐진 상태.**
- 펼침은 여전히 좌우 따로 움직인다.

## 변경 파일

- 변경: `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js`

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 좌·우 각각 비우는 자리가 들어갔는지 | 확인 (받을 때 1곳 · 담을 때 1곳) |
| **실제 화면** | **미검증** |

## 참고 사항

처음 모양은 **뿌리만 펼침**이다. 전부 접힌 채로 시작하는 것이 나으면 말씀만 하시면 바꾼다.

---

# [이어서 · 2026-09-11] 처음엔 전부 접힘 · 「다른 것만」은 어긋난 데까지만 펼침 · 찾기는 값도 본다

## 지시

장군님:
- "전체 접어주고, 상단 체크박스에 **다른것만** 선택하면 다른게 있는 부분까지 펼쳐줘 좌우측 트리 동일하게"
- "상단 이름찾기는 **값까지** 같이 필터 걸어줘"

## 무엇을 바꿨나

### 1) 펼침 규칙

| 때 | 어떻게 |
|---|---|
| 담은 직후 | **전부 접힘** — 뿌리까지 접는다(전에는 뿌리만 펼쳐져 있었다) |
| **다른 것만** 켬 | **어긋난 것이 아래에 있는 자리까지만** 펼친다. 어긋난 자리 자체는 안 펼친다 |
| **찾기** 켬 | 걸린 자리가 보이도록 길을 연다 |

★ **좌우가 똑같이 펼쳐진다** — 어긋남 목록은 하나이고 양쪽이 그것을 보기 때문이다.
(펼침 **상태**는 여전히 좌우 따로 — 손으로 펼친 것은 그 쪽만 움직인다.)

### 2) 찾기가 값도 본다

- 이름뿐 아니라 **값**에 그 글자가 있으면 걸린다.
- 덩어리는 값이 없으므로 이름만 본다. 그 속은 아래에서 따로 훑는다.
- 아주 긴 값은 **앞 2,000자만** 본다(느린 PC 에서 멎지 않게).
- 이름만 보는 것이 아니게 되었으므로 칸 안내 글자를 **찾기 (이름·값)** 으로 바꿨다.

## 변경 파일

- 변경:
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/index.html` (칸 안내 글자)
  - `.works/데이터모니터/02_3.0에서_보내기.md`

## 검증

**실제 소스에서 걸러내기 부분을 잘라 내 돌렸다 — 8가지 전부 통과.**

| 확인한 것 | 결과 |
|---|---|
| **값으로도 찾힌다**(이름에 없고 값에만 있는 글자) | **통과** |
| 좌측만 좁혀지고 우측은 그대로 | 통과 |
| 가는 길만 남고 걸린 자리 아래는 보인다 | 통과 |
| 다른 것만 + 찾기 = 둘 다 만족해야 | 통과 |
| `node --check` | 통과 |
| **실제 화면(펼침 규칙 포함)** | **미검증** |

---

# [이어서 · 2026-09-11] 걸러내기를 걸어도 접힌 채로 남던 것

## 지적받은 것

장군님:
- "상단 필터를 걸면 **해당 노드까지 다 펼쳐줘야지**"
- "중간에 노드를 접었다고 하더라도 **엔터 치면 다시 필터 동작해서 펼쳐줘**"

## 원인 (내 잘못, 두 가지)

1. **손으로 접어 둔 기록이 걸러내기보다 먼저였다.** 펼침 여부를 볼 때 손 기록을 맨 먼저 보게 해 둬서,
   접어 둔 자리 아래에 찾은 것이 있으면 접힌 채로 남았다.
2. **[다른 것만] 이 찾기보다 먼저였다.** 둘 다 켜면 찾기 규칙을 아예 안 타서, 찾은 자리가 안 펼쳐졌다.

## 어떻게 고쳤나

| 고친 것 | 내용 |
|---|---|
| 순서 | **찾기를 [다른 것만] 보다 먼저** 본다. 둘 다 켜도 찾은 자리까지 길이 열린다 |
| 손 기록 | 걸러내기를 건드리면(찾기 글자 입력 · X 로 지우기 · **다른 것만** 켜고 끄기) **그 쪽 펼침 기록을 비운다.** 그 뒤 손으로 접고 펴는 것은 다시 정상으로 먹는다 |
| **Enter** | 글자가 그대로여도 **다시 건다.** 중간에 접어 둔 것이 있어도 그때 다시 펼쳐진다 |

## 변경 파일

- 변경:
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js`
  - `.works/데이터모니터/02_3.0에서_보내기.md`

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 앞서 만든 걸러내기 시험 8가지(값 찾기 포함) | 전부 그대로 통과 |
| 펼침 기록 비우는 자리가 다 들어갔는지 | 확인 (글자 입력 · X · 다른 것만 · 담을 때) |
| **실제 화면(펼침 동작)** | **미검증** |

---

# [이어서 · 2026-09-11] 편의 기능 — 어긋난 자리로 바로 가기

## 지적받은 것

장군님: "사용자 고려를 좃도 안하고 짜니까 그런생각을 못하는거야. 비교 모니터링인데 쓰기 존나 불편해 편의기능 좃도 없어"

**맞는 지적이다.** 어긋난 자리가 수천 곳인데 **어디가 다른지 찾아 내려갈 방법을 안 만들었다.**
비교 도구에서 가장 기본인 것을 빠뜨렸다.

## 넣은 것

| 조작 | 하는 일 |
|---|---|
| **아래 화살표** 단추 · **F3** | 다음 어긋난 자리로. 끝나면 처음으로 돈다 |
| **위 화살표** 단추 · **Shift+F3** | 이전 어긋난 자리로 |
| 그 옆 숫자 | 지금 **몇 번째 / 모두 몇 곳** |
| **모두 접기** | 좌우를 전부 접는다 |
| 오른쪽 끝 글자 | **다름 N · 3.0만 N · 4.0만 N (총 N곳)** — 종류별로 몇 곳인지 |

- 이동하면 **좌우가 같은 자리에 함께 선다.** 가는 길도 자동으로 펼쳐진다.
- 한쪽에만 있는 자리면 그 쪽만 선다.
- **이동 목록에는 어긋남의 꼭대기만 넣는다** — 한쪽에만 있는 큰 덩어리는 그 아래가 전부 어긋난 자리가 되는데,
  그것까지 넣으면 [다음 차이]를 수천 번 눌러야 다음 덩어리로 간다.
- 이동·선택은 공통 트리가 주는 기능을 그대로 쓴다(자체 구현 안 함).

## 일부러 안 넣은 것

- **모두 펼치기** — 2만 자리가 넘어 느린 PC 가 멎는다. 대신 **다른 것만** 이 어긋난 데까지 펼쳐 준다.
- **좌우 스크롤 맞물리기** — 좌우 줄 수가 달라 맞물려도 줄이 안 맞는다.
  같은 자리를 보려면 **[다음 차이]** 가 정답이라 그쪽을 만들었다.

## 변경 파일

- 변경:
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/index.html`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/css/index.css`
  - `.works/데이터모니터/02_3.0에서_보내기.md`

## 검증

**실제 소스를 그대로 잘라 내 돌렸다 — 9가지 전부 통과.**

| 확인한 것 | 결과 |
|---|---|
| 어긋난 자리는 아래까지 다 센다 | 통과 |
| **이동 목록에는 꼭대기만 들어간다** | **통과** |
| 값 다름 · 3.0만 · 4.0만 꼭대기가 목록에 있다 | 통과 |
| **그 덩어리 자손은 목록에 없다** | **통과** |
| 종류별 수가 맞다 | 통과 |
| 윗자리 계산(목록 자리 표기 포함) | 통과 |
| 앞 시험(걸러내기 8가지 · 비교 14가지) | 전부 그대로 통과 |
| `node --check` · 화면 id 4개 짝 맞음 | 통과 |
| **실제 화면** | **미검증** |

## 참고 사항

**윗자리 계산에서 결함 하나를 시험이 잡아냈다** — 뿌리 이름 자체에 점이 들어 있어(`oAPP.attr`),
계속 올라가면 실제로 없는 자리(`oAPP`)가 나왔다. 뿌리에 닿으면 멈추게 고쳤다.

---

# [이어서 · 2026-09-11] 찾기 깊이 제한 제거 + APPID·GUINR 수집 제외

## 지적받은 것

장군님: "왜 6단계로 니 멋데로 제약을 걸어? 그게 모니터링이냐?"
그리고 지시: "추출 필드 중에 APPID 랑 GUINR은 제외해"

## 1) 찾기 깊이 제한 — 내가 임의로 건 제약이었다

**왜 걸었나**: 줄을 그릴 때마다 그 아래를 **재귀로 다시 훑는** 방식으로 만들어서 느렸다.
그래서 6단계로 막아 뒀다. **모니터링 도구에 있을 수 없는 제약이다.**

**어떻게 고쳤나**: 방식을 바꿨다.
**찾는 글자가 바뀔 때 그 쪽 데이터를 처음부터 끝까지 한 번만 훑어 표로 만든다.**
그리면서는 표만 보면 되므로 **깊이 제한이 필요 없고 오히려 더 빠르다.**

- 걸린 자리 표 / 그 자리로 가는 길 표를 각각 만든다.
- 값 앞 2,000자만 보던 제한도 **없앴다** — 값 전체를 본다.
- 찾기가 끝나면 **걸린 개수와 걸린 시간**을 로그에 남긴다.
- **걸린 줄을 색으로 표시**했다. 머리줄에 **찾음 N곳** 이 나온다. 전에는 뭐가 걸렸는지 볼 수가 없었다.
- 펼침은 **걸린 자리까지만** 연다(그 아래는 접어 둔다).

## 2) APPID · GUINR 수집 제외

앱마다·열 때마다 값이 달라 두 프로그램을 견주면 **늘 「다름」으로 나온다.**
그것 때문에 진짜 어긋난 자리가 묻힌다. **깊이와 상관없이** 그 이름이면 안 담는다.

## 변경 파일

- 변경:
  - `www/ws30/ws10_20/js/ws_html5_datamon.js` (안 담는 칸)
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` (찾기 방식 교체)
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/css/index.css` (걸린 줄 색)
  - `.works/데이터모니터/02_3.0에서_보내기.md`
- 다시 뽑음: `.works/데이터모니터/03_3.0에_심을_수집코드.js`
- 백업: `www/ws30/ws10_20/js/_ws_html5_datamon.js.20260911.skipkeys.bak`

## 검증

**실제 소스를 그대로 돌려 확인했다.**

| 확인한 것 | 결과 |
|---|---|
| **11단계 깊이도 찾는다** (옛 코드는 6단계까지) | **통과** |
| 값에 있는 것도 찾는다 · 가는 길이 전부 열린다 | 통과 |
| 가는 길 밖 형제는 걸러진다 | 통과 |
| **APPID·GUINR 이 뿌리·깊은 자리·목록 안 어디서도 안 담긴다** | **통과** |
| 나머지 값은 그대로 남는다 | 통과 |
| 결과 글자 어디에도 그 두 이름이 없다 | 통과 |
| 앞 시험 전부(비교·이동·모델·Core 합치기) | 전부 그대로 통과 |
| `node --check` (수집 코드 · 별창 · 3.0 파일) | 통과 |
| 3.0 파일 안 수집 코드 == 4.0 것 | 동일 |
| **실제 화면** | **미검증** |

## 참고 사항

**3.0 에 심은 파일을 다시 심어야 한다** — 수집 규칙이 또 바뀌었다.

---

# [이어서 · 2026-09-11] 3.0 것을 받을 때 4.0 것을 자동으로 담던 것 — 뺌

## 지적받은 것

장군님: "3.0에서 전송 눌렀는데 왜 갑자기 4.0 데이터까지 같이 나와?"
"4.0 지금 담기 버튼이 이미 있는데?" / "자동으로 하면 안되"

## 원인 (내 판단이 틀렸다)

3.0 것이 도착하면 **4.0 것도 그 순간 자동으로 담게** 만들어 뒀다.
"양쪽 시점을 맞춰 준다"는 내 판단이었는데, **단추가 이미 있는데 자동으로도 담으니**
3.0 것만 보냈는데 오른쪽까지 바뀌어 **무슨 일이 일어났는지 알 수 없다.**

## 어떻게 고쳤나

**자동 담기를 뺐다.** 4.0 쪽은 **[4.0 지금 담기] 를 누를 때만** 담는다.

- 3.0 에서 보내면 **왼쪽만** 바뀐다.
- 오른쪽은 **단추를 누른 그 순간**을 담는다.
- 한쪽을 눌러도 반대쪽은 그대로다.

## 변경 파일

- 변경:
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js`
  - `.works/데이터모니터/02_3.0에서_보내기.md` (순서 갱신 — 보내기 뒤에 담기를 누른다)

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 4.0 것을 담으라고 부르는 자리 | **단추 하나뿐** (받는 자리에는 없음) |
| **실제 화면** | **미검증** |

---

# [이어서 · 2026-09-11] 모두 접기를 좌우 트리 자기 툴바로

## 지적받은 것

장군님: "모두 접기를 여기다 두라는게 아니고 **좌우 트리 상단 툴바를 하나 만들어서 각 트리 별로 버튼을 만들으라고**"

## 무엇을 바꿨나

- 위쪽 줄에 있던 **모두 접기** 를 **뺐다.**
- 좌우 각 트리의 머리줄(그 트리의 툴바)에 **접기 아이콘**을 하나씩 뒀다.
- **누른 쪽만** 접힌다. 반대쪽은 건드리지 않는다.

각 쪽 툴바에 이제 **찾기 칸 + 접기 아이콘**이 들어간다. 거기 있는 것은 전부 그 쪽에만 걸린다.

## 변경 파일

- 변경:
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/index.html`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/css/index.css`
  - `.works/데이터모니터/02_3.0에서_보내기.md`

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 좌·우 단추가 화면·코드 양쪽에 1:1 | 통과 |
| 위쪽 줄의 옛 단추가 남았는지 | **0건** |
| **실제 화면** | **미검증** |

---

# [이어서 · 2026-09-11] 툴바 정리 — 차이 이동 제거 · 쪽마다 전체 펼침/접힘

## 지시

- "이딴것도 그냥 빼" (위쪽 줄의 **차이 이동 화살표 + 몇 번째 숫자**)
- "보통 트리 상단 툴바에 전체 접힘 / 전체 펼침 버튼 있잖아 그렇게 하라고 **버튼 두개로**"

## 무엇을 바꿨나

| 뺀 것 | 넣은 것 |
|---|---|
| 위쪽 줄의 **차이 이동 화살표 2개 + 몇 번째 숫자** (F3 단축키 포함) | 좌우 **각 트리 툴바**에 **전체 펼침 · 전체 접힘** 두 단추 |

- 각 트리 머리줄이 그 트리의 툴바다 — **찾기 칸 · 전체 펼침 · 전체 접힘**. 전부 **그 쪽에만** 걸린다.
- 차이 이동에 쓰던 코드(이동 목록·윗자리 계산·단축키)는 **전부 지웠다**(죽은 코드 안 남김).
  단, 찾기가 쓰는 뿌리 판별은 남겼다.

## 전체 펼침을 어떻게 처리했나 (느린 PC)

**자리마다 "펼침"을 적어 두지 않는다.** 2만 자리가 넘는 데이터에서 자리마다 적으면 느린 PC 가 멎는다.
**그 쪽이 전체 펼침인지 표시 하나만** 두고, 펼침 여부를 물을 때 그 표시를 본다.

- 걸러내기(찾기·다른 것만)를 건드리거나 새로 담으면 **전체 펼침 표시는 내려간다.**
- 전체 펼침·접힘은 **걸린 시간을 로그에 남긴다.**

## 변경 파일

- 변경:
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/index.html`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js`
  - `www/ws30/ws10_20/Popups/dataMonitor/Popup/css/index.css`

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 차이 이동 관련 코드·화면 요소가 남았는지 | **0건** |
| 좌우 펼침·접힘 단추 4개가 화면·코드 1:1 | 통과 |
| **실제 화면** | **미검증** |

## 아직 못 짚은 것

**"스크롤이 많이 건너뛴다"** — 소스만 봐서는 원인을 못 짚었다.
행 높이는 공통 가상 스크롤이 첫 줄을 재서 스스로 맞추게 돼 있고(기본값 36 → 실측값으로 교체),
좌우 칸은 스스로 스크롤하지 않게 막아 두어 스크롤 자리가 겹치지도 않는다.
**실제 화면을 재 봐야 안다.**

---

# (이어서) 2026-09-11 15:20 — 스크롤이 한 번에 너무 많이 건너뛰는 것 수정

## 요청

장군님: "스크롤 이동이 한번에 너무 많이 이동된다고", "cdp 붙여라"

## CDP 실측 — **못 했다(붙지 않음)**

숨기지 않고 적는다. 다음 agent 는 여기서 시간 쓰지 마라.

- 9222 = **packaged 앱(`U4A Workspace.exe`, PID 38076)** 이 선점하고 있었다.
- 4.0 nobuild(browser process PID 57716)도 같은 9222 를 요청했으나 **이미 점유돼 바인딩 실패** →
  4.0 쪽에는 listening port 가 하나도 없다(9999 는 데이터모니터 수신 서버일 뿐).
- 그래서 `/json/list` 에 데이터모니터 별창 target 이 아예 안 나온다.
- `%APPDATA%\com.u4a_ws3.app.dev\DevToolsActivePort` 에 `9222` 가 적혀 있어도 **실제로는 안 열려 있다** — 이 파일을 믿지 마라. `netstat -ano` 로 확인해라.
- **붙이는 법**: packaged 앱을 닫고 → 4.0 을 재시작한다. 그러면 9222 를 4.0 이 잡는다.

## 원인 (소스로 확정)

공통 컬럼 트리(`U4AUI.makeColumnTree`)는 **넘겨받은 host 요소 자체에**
`wheel` / `scroll` listener 와 `ResizeObserver` 를 건다(가상 스크롤 컨테이너 = host).
또 컬럼 리사이즈 grip 도 host 에 hover 처리기를 건다.

비교 뷰는 트리를 다시 만들 때 `oHost.textContent = ""` 로 **자식만 지우고 같은 host 를 재사용**했다.
`textContent = ""` 는 **host 자신에 걸린 listener 를 지우지 않는다.**
→ 다시 만들 때마다 `wheel` listener 가 **한 개씩 쌓인다.**
→ 휠 한 칸에 `scrollTop += deltaY` 가 **쌓인 개수만큼** 실행돼 N 배로 건너뛴다.

쌓이는 시점(전부 `_cmpRebuildOne` / `_cmpRefresh` 를 탄다):
찾기칸 Enter · 찾기칸 지우기(X) · [다른 것만] 체크 · [전체 펼침] · [전체 접힘] · [4.0 지금 담기] · 3.0 에서 전송 수신.
**누를 때마다 +1** 이라 쓰면 쓸수록 심해진다. 장군님이 "쓸수록 심하다"고 느낀 것과 일치한다.

## 변경 내용

- `_cmpFreshHost(bLeft)` 추가 — 다시 그릴 때 스크롤 host 를 **새 `div` 로 갈아 끼운다**(같은 id·class).
  옛 listener 는 옛 요소와 함께 사라진다. 좌우 각각 독립.
  host 가 부모에 안 붙어 있으면 갈아 끼울 수 없으므로 `CMP_HOST_DETACHED` 경고를 남기고 옛 방식(비우기)으로 물러난다.
- `_cmpRebuildOne` — `oHost.textContent = ""` → `_cmpFreshHost(bLeft)`
- `_cmpRefresh` — 좌우 `textContent = ""` → `_cmpFreshHost(true) / _cmpFreshHost(false)`

공통 자산(`u4a-ui.js`)은 **안 건드렸다.** 소비 화면에서 해결했다.

## 변경 파일

| 구분 | 파일 |
|---|---|
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` |
| 백업 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/_index.js.20260911.scroll.bak` |

## 변경 이유

`textContent = ""` 로는 host 자신의 listener 가 안 지워진다 — 요소를 통째로 바꾸는 것이 확실하다.

## 영향 범위

- 비교 뷰의 좌우 트리만. tree view(`_renderTree`)는 원래 트리를 재사용하므로 해당 없음 — 안 건드렸다.
- 다시 그릴 때 스크롤 위치가 맨 위로 간다 — 원래도 `textContent = ""` 때문에 그랬으므로 **동작 변화 없음**.

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| **실제 화면** | **미검증** — CDP 가 안 붙어 못 쟀다 |

## 아직 못 짚은 것

**"필터 걸린 상태에서 접기/펼치기가 동작 안 한다"** — 원인 미확정.

지금까지 소스로 확인한 것(다음 agent 는 여기서부터):
- 이름 칸 클릭 처리기는 펼침 화살표를 직접 누른 경우를 이미 걸러낸다 → **이중 토글은 아니다.**
- 접으면 펼침 기록에 `false` 가 써지고, `isExpanded` 는 그 기록을 **맨 먼저** 본다 → 논리상 접혀야 한다.
- **의심 남은 곳**: 찾기가 걸린 상태에서 `hasChildren` 이 `_cmpKids(...).length > 0` 으로 바뀐다.
  찾기에 걸린 노드 자신의 자식 중에 걸린 것이 없으면 자식이 전부 걸러져 **화살표가 아예 사라진다** →
  "펼쳐지지 않는다"로 보일 수 있다. **미확인. 실측 필요.**

---

# (이어서) 2026-09-11 16:10 — ① 밑줄 두 개로 시작하는 칸 빼기 ② 제외 규칙(정규식) 필터

## 요청

1. 장군님: "필드명 시작이 "_"(언더바) 가 있으면 제거" → 되물음 후 정정: **"언더바 두개, 하나는 말고 두개만"**
2. 장군님: "좌우 트리 상단 툴바에 필터 버튼을 두고, vscode 검색의 필터처럼 **정규식으로 제외할 항목을 추가**하는 기능. 거기다 추가하면 **localStorage 에 저장**해야 되고 다시 실행하면 이전에 필터 넣은게 나와야됨"
3. 처음에 좌우 공용 한 벌로 만들었다가 장군님 지적으로 되돌림: **"각각 넣어야되 지금 각각 데이터 구조가 달라서 동일하게 필터가 적용되면 안된다고"**

## 1) 밑줄 두 개로 시작하는 칸 빼기 (수집 코드)

- `C_SKIP_PREFIX = "__"` 와 `_isSkipKey(sKey)` 추가 — 이름이 딱 맞는 것(`APPID` · `GUINR`)과 밑줄 두 개로 시작하는 것을 **한 함수로 모아** 판정.
- 거는 곳 3군데: 일반 객체 자식 루프 · UI 부품 안(화이트리스트 통과분에도 건다) · 뿌리 고르기.
- **밑줄 하나(`_T_0015` · `_MODEL` · `_BIND_AGGR` · `_OBJID` · `_EMBED_AGGR`)는 그대로 담는다.**
- 이 규칙으로 UI 부품 화이트리스트의 `__PARENT` · `__UIFND` 두 개가 빠진다. 그래서
  `__PARENT` 를 UI ID 로 바꿔 담던 분기는 **도달할 수 없게 되어 지웠다.**
  (화이트리스트 목록 자체에는 두 이름을 남겨 뒀다 — 규칙이 되돌아가면 그대로 살아난다.)
- 3.0 에 심는 파일도 다시 만들었다(생성기 = scratchpad `build_30.py`).

## 2) 제외 규칙(정규식) — 좌우 **각각**

- **쪽마다 따로.** 두 프로그램은 지금 데이터 구조가 달라 같은 정규식이 양쪽에 똑같이 안 먹는다.
- 거는 대상 = **path 전체**(예: `oAPP.attr.T_UI[3].UIATV`). 대소문자는 가리지 않는다.
- 걸린 자리는 **그 아래까지 통째로** 빠진다 — 화면 · 찾기 · 어긋난 자리 세기 **전부**에서.
- 어긋난 자리 세기는 좌우를 함께 보므로 **어느 한쪽에서라도 뺀 자리는 안 센다.**
  (화면에 안 보이는 자리가 개수에만 남으면 "다른 것만" 을 켰을 때 빈 곳을 찾아 헤매게 된다.)
- 화면: 좌우 머리줄에 깔때기 단추(규칙이 있으면 그 위에 개수). 누르면 그 쪽 머리줄 **바로 아래**에
  규칙 줄이 펼쳐진다 — 입력칸(Enter 로 추가) + 넣어 둔 규칙 칩(각 칩에 X) + 전부 지우기.
- 정규식이 아니면 **안 받고** 입력칸에 공통 방식(`data-vs` + 안내 줄)으로 이유를 보여 준다.
- 적어 두는 곳 = `localStorage`. 키는 `u4a.datamon.cmp.exclude.L` / `.R`. 창이 뜰 때 되살린다.
- 규칙 수 상한 50 — 자리마다 그만큼 견줘야 해서 느린 PC 가 멎는다.
- RegExp 는 **바뀔 때 한 번만** 만들어 둔다(자리마다 새로 만들면 2만 자리에서 그대로 멎는다).

## 변경 파일

| 구분 | 파일 |
|---|---|
| 변경 | `www/ws30/ws10_20/js/ws_html5_datamon.js` (밑줄 두 개) |
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/index.html` (제외 규칙 화면) |
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` (제외 규칙) |
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/css/index.css` (제외 규칙 모양) |
| 다시 만듦 | `.works/데이터모니터/03_3.0에_심을_수집코드.js` |
| 백업 | `_ws_html5_datamon.js.20260911.dunder.bak` · `_index.html.20260911.exclude.bak` · `_index.js.20260911.exclude.bak` · `_index.css.20260911.exclude.bak` |

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` (수집 코드 · 별창 · 3.0 배포 파일) | 통과 |
| 화면 id ↔ 코드 1:1 (제외 규칙 12개) | 통과 |
| **실제 소스를 잘라 내 node 에서 돌린 제외 로직 시험 13건** | **전부 통과** |
| **실제 화면** | **미검증** — 앱 재시작 필요 |

시험 13건 = 규칙 없을 때 안 빠짐 / 좌 규칙이 우에 안 먹음 / 좌우 자식 목록이 각각 다르게 걸러짐 /
path 로 걸림(목록 칸 `[0]` 포함) / 걸린 자리 아래까지 빠짐 / 윗자리는 안 걸림 / 대소문자 안 가림 /
뿌리도 뺄 수 있음 / 규칙 비우면 되돌아옴 / 틀린 정규식이 섞여도 안 터지고 나머지는 먹음.
시험 파일 = scratchpad `test_exclude.js`.

## 참고 사항

- 공통 자산(`u4a-ui.js`)은 안 건드렸다. 입력칸 오류 표시는 공통이 쓰는 `data-vs` 방식을 그대로 따랐다.
- 제외 규칙 줄은 비교 영역 안에 있어 다른 뷰로 가면 같이 숨는다(따로 숨기는 코드 없음).

---

# (이어서) 2026-09-11 16:45 — 제외 규칙 여러 줄 한꺼번에 넣기

## 요청

장군님: **"정규식은 여러건 넣을 수도 있잖아"**

## 이미 되던 것 / 없던 것

- 되던 것: Enter 칠 때마다 규칙이 **하나씩 쌓인다**(상한 50). 여러 규칙 중 **하나라도 걸리면** 그 자리가 빠진다.
- 없던 것: **여러 줄을 한 번에 붙여넣기.** 이것을 넣었다.

## 변경 내용

- `_excAdd(bLeft, sTxt, bDefer)` — 세 번째 인자를 더했다. `true` 면 적어 두기·다시 그리기를 안 한다.
  여러 개를 넣을 때 한 개마다 전체를 다시 세면 자리 수만큼 곱해져 느린 PC 가 멎는다.
- `_excAddMany(bLeft, sText)` 추가 — **줄바꿈으로만** 쪼갠다.
  **콤마로는 안 쪼갠다** — 정규식 안에 콤마가 들어갈 수 있다(`a{1,3}`).
  앞뒤 공백은 잘라 내고(CR 도 여기서 없어진다) 빈 줄은 건너뛴다. 다시 세기는 **맨 끝에 한 번만.**
  못 넣은 줄이 있으면 **몇 줄인지 칸에 적어 준다** — 조용히 버리면 왜 안 들어갔는지 모른다.
- 입력칸 `paste` 배선 — 붙여넣는 글에 줄바꿈이 있으면 칸에 글자로 넣지 않고 **규칙으로 받는다.**
  한 줄짜리 붙여넣기는 평소대로 칸에 들어간다.

## 변경 파일

| 구분 | 파일 |
|---|---|
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` |

## 손댔다가 되돌린 것 (다음 agent 는 주의)

패치를 **heredoc** 으로 넣다가 `\r` · `\n` 두 글자가 **진짜 개행으로 바뀌어** 정규식 두 개가 세 줄로 깨졌다
(`node --check` 가 잡았다). 고치는 스크립트가 줄 수를 잘못 세어 `ev.preventDefault();` 한 줄을 같이 지웠고,
백업본과 줄 단위로 맞대 보고 되살렸다(지금은 지워진 줄 없음 — 오류번호 주석 한 줄만 바뀜).
**교훈: 역슬래시가 든 코드를 heredoc 으로 넣지 말 것.** 파일로 쓰거나 문자열 함수로 우회한다.
실제로 이 자리는 정규식을 아예 안 쓰게 바꿨다(`split("\n")` + `trim()` 이 CR 을 없앤다).

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 백업본 대비 **의도치 않게 지워진 줄** | 없음(오류번호 주석 한 줄만 변경) |
| 실제 소스를 잘라 내 node 에서 돌린 시험 — 제외 로직 13건 | 전부 통과 |
| 실제 소스를 잘라 내 node 에서 돌린 시험 — **여러 줄 넣기 17건** | **전부 통과** |
| **실제 화면** | **미검증** — 앱 재시작 필요 |

여러 줄 시험 17건 = 세 줄 한 번에 들어감 / 다시 세기는 한 번만 / 반대쪽에는 안 들어감 /
빈 줄·앞뒤 공백 처리 / 윈도우 줄바꿈(CR+LF) / 틀린 줄·겹친 줄만 빠지고 나머지는 들어감 /
몇 줄 빠졌는지 알려줌 / 한 줄짜리 / 전부 틀리면 0건이고 다시 세지도 않음 / 상한 50 지킴.
시험 파일 = scratchpad `test_exclude_many.js`.

---

# (이어서) 2026-09-11 17:40 — 제외 규칙이 **이름**에도 걸리게 + 대소문자 구분 되돌림

## 요청 / 지적

1. 장군님 화면: 오른쪽에 `^(?!o).*` 를 넣었는데 `DnDRandKey` · `POSIT` · `_oServerWorker` 가 그대로 남았다.
2. 장군님: "정규식은 대소문자 구분 안해? 원래?"

## 1) 이름에도 걸리게 (장군님 지적 ①)

**원인**: 제외 판정을 **path 전체에만** 걸고 있었다. path 는 늘 `oAPP.attr...` 로 시작하므로
`^(?!o).*` 같은 **이름 기준** 정규식은 어디에도 안 걸린다. 기능은 정상이었고 **거는 대상이 달랐다.**

**수정**:
- `_cmpKeyOfPath(sPath)` 추가 — 그 자리의 **이름**(마지막 칸)을 뽑는다.
  뿌리는 이름 자체에 점이 들어 있어(`oAPP.attr`) 쪼개지 않고 그대로 쓴다.
  목록 칸(`T_UI[3]`)은 대괄호를 떼고 번호만 남긴다.
- `_cmpExcluded` 가 **이름과 path 둘 다** 본다. 둘 중 하나라도 걸리면 뺀다.
  그래야 `^(?!o).*`(이름 기준)와 `^oAPP[.]DATA$`(path 기준)를 **둘 다** 쓸 수 있다.
- 화면 안내 글도 `제외할 이름·path (정규식) - Enter 로 추가` 로 바꿨다.

## 2) 대소문자 구분 되돌림 (장군님 지적 ②)

정규식은 **원래 대소문자를 구분한다.** 내가 `i` 를 붙여 놨던 것이 잘못이었다.
`i` 가 붙으면 `^(?!o).*` 가 대문자 `O` 로 시작하는 이름까지 "o 로 시작" 으로 쳐서 안 빼 버린다.
→ **`i` 를 뺐다.** 규칙 두 자리(만들 때 · 받을 때 검사) 모두.
가리기 싫으면 `[Oo]` 처럼 규칙 안에 직접 적으면 된다.

## 변경 파일

| 구분 | 파일 |
|---|---|
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` |
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/index.html` (안내 글 2곳) |

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 소스를 잘라 내 node 에서 돌린 시험 — 제외 로직 **15건** | 전부 통과 |
| 소스를 잘라 내 node 에서 돌린 시험 — 여러 줄 넣기 **17건** | 전부 통과 |
| 소스를 잘라 내 node 에서 돌린 시험 — **이름 기준 · 장군님 화면 그대로 15건** | 전부 통과 |
| **실제 화면** | **미검증** — 앱 재시작 필요 |

이름 기준 시험은 장군님 화면의 칸을 그대로 넣고 `^(?!o).*` 를 걸어
**남는 칸이 `oMainBroad` · `oModel` · `oServerInfo` · `oShortcut` 뿐**임을 확인했다.
뿌리(`oAPP.DATA` · `oAPP.attr`)는 안 빠진다.
시험 파일 = scratchpad `test_exclude_key.js`.

## 참고 사항

테스트 harness 의 `_cmpIsRoot` 는 소스에서 잘라 오지 않고 **stub** 으로 바꿨다 —
잘라내기 경계가 자주 바뀌어 시험이 계속 깨졌다. 판정 로직 자체는 소스 그대로 쓴다.

---

# (이어서) 2026-09-11 18:20 — 화면 글자를 읽으면 바로 알게 고침

## 지적

장군님: "용어 시발 뿌리가 무슨뜻인데" / "함부로 번역하지마" / "전문용어 쓰라고" /
"상단에 내용이 정확히 뭘 의미하는지 구분이 안되" /
"총 20000곳이 무슨 말이고 뭐가 너무 많아서 끊음이 무슨말이고" /
"꼭 질문해야 알아 듣게 만들어" / "사용자의 배려란 좃도 없어"

**전부 내 잘못이다.** 개발자인 나만 아는 축약을 화면에 박아 놓고, 사용자가 물어봐야 뜻을 알게 만들었다.

## 무엇이 잘못이었나

| 예전 글자 | 왜 못 알아보나 |
|---|---|
| `뿌리 2개` | `root` 를 내가 한글로 지어냈다. 무슨 뜻인지 물어봐야 안다. |
| `다름 25` | **무엇끼리** 다른지 안 적었다. |
| `3.0만 19929` | 3.0 에만 **있다**는 말인지 3.0 **에서만** 센 건지 모른다. |
| `총 20000곳` | `곳` 이 무슨 단위인지 모른다. 쉼표도 없어 자릿수를 못 센다. |
| `너무 많아 끊음` | **누가** 끊었는지, **왜** 끊었는지, 그래서 **실제로는 몇 개인지** 하나도 모른다. |
| `9999 열림 - 3.0 에서 보내십시오` | 포트 번호만 덩그러니. 무엇이 열렸는지 모른다. |
| `다른 자리가 없습니다` | `자리`(= node) 도 내가 지어낸 말이다. |

## 고친 내용

**위쪽 줄**
```
값이 다름 25 · 3.0 에만 있음 19,929 · 4.0 에만 있음 46 · 합계 20,000
  ※ 20,000개까지만 세고 멈췄습니다 (실제로는 더 있습니다)
```
마우스를 올리면 각 항목이 무엇을 센 것인지 풀이가 뜬다(끊겼을 때는 왜 끊었는지까지).

**좌우 머리줄**
```
3.0 에서 받은 시각 17:36:10 · 맨 위 항목 2개 · 찾기에 걸린 항목 12개
4.0 을 담은 시각 17:30:56 · 맨 위 항목 2개
```
아직 없을 때 : `3.0 에서 아직 받은 것이 없습니다` / `4.0 을 아직 담지 않았습니다`
마우스를 올리면 `맨 위 항목` 이 무엇인지, 어떻게 채우는지 나온다.

**받을 준비 상태**
```
3.0 이 보내는 것을 받을 준비가 됐습니다 - 3.0 에서 보내기를 누르십시오
3.0 에서 받을 준비를 못 했습니다 (EADDRINUSE)
```
포트 번호는 풀이(마우스 올림)로 내렸다. 못 열었을 때는 **그래서 무엇이 안 되는지**까지 적는다.

**그 밖**
- 큰 수에 천 단위 쉼표.
- `다른 자리가 없습니다` → `다른 것이 없습니다 - 이 쪽은 양쪽이 똑같습니다.`
- `찾는 것이 없습니다` → `찾기에 걸린 것이 없습니다.`
- 주석·설명에 쓰던 `뿌리` 도 전부 `root` 로.

※ **종류를 나타내는 내부 값**(`"다름"` / `"3.0만"` / `"4.0만"`)은 코드 안 상수라 그대로 뒀다.
   화면에 나가는 글자만 바꿨다.

## 변경 파일

| 구분 | 파일 |
|---|---|
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` |
| 백업 | `js/_index.js.20260911.excbox.bak` |

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 백업본과 줄 단위 대조 — 의도 밖으로 사라진 줄 | 없음(바꾼 32줄 전부 의도한 교체) |
| **실제 화면** | **미검증** — 앱 재시작 필요 |

## 다음에 또 이러지 않으려고 남긴 것

메모리 `ui-text-must-be-self-explanatory` 신규 저장.
요지 = **숫자 라벨엔 무엇을 셌는지 적고, 상한에 걸려 끊었으면 누가·왜·실제로는 몇 개인지 적는다.
줄이 짧아야 하면 마우스 올림 풀이를 반드시 붙인다. 포트 번호·코드만 덩그러니 적지 않는다.**

## 아직 안 한 것

제외 규칙을 **딱지(칩) 목록 → 여러 줄 편집 상자**로 바꾸는 작업(장군님 지적 "토큰 형식으로 계속
추가하면 불편하지 않나")은 화면 markup 만 바꿔 둔 상태에서 멈췄다. **다음에 이어서 한다.**

---

# (이어서) 2026-09-11 18:40 — [4.0 지금 담기] 가 보던 자리를 흐트러뜨리지 않게

## 요청

장군님: "[4.0 지금 담기] 버튼 눌렀다고 해서 트리가 펼쳐지거나 접혀지거나 하지 말라고
그냥 있는 데이터만 뿌리라고"

## 무엇이 문제였나

담을 때마다 그 쪽 펼침·접힘 기록을 **통째로 비우고** 있었다.
그래서 보던 자리가 접히거나, 걸러내기 기준대로 딴 데가 펼쳐져 **어디를 보고 있었는지 잃어버렸다.**

(이건 앞선 2026-09-11 지시 "담을 때마다 펼침 상태 뒤섞임" 을 고치려고 넣은 코드였다.
 이번 지시가 그것을 대체한다 — 비우지 않는다.)

## 고친 내용

- **[4.0 지금 담기]** : 펼침·접힘 기록을 **안 비운다.** 데이터만 새로 담고 다시 그린다.
- **3.0 에서 받았을 때도 같다** — 같은 성격이라 같이 고쳤다(요청은 4.0 쪽만이었으나
  좌우 동작이 달라지면 더 헷갈린다. **보고 후 되돌릴 수 있다**).
- **스크롤 위치도 지킨다** — 다시 그릴 때 스크롤 자리를 새 요소에 물려 주고, 줄을 다 그린 뒤
  되돌린다(`_cmpRestoreTop`). 안 그러면 펼침은 그대로인데 화면만 맨 위로 튄다.

## 변경 파일

| 구분 | 파일 |
|---|---|
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` |

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| **실제 화면** | **미검증** — 앱 재시작 필요 |

---

# (이어서) 2026-09-11 18:55 — tree 에서 글자 드래그 복사

## 요청

장군님: "트리에 텍스트좀 복사되게 해줘"

## 왜 안 됐나

공통 정책이 **앱 전역에서 글자 선택을 막고**, 표(`tbody`)만 되살려 둔다(`.analy/16 §2.3`).
tree 는 그 목록에 없어서 이 창에서 값을 긁을 수가 없었다.

## 고친 내용

1. **글자가 잡히게** — 공통 파일은 건드리지 않고 이 창에만 덧댔다(`css/index.css`).
   - tree 뷰·비교 뷰의 **줄 전체**를 선택 가능으로. 줄 전체를 열어야 셀 여백에서 드래그를
     시작해도 잡힌다(표에서 이미 같은 문제를 겪고 얻은 결론).
   - **펼침 화살표는 계속 금지** — 눌러서 접었다 폈다 하는 것이지 긁는 것이 아니다.
2. **잡고 떼도 줄이 안 접히게** — 이름 칸을 눌러 접었다 펴는 처리기에
   공통 판정(`U4AUI.isTextDragSelecting`)을 넣었다. 없으면 경고 로그를 남기고 지나간다.
   (공통 tree 의 **행 선택**에는 이 가드가 원래 있었다 — 내 접기/펴기 배선에만 빠져 있었다.)

## 변경 파일

| 구분 | 파일 |
|---|---|
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/css/index.css` |
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` |
| 백업 | `css/_index.css.20260911.copytext.bak` · `js/_index.js.20260911.copytext.bak` |

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| **실제 화면** | **미검증** — 앱 재시작 필요 |

## 한계 (미리 적어 둔다)

tree 는 **보이는 줄만 그리는 방식**이라, 드래그로 잡은 채 화면 밖까지 스크롤하면
밖으로 나간 줄은 DOM 에서 빠져 그만큼은 복사되지 않는다. **한 화면 안에서 긁는 것은 된다.**

---

# (이어서) 2026-09-11 19:20 — 배열을 번호가 아니라 "값이 겹치지 않는 칸" 으로 짝지어 견줌

## 지적

장군님: "배열위치가 다른건 다르다고 판단할 꺼 아니야?"
되물었을 때: **"배열마다 매칭되는 필드가 다른데 어떻게해?"** → 사람이 지정할 수 없으니 **스스로 찾게** 한다.

## 무엇이 문제였나

배열을 **번호 순서대로**(0번↔0번, 1번↔1번) 견줬다.
그래서 한쪽에 항목이 **하나만 더 있어도 그 뒤가 전부 한 칸씩 밀려 통째로 어긋난 것**으로 나왔다.
화면의 `3.0 에만 있음 19,929` 도 이것 때문일 가능성이 크다. **미확인 — 앱 재시작 후 숫자로 확인할 것.**

## 고친 내용

1. **짝지을 칸을 배열마다 스스로 찾는다** (`_cmpArrKeyOf`)
   - 첫 항목의 홑값 칸을 후보로 잡고(양쪽이 다 있으면 **양쪽 첫 항목에 둘 다 있는** 칸만),
   - 그 배열 **전체에서 값이 서로 겹치지 않는** 칸만 남기고,
   - 그중 **좌우가 가장 많이 맞아떨어지는** 칸을 고른다.
   - 못 찾으면 **예전처럼 번호로** 떨어진다(값만 든 배열·빈 배열 등).
   - 후보는 최대 20개까지만 본다(항목 수천 개 배열에서 칸마다 전수 검사하면 느린 PC 가 멎는다).
   - 고른 칸은 로그로 남긴다(`ARRAY_KEY_PICKED path=… field=… matched=…`).
2. **자리 이름(path)도 번호 대신 그 값으로** — `T_0014[OBJID=INPUT51]`.
   안 그러면 왼쪽 9번과 오른쪽 3번이 같은 것인데 자리 이름이 달라 **어긋난 자리 색칠이 딴 줄에 붙는다.**
   tree 에 보이는 이름도 `9` 대신 `OBJID=INPUT51` 이 되어 오히려 알아보기 쉽다.
3. **tree 자식도 같은 규칙**(`_cmpKids`) — 견줄 때 정해 둔 칸을 그대로 쓴다.
   아직 한쪽만 담겼으면 그 쪽만 보고 정하고, 양쪽이 담기면 다시 센다.

## 변경 파일

| 구분 | 파일 |
|---|---|
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` |
| 백업 | `js/_index.js.20260911.arrkey.bak` |

## 손댔다가 되돌린 것 (다음 agent 주의)

이 파일은 **줄바꿈이 CRLF** 다. 패치 스크립트가 그것을 모르고 LF 기준으로 찾다가 계속 실패했고,
고치는 과정에서 **파일 전체 줄바꿈이 `CR CR LF` 로 두 번 붙었다**(2,852줄).
바로 되돌렸다(`CR CR LF` → `CR LF`, 실측 0건 남음 / CRLF 2,852 / 순수 LF 0).
**교훈: 이 파일을 스크립트로 고칠 때는 CRLF 를 먼저 확인하고, 복원 코드를 두 번 넣지 말 것.**

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 줄바꿈 확인 | CRLF 2,852 · `CR CR LF` 0 · 순수 LF 0 (정상) |
| 백업본과 줄 단위 대조 | 바뀐 8줄 전부 의도한 교체, 사라진 줄 없음 |
| **소스를 잘라 내 node 에서 돌린 배열 짝짓기 시험 13건** | **전부 통과** |
| 앞서 만든 시험 재확인 (제외 15 · 여러 줄 17 · 이름 기준 15) | 전부 통과 |
| **실제 화면** | **미검증** — 앱 재시작 필요 |

배열 시험 13건 = 앞에 항목이 하나 더 있어도 뒤가 안 밀림 / 순서가 뒤바뀌어도 값만 견줌 /
값만 든 배열은 번호로 / 값이 겹치는 칸은 안 고름 / 후보가 여럿이면 많이 맞는 칸을 고름 /
한쪽만 있어도 정해짐 / 빈 배열·값 배열은 null / 이름 모양(`칸=값`) / 실제와 닮은 섞인 경우.
시험 파일 = scratchpad `test_arrkey.js`.

---

# (이어서) 2026-09-11 20:10 — 제외 규칙을 파일에 적는다 (앱 꺼도 안 사라지게)

## 지적

장군님: "혹시 프로그램 껏다 키면 필터 정규식 다 사라지냐? 왜그런거 같지?" → **사라진다.**

## 원인 (소스로 확정)

이 창은 `Popups/dataMonitor/index.js` 에서 **`webPreferences.partition = SESSKEY`** 로 뜬다.
그 이름에 **`persist:` 접두가 없다.**

Electron 은 `persist:` 로 시작하지 않는 `partition` 을 **메모리에만 있는 session** 으로 다룬다.
앱을 끄면 그 session 의 `localStorage` 가 통째로 없어진다 → 제외 규칙이 다 날아간다.

**프로젝트 전체에 `persist:` 를 쓰는 곳이 0건이다** — 다른 별창들도 같은 상태다(참고용, 이번에 안 건드림).
세션 키가 로그인마다 새로 생기는지는 **미확인** — 그렇다면 `persist:` 를 붙여도 매번 새 저장소가 된다.

`partition` 은 로그인·쿠키와 얽힌 앱 전체 정책이라 **내가 건드리지 않았다**(월권).

## 고친 내용

`localStorage` 를 버리고 **파일**에 적는다.

- 자리 = `<userData>/u4a-datamon-exclude.json` (`APP.getPath("userData")`).
  이 자리는 **session 과 무관**해서 세션 키가 바뀌어도 그대로다.
- 한 파일에 좌우를 같이 담는다: `{ "L": [...], "R": [...] }`
- 읽기 = 창이 뜰 때 한 번(`_excLoadAll`). 쓰기 = 규칙이 바뀔 때.
- 파일이 없으면 그냥 빈 상태로 시작한다(로그 한 줄).
- 파일이 망가졌거나(JSON 아님) 모양이 이상하면 **무시하고 빈 상태**로 가되 **왜 그런지 로그로 남긴다.**
- 손으로 고친 파일이 들어올 수 있으므로 읽을 때 **글자인 것만, 상한 50 까지만** 받는다.

## 변경 파일

| 구분 | 파일 |
|---|---|
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` |

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| `localStorage` 쓰던 자리가 남았는지 | **0건** |
| **실제 소스를 잘라 내 진짜 파일로 돌린 시험 11건** | **전부 통과** |
| **실제 화면** | **미검증** — 앱 재시작 필요 |

시험 11건 = 파일 없으면 빈 상태 / 적으면 파일이 생김 / 다시 읽으면 좌우 그대로 /
읽어 온 규칙이 실제로 먹음 / 반대쪽엔 안 먹음 / 망가진 파일은 무시 /
글자 아닌 값·빈 글자는 걸러냄 / 배열 아니면 빈 목록 / 상한 50 / 한 파일에 좌우가 같이.
시험 파일 = scratchpad `test_excfile.js` (임시 폴더에 진짜 파일을 쓰고 지운다).

## 남은 것 (장군님 판단 대기)

제외 규칙을 **정규식 없이** 넣게 하는 안을 드렸고 아직 착수 안 했다:
① tree 우클릭 → 그 줄을 본보기로 **패턴을 대신 만들어 주고 몇 개 빠지는지 숫자까지** 보여 준다
② 규칙 칸을 **별표(`*`) 방식**으로(정규식은 `/…/` 로 감쌀 때만)
③ 치는 동안 **몇 개 빠지는지** 옆에 표시
④ 늘 값이 다른 칸(시각·랜덤키)은 프로그램이 먼저 찾아 제안

---

# (이어서) 2026-09-11 20:50 — 제외 규칙 입력 방식 (작업 중 · 방향 한 번 틀렸다가 되돌림)

## 지적

장군님: "사용자에게 어디까지 정규식이 되는지를 설명할 수가 없잖아 / 사용자도 분명 정규식은 직접
할 수 없으니까 ai한테 물어볼텐데" → 이어서: **"다들 쓰는 방식 말고 사용자도 ai한테 정규식을 물어볼꺼라고"**

## 내가 틀렸던 방향 (기록해 둔다 — 다음 agent 는 같은 판단 하지 말 것)

처음에 **정규식을 버리고 별표(`*`) 방식으로 바꾸려** 했다. 파일 찾기에서 다들 쓰는 방식이라
설명이 쉽다는 이유였다. **틀렸다.**
사용자는 결국 AI 에게 물어보는데, **AI 는 정규식을 준다.** 별표 전용으로 바꿔 놓으면
**AI 가 준 답이 안 먹어서 오히려 더 나빠진다.** 장군님이 바로 그 점을 짚었다.

## 지금까지 들어간 것 (별표 갈래 — 버리지 않고 살린다)

- `_excToRe(sRule)` 추가 — 적은 글자 한 줄을 RegExp 로 바꾼다.
  `/…/` 로 감싸면 그 안을 **정규식 그대로**, 아니면 **별표 방식**
  (`*` = 아무 글자 여러 개 / `?` = 한 글자 / 나머지는 글자 그대로, 앞뒤를 묶어 딱 그 이름).
- `_excCompile` · 넣을 때 검사가 이 함수를 쓰도록 교체. 못 알아먹는 글자는 안 받고 칸에 이유를 적는다.
- 본보기 목록 `C_EXC_SAMPLES` (밑줄로 시작 / KEY 로 끝 / 가운데 TIME / 딱 그 이름 / 통째로 / 정규식).
- 화면(`index.html`): 규칙 줄을 다시 짰다 —
  입력칸 + **몇 개 빠지는지 적을 자리** + **물음표 단추**(본보기) + 전부 지우기.
  id 추가 = `dmCmpExcHintL/R` · `dmBtnExcHelpL/R` · `dmCmpExcHelpL/R`.

## 아직 안 한 것 (다음에 이어서)

1. **자동 판별** — `^ $ [ ] ( ) { } | \ +` 가 보이면 **정규식**으로, 아니면 **별표**로 읽는다.
   (`*` `?` `.` 는 별표 방식에도 쓰이므로 판별에서 뺀다.)
   그래야 AI 가 준 `^_` 도 먹고 사용자가 쓴 `_*` 도 먹는다. 어느 쪽으로 읽었는지 칸 옆에 표시.
2. **[AI 에게 물어보기] 단추** — 누르면 클립보드에 담는다:
   이 화면 규칙이 무엇에 걸리고 걸리면 어떻게 되는지 + 지금 화면의 **실제 이름 견본** 몇 개 +
   "원하는 것을 말할 테니 규칙 한 줄을 만들어 달라"는 부탁.
   → **장군님이 사용자에게 설명할 일이 없어진다**(이번 지적의 핵심).
3. **몇 개 빠지는지 미리 보기** — 자리(`dmCmpExcHintL/R`)만 만들어 두고 **배선 안 함**.
4. 물음표 단추 · 본보기 목록 **배선 안 함**(자리만 있다).
5. tree 우클릭 → 패턴 후보 + 개수 (그다음).

## 변경 파일

| 구분 | 파일 |
|---|---|
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/index.html` |
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` |
| 백업 | `_index.html.20260911.glob.bak` · `js/_index.js.20260911.glob.bak` · `css/_index.css.20260911.glob.bak` |

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 화면 id 목록 확인 | 새 id 6개 들어감 |
| **동작** | **미검증** — 위 "아직 안 한 것" 때문에 물음표·개수 표시는 **아직 아무 일도 안 한다** |
| **실제 화면** | **미검증** |

## 주의 (다음 agent)

**이 상태로 두면 화면에 물음표 단추와 빈 자리가 보이는데 눌러도 아무 일이 없다.**
다음 턴에 위 1~4 를 마저 배선해야 한다.

---

# (이어서) 2026-09-11 22:10 — 제외 규칙을 **칸 두 개**로 (정규식도 그대로 먹게)

## 지적

장군님 예시: **"oAPP.attr 밑에 시작이 언더바로 시작하는것만 하려면 어떻게?"**
내가 준 답 → `/^oAPP\.attr(\..*)?[.\[]_[^.\[\]]*\]?$/`
장군님: **"되지도 않는데 뭔 개소리야"**

두 가지가 겹쳤다.
1. 그 규칙은 **지금 떠 있는 앱에 없는 코드**(슬래시로 감싸면 정규식)를 전제로 한 것이었다.
   슬래시까지 통째로 정규식이 되어 아무것도 안 걸린다. **내 잘못.**
2. 애초에 **저런 걸 쓰게 만든 설계가 잘못**이다.

## 고친 내용

### ① 칸을 둘로 나눴다

```
어디 밑에서   [ oAPP.attr ]   ← 비우면 전체
어떤 이름     [ _*        ]   ← 비우면 그 밑 전부
```

- 규칙 하나 = `{ U: "어디 밑", N: "어떤 이름" }`
- **U 만** : 그 자리와 그 아래 전부 / **N 만** : 어디에 있든 그 이름 / **둘 다** : 그 밑에서 그 이름만
- 판정은 정규식 한 줄로 짜내지 않고 **코드로 직접 확인**한다(`_excUnderOk` + 이름 검사).
- 예전에 적어 둔 글자 한 줄짜리 규칙도 `{U:"", N:그 글자}` 로 읽어 준다(파일 호환).

### ② 이름 칸은 **정규식·별표를 자동으로 알아본다**

장군님 지적("아무 AI 한테나 물어보면 저렇게 정규식을 알려주냐") 때문이다.
사용자는 AI 에게 물어 `^_` 같은 정규식을 받아 온다 — **그대로 붙여 넣어도 먹어야 한다.**

- `^ $ [ ] ( ) { } | \ +` 중 하나라도 보이면 → **정규식**으로 읽는다
- 아니면 → **별표 방식**(`*` = 여러 글자 / `?` = 한 글자 / 나머지는 글자 그대로, 앞뒤를 묶어 딱 그 이름)
- `/…/` 로 감싸면 무조건 정규식
- ★ `*` `?` `.` 는 판별에서 뺐다 — 안 그러면 `_*` · `oAPP.DATA` 가 정규식으로 읽혀 뜻이 달라진다

### ③ 치는 동안 **몇 줄이 사라지는지** 칸 옆에 보여 준다

`_excCountHit` — 설명서 대신 이 숫자가 맞는지 틀린지를 알려 준다.
못 알아먹는 글자면 "이름을 못 알아듣습니다". 상한 5만 자리(칠 때마다 도는 자리라 느린 PC 고려).

### ④ 물음표 단추 → 본보기 6개. 누르면 **두 칸에 그대로 들어간다.**

## 변경 파일

| 구분 | 파일 |
|---|---|
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/index.html` (칸 두 개 · 물음표 · 개수 자리) |
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` (제외 규칙 묶음 통째 교체 + 배선) |
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/css/index.css` (칸 두 개 · 본보기 목록) |
| 백업 | `_index.html.20260911.glob.bak` · `js/_index.js.20260911.glob.bak` · `css/_index.css.20260911.glob.bak` |

## 손댔다가 되돌린 것 (다음 agent 는 반드시 읽을 것)

제외 규칙 묶음을 **통째로 갈아 끼우면서 그 사이에 있던 「배열 짝짓기」 함수 7개를 같이 지웠다.**
(`_cmpArrKeyOf` · `_cmpArrMap` · `_cmpArrKeyUnique` 등 — 부르는 자리만 남고 정의가 사라졌다.)
**`node --check` 는 통과했다** — 함수가 없어도 문법은 맞기 때문이다. 시험이 잡았다.
백업에서 그 묶음을 그대로 오려 원래 자리에 되돌렸다(4,237자).

**교훈: 큰 묶음을 통째로 교체할 때는 그 범위 안에 다른 기능이 끼어 있는지 먼저 확인하고,
바꾼 뒤에는 `node --check` 만 믿지 말고 기존 시험을 전부 다시 돌릴 것.**

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 줄바꿈(CRLF) 깨짐 | 없음 |
| 화면 id ↔ 코드 1:1 (제외 규칙 22개) | 통과 |
| **칸 두 개 시험 21건** (장군님 예시 그대로) | **전부 통과** |
| **정규식/별표 자동 판별 시험 12건** | **전부 통과** |
| 배열 짝짓기 시험 13건 (되살린 뒤 재확인) | 전부 통과 |
| **실제 화면** | **미검증** — 앱 재시작 필요 |

칸 두 개 시험 = `oAPP.attr` + `_*` 로 **바로 밑·더 아래·배열 밑 밑줄 항목이 전부 빠지고**,
가운데 밑줄(`T_UI`)과 다른 root 밑 밑줄(`oAPP.DATA._hidden`)은 **안 빠지는 것**까지 확인.
몇 줄 사라지는지 세는 것도 실제 개수로 확인(6줄 · 3줄 · 0 · 못 알아먹으면 -1).
시험 파일 = scratchpad `test_two.js` · `test_auto.js`.

## 아직 안 한 것

- **옛 시험 2개가 낡았다**(`test_excfile.js` 4건 · `test_exclude_key.js` 6건 실패).
  규칙이 글자 배열 → `{U,N}` 으로 바뀌어 **시험이 옛 모양을 기대**하는 것이지 코드 결함이 아니다.
  실제 값은 `{"U":"","N":"^_"}` 로 정상 변환됐다. **시험 갱신 필요.**
- tree 우클릭 → 그 줄 기준 패턴 후보 + 개수 (다음)

---

# (이어서) 2026-09-11 22:40 — 3.0 에 심을 파일 이름을 영어로

## 요청

장군님: "js 이름을 좀 영어로 만들어 다른사람한테 전달할건데"

## 변경

| 옛 이름 | 새 이름 |
|---|---|
| `03_3.0에_심을_수집코드.js` | **`datamon-collector-ws30.js`** |
| `04_3.0_보내기만.js` | **`datamon-sender-ws30.js`** |

- 만드는 쪽(scratchpad `build_30.py`)의 내보내는 자리도 새 이름으로 바꿨다 — 다시 만들어도 그 이름이다.
- 문서·파일 안에서 옛 이름을 가리키던 자리 2곳도 같이 고쳤다.
- 내보내기 전에 **다시 만들었다** — 그동안 바뀐 수집 규칙이 전부 들어갔다.

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` (두 파일) | 통과 |
| 수집 규칙이 4.0 과 같은지 (`APPID`·`GUINR` 빼기 / 밑줄 두 개 빼기 / 모델은 oData 만 / UI 부품 화이트리스트) | 전부 들어감 |
| **4.0 수집 함수가 글자 그대로인지** (`_snap` · `_isSkipKey` · `_snapUiPart` · `_pick`) | **4개 블록 전부 일치** |
| 옛 이름을 가리키는 자리가 남았는지 | 0건 |

---

# (이어서) 2026-09-11 23:30 — 제외 규칙을 **칸 하나**로 (path 에 별표)

## 지시

장군님: "input 이 두개가 있으니까 더 헷갈리는데.. input 하나만 두고 좀 쉬운 정규식으로 안되냐?
전체 내용중에 언더바 제거하려면 `*_*` 이렇게 준다던지..
특정 하위에서만 언더바 제거하고 싶으면 `oAPP.attr._*` 이렇게 하던지"

**장군님이 낸 방식 그대로 만들었다.** 칸 둘로 나눈 것은 내 판단이었고 더 헷갈렸다.

## 적는 법 — 두 줄이면 끝난다

1. **별표(`*`)를 쓰면 path 전체**에 맞춘다. `*` = 아무 글자 여러 개 / `?` = 한 글자
2. **별표가 없으면 이름**이 그것인 줄을 뺀다

| 적는 것 | 뜻 |
|---|---|
| `*_*` | 어디에 있든 밑줄이 든 것 전부 |
| `oAPP.attr._*` | oAPP.attr **바로 밑**에서 밑줄로 시작하는 것 |
| `oAPP.attr.*_*` | oAPP.attr 밑이면 **깊이 상관없이** 밑줄이 든 것 |
| `oAPP.DATA*` | oAPP.DATA 와 그 아래 통째로 |
| `UIATV` | **이름**이 UIATV 인 줄 (어디에 있든) |
| `^_` · `/^_/` | 정규식도 그대로 (AI 에게 받은 것을 붙여 넣어도 된다) |

- 정규식 판별은 `^ $ [ ] ( ) { } | \ +` 가 보이면. `*` `?` `.` 는 별표 방식 글자라 판별에서 뺐다.
- 정규식은 **path 와 이름 둘 다** 맞춰 본다(AI 가 `^_` 를 주면 이름에 걸려야 한다).
- **점(`.`)은 별표 방식에서 그냥 점**이다 — 정규식 함정이 없다.
- 칸 옆에 **어떻게 읽었는지(path/이름/정규식) + 몇 줄이 사라지는지** 를 치는 동안 보여 준다.
- 물음표 단추 → 본보기 7개 + 맨 위에 규칙 두 줄. 누르면 그대로 칸에 들어간다.
- 예전에 칸 두 개로 적어 둔 것({U,N})도 읽어서 한 줄로 합친다(파일 호환).

## 변경 파일

| 구분 | 파일 |
|---|---|
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/index.html` (칸 하나로) |
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/js/index.js` (제외 규칙 묶음 통째 교체 + 배선) |
| 변경 | `www/ws30/ws10_20/Popups/dataMonitor/Popup/css/index.css` (본보기 안내 줄) |
| 백업 | `_index.html.20260911.one.bak` · `js/_index.js.20260911.one.bak` |

## 또 같은 실수를 했다 (기록해 둔다)

제외 규칙 묶음을 통째로 갈아 끼우면서 **배열 짝짓기 함수 7개를 또 지웠다.**
직전 turn 에 똑같이 겪고 이력에 적어 뒀는데도 반복했다. **`node --check` 는 또 통과했다.**
시험(`test_arrkey.js`)이 잡아서 백업에서 되살렸다(4,237자).

**다음 agent 는 이 파일의 제외 규칙 묶음을 교체할 때 반드시:**
1. 교체 범위 안에 **배열 짝짓기 묶음**이 들어 있는지 먼저 확인할 것
2. 교체 뒤 `grep -c "_cmpArrKeyOf"` 로 **정의가 남았는지** 셀 것 (부르는 자리만 남으면 8, 정의까지 있으면 15)
3. `node --check` 만 믿지 말고 **기존 시험을 전부 다시 돌릴 것**

## 검증

| 항목 | 결과 |
|---|---|
| `node --check` | 통과 |
| 줄바꿈(CRLF 3,188 / 깨짐 0 / 순수 LF 0) | 정상 |
| 화면 id ↔ 코드 1:1 (제외 규칙 20개) | 통과 |
| 옛 칸 두 개 자취(`excUnder`)가 남았는지 | **0건** |
| **칸 하나 시험 30건** (장군님 예시 그대로) | **전부 통과** |
| 배열 짝짓기 시험 13건 (되살린 뒤 재확인) | 전부 통과 |
| **실제 화면** | **미검증** — 앱 재시작 필요 |

시험 30건 = 장군님이 낸 `*_*` · `oAPP.attr._*` 두 가지를 그대로 넣어
**걸려야 할 것이 걸리고 안 걸려야 할 것은 안 걸리는지** 전부 확인.
그 밖에 깊이 무관(`oAPP.attr.*_*`) · 통째로(`oAPP.DATA*`) · 이름만(`UIATV`) ·
정규식(`^_`, `/…/`) · 점이 글자 그대로인지 · 몇 줄 사라지는지 · 옛 규칙 읽어 오기.
시험 파일 = scratchpad `test_one.js`.
