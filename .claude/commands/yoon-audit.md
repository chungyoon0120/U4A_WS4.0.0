---
description: 버그 코드번호(br19 등) 단위로 수정→검수요청→검수결과 반영→수정완료 왕복. 인자로 4단계 분기.
argument-hint: <코드번호> <고쳐라 | 검수 작성해라 | 결과 확인해라 | 수정 완료해라>
allowed-tools: Read, Write, Edit, Grep, Glob, Bash, mcp__notion-multi__list_workspaces, mcp__notion-multi__query_database, mcp__notion-multi__get_page, mcp__notion-multi__update_page_properties, mcp__notion-multi__append_text
---

사용자 입력: `$ARGUMENTS`

**입력이 `help`(또는 비어 있음)이면, 아래 "분기 0 — help"의 사용법 표만 그대로 출력하고 끝낸다.** 다른 작업은 하지 않는다.

그 외에는 **먼저 `.audit/README.md` 규약을 읽는다.** 이 명령의 모든 단계는 그 규약(폴더 구조·파일명·역할 경계)을 따른다.

입력에서 **① 코드번호(예: br19) ② 동작**을 파악해 아래 4갈래 중 하나만 수행한다.
- 코드번호가 없으면 직전 대화에서 다루던 코드번호를 쓰되, 불명확하면 되묻는다.
- 동작 문구가 애매하면 되묻는다. **한 번에 한 단계만** 한다(여러 단계 몰아서 금지).
- 폴더명은 코드번호를 소문자로 통일한다(예: `.audit/br19/`).

### 노션 대상 (고정)

이 명령의 **모든 노션 작업**은 `notion-multi` MCP의 **U4A 워크스페이스**에서, 아래 **이슈 리포트 DB만** 바라본다. 다른 워크스페이스·다른 DB는 쓰지 않는다.
- **`database_id` = `bdd7e18b-1cd6-8396-a2b0-81900fb593d1`** (이슈 리포트 DB. 이 ID로 바로 접근한다).
- 그 DB의 속성: 코드번호 = **`코드`**(예: `BR19`), 상태 = **`상태`**(예: `수정완료`·`접수`), 내용 = **`내용`**, 화면 = **`화면`**.
- 모든 `query_database`·`get_page`·`update_page_properties` 호출에 `workspace:"U4A 워크스페이스"`을 넣는다.

---

## 분기 0 — "help" (사용법 안내)

아래 표와 흐름을 그대로 출력한다. 파일 생성·수정 등 다른 작업은 하지 않는다.

**`/yoon-audit <코드번호> <동작>` — 버그 하나를 수정→검수→반영→완료까지 왕복하는 명령.**

| 입력 예시 | 하는 일 |
|---|---|
| `/yoon-audit br19 고쳐라` | 이슈 리포트 DB에서 br19 내용 읽고 → 원본·`.analy` 기준으로 코드 수정 |
| `/yoon-audit br19 검수 작성해라` | `.audit/br19/01_request.md`(검수 요청서) 작성 후 멈춤. 검수는 코덱스·안티가 함 |
| `/yoon-audit br19 결과 확인해라` | 코덱스·안티 검수결과(`02_audit_*.md`) 둘 다 읽고 `03_response.md`로 반영·해명 + 테스트 시나리오 작성 |
| `/yoon-audit br19 수정 완료해라` | 이슈 리포트 DB의 `상태`를 "수정완료"로 바로 변경 + 테스트 그룹 완료 시 히스토리 이관 |
| `/yoon-audit help` | 이 사용법 표 |

**진행 순서:** ① 고쳐라 → ② 검수 작성해라 → (코덱스·안티가 각자 검수) → ③ 결과 확인해라 → ④ 수정 완료해라

**주의:** 각 동작에 코드번호를 붙인다. 검수는 Claude가 대신하지 않고 코덱스·안티 세션에서 직접 시킨다.

---

## 분기 1 — "고쳐라" (버그 수정)

1. `notion-multi`로 **U4A 워크스페이스의 이슈 리포트 DB**에서 해당 코드번호 행을 읽는다(위 "노션 대상(고정)" 참조).
   - `query_database {workspace:"U4A 워크스페이스", database_id:"bdd7e18b-1cd6-8396-a2b0-81900fb593d1", match_text:"<코드번호>"}`로 `코드`가 그 번호인 행의 `page_id`를 잡고 → `get_page {workspace:"U4A 워크스페이스", page_id}`로 **내용·상세**를 읽는다.
2. 읽은 버그 내용대로 **코드를 수정**한다.
   - 기준은 항상 **원본(as-is) + `.analy` SSOT**. 원본에 없는 UX·동작 임의 추가 금지.
   - 화면/UI 관련이면 `.analy` 해당 문서를 먼저 읽고 규칙대로.
3. **상태는 바꾸지 않는다.** 상태 "수정완료" 변경은 분기 4에서, 장군님 테스트 통과 후에만.

---

## 분기 2 — "검수 작성해라" (검수 요청서 작성)

1. `.audit/README.md` 규약대로 **코드번호를 폴더명으로** `.audit/<코드번호>/` 폴더를 만들고 그 안에 `01_request.md`를 작성한다.
2. `01_request.md`에 담을 것(규약 §3 형식):
   - **검수 대상**: 파일 경로·함수명·기능 이름
   - **변경 요약**: 무엇을 왜 바꿨나 (원본 대비)
   - **검수 포인트**: 정확성 버그·원본 1:1 여부·공통 자산 소비·반응형 등 봐달라는 항목
   - **근거**: `.analy` 절 번호 / 원본 파일 경로
3. `.audit/00_현황판.md`에 해당 코드번호 행을 추가/갱신(검수요청 칸 `✅`).
4. **★역할 경계(강제): 여기까지만 한다.**
   - ❌ `02_audit_codex.md`·`02_audit_agy.md`(검수 결과)를 **직접 쓰지 않는다**.
   - ❌ 검수를 대신하려고 `codex`·`agy` CLI를 **돌리지 않는다**(`codex-agent`·`agy-agent` 스킬도 여기선 쓰지 않음).
   - 검수는 장군님이 코덱스·안티 세션에서 직접 시킨다.

---

## 분기 3 — "결과 확인해라" (검수 결과 반영·해명)

1. `.audit/<코드번호>/`의 `02_audit_codex.md` + `02_audit_agy.md`를 **둘 다** 읽는다(있는 것만).
2. 두 검수의 지적을 **한 표로 합쳐** `03_response.md`를 작성한다: 항목별 **어느 검수자 / 반영함 / 반론(이유)**. 같은 지적이면 한 줄로.
3. 반영이 필요한 지적은 **코드를 수정**한다(원본·`.analy` 기준). 반론이면 이유를 남긴다.
4. `.audit/00_현황판.md`의 보완·해명 칸 갱신.
5. **수정을 마쳤으면 테스트 시나리오를 작성**한다. 저장 위치는 `.audit`가 **아니라** 테스트 관리 표준 폴더인 **`.works/<화면영역명>/00_현황판.md`**다.
   - 버그가 속한 **화면 영역 폴더**를 찾아 그 `00_현황판.md`에 항목을 추가한다(예: `.works/미리보기위치이동/`, `.works/bindpopup/`, `.works/ws20attrchange/`). 그 영역 폴더가 없으면 새로 만든다.
   - 새 미테스트 그룹/항목은 **현황판 맨 위**('지금 할 것')에 둔다. 그룹 안 항목은 코드 번호 순서 그대로, 자리 이동 금지.
   - 형식: `| 코드 | 동작 | 조작·확인 포인트 | O/X(☐) |`, 각 행 끝 `☐`. 산문·`- [ ]` 나열 금지.
   - 조작은 **①②③ 순서로 쪼개고**, 버튼·영역은 **화면에 적힌 이름 그대로**, **쉬운 말**로. 제3자가 처음 봐도 따라할 수준.
   - **실제 화면 조작으로 재현되는 것만** 적는다(정상 조작으로 못 가는 내부 방어분기는 넣지 않는다).
   - 작성한 표는 **답변에도 함께** 보여주고, 그 `00_현황판.md` 경로를 링크로 안내한다.

---

## 분기 4 — "수정 완료해라" (상태 변경)

1. **별도 질문 없이 바로** `notion-multi`로 **U4A 워크스페이스의 이슈 리포트 DB**에서 그 행의 `상태`를 "수정완료"로 바꾼다(위 "노션 대상(고정)" 참조). (이 명령을 실행한 것 자체가 장군님의 지시다.)
   - `query_database {workspace:"U4A 워크스페이스", database_id:"bdd7e18b-1cd6-8396-a2b0-81900fb593d1", match_text:"<코드번호>"}`로 `코드`가 그 번호인 행의 `page_id`를 잡는다.
   - `update_page_properties {workspace:"U4A 워크스페이스", page_id, properties:{상태:"수정완료"}}`로 `상태`를 "수정완료"로 변경.
2. 변경 결과(성공/실패 + 노션 URL)를 보고한다.
3. **테스트 현황판 정리(히스토리 이관).** 이 코드번호가 속한 그룹의 **모든 항목이 통과(✅ O)됐으면**, `.works/<화면영역명>/00_현황판.md`에서 그 그룹을 **그룹째** `.works/<화면영역명>/00_히스토리.md`로 옮긴다.
   - 그룹 중 일부만 통과(✅ O)한 상태면 **이관하지 않는다**(그 자리 표시만 유지). 그룹 전 항목이 ✅ O일 때만 그룹째 이관.
   - `00_히스토리.md`가 없으면 새로 만든다.

---

## 공통

- 각 단계 끝에 **무엇을 했는지 한 줄 보고**(만든 파일 경로 / 바꾼 상태 / 남은 단계).
- 규약과 어긋나거나 추측한 부분이 있으면 함께 밝힌다.
