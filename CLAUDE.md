# 프로젝트 작업 규칙

## ★ 최우선 규칙 (필수)

- **★원본(as-is) 소스 위치 = 두 곳.** 담당 개발자가 둘이라 영역별로 나뉘어 있다.
  - ① 전체 = `C:\Users\socce\Documents\Github\U4A_WS3.0.0`
  - ② **디자인 영역(`design/`) + 담당 팝업 5개** = `C:\Users\socce\Documents\Github\U4A_WS_DESIGN`
    (값도움 OTR 팝업 / 바인딩 팝업 / 테마 만들기 / 옵션 팝업 / WebDynpro 변환 기록)
    → 이 영역은 **② 를 먼저 본다.** ② 가 상류이며 실측상 더 앞서 있다(디자인 영역에만 있는 폴더 5개, 파일 9개 상이).
  "원본 참고" 지시는 **이 두 곳에서만 읽는다. 읽기 전용 — 쓰기 금지.** 폴더 속 구조가 작업 폴더와 같다.
  **작업 폴더(`U4A_WS4.0.0\www`)를 원본으로 믿지 말 것** — 원본 파일 173개가 이미 수정돼 섞여 있다.
  `C:\Users\socce\Documents\U4A_WS3.0` 도 **원본이 아니다**(원본 복사본에 변환을 일부 진행, 22개 수정됨).
  상세 = `.analy/18_원본소스_위치_SSOT.md`
- **철저히 원본(as-is) 소스 기반으로 먼저 작업한다.** 원본에 있는 속성·동작·구조를 그대로 이식하고,
  원본에 **없는 UX·요소·옵션·스타일·레이아웃을 임의로 추가하거나 변경하지 않는다.**
  (예: 원본에 없는 아이콘/빈상태 디자인/iframe sandbox/건수 배지/레이아웃 재해석 등 임의 창작 금지.)
- **★★작업 폴더 안은 전부 고쳐도 된다 — 사본 폴더 포함**(장군님 지시 2026-09-01, 이전 지시 대체).
  진짜 원본은 **다른 폴더·다른 깃**으로 따로 관리하므로, 현재 프로젝트 폴더(`U4A_WS4.0.0`) 안에 있는
  파일은 **사본 폴더(`www/ws30/ws10_20/design/`)든 그 밖이든 수정 가능**하다.
  - **앞으로 진짜 원본이 바뀌어도 소스를 통째로 옮기지 않는다.** 목표는 **소스 동기화가 아니라
    「기능이 같으면 성공」**이다. 진짜 원본을 **참고**해서 사본 폴더 안 로직을 고친다.
  - 이유: 사본 파일도 지금 실제로 실행·참조되고 있고 그중 이미 고친 부분도 있어서,
    진짜 원본이 바뀌었다고 무차별로 갈아엎는 건 말이 안 된다.
  - ※ **진짜 원본 폴더 2곳은 여전히 읽기 전용**(위 항목). 거기에 쓰지 않는다.
  - ※ 새 로직은 되도록 내 파일(`js/ws_html5_*.js` 계열)에 두되, 사본 폴더 안을 고쳐야 할 땐 고친다.
  - (폐지) 2026-08-18 「원본 파일 한 줄도 금지 · 통째 덮어쓰기 대비」 지시 — 위 규칙으로 대체.
    옛 잔여 목록 `.works/원본덮어쓰기/00_현황판.md` 은 이력용.
- 이상한 점·개선안이 보여도 **코드에 반영하지 말고 보고만** 하며, **별도 지시가 있을 때만** 적용한다.
  확신이 없으면 만들지 말고 **먼저 묻는다.**
- 공통 UX 표준(`.analy/16`·공통 컴포넌트) **소비**는 하되, 원본에 없는 신규 UX **창작**은 하지 않는다.

## 소스 탐색 규칙 (필수)

- 기존(as-is) 소스 중, **폴더명이 `_`(언더바)로 시작하는 폴더는 탐색 대상에서 제외**한다.
  (백업·구버전·실험/폐기 코드이며, 현행 산출물이 아니다. 예: `_backup_ui5_*`,
  `_module_dialog_pattern`, `_ServerList_ui5` 등.)
- 검증·참조·변환 근거를 찾을 때 이 폴더들의 내용을 현행으로 인용하지 않는다.

## UX / 화면 / 컨버전 작업 규칙 (필수)

이 프로젝트의 UX·화면·UI5→HTML5 컨버전 표준의 **단일 출처(SSOT)는 `.analy/` 문서 세트**다.
화면, UI, 레이아웃, 모달/팝업, 트리, 폼, 입력칸, 그리드 등 **화면 요소를 만들거나
U4A WS → HTML로 변환하는 모든 작업**은 아래 절차를 따른다.

### 1. 시작 전 반드시 읽기
- `.analy/00_개요_및_컨버전전략.md` — 문서 인덱스로 어떤 문서를 봐야 하는지 먼저 파악한다.
- `.analy/13_AI_작업지시_가이드.md` — AI 작업 방식·지시 템플릿·가드레일. 이 문서의 원칙을 그대로 따른다.
- **`.analy/16_공통_화면UX_표준.md`** — 모든 화면 공통 표준(Bootstrap 베이스, 모달 헤더/푸터 48px,
  드래그·리사이즈, 트리=서버리스트). 화면 작업 시 항상 함께 적용한다.
- 작업 대상에 해당하는 **기능 문서 + `.analy/12_테마_컨버전_전략.md`(테마)**를 끝까지 읽고 시작한다.
  - 입력칸(input) 관련: `.analy/15_공통_입력UX_가이드.md`
  - 팝업/모달: `.analy/06_팝업.md`
  - 로그인/서버리스트(트리 포함): `.analy/02_로그인_서버리스트.md`
  - 디자인 영역: `.analy/05_디자인영역.md`
  - 그 외 매핑은 13번 문서 3절의 표를 참고한다.

### 2. 진행 방식 (13번 문서 원칙)
- **한 번에 작업 단위 하나.** "전부 컨버전"이 아니라 해당 문서의 `AI 컨버전 실행 사양` 절 하나씩.
- 각 문서의 작업 순서를 그 순서대로 수행하고, **산출물 트리에 명시된 파일만** 만들거나 고친다.
- 모든 설계 결정은 **문서의 절 번호로 근거**를 댄다. 문서에 없으면 **추측하지 말고 사용자에게 묻는다.**
- 모양/동작은 화면마다 새로 만들지 말고 **공통 자산(`shell.css`, `u4a-ui.js`, `tokens.css`)을 소비**한다.
- 색은 `theme/tokens.css`의 의미 토큰만 사용한다. **하드코딩(hex) 금지**, 문구는 메시지 키 사용.
- **반드시 반응형** — 고정 px 폭 금지(12번 7장).

### 3. 완료 보고
- 기능 인벤토리(기존 동작 → 구현 위치 → 검증, 누락 0)
- 각 단계 수용 기준 통과/실패 표
- 12번 6.6 체크리스트(A~J) 점검 결과
- 문서와 어긋났거나 추측한 부분 목록

## superpowers 스킬 운용 규칙 (필수)

이 프로젝트에는 superpowers 플러그인이 설치돼 있으나, **UI5→HTML5 변환 작업의
정답은 항상 원본(as-is) + `.analy` SSOT**이므로 다음과 같이 제한적으로만 사용한다.

- **brainstorming(발산/설계)은 건너뛴다.** 변환은 창의가 아니라 원본 이식이므로,
  설계 발산 단계의 결과물은 이미 원본과 `.analy` 문서로 확정돼 있다. 발동하더라도
  "정답은 원본"으로 즉시 통과시키고 다음 단계로 넘어간다. **임의창작 금지 규칙이 상위.**
- 실제 파이프라인: **writing-plans(계획, `.analy` 절 번호 근거) →
  executing-plans / subagent-driven-development(구현) →
  verification-before-completion + systematic-debugging(검증)**.
- 이 파이프라인은 기존 규칙(항목별 테스트 게이트, 로직+배선 감사, 소스 기반 검증)과
  동일한 결이며, **`.analy` SSOT·원본우선 규칙이 어떤 스킬보다 항상 최상위**다.
