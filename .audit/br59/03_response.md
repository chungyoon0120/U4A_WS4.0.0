# BR59 검수 결과 반영·해명 (03)

## 종합
두 검수 **모두 통과(Pass)·필수 수정 0건**. 반영할 코드 수정 없음(현재 구현이 원본 산식과 1:1 일치).
- **코덱스**: 통과. 그룹 위치 산식·UA026 차감·미리보기 호출 가드·양 경로(Up/Down·Move Position)·미리보기 index 소비·T_9011 미로드·클로저 안전성까지 전부 통과. 독립 서브에이전트 2기 반박 재검수도 통과 유지.
- **안티(Antigravity)**: 통과. 서브에이전트 2기 심층 팩트체크 — 그룹 index/UA026 감산 공식 원본 1:1, 경계 조기 탈출·음수 index 차단, D&D 형제 경로와 수학적 동등성, 동기 트랜잭션 무결성 전부 통과. 지적 0.

## 지적별 반영/반론 (두 검수 취합)

| # | 지적/검토 항목 (검수자) | 판정 | 조치 |
|---|---|---|---|
| 1 | 그룹 위치 산식 `_aggrPos` = 원본 `contextMenuUiMove` `l_indx1`/`l_indx2` 1:1 (코덱스·안티) | 통과 | 변경 없음. 같은 `UIATT` 필터 후 OBJID 위치. |
| 2 | UA026 차감 `_aggrPrevIndex` = 원본 `l_cnt`(같은 그룹 위치 − 앞쪽 UA026 수) 1:1, 판정식 `CATCD==="UA026" && FLD02!=="X"`, `FLD01===UILIB` (코덱스·안티) | 통과 | 변경 없음. |
| 3 | 미리보기 호출 가드 `iAggrBefore !== iAggrAfter` = 원본 `l_indx1 !== l_indx2` (코덱스·안티) | 통과 | 변경 없음. 다른 그룹 형제만 지나친 이동은 미리보기 no-op이 맞음(미리보기가 그룹별로 자식 소유). |
| 4 | 트리 splice(전체 배열) 유지 = 원본 의미 동일 (코덱스) | 통과 | 변경 없음. 트리는 전체 자식 순서 표시, 미리보기 index만 그룹 기준 보정. |
| 5 | 양 경로 커버리지(트리·미리보기 Up/Down 위임, Move Position 0-based 위임) (코덱스·안티) | 통과 | 변경 없음. 두 경로 동일 보정 산식 공유. |
| 6 | 경계 조건(첫/마지막·단일 자식·동일 위치·음수 index·대상 자신이 UA026) (코덱스·안티) | 통과 | 변경 없음. 경계 가드가 busy/Undo 앞에서 조기 return, `iPos<0→0`·`iIdx<0→0` 이중 방어. |
| 7 | T_9011 미로드 시 UA026 차감만 생략(빈 배열), 그룹 기준 index는 유지 (코덱스) | 통과(강건성 참고) | 변경 없음. 정상 라이브러리 계약에서는 배열이라 무해. BR59 핵심(그룹 기준 위치)은 손상 상태에서도 유지. |
| 8 | 클로저 안전성(`sUilib` 매 반복 캡처, 동기 findIndex) (코덱스·안티) | 통과 | 변경 없음. |
| 9 | 기존 후처리(Undo·splice·refresh·선택·change flag·바인딩 팝업·`_broadBusy` try/finally) 유지 (코덱스) | 통과 | 변경 없음. |

## 반영한 수정 위치
- **없음.** 두 검수 모두 통과·필수 수정 0건. `br59 고쳐라` 단계의 구현이 그대로 유지된다.
  - `www/ws30/ws10_20/js/ws_html5_ws20_edit.js` — `_aggrPos`/`_aggrPrevIndex` 신규, `_moveUI`·`fnWs20MoveUIToIndex` 미리보기 index 보정. (변경 없음)
- `node --check` 통과(코덱스가 edit.js·prev.js 둘 다 재확인).

## 남은 것
- 실화면 테스트: `.works/미리보기위치이동/00_현황판.md` BR59-1~5 (본 03 단계에서 작성). 앱을 껐다 켠 뒤 확인.
