# BR25 보완·해명 (03_response)

## 검수 종합
- **코덱스: 통과 · 지적 없음.**
- **안티: 통과 · 지적 없음.**
- 두 검수 모두 "확인창 직후 `_busy(false)`가 원본 `attrBindCallBackAggr`(uiAttributeArea.js:4753·4831)의 `setBusy("")`와 시점·동작 1:1 일치"로 판정. BUSY 짝 대칭, 공통 자산 미수정, 메시지 키(122+123) 보존, 오류 삼킴 없음, `node --check` 통과 확인.

## 지적별 반영/반론 표

| # | 검수자 | 지적/참고 | 반영 여부 | 사유 |
|---|---|---|---|---|
| 1 | 코덱스·안티 | 정확성·원본 1:1·BUSY 대칭·공통 미수정·구문 — 전부 통과, 수정 요구 없음 | 해당 없음 | 지적 0건 |
| 2 | 코덱스(잔여 참고) | 공통 확인창이 `<dialog>` 미지원/`showModal()` 실패 시 동기 `window.confirm()`으로 폴백하면, YES 콜백이 `_confirm()` 반환 전에 실행돼 뒤따르는 `_busy(false)`가 콜백의 재잠금을 다시 꺼버릴 수 있음 | **미반영(보고만)** | ① 대상 환경(Electron Chromium)은 `<dialog>.showModal()` 지원 → BR25 재현 경로에 **해당 없음**(코덱스도 "결함으로 판정하지 않음"). ② 원본 `attrBindCallBackAggr`도 동기 폴백 정규화 코드가 없음 → 여기서 추가하면 **원본에 없는 방어 창작**(임의창작 금지 규칙 위반). ③ 원본 1:1 유지가 상위 원칙이므로 손대지 않음. 필요 시 별도 지시로 검토. |

## 반영한 수정
- 없음(두 검수 모두 통과, 수정 요구 지적 0건).
- 이번 변경분은 분기 1에서 이미 반영 완료: `www/ws30/ws10_20/js/fnBindPopupOpen.js` 공유 확인창 함수 `_confirm` 말미에 `_busy(false)` 1줄 + 주석. (원본 uiAttributeArea.js:4753·4831 1:1)

## 결론
- 코드 추가 수정 없이 실화면 테스트 단계로 진행. 테스트 시나리오는 `.works/bindpopup/00_현황판.md`에 추가.
