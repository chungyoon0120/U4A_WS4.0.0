# BR41 검수 결과 반영·해명 (03)

## 종합 판정
- **코덱스: 통과 / 지적 없음** (서브에이전트 3축 독립 재검수도 통과 유지 — 원본↔현행 무작위 500상태 비교, 전체 모듈 단건·멀티 VM 시험 PASS)
- **안티: 통과 / 지적 없음** (제안 3건, 모두 비차단)

두 검수 모두 필수 지적 0. 핵심 로직(삭제 대상 키 정확성·하위 재귀·prev 삭제 전 호출 순서·원본 1:1·삭제 경로 전용·중복 무해·클로저 안전·가드)은 양측 통과.

## 지적·제안 취합표

| # | 출처 | 항목 | 처리 | 이유 |
|---|---|---|---|---|
| 1 | 코덱스 관찰1 / 안티 제안1 | 동일 키 중복 T_CEVT 행이 있으면 `findIndex+splice`가 최초 1건만 지움 (`filter`로 전건 제거 제안) | **반론(미반영)** | 원본 `delUiClientEvent`가 `findIndex` 1건 삭제 = 원본 계약. `filter` 전건 삭제는 원본에 없는 동작(임의창작 금지). 정상 편집기는 같은 키를 덮어써 중복을 안 만들어 정상 데이터에서 재현 불가(양측 동일 인정). |
| 2 | 안티 제안2 | 삭제 호출부 `catch (e) { }`가 오류를 조용히 삼킴 → `console.error` 로깅 권장 | **반영** | 프로젝트 규칙 `code.md`(오류 삼킴·조용한 catch 금지, 표면화)와 합치. BR41 호출 2곳에 `console.error` 추가. |
| 3 | 안티 제안3 | UI5 전용 `uiDesignArea.js`가 뒤늦게 로드되면 그쪽 구버전 함수가 이식본을 덮어쓸 위험 | **반론(미반영)** | 현행 HTML5 호스트에 `uiDesignArea.js` 미로드(실영향 0, 안티도 인정). 이식 정의는 `if (typeof … !== "function")` 가드로 등록 → 이미 있으면 재정의 안 함. 가정적 빌드정책 변경 대비는 별건 권고로 남김. |
| 4 | 코덱스 관찰2 | HM 행이 있어도 `prev._T_0015`가 빈 배열이면 원본·현행 모두 선행 return | **반론(무변경)** | 원본 순서 1:1(원본도 `_T_0015` 빈 배열이면 HM 블록 전에 return). 정상 HTML 저장은 content 속성행을 함께 갱신하므로 정상 흐름 실패 근거 없음. |
| 5 | 코덱스 관찰3 | 호출부 catch가 예외를 삼켜 진단성 낮음 | **반영(#2로 해소)** | #2 로깅 추가로 진단성 확보. |

## 반영 코드 수정
- `www/ws30/ws10_20/js/ws_html5_ws20_edit.js`
  - 394행 `_removeNodePreview` 내 `delUiClientEvent(n)` 호출 catch → `console.error("[HTML5][BR41] delUiClientEvent cleanup error:", e)`
  - 414행 `_purgePrevSubtree` 내 `delUiClientEvent(c)` 호출 catch → 동일 로깅
- 핵심 로직(이식 함수 본문·삭제 배선·호출 순서)은 **변경 없음** — 양측 통과분 유지.
- `node --check` 통과.

## 결론
- 필수 반영 = 오류 표면화 로깅 1종(안티 제안2). 나머지(중복 filter·오버라이드 게이트·HM 선행return)는 원본 1:1·현행 구조상 반론.
- 코드 동작 = 원본 `delUiClientEvent` 계약과 동일. 다음 = 실화면 테스트(`.works/ws20attrchange/00_현황판.md`).
