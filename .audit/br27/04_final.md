# 04_final — BR27 최종 검증

## 최종 판정: 통과

- 코덱스(02)·안티(02): P1(재오픈 세션경계 유령 선택) 지적 → **03에서 반영**.
- 반영 내용:
  1. 필드 목록에 "보이는 만큼만 그리기"(가상) 적용 — 원본 sap.ui.table.TreeTable 가상 성질 복원.
  2. 공통 트리 "줄 선택"이 화면 밖 왕복 후에도 강조 유지(가상일 때만, 가드) — u4a-ui selectByKey.
  3. 재오픈 세션경계에서 이전 선택 명시 해제(유령 강조 방지) — lf_renderTree.
  4. 선택 불가 줄 클릭 시 선택 해제 통일 — lf_selRow.
- 반론(03): "이전 바인딩 줄을 활성일 때만 선택·스크롤" 게이팅은 원본 스크롤(setFirstVisibleRow) 회귀를 유발해 **미채택**(원본 그대로).
- `node --check` 두 파일 통과.

## 상태
- 실화면 테스트 BR27-1~6 **전 항목 통과**(2026-08-13) → `.works/bindpopup/00_히스토리.md` 이관.
- 노션 이슈 리포트 DB BR27 = **수정완료**.

## 변경 파일
- `www/ws30/ws10_20/js/fnBindPopupOpen.js` — 가상 켬 + 재사용 세션 선택 초기화 + 선택 불가 줄 클릭 해제 통일.
- `www/ws30/ws10_20/theme/u4a-ui.js` — 공통 트리 selectByKey 가상 선택 강조 영속(가드).
