# 03_response — BR44 검수 결과 반영·해명

## 요지
- **안티=통과(Pass)**, **코덱스=수정필요(P1 1건, 독립 서브에이전트 교차확인)**.
- BR44 본체(UW03/UW08/UW10 세 검사의 정적 이식 — 순서·파라미터·메시지·fail-closed·busy)는 **양측 모두 원본 1:1 확인**.
- 코덱스 P1(개인화 조회 await 동안 대상 삭제 시 고아 삽입)은 **두 갈래로 처리**:
  **(a) await 후 대상 존재 재확인 = 반영**(고아 삽입 차단), **(b) 여섯 검사 전체 재검증 + WS20 전체 잠금 = 반론**(원본에 없는 과한 방어·선재 공통, 별건 권고). BR43 P1 처리와 동일 결.

## 지적 통합 표

| # | 검수 항목 | 검수자 | 판정 | 처리 |
|---|---|---|---|---|
| 1 | 세 검사 순서 UW03(5079)→UW08(5110)→UW10(5142)·조건·파라미터 원본 일치 | 코덱스·안티 | 통과 | — |
| 2 | 214 치환 인자(is_0022.UIOBJ, is_tree.OBJID, is_0023.UIATT) 원본 삽입경로 일치. D&D 는 OBJID·OBJID 지만 삽입은 생성 전이라 UIOBJ(클래스명) 사용이 원본 의도 | 코덱스·안티 | 통과 | — |
| 3 | UW03 자체 306 토스트, UW08/UW10 214 후 취소, 분기별 다음검사 미실행(중복 토스트 없음) | 코덱스·안티 | 통과 | — |
| 4 | fail-closed(designChkFixedParentUI 미정의 시 취소), checkDeny/Allow 동일파일 정의로 직접호출, helper 계약(없으면 false/true) 보존 | 코덱스·안티 | 통과 | — |
| 5 | 동기 거부가 프리셋 조회·Undo push·트리/미리보기 변경보다 먼저 종료, 호출측 `.then` 이 자식창 BUSY 해제 | 코덱스·안티 | 통과 | — |
| 6 | KEEP-UI5·메시지 DB 무수정, node --check 통과, 제품코드 무수정 | 코덱스·안티 | 통과 | — |
| 7a | **세 검사(및 카디널리티) 통과 후 개인화 조회 `await` 동안 대상 삭제 → 재개 시 화면 트리엔 없고 prev/미리보기에만 자식(고아). 저장 시 T_0014(현행 트리)엔 없고 T_0015(prev 전체 순회)엔 고아 포함 → payload 오염** | 코덱스(P1) | **반영** | await 완료 후·`fnWs20PushUndo()` 직전에 **대상 존재 재확인**(`_node(is_tree.OBJID)`), 없으면 조용히 취소(스냅샷·삽입 없음). 개인화 적용(`_isPresetAttr`) 경로에만 적용(await 가 그 경로에만 존재). 원본은 이 조회가 동기라 창이 없었음 → 비동기 이식으로 생긴 창을 막아 **원본의 원자성 재현**. |
| 7b | 변이 직전 cardinality·UA039·UA040·UW03·UW08·UW10·BR43 **전체 재검증** | 코덱스(P1 권고) | 미반영(과함) | 원본은 동기라 단 1회 검사만 함(재검증 없음). 비동기로 생긴 실제 결함은 "대상 소멸"뿐이고 7a 로 차단됨. await 중 동일 aggregation 상태변화(바인딩 추가 등)는 재현성 극저·원본도 미보호 → 전체 재검증은 원본에 없는 창작. |
| 7c | `designAddUIObject` 전체 수명 동안 **WS20 로컬 편집/Undo 잠금(작업 세대 토큰)** | 코덱스(P1 권고) | **반론** | 원본 UI5 에 없는 방어. BR44 가 만든 결함 아님(개인화 await 는 선재). 모든 삽입/편집 공통이라 BR44 국소 범위 밖 → **별건 권고**(WS20 삽입 중 메인 재진입 방지). 7a 로 BR44 경로의 고아는 차단. (BR43 6c 와 동일 입장.) |

## 반영 내역
- 파일: `www/ws30/ws10_20/js/ws_html5_ws20_edit.js` (`designAddUIObject`)
  - 개인화 조회 `await`(약 965) 직후·`fnWs20PushUndo()`(약 967) 직전에:
    ```js
    if (_isPresetAttr && !_node(is_tree.OBJID)) {
        console.warn("[HTML5][WS20][insert] 개인화 조회 대기 중 추가 대상이 사라짐 — UI 추가 취소(고아 삽입 방지)");
        return;
    }
    ```
  - `_node`(edit.js 51)는 현재 트리에서 OBJID 로 노드 조회(getTreeData 우선, 없으면 재귀). 대상이 await 중 삭제되면 null → 취소.
  - BR44 세 검사 본체(UW03/UW08/UW10)는 무변경.
- 검사: `node --check` 통과. 원본 KEEP-UI5 무수정.

## 반론/미반영 요약
- 7b(전체 재검증): 원본은 1회 검사만, 재현 가능한 실제 결함은 대상 소멸뿐 → 7a 로 충분. 나머지는 원본에 없는 창작이라 미반영.
- 7c(WS20 전체 잠금·세대 토큰): 원본에 없는 방어·선재 공통 결손 → 별건 권고(BR43 6c 와 동일).

## 테스트 시나리오
- `.works/UI추가팝업/00_현황판.md` (BR44 그룹). 실화면 대기.
- 개인화 조회 대기 중 대상 삭제 케이스(7a)는 타이밍 매우 좁아 실화면 재현이 어려움 → **코드 방어로만** 두고 테스트 항목엔 넣지 않음(정상 조작으로 안정 재현 불가한 내부 방어 제외 규칙). 테스트는 세 검사의 거부/허용 동작만.

## 결론
- BR44 본체는 원본 1:1(양측 통과). 코덱스 P1 의 실제 결함(고아 삽입)은 **대상 존재 재확인 1건으로 반영**해 차단. 무거운 전체 재검증·전체 잠금은 원본 근거 없어 별건 권고.
