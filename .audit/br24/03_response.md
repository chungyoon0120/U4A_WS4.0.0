# BR24 검수 반영·해명 (03_response)

## 검수 판정 요약
- **코덱스**: 수정필요 2건 (P1, P2)
- **안티**: 통과 (지적 없음)

## 지적별 처리표

| # | 검수자 | 지적 | 처리 | 근거·위치 |
|---|---|---|---|---|
| P1 | 코덱스 | 서버이벤트 목록 **조회 실패**(통신오류·세션오류·서버 `RETCD!=="S"`)도 빈 목록으로 정상 resolve되어, 정상 등록 이벤트를 "목록에 없음"으로 오판→빨간표시. 원본은 성공 응답에서만 검증. | **반영** | `getServerEventList` 는 성공에만 콜백(f_cb) 호출(`ws_html5_ws20_attr.js:486`), 실패엔 미호출(줄 464·493). 누락 검증을 `.then` 대신 **성공 콜백(fnOk) 안**으로 이동 → 실패 시 검증 자체가 안 돎(기존 목록·기존 오류상태 보존). 원본 `uiAttributeArea.js:1087-1113`(success 경로)와 동일 구조. |
| P2 | 코덱스 | 요청서/원본은 "빨간 테두리만"인데 `.u4aWs20AttrCombo.err .u4a-combo__text{color:var(--error)}` 가 선택 글자색까지 빨갛게 → 과함. | **반영** | `ws20.css` 콤보 글자색 규칙 삭제. 테두리(`border-color:var(--error)`)만 유지 = 원본 `valueState=Error` 결과. |

## 안티 검수
- 이슈1(아이콘 렌더)·이슈2(오류표시) 모두 통과. 상태 대칭성(설정·유지·해제)·토큰 사용·레이아웃 무결 확인. 별도 반영 없음.

## 반영 후 수정 위치
- `www/ws30/ws10_20/js/ws_html5_ws20_attr.js`
  - 이벤트 DDLB `onOpen`: 누락 검증을 성공 콜백 `fnOk` 내부로 이동(P1). 반환 Promise 는 성공/실패 전 경로 resolve라 드롭다운은 항상 열림.
- `www/ws30/ws10_20/WS10/css/ws20.css`
  - `.u4aWs20AttrCombo.err` 텍스트 색 규칙 제거, 테두리만(P2).
- `node --check` 통과.

## 남은 확인(실화면)
- 정상 등록 이벤트 + 조회 성공 시 빨간표시 안 뜸(P1 회귀 확인).
- 통신오류 유발 시 정상 이벤트에 빨간표시 안 뜸(P1 핵심).
- 미존재 이벤트값 펼침 시 테두리만 빨강(글자색 정상, P2).
