# 검수 전체 현황판

> 규약: `.audit/README.md`. 칸 표시 `☐` 아직 / `✅` 완료 / `❌` 문제있음.
> 새 기능 검수 시작 시 맨 위에 행 추가. 각 도구는 자기 칸만 갱신(행 위치·순서 고정).

| 기능명 | 검수요청(01) | 검수-코덱스(02) | 검수-안티(02) | 보완·해명(03) | 최종(04) | 비고 |
|---|---|---|---|---|---|---|
| br49 | ✅ | ✅ | ✅ | ☐ | ☐ | 안티 심층 검증 완료·지적 0. 1) 패널 재배치 시 Iframe 분리/재부착에 따른 100% 자동 재로드 및 preview/index.js(9880행)의 자체 drawPreview+ROOT 선택 Self-heal 라이프사이클 증명. 2) 재배치 시(_bReparented===true) 중복 fnWs20LoadPreview 호출 제거로 destroyAggregation 내 instanceof 충돌(Critical Error) 원천 차단 확인. 3) _ws20ArrangeSplit의 true/false 반환 분기 정확성 및 타 호출처 3곳 사이드이펙트 0% 검증. 4) 순서 무변경 시 기존 재로드 안전 유지. 무결점 통과 ※2026-08-18 수정 보강: 위 검수는 "중복 호출 제거만" 버전 대상. 재로드 self-heal 도 그 초기 removePreviewPage 가 무효화된 옛 컨텍스트의 _page1 에 destroy 를 걸어 같은 크래시 여지 존재 → **원본 lf_frame(219~225행)처럼 재-draw 전 미리보기 참조 5종 먼저 delete 추가**(removePreviewPage 조기 return, destroy 원천 차단). 보강본 재검수 권장 |
| br45 | ✅ | ❌ | ❌ | ✅ | ☐ | 코덱스·안티 동일 P1: attrUploadUrlException 기존행 보정 분기(attr.js 1481·1496) `UIAT` 오타→개인화/타앱 복붙 시 uploadUrl 이 현재 APPID 로 갱신 안 됨. **원본 대조: 오타는 원본(uiAttributeArea.js 7032·7047)에도 동일 → 내 이식은 1:1 충실.** ★장군님 결정(2026-08-18)=**미반영(원본 1:1 유지·원작자 회신)**. 실제 적용=호출 배선(삽입 루프)만→같은 앱 신규 추가는 정상. 오타 영향범위 조사(소비처 전수): `.UIAT` read 0건(보정 완전 무력화), 유효 소비=저장/개인화/복붙/속성표시 4곳, 미리보기 무관. 영향=cross-app(다른앱 개인화 적용·타앱 복붙·그 상태 저장)에서만 옛 앱 주소 서버 전파, 단일앱 무해. 테스트=`.works/UI추가팝업` BR45-E1~E4(영향도)+BR45-1~3(정상배선) 대기. node --check 통과 |
| br42 | ✅ | ❌ | ❌ | ✅ | ✅ | 실화면 BR42-1~3 전부 통과·히스토리 이관. 노션=수정완료. 코덱스·안티 동일 P1(Reset 루프에 dropAble/autoGrowing 연동 이벤트 초기화 attrSet*Exception(,false,true) 누락, 원본 uiAttributeArea.js:2222·2225) → **원본 2줄 1:1 복원 완료**(attr.js:4511~4517, 존재가드+console.error). 재검증: 두 예외처리 함수 본체 내부에 fnWs20PushUndo 없음 → BR42 단일 스냅샷 유지(첫 변경 직전 스냅샷 이후 이벤트 초기화 → Undo 1번에 프로퍼티+연동이벤트 함께 복원, 코덱스 T_CEVT 보존 확인). Undo 병합 자체는 양측 통과·무결. 반론 0. node --check 통과 |
| br47 | ✅ | ✅ | ✅ | ✅ | ✅ | 실화면 WC1~WC5 전부 통과·히스토리 이관(2026-08-18). ★검수 통과 후 실화면 WC1 재실패 → 진단(임시 로그)으로 원인 확정: 창(팝업)류는 미리보기에서 열리며 iframe 이 화면초점 선점, 메뉴 뜰 때 이미 active=IFRAME → 전역 iframe-blur-close 가 ≈2ms 뒤 합성 mousedown+.u4a-menu 직접제거로 즉시 닫음. [선택 await→틱→메뉴]만으론 불가(초점선점이 메뉴 표시시점에 이미 성립). **수정=메뉴 표시 시 oMenu.focus()로 active를 메뉴(부모문서)로 이동→teardown 가드(activeElement!==IFRAME) 조기 return→닫기 통째 skip**. 공통 u4a-ui.js 미수정(스코프내 edit.js). 상태기반이라 PC속도 무관(고정 ms 대기 없음, setTimeout(0)=순서양보). 코덱스·안티 둘 다 통과·필수수정 0. node --check 통과. 노션 상태변경 대기. 코덱스/안티 심층 검증 완료·지적 0. 1) [await 선택 → 한 틱 → 메뉴] 직렬화 복원이 매크로 태스크 큐(Macro Task Queue) 상에서 Iframe blur 이벤트를 선제적으로 완전 소진(Drain)시켜 메뉴 닫힘(Race Condition)을 원천 차단함이 입증됨. 2) await 이전 `e.clientX` 캐싱이 브라우저 이벤트 풀링 특성상 완벽한 Immutable 방어 조치이며, await 이후 노드(n)가 Stale 상태가 되더라도 순수 OBJID 기반 중앙 모델 재조회로 크래시를 원천 차단하는 방어적 로직(Defensive)임이 팩트체크됨. 무결점 통과 |
| br44 | ✅ | ❌ | ✅ | ✅ | ✅ | 실화면 BR44-1~3 전부 통과·히스토리 이관(2026-08-18). Codex P1 1건(독립 서브에이전트 교차확인)/안티 통과. UW03→UW08→UW10 정적 이식은 원본과 일치. 03 반영: P1(검사 뒤 프리셋 await 동안 대상 삭제→재개 시 화면엔 없고 prev/미리보기 고아→저장 payload 오염)=**await 후·fnWs20PushUndo 직전 대상 존재 재확인(_node) 추가**, 개인화 적용 경로에만, 없으면 조용히 취소(원본 동기 원자성 재현). 여섯 검사 전체 재검증=원본 1회검사라 과함·미반영, WS20 전체 잠금=원본 없는 방어·선재 공통이라 반론(별건, BR43 6c 동일). node --check 통과. 실화면 대기 |
| br43 | ✅ | ❌ | ✅ | ✅ | ✅ | 실화면 BR43-1~3 전부 통과·히스토리 이관. 노션=수정완료. 안티=통과, 코덱스=수정필요(P1). 정적 clamp·021·cardinality 상호배타는 양측 원본 일치. 03 반영: P1 핵심(undo push→`_readAttrPreset` await→삽입 사이 재진입 창)=**await 를 push 앞으로 이동**해 push~삽입 사이 await 제거(원자성 확보, 잠금 미추가). 변이 직전 재검증=await 사라져 불요(미반영). WS20 재진입 잠금·세대 토큰=원본에 없는 방어·선재 공통이라 반론(별건 권고). node --check 통과. 실화면 BR43-1~3 대기 |
| br41 | ✅ | ✅ | ✅ | ✅ | ✅ | 실화면 DE1~DE4 전부 통과·히스토리 이관. Codex/안티 둘 다 통과·필수지적0. Codex 서브에이전트 3축 재검수도 통과 유지(원본/현행 무작위 500상태 비교, 단건 부모+2단계 자손·멀티 VM PASS). 03 취합: 안티 제안2(삭제 호출부 조용한 catch→오류 표면화)=반영(edit.js 394·414 console.error, code.md 규칙 합치). 제안1(중복행 filter 전건삭제)=반론(원본 findIndex 1건 계약, 정상데이터 중복 없음). 제안3(uiDesignArea 늦은 로드 오버라이드)=반론(현행 미로드+typeof 등록가드, 가정적). 핵심 로직 변경0. node --check 통과. 노션 상태변경 대기 |
| br40 | ✅ | ❌ | ✅ | ✅ | ✅ | 실화면 DD1~DD4 전부 통과·히스토리 이관. 노션=수정완료. 03 취합: 두 검수 P1 동일(delDesc 함수 본체 부재→호출 no-op→T_DESC 잔존). 재확인=**현재 파일에 delDesc 본체 정상 존재**(attr.js:1400~1413, 원본 7783행 findIndex/-1 return/splice+T_DESC 가드). 검수본은 병행세션이 검수 중 되돌린 옛 상태(BR39 동일). 나머지(본인/하위 재귀 커버리지·삭제 경로 한정·Undo 스냅샷·멱등·원본 1:1·안티 P2 메모리 무위험)는 양측 통과·변경0. node --check 통과 |
| br39 | ✅ | ✅ | ❌ | ✅ | ✅ | 실화면 BR39-1~4 전부 통과·히스토리 이관. 노션=수정완료. 03 취합: 두 검수 모두 **코드는 정확·완벽** 일치(코덱스 통과, 안티 수정필요이나 권고안 a=문구정정). 쟁점=요청서·주석 "오류 UI 복귀 시 재표시" 문구가 원본과 다름. 원본 setUIInfo(uiAttributeArea.js:7462) 소스확인: UI 선택마다 uiinfo 새로 만들고 OBJID_stat 미보존 → **"다른 UI 이동 시 이전 오류 폐기"가 원본 계약, "복귀 시 재표시"는 원본에 없음**(이슈 기대결과도 재표시 요구 없음). 반영=주석/수용기준에서 "복귀 시 재표시" 삭제·정정, **코드 동작 변경 0**(현재 구현이 이미 원본 계약과 일치). 안티 P1(상태유실 결함)=반론: 별도 오류상태 Map은 원본에 없는 신규 동작이라 미채택(임의창작 금지). ①오류표시 동기화 ②`.data("OBJID")` 갱신은 양측 통과. ※검수 중 병행세션이 파일을 BR39 이전으로 되돌려 두 수정 재적용, node --check 통과. 실화면 .works/ws20attrchange BR39-1~4 대기 |
| br38 | ✅ | ✅ | ✅ | ✅ | ☐ | 코덱스/안티 심층 교차검증 완료·지적 0. 03 취합: 반영 코드수정 0(확정). 원인=미리보기 선택표시 함수 fn_mark 가 선택 강조·테두리만 하고 선택 UI 화면 안 이동이 빠짐 → scrollIntoView(block:nearest) 복원(밖이면 이동·보이면 유지). 근거 3종(이슈 기대결과·반대방향·원본 uiAttributeArea.js:8303). 1) 안티그래비티 초기 P1(Scroll Chaining 부모 튀김 결함) 오판 번복: WS20 메인 레이아웃이 겹겹의 `overflow: hidden` 고정 구조이므로 Iframe 연쇄 스크롤이 물리적으로 차단됨(안전). 2) `setTimeout` 0ms 트릭이 스크롤 이벤트 루프와 Overlay(선택 레이어) 좌표 갱신(Paint 1 & Paint 2)을 완벽히 동기화시켜 어긋남(Flickering)을 원천 차단함이 증명됨. 3) null/typeof 방어가드 완벽. 무결점 통과 |
| br37 | ✅ | ✅ | ✅ | ✅ | ☐ | 03 취합: 두 검수 통과·반영필요 0(추가 코드수정 없음). 원인=화면CSS(.u4aWs20InsRow display:flex)가 hidden 이겨 항상 표시, `.u4aWs20InsRow[hidden]{display:none}` 1줄로 원본 setVisible 의도 복원. 상태 5전이(최초·미선택/공백·단일·다건·복귀) 원본1:1, 명시도(0,2,0)>기본(0,1,0)·반응형 display 무재선언·데이터 상수1 주입 무결. 테스트 .works/UI추가팝업 BR37-1~5. 코덱스/안티 심층 교차검증 지적 0. 1) JS 토글 로직이 원본 UI5 `setVisible` 라이프사이클(초기/빈값/단일 숨김, 다건 노출)과 100% 1:1 일치함 입증. 2) 추가된 CSS `[hidden]`(명시도 0,2,0)이 기존 flex 규칙을 오버라이드하여 레이아웃 깨짐(Zero Side-effect) 없음. 3) 빈번한 토글 시에도 Reflow 프레임 드랍 전혀 없음. 4) 숨김 시 강제 초기화(1)가 확정 시점에 DOM을 바이패스하고 상수 1로 데이터 동기화되어 Data Sync Loss 0% 증명 완료. 무결점 통과 |
| br36 | ✅ | ✅ | ❌ | ✅ | ✅ | 코덱스 통과. 안티 P2(오류 catch에서 try안 선언 `_onEscKey` 참조→엄격모드 ReferenceError 삼킴→keydown 리스너 누수)=Node 재현으로 실재 확인 → **장군님 지시로 반영**: ESC 함수 이름칸만 try 밖(함수 스코프)에 선언(`var _onEscKey;`), 본체 배정은 try 안 유지(함제 `lf_cancel` 참조 보존). Node로 오류경로 리스너 정리·ESC 정상 재확인, node --check 통과. BR36 본수정(진입부 시작BUSY+짝해제)은 양측 정확성 통과. ★장군님 지적(2026-08-14): ESC에 취소 안내(001) 붙인 건 원본에 없음(원본 insertUIPopop.js:44~53 dialog=escapeHandler 없어 ESC는 기본 닫힘·안내 없음, 안내는 닫기88/취소782 press에만) → ESC 를 lf_close(무안내)로 원복했다가, ★장군님 최종 통일 결정(2026-08-14): 닫기 경로 일관성 위해 ESC 도 취소 안내(001) 뜨게 재변경(원본 UI5 ESC-무안내가 오히려 결함). 이제 ESC·닫기(X)·취소 모두 001 안내, 확정만 무안내. node --check 통과. 실화면 BR36-1~4 전부 통과·히스토리 이관. 노션=수정완료 |
| br34 | ✅ | ❌ | ❌ | 🔁 | ☐ | ★장군님 지시(2026-08-14) "원본 소스 동작과 똑같이": 툴팁 아닌 **원본 valueStateMessage(말풍선) 재현**. ①오류 시 빨간 테두리+오류 안내문구 팝오버(문서 최상단 fixed, _bindVsShow 방식), 값 확정 직후 자동표시+포커스/클릭 표시, 재렌더 정리, 툴팁=값(원본 uiAttributeArea.js:736·831). ②기본값 복구 원본 그대로(months=1 실화면 확인). ③지우기(X)는 원본대로 항상표시(앞서 넣었던 X숨김·비우기·안내 전부 철회). ④★실행(액티브) 후 값칸 빨간 잔존 — **진짜 원인 실측 확정(CDP로 살아있는 앱 직접 추적)**: 실행/체크가 부르는 오류점검 함수 **oAPP.fn.chkExcepionAttr·chkExcepUiTable 이 런타임에 undefined**(원본은 uiPreviewArea/미리보기 메인 안에서 정의하나 HTML5는 그 UI5 메인을 실행 안 함). ev_pressActivateBtn(ws_events.js:1528)은 `typeof chkExcepionAttr==="function"` 가드라 **점검 통째로 skip → attrClearErrorField 미호출 → 빨간 잔존 + 실제로 검증 없이 "액티브 성공"**. ev_pressSyntaxCheckBtn(1320)은 가드 없이 호출 → **undefined 예외로 busy 멈춤**. attrClearErrorField 자체는 정상(CDP로 직접 호출 시 데이터·DOM 다 지워짐 확인). → **두 함수를 원본(WS_DESIGN main.js:1315·1371) 1:1 이식**해 ws_html5_ws20_attr.js 에 가드 정의. CDP 실측 재검증: 오류 데이터 1→0, 화면 빨간 1→0(액티브가 실제 지움). 앞서 헛짚은 refresh 훅 재렌더는 되돌림. node --check 통과. **파일에 함수 있음≠런타임 로드됨**(감사 반성점). ⑤값칸에 onEnter=확정 배선(원본 sap.m.Input 엔터=change 재현) — 엔터로도 정상값 확정→오류 해제, 잘못된 값→오류 표시. ⑥★근본 버그 확정(서브에이전트 독립 검증): 값 변경 후 fnRenderWs20AttrRows(ROWS.innerHTML="")로 포커스 입력칸 제거 시 dirty change가 동기 재발화→fnWs20AttrChange 동기 중첩 재귀→안쪽이 attrClearErrorField로 방금 세운 valst 초기화+busy 조기해제→오류 표시 소실(범위밖 값 넣어도 오류 안 뜸). → fnWs20AttrChange 진입부 동기 재진입 방지 플래그(oAPP.attr._fnAttrChangeBusy) 추가, finally 해제. autoGrowing·M03·확인창 등 정상 재진입은 전부 비동기라 미차단(검증). 남은 참고(별건): 점검모듈 미로드 시 조용 skip(정상앱 무관), blur 후 refocus 포커스 되뺏김(테두리 정상). node --check 통과. 실화면 BR34-1·7·8 테스트 대기 |
| br32 | ✅ | ❌ | ❌ | ✅ | ✅ | 취합·반영(03): P1-1(최초 loadJs 실패 시 화면잠금 고착)=반영 — 디스패처 선-잠금 제거, 편집기 여는 함수가 스스로 잠금 관리(고착 여지 제거). P1-2(신규창 loadURL 실패·초기화 전 closed)=반론(별도 이슈): Edit메뉴 CSS/JS 편집기도 동일 선-잠금+위임 → 선재·공통, BR32 범위 밖. ★장군님 실화면 지적 반영: 원본은 값 전체가 아니라 **더블클릭한 그 단어(공백 기준)만** 취득해 편집기에 넘겨 그 단어를 찾아 블럭 처리 → 디스패처가 입력칸 커서 위치의 공백 토큰만 잘라 전달(편집기 검색은 원상복구). 실화면 BR32-1·1b·2~6 전부 통과·히스토리 이관. 노션=수정완료 |
| br33 | ✅ | ✅ | ✅ | ✅ | ☐ | 코덱스·안티+독립 반박 재검수 모두 통과·필수지적 0. 원본 가드·호출, 값칸 한정 제외, 중복방지, stale 참조, BR32 불교차 확인. 첫 click 비동기 조회와 dblclick 이동 공존도 원본 의도와 동일. 03의 공용 `execControllerClass` 동기 예외 엣지는 이식 오류가 아니어서 Pass 유지. 실화면 SAP GUI/느린 조회 UX 검증 권고 |
| br35 | ✅ | ❌ | ✅ | ✅ | ✅ | 실화면 BR35-1~3 전부 통과·히스토리 이관. 노션=수정완료. 판정 갈림: 안티=통과, 코덱스=수정필요(P1). 정상 경로(원본 UA040 인자·차단 순서 1:1, Undo 이전 차단=유령노드0, 팝업 잠금 대칭)는 양측 통과. P1=점검함수(dnd.js 정의) 부분 로드 실패 시 `typeof` 가드가 fail-open→금지 UI 재유입(트리 렌더러는 tree.js, 점검함수는 dnd.js로 파일 분리 확인). 03 반영: fail-open→**fail-closed**(함수 없으면 console.error 후 추가 취소, 원본 무조건 호출·붙여넣기 부재취소와 동일). 안티 P4(fail-open 강등 옹호)는 막는 점검엔 반대라 정정. 로더 실패후 WS시작 미차단=공통 인프라 선재이슈·범위밖 반론. node --check 통과. 실화면 BR35-1~3 대기 |
| br29 | ✅ | ✅ | ✅ | ✅ | ✅ | 코덱스·안티+독립 서브에이전트 재검수 통과·필수지적 0. 3종/9개 키·메시지283·확인/취소/바인딩건, 단일 스냅샷과 `_T_0015`+`T_CEVT` 복원, 재진입·ACTCD·BUSY 대칭 확인. 03=공통 잔여참고(확인 콜백 designRefershModel await) 반영(콜백 async+await, 원본 uiAttributeArea.js:3183 계약과 동일). 실화면 BR29-1~6 전부 통과·히스토리 이관. 노션=수정완료 |
| br31 | ✅ | ✅ | ✅ | ✅ | ☐ | 코덱스/안티 둘 다 통과·필수지적0(콤보 잠금 강건성·Undo 복원 무결). 03=코덱스 잔여참고2건 처리: (1)초기화 3개 호출 조용한 catch→console.error 표면화(규칙 합치, 반영), (2)요청서 autoGrowing 미이식 메모=BR29로 이미 이식됨(정정, 무관). 실화면 테스트 대기 |
| br28 | ✅ | ❌ | ❌ | ✅ | ✅ | 취합·반영(03): 과잉방어=해소확인(스크롤이 활성조건 밖·안티 거짓반박). 미바인딩 재오픈 옛 스크롤 잔존(P1)=반영(목록 맨위 초기화), 닫힘 후 rAF 정리=반영. 완전 비동기 직렬화(세대토큰 P2)=반론(원본에 없는 방어·BR28 직접원인 아님, 보장범위=예약 rAF취소로 한정·별도이슈 권고). 노션=수정완료 |
| br27 | ✅ | ❌ | ❌ | ✅ | ✅ | 코덱스·안티 P1(재오픈 세션경계 유령 선택) 반영: 새로 그리기 직전 선택 명시 해제(lf_renderTree)로 재오픈/새로고침/빈·오류 렌더 모두 초기화 → 유령 강조 차단. + 선택불가 행 클릭 해제 통일. ★활성(enable) 게이팅은 원본 스크롤(setFirstVisibleRow) 회귀라 미채택(반론1, 처음 넣었다 되돌림) — lf_setSelectTree 원본 그대로. 실화면 BR27-1~6 전부 통과·히스토리 이관. 노션=수정완료 |
| br26 | ✅ | ❌ | ❌ | ✅ | ✅ | 안티 지적(멀티 체크 삭제 경로 자식 prev 잔존) 반영: 체크 삭제 루프에도 `_purgePrevSubtree` 배선. 코덱스 P1(이벤트·설명·바인딩·팝업 수집정보 정리)은 반론=BR26 이전부터 최상위도 미정리인 선재·광범위 결손, 측정범위(prev) 밖 → 별도 이슈 권고. 노션=수정완료 |
| br25 | ✅ | ✅ | ✅ | ✅ | ✅ | Codex·안티 둘 다 통과·지적0(확인창 직후 BUSY 해제=원본 uiAttributeArea.js:4753·4831 1:1, YES 재잠금·공통 finally 해제, 취소·팝업 종료·BUSY 대칭). 코덱스 잔여참고(표준밖 window.confirm 폴백)=대상환경 미해당+원본에 없는 방어라 미반영(03). 실화면 BR25-1~3 전부 통과·히스토리 이관. 곁들여 SEL(모달 트리 줄 선택표시 소실)도 수정·통과. 노션=수정완료 |
| br24 | ✅ | ❌ | ✅ | ✅ | ✅ | 코덱스 P1(조회 실패 오판 방지)·P2(과도한 텍스트 색상 제거) 지적사항 전부 반영(03). 안티 최종 검수(04) 통과. 실화면·재검수 대기 완료 |
| br23 | ✅ | ✅ | ✅ | ✅ | ✅ | 코덱스·안티 재검수 둘 다 통과·지적0(A 모듈 선로드·OBJID·전 종료 잠금회수, B 툴바 경계, C 가상 트리 재사용, D 팝업 내부 드롭 차단). 03=반영/반론 없음. 실화면 개인화 팝업 위치·크기 7건(PFC1~7)+내 패턴 진입 5건(BR23-1~5) 전부 통과. 전체창/리사이즈·공통 팝업 리사이즈 화면가둠 추가 반영. 노션=수정완료, 커밋 8ed6da46 푸시 |
| br22 | ✅ | ✅ | ✅ | ✅ | ✅ | Codex·안티 둘 다 통과·지적0(공용 함수 공유, UI5 Dialog·서버조회 차단, BUSY·단축키 잠금 회수, 로드순서·null인자 검증). 실화면 BR22-1~4 전부 통과·히스토리 이관. 노션=수정완료 |
| br21 | ✅ | ❌ | ✅ | ✅ | ✅ | 코덱스2·안티3 지적 정리: APP폴백·노드부재폴백·화면잠금누수·_done중복가드 4건 반영, 비동기 재잠금 1건 반론(원본/트리 SSOT 미재잠금). 실화면 BR21-1~4 전부 통과·히스토리 이관. 노션=수정완료 |
| br20 | ✅ | ❌ | ✅ | ✅ | ✅ | 코덱스 수정필요 3건 전부 반영(03): 빈 설명이면 `_DESC` 미생성(원본1:1)·위임불가시 깨진원본 미호출+잠금만 해제·저장 실패 표면화. 안티 통과. 실화면 BR20-1~3 통과. 노션=수정완료 |
| br18 | ✅ | ❌ | ❌ | ✅ | ✅ | 장군님 지시"원본대로" 반영: A(오픈오류해제)·B(확정BUSY 복원)·D(Enter·닫기키)·E(미리강조 HTML5 재현) 반영, 미리보기+트리 팝업 통일. C=그냥두기, F·G 반론. 실화면 RS1~RS6·RS3b·MP1~MP7 전부 통과. 노션=수정완료. 커밋 f8435614 푸시 |
| br17 | ✅ | ✅ | ✅ | ✅ | ✅ | 미리보기 우클릭 위/아래 이동 예외·화면잠금 → 트리 이동으로 위임. 코덱스·안티 둘 다 통과(지적 없음). 노션=수정완료. 실화면 테스트 대기 |
| br19 | ✅ | ❌ | ❌ | ✅ | ✅ | 코덱스·안티 수정필요 2건(오류경로 해제·이중rAF) 전부 반영(03). 실화면 BR19-1~5 전부 통과 + 취소 안내 공통알림 수정. 노션=수정완료 |
| br16 | ✅ | ❌ | ❌ | ✅ | ✅ | 지적1(try 범위)·2(await) 반영. 지적3(공통 방송모듈 변경)은 미반영-공통코드·원본1:1 규칙상 반론. 실화면 통과·노션=수정완료 |
