# 검수 전체 현황판

> 규약: `.audit/README.md`. 칸 표시 `☐` 아직 / `✅` 완료 / `❌` 문제있음.
> 새 기능 검수 시작 시 맨 위에 행 추가. 각 도구는 자기 칸만 갱신(행 위치·순서 고정).

| 기능명 | 검수요청(01) | 검수-코덱스(02) | 검수-안티(02) | 보완·해명(03) | 최종(04) | 비고 |
|---|---|---|---|---|---|---|
| br65 | ✅ | ☐ | ☐ | ☐ | ☐ | 바인딩 팝업(별창) aggregation 재바인딩 확인창 **취소 시 BUSY 미해제** — 원본 index.js:6950 attrBindCallBackAggr 의 BUSY 3단계(6976/6990/6999 · 7051/7064/7075)가 HTML5 `designArea/bindWrite.js` 에 통째로 미이식(주석에 'P6 생략' 명시)되어 있었다. 취소는 WS20 왕복이 없어 해제 주체가 사라짐. 3단계 1:1 복원. 검수 포인트 = sOption 유/무 구분(방송 여부), 종료 분기 off 짝, designArea.js:800 `_confirmAdditApply` 와의 중복 여부. |
| br64 | ✅ | ☐ | ☐ | ☐ | ☐ | 바인딩 팝업: 잘못된 Conversion Routine 이 D&D 바인딩에 적용됨. 원인=드래그 시작 시 추가속성 오류 점검(`checkAdditData`)이 2026-08-05 에 미정의로 꺼져 있어 오류값이 payload 에 안 실림(끌기·드롭 차단 배선은 이미 존재). 원본(`U4A_WS_DESIGN\index.js:8394`)이 명확히 차단(`T_MPROP` 중 `_error===true` 행 있으면 RETCD=E, 메시지 146) → 그 함수를 HTML5 스토어(`oAPP.attr.additRows`, 적용 버튼과 동일 배열) 기준으로 1:1 정의. 파일 1개(`additInfoArea.js` 725~745 신규), 원본 무수정, node --check 통과. 검수 대기. |
| br63 | ✅ | ❌ | ❌ | ☐ | ☐ | 코덱스·안티 심층 검증 완료 (수정 필요 P1 1건). 1) [P1] 원본 broadcastChannelBindPopup.js:278의 선행 계약인 await moveDesignPage()가 누락된 채 C2(setAdditBindButtonEnable)가 즉시 실행되어, 동일속성 화면(bSyncEqualityScreenActive) 진행 중 우측 바인딩 버튼이 조기 활성화되고 Busy가 조기 해제되어 중첩 바인딩 데이터 경쟁을 유발함(C2 제거 또는 await moveDesignPage 선행 필요). C1(setAdditialListData 제거) 및 D&D 후 속성값 보존은 완벽. |
| br58 | ✅ | ❌ | ❌ | ✅ | ✅ | 03 취합: **반영 5건 / 보류 1건 / 반론 1건.** 정상 경로는 두 검수 모두 통과 판정, 지적은 전부 **실패했을 때의 뒤처리**에 몰렸다. **반영1(P1, 양쪽)** loadURL 은 Electron 14 에서 약속(Promise)을 돌려줘 동기 catch 로 못 잡는다 → 문서 로드 실패·다 뜨기 전 창 닫힘·화면 프로세스 종료 때 회색 대기 표시가 영영 안 풀렸다. 멱등 정리기 1개를 만들어 성공/로드거부/로드실패/프로세스종료/조기닫힘 5갈래에 모두 연결(1회만 동작), 표시 처리(show·서서히나타내기·가운데배치)도 감싸 어떤 실패에도 해제까지 도달. **반영2(P1, 양쪽)** 통합 도움말 opener 가 async 라 거부가 약속으로 오는데 3곳이 안 받아 대기 표시 잔류 → 세 곳 모두 .catch() 연결(성공 경로 무변경, 실패 때만 회수). 원본도 await 안 하지만 busy 짝 규칙이 상위. **반영3(P2, 양쪽)** 트리·미리보기만 패치판정과 opener 존재검사를 && 로 묶어(BR58 이전부터) 패치 서버인데 opener 미로드면 구버전 도움말로 오폴백 — 원본 3곳 모두 존재검사 없음(전수 확인) → 분리하고 3영역 계약 통일(판정실패 WS20HELP-10/20/30, opener없음 -11/21/31 = 오류 후 종료, 폴백은 패치=false 일 때만). **반영4(P1, 코덱스 단독)** ★BR58 이 만든 위험 — loadLibrary 의 ajax error 갈래가 다음 파일로 재귀하지 않아, 신규 파일을 목록 앞에 둔 탓에 그 파일 하나가 빠지면 WS20 전체가 안 실림. 공통 로더는 안 건드리고 **등록을 목록 맨 뒤로 이동**(이 파일은 정의만 하고 호출은 버튼 누를 때라 순서 무관 — 로드시점 참조 0건 확인). 요청서의 '미로드 시 WS20HELP-12/22/32 로 표면화' 설명도 부정확했음을 정정. **반영5(P2, 코덱스)** did-finish-load 를 once + 멱등 처리로 → 창 재로드 시 타 작업 잠금 오해제 방지. **보류1(P2, 양쪽)** 화면에 추적코드 WS20HELP-02 표시 — 원본에 창 실패용 안내 키가 아예 없고(파일없음 377 뿐), 안내를 빼면 무반응이 되어 더 나쁨. BR51(WS20ATTR-CHK01, attr.js:4161)과 동일 형태로 이미 운영 중 → 일관성 위해 현행 유지, **정식 메시지 번호 1개 지정 요청**(지정 시 BR51 자리와 함께 교체). **반론1(P2, 코덱스)** 영어 도움말 문서가 실제 영어가 아님 → 작업폴더 6개 파일이 **원본과 지문 완전 일치**(영어 디자인·미리보기는 한국어 문서와 같은 파일, 영어 속성도 본문 한국어 501자) = **원본 자산 문제**, 원본 무수정 규칙상 손댈 수 없음 → 별건. 요청서 검수포인트 H 의 '6개 존재'가 경로 존재만 확인한 표현이었음도 정정. **별건 보고** loadLibrary 의 error 갈래가 재귀·최종 콜백을 안 하는 것은 공통 로더 결함(어느 파일이든 하나 실패하면 그 뒤 통째 사망) — 파급 커서 미조치, 지시 대기. node --check 5파일 통과, 원본 폴더 무수정, 백업 _*.br58fix1bak. 작업 중 확인: prev.js 는 검수 이후 BR58 무관한 다른 작업(미리보기 오류 표면화)으로도 수정돼 있었고 BR58 부분은 온전. 재확인 테스트 = .works/도움말팝업 HLP11~16 대기 **재확인 완료(2026-08-31)** — HLP11 장군님 확인, HLP12(현재 서버 3곳 회귀)·HLP13(5회 반복 후 대기표시·단축키잠금 풀림)·HLP15(폭 좁혀 ⋯ 접힌 상태에서 메뉴로 열기)·HLP16(닫은 뒤 잠금 풀림) 자동 통과. HLP14(도움말 문서 폴더명 변경)는 **항목에서 제외** — 장군님 지적대로 빌드 시 도움말 문서가 앱 파일 안으로 말려 들어가고(포함 목록에 www 전체, 바깥 리소스 아님) 배포본에서 폴더명을 바꿀 수 없어 **정상 조작으로 도달 불가**. 현황판 비움 → .works/도움말팝업 히스토리 이관. **최종 마감(2026-08-31)** — 남은 항목 0, 04_final.md 작성. 장군님 결정 2건: (가) 도움말 창 열기 실패 안내는 **메시지 번호를 만들지 않고 그대로 둔다** — 도움말 문서가 앱 파일 안으로 말려 들어가(asar:true, 포함목록 www 전체) 배포본에서 파일이 없거나 못 읽는 일이 사실상 없다 = **도달 불가 자리**. 번호 소모 대신 추적 코드 노출 유지(소스 주석에 근거 기록). BR51 은 별건 → 무변경. (나) **파일 목록 읽기 공통처리 별건 = 폐기** — 목록 38개가 모두 앱 파일 안으로 말려 배포본에서 한 개만 빠지는 상황이 없고, 빠졌으면 빌드 오류라 배포 전에 걸린다 → 부팅 순서 미수정(맨 뒤 이동은 유지). **즐 별건 1건만 남음** — 영어 도움말 문서 본문이 한국어(진짜 원본과 지문 일치 = 원작자 몫). |
| br62 | ✅ | ✅ | ✅ | ✅ | ☐ | **코덱스·안티 둘 다 수정필요 → 지적 4건 전부 반영, 반론 0건.** 두 검수가 독립적으로 같은 지적 2건에 도달: ①**자리 옮기기 되돌리기에서 미리보기 떼어내기 인자가 틀림** (복원할 옛 위치를 넘겨 지금 붙어 있는 자리에서 안 떨어짐 → 같은 UI가 두 군데 남거나 부모 중복 할당 오류) → 지금 붙어 있는 위치 기준으로 교정(원본 D&D 되돌리기도 '현재에서 지우고 이전 상태로 다시 넣기' 순서), ②**부분 갱신 내부 실패를 삼켜** 절반만 갱신되고도 성공 처리 → 약속한 전체 다시 그리기 폴백이 안 걸림 → 되돌리기 전용 엄격 호출 신설(공용 호출 계약은 그대로 두어 파급 최소, 코덱스 제안 채택)하고 속성 되돌리기 catch 4곳도 로그 후 던지게. ③**코덱스 단독 P1**: 원본은 되돌리기 시작 즉시 화면잠금+단축키잠금+자식창 잠금을 걸고 모든 종료에서 역순 해제하는데 HTML5는 자식창 잠금·버튼 재진입 차단만 있었음(원본 소스 직접 확인: 시작 3종, 종료 5경로 역순) → 원본대로 배선, finally 역순 해제. ④**안티 단독 P2**: 전체 다시 그리기 폴백 후 곧바로 스크롤 복원하면 화면 높이가 안 잡혀 무시됨 → 그리기 완료 다음 틱(2틱+상한 120ms)에 복원. **무변경 1건(사실 인정)**: 이름 바꾸기 되돌리기가 원본(이름표만 교체)과 달리 그 UI 한 그루를 다시 그림 — 되돌리기 전후 비교 방식의 구조적 귀결이며 안티도 '기능 정상' 통과 처리, 원본식으로 맞추려면 되돌리기 자료를 동작별 기록으로 재설계해야 해 별건 권고. 수정 파일 1개(`ws_html5_ws20_edit.js`), 원본·원본 사본 무수정, 백업 `_ws_html5_ws20_edit.js.br62auditbak`, node --check 통과. **남은 위험**: 미리보기가 한 번이라도 실패하면 그 되돌리기는 전체 다시 그리기로 넘어감(의도한 계약, 깜빡임 발생). 실화면 테스트 BR62-1~14 미실시 → `.works/되돌리기다시하기/00_현황판.md`. 상세 `br62/03_response.md`. | 이전 기록: **코덱스·안티 2차 심층 검증 완료 (수정 필요).** A(이름변경 앵커)·C(삭제 앵커) 통과. B·D 부분 갱신 지적 3건: ① **[치명/P1]** 이동 되돌리기 시 `prevRemoveUiObject`에 복원할 옛 위치(`oOld`)를 전달하여, 현재 부모(`oNew`)에서 UI를 분리하지 못해 발생하는 중복 부착 및 참조 누수(oNew 전달 필요). ② **[치명/P1]** `_applyAttrPreview` 및 `setMove` 내부 `try-catch`가 예외를 삼켜 부분 반영 실패 시 설계된 `drawPreview` 전체 재생성 안전 폴백이 무력화됨(throw 전파 필요). ③ **[안정성/P2]** 전체 재생성 폴백 시 DOM 렌더링 틱 미고려로 인한 스크롤 복원(`_restorePrevScroll`) 무효화 위험(타이밍 보정 권장). 실화면 BR62-1~9 미실시. 상세 `br62/02_audit_agy.md`·`br62/02_audit_codex.md`. |
| br54 | ✅ | ❌ | ❌ | ✅ | ✅ | 실화면 BR54-1~7 전부 통과·히스토리 이관(2026-09-01). 노션=수정완료. 테스트 중 BR54-4 실패(되돌리기 후 미리보기에서 UI 사라짐) 발견 → 되돌리기가 UI 를 부수고 다시 만들며 부모에 재부착을 안 하던 것을 원본 undoRedo CL_CHANGE_ATTR.executeChangeAttr(2127~2207) 대로 **값만 다시 꽂는 방식**(setModelBind+previewUIsetProp)으로 교체(ws_html5_ws20_edit.js, 자리이동 경로는 무변경). 03 취합: **반영 1건 / 반론 2건 / 별건 1건.** **★코덱스 P1(RESET 누락)이 정확 → 원본 호출처 전수 대조로 더 큰 구조 이탈 발견·수정**: 원본은 값 변경이 두 함수 (사용자 편집 진입점 attrChange 1781=예외처리 갈래 있음 / 값 반영 일꾼 attrChangeProc 1929=갈래 없음, 호출처 27 대 23)인데 HTML5 는 fnWs20AttrChange 하나로 합쳐서, 원본이 attrChangeProc 를 직접 부르던 경로까지 갈래를 타게 됐다. RESET 은 그중 유일한 반복 호출이라 재진입 잠금이 비동기 완료까지 걸린 채 뒤 행이 전부 조기 return → 수집·검증·스타일 누락(코덱스 지적 그대로). → 5번째 스위치 bSkipAttrExc 추가 후 **원본 attrChangeProc 경로 11개 지점에만 적용**(attr.js 3865·4251·4258·4313·4320·4337·4826(RESET) / fnBindPopupOpen 220·227·288·297 / fnDumpWritePopupOpen 117 / fnInitPreScreenPopupOpen 87), 각 줄에 원본 근거 주석. attrChange 경로(값 입력칸·우클릭 메뉴·동일속성 적용·단축키·바인딩 등)는 무변경. **반론1**(코덱스 P1-1·안티 P1 = imageCompress 가 UI5 전용이라 죽음): 사실은 동의하나 BR54(갈래 배선)가 만든 결함 아님 — 원본에서도 UI5 있어야 돌고, UW13 매핑도 서버 데이터라 미확정(코덱스도 조건부 표기). 원본 UI5 팝업의 HTML5 변환은 별도 항목 → **별건 분리 보고**. **반론2**(안티 P2 fail-open, 코덱스는 철회): 그룹이 없으면 등록 속성도 0건이라 '오염될 값' 자체가 없어 시나리오 자기모순. 판정 throw 도 정상 경로 도달 불가. 다만 패치 켜짐+그룹 없음은 설치 이상이므로 **오류로 드러내도록 진단만 보강**(흐름 무변경). node --check 4파일 통과, 원본 무수정. 테스트 = .works/ws20attrchange BR54-1~6(회귀 6건, 본기능은 재현 불가라 제외). **★심층 재검토(08-31)**: 원본을 함수 경계로 파싱해 지도를 만들고 전수 재대조 — 스킵 11곳·유지 전 지점 모두 정확(오탐·누락 0). HTML5 이식본이 원본과 함수명이 같아 1:1 대조 가능했고, 이름이 비슷한 attrSelOption2F4HelpReturnFIeld(반환필드 선택=attrChange 계열, 유지)와 attrSelOption2F4HelpIDDel(지움=attrChangeProc 계열, 스킵)이 정확히 갈렸음을 확인. 주석의 원본 인용 줄번호 4곳 오기 정정(기능 매핑은 맞음, 동작 영향 0). **추가 수정 2건**: ⓐ 전용 모듈 실행을 옮겨적기에서 **원본 모듈 직접 호출**로 변경 — 원본은 design/attributesArea/ 에서 전용 모듈을 불러 상대경로 기준이 거기이므로, js/ 에서 옮겨 부르면 다른 곳을 찾게 됨(코덱스 참고사항 '경로 기준 URL 차이' 해소). 옮겨적은 건 동기 판정 2줄만 남음. ⓑ 원본 1843 은 모듈이 true 일 때만 값 처리를 건너뛰므로, true 가 아니면 잠금 해제 후 예외갈래만 건너뛴 재진입으로 값 처리 이어감. attrSetDropAbleException(원본 3474 Proc)은 공통 함수를 안 거쳐 무관 확인. **남은 원본 차이(범위 밖·보고만)**: attrChange 의 조기종료 2개(ROOT 문서 속성 1795 미이식 / dropAble 1815 위치 이동)가 갈래보다 앞이라, 그 속성이 코드마스터 등록 시 원본은 갈래 미도달·HTML5 는 도달. BR54 이전부터의 차이. node --check 4파일 재통과. |
| br55 | ✅ | ❌ | ❌ | ✅ | ✅ | 03 취합: 코덱스·안티 **동일 지적 2건**. **P1-2 반영** — 오류목록 표시(fnMultiFooterMsg)가 진입 즉시 본창 busy + 자식창 BUSY_ON 을 함께 걸므로, 내가 만든 WS20ACT-CHK02 회수 갈래에서 본창만 풀면 자식창이 잠긴 채 남는다 → `oMainBroad BUSY_OFF` 방송 추가(수신부 ws_fn_broad.js 42~46 확인, 프로젝트 기존 회수 갈래와 동일 형식). **P1-1 반론** — 오류목록 창의 비동기 로드 실패(did-fail-load·render-process-gone·ready 전 closed) 회수 부재는 **원본에도 똑같이 없음**(원본 fnDialogPopupOpener.js 2968~3082 실측: loadURL 반환 미처리·did-fail-load 없음·closed 에 busy 해제 없음, 현행과 실패처리 코드 차이 0). 원본 결함이고 fnMultiFooterMsg 를 Promise 반환형으로 바꿔 호출규약을 고치는 범위 밖 변경이라 임의 적용 금지 → 원본 잠재버그로 별도 등록·일괄 적용은 장군님 지시 대기. CHK01 은 BUSY_ON 이전 단계라 본창 해제만으로 충분(코덱스도 동의). 실화면 테스트 BR55-1~4 = `.works/실행점검/00_현황판.md`. node --check 통과. **04 최종(2026-09-01)**: 장군님 지시로 ①점검 경로 전수 조사 ②크리티컬 공통 처리 보완. 조사=점검부에 방어 없는 참조 7곳(E1~E7) 확인, 전부 원본 동일 → 점검부는 손대지 않고 부르는 쪽에서 차단. 미리보기 항목(prev) 없음(E1·E2·E5)은 도달 가능 근거 3건 확보(_prev 조용한 skip / 되돌리기 복원이 스냅샷 키만 복구 / prev 생성은 미리보기 iframe 전담), UA050 없음(E3)은 서버데이터라 미확인, zTREE 없음(E4)은 재현 경로 미발견. 보완=`_ws20CriticalError` 공통함수 추가(ws_events.js 1552~) — 기존 전례(ws_common.js fnJsonParseError 4009~4027 + ws_usp.js fnCriticalErrorWs30 4048~) 그대로: 콘솔 오류코드 → 화면 잠금 해제 → 자식창 BUSY_OFF(해당 구간만) → 작업표시줄 깜빡임 → 공통 오류 팝업 KIND 20 `/U4A/MSG_WS 192`+추적코드+예외원문 → 호출측 return(fail-closed). 적용 4곳 WS20ACT-CHK01/02 + **신규** WS20SYN-CHK01/02(신텍스체크는 종전 무방비라 점검 터지면 안내없이 화면 멈춤). 세션 강제 로그오프 콜백은 미부착(미저장 설계 유실 우려 → 장군님 판단 대기). 스텁 실행 검증 4케이스 통과(정상=서버호출 1·잠금유지 / 점검터짐=서버호출 0·팝업1·잠금해제 / 목록창실패=BUSY_OFF 방송 / 목록창정상=원본대로 잠금유지). 실화면 회귀 BR55-5·6 추가.
| br53 | ✅ | ❌ | ❌ | ✅ | ✅ | 03 취합: **코덱스 P2 1건 + 안티 P2 2건 → 겹치는 것 합쳐 2건, 둘 다 반영·반론 0**. **P2-1(두 검수 동일)** 이동·대기 구간에 화면이 열려 있어 그 사이 속성 줄이 다시 그려지면 사라진 값칸을 기준으로 펼침목록이 화면 구석에 뜨고 그 목록을 고르면 이전 줄 데이터가 바뀜 — **BR53 이 대기를 넣으며 내가 만든 틈이 맞음**(BR53 이전엔 조회 직후 같은 처리 안에서 곧바로 펼쳐 틈이 없었음). =**반영**: 원본이 잠금을 쥐는 구간 그대로 재현 — 원본은 목록을 받은 뒤에도 잠금을 계속 쥐고 **펼치기 바로 직전에** 푼다(uiAttributeArea.js 1128~1148 = setAttrFocus→잠금해제→`_oUi.open()`, F4 경로 1335~1344 동일). HTML5 는 목록 조회 함수가 **콜백 직전에** 화면잠금을 먼저 풀어버려(attr.js 647행) 틈이 생겼으므로, 이동 시작 직전 화면잠금을 다시 걸고 목록 펼치기 직전에 푼다. 소유 표시로 1회만 해제(남의 잠금 보호), 조회 실패·예외도 마지막 단계를 지나 반드시 해제. 차단 근거 소스 확인=화면잠금 본체가 `document.body.style.pointerEvents="none"`(resources/index.js 873~889) 라 프레임 안 WS20 의 다른 줄 클릭·변경항목 보기 토글이 **조작 자체가 안 됨** → 두 검수 재현 시나리오 소멸. 부수 확인=화면잠금은 켤 때 커서 위치를 기억했다 끌 때 되돌려 주므로(898~918) BR53 의 "그 줄 값칸에 커서" 결과와 상충하지 않음(오히려 보강). 깜빡임 없음(껐다 켜는 것이 같은 실행 단위라 그 사이 그리기 없음). **P2-2(안티)** 스크롤 대기 상한이 프레임 횟수뿐이라 창 최소화·가림 시 프레임이 안 돌아 약속이 영영 안 끝날 수 있음 =**반영**: 실제 경과시간 타이머(800ms) 병행, 먼저 끝난 쪽이 종료·나머지 정리. 코덱스는 제품 창 설정(backgroundThrottling 꺼짐) 근거로 이 지적을 참고로 하향했으나, **P2-1 반영으로 이 대기가 화면잠금을 쥐게 되어** 안 끝나면 잠금이 영구히 남으므로 위험 등급을 올려 안티 권고대로 상한 보장. 프레임 조건도 `iTick > 40`(41프레임) → `>= 40`(40프레임)으로 정정(코덱스 참고 지적). **미반영 1건(허가 대기)**: 두 검수 모두 공통 드롭다운(`theme/u4a-ui.js` `_open`)에 "값칸이 화면에서 사라졌으면 열지 마라"(isConnected) 가드를 권고 — **공통 자산 직접 수정 금지 규칙**이라 미반영, 스코프 override 로는 불가(펼치기 결정이 공통 함수 안이고 `_requestOpen` 은 내 약속이 성공이든 실패든 똑같이 `_open()` 호출 — 소스 확인). 화면잠금으로 **정상 조작 재현은 불가**해졌고 남는 건 다른 창 알림이 그 짧은 사이 속성 줄을 다시 그리는 비정상 경로뿐 → 한 줄 가드 허가 요청. 양측 통과·무변경 항목: 이동→문구 재설정→항목 갱신→대기→펼치기 순서가 원본 의미 보존, `bKeepRow` 와 BR52 선택 유지/해제 계약 정합, 기존 2인자 호출부(fnWs20SelectUI) 종전 경로 유지, 값칸 선택자 한정으로 아이콘 버튼 오선택 제거(**한 값칸에 주 값 컨트롤이 둘 이상 생기는 경로 없음**을 코덱스가 확인 → 01 요청서 질문 해소), 등록 이벤트·조회 실패 경로 무회귀·BR24 오판 방지 유지. node --check 통과. 백업 `_ws_html5_ws20_attr.js.br53fixbak`. ★테스트 범위 정정(장군님 지적 2026-08-21 "이미 콤보를 열었는데 무슨 이동?"): **지적이 맞음**. 이 호출 지점은 사용자가 그 값칸을 직접 눌러 시작하는 경로라 **그 줄은 이미 보이고 커서도 이미 그 값칸에 있음** → 원본 처리 4가지 중 **화면 이동·커서는 눈에 보이는 변화 0**(노션 이슈 본문에도 "체감 작음" 명기). 이동·커서 항목 삭제, **실제 효과는 ①위쪽 요약영역 접힘 ②그 줄 선택 강조 둘뿐**. 코드는 유지(원본이 부르는 함수를 그대로 부르는 것이 1:1, 접기·강조만 떼어 부르는 건 원본에 없는 변형). 접기가 목록을 실제로 스크롤시키므로 대기·잠금 처리는 여전히 필요. **보고만**: 이 경로에서 위쪽 영역을 접는 것이 사용자에게 이득인지 의문(누르려던 값칸이 화면에서 움직임) — 원본대로 두었고 빼려면 지시 필요. ★장군님 지적으로 이슈 자체가 실화면 미성립 확인: 노션 증상(그 행으로 화면 이동 없음)은 **원본 코드에 호출이 있다는 사실만 보고 적은 것**이며, 사용자가 그 값칸을 직접 눌러 시작하는 경로라 이동할 곳도 옮길 커서도 없음. **결정=원본대로 두고 마무리**(코드 원복 없음, 원본 호출 1:1 유지 + 그 호출 때문에 필요해진 보호도 유지 — 빼면 프로퍼티 적은 요소에서 펼친 목록이 바로 닫히는 새 결함). 화면에서 달라지는 건 **줄 선택 표시 하나뿐**. **곁들여 진짜 결함 1건 발견·수정**: 좌측 선택 줄을 움푹한 표시와 같은 자리(box-shadow)에 그려, 값이 든 줄(=항상 변경됨 줄)에서 **마우스를 올렸을 때만 파란 줄이 나타났다 떼면 사라짐**(마우스올림 규칙이 한 단계 세서 그때만 이김) → 줄을 ::before 로 분리해 항상 보이게(ws20.css, 백업 `_ws20.css.br53selbak`). 속성 줄 전체 공통이라 BR52 에도 적용. 또한 변경됨 판정은 **기본값과 다르면 참**이라 이벤트 값이 든 줄은 언제나 변경됨 줄 — 종전 "값을 바꾼 적 있으면" 단서는 틀렸음(BR52·BR53 테스트 문구 모두 정정). 테스트 **BR53-1 한 건**으로 축소(당연한 정상동작·회귀 항목 삭제). 실화면 `.works/ws20attrchange` BR53-1 대기 실화면 BR53-1 통과·히스토리 이관(2026-08-30). 노션=수정완료. ★두 검수 권고(공통 드롭다운에 "값칸이 화면에서 사라졌으면 목록 열지 마라" 1줄)=**최종 미반영 확정**(장군님 결정 2026-08-30). 사유: 원본에 없는 방어이며 원본은 구조상 이 사고가 안 남(UI5 는 재렌더해도 값칸 컨트롤 인스턴스 유지 → open 이 제자리, HTML5 는 innerHTML="" 로 줄을 새로 만들어 옛 값칸이 고아가 되는 구조 차이) + 눌렀는데 무반응이라 체감상 부적절 + 이동구간 화면잠금으로 정상조작 재현 불가. 상세 `.audit/br53/03_response.md` 6절. | ★**선택 표시 최종 철회(장군님 결정 2026-08-30)** — 원본 속성표는 클릭 선택이 켜진 sap.m.Table(319행)이라 그 기본 성질이 보인 것뿐이고 `getSelectedItem`/`selectionChange` 등 **선택 결과 소비처가 원본 전체 0곳**(전수 확인) → 화면상 무의미. 오류 인지는 값칸 빨간 테두리+안내 문구로 충분. attr.js 선택키 보관·재렌더 복원·해제 3지점 + ws20.css 선택 규칙 + edit.js `bNoRowSelect` 옵션까지 전부 제거(잔여 0 확인). 이동·접힘·커서·I/W 는 원본대로 유지. node --check 통과. 백업 `_*.br52selrmbak`. |
| br52 | ✅ | ❌ | ❌ | ✅ | ☐ | 03 취합: 코덱스·안티 지적 동일 3건 **전부 반영**(반론 0). **P2-1**(선택이 DOM 에만 있어 재렌더 시 소실)=선택키 상태 보관(`oAPP.attr._attrSelUiatk`)+`fnRenderWs20AttrRows` 행 생성 루프가 매 세대 복원+`_updateAttrList` 진입부 해제 — 원본 "같은 UI 재렌더 유지 / 다른 UI 전환 시 removeSelections(7139~7150)" 구분 1:1 재현(setAttrFocus 의 수동 지정/해제 블록은 제거, 렌더가 단일 소스). **P2-2**(선택 CSS 배경만)=공통 선택 행 세트 전체 소비(shell.css 686~691: `--selected-text`+굵기600+inset accent 줄, hover 동일)+라벨 글자색 동반 지정 — 진한 선택배경 테마(mac/suse/95) 대비 확보, 토큰만 사용. **P2-3**(TYPE I/W 누락)=원본 8246~8267 E/S/I/W 4종 복원. 남긴 참고: 선택+변경행 중첩 시 라벨은 변경 강조가 이김(원본도 중첩 구조)·키보드 선택 의미 미부여(속성 행은 클릭 선택 개념 없음, 임의창작 금지). node --check 통과. **★장군님 실화면 지적 3건 → 원본 대조로 전부 규명(코드 변경 0)**: ①"선택 강조가 안 보인다"=원본 `changedDataMarker`의 `.changeValue` CSS 가 테마 조건까지 붙은 고명시도(`html[sapUiTheme][dark] .u4aWs20Page .u4aWsDesignAttr .changeValue`, attrPresetPopup/list/css/index.css:27~50)로 배경·그림자·라운드를 지정해 **원본에서도 UI5 기본 선택표시를 덮음** → 값 바꾼 줄에서 선택 강조 비노출이 원본 동작, 내 구현이 원본 1:1(테스트 시나리오가 하필 변경된 줄을 지목한 게 오류). ②"부드러운 스크롤 아님"=접기(`_attrHeaderExpanded(false)`)가 먼저 즉시 scrollTop 이동 → 뒤이은 smooth `scrollIntoView(block:"nearest")` 는 이동량 0 이라 애니메이션 비가시. 원본도 동일 순서(8338→8346)라 1:1. ③"커서 안 감"=Find 창은 링크 클릭 후에도 닫히지 않아(fnFindPopupOpen 에 close 없음) 별창이 앞에 남음 → 부모창 focus 비가시. 원본도 동일 구조. **★테스트 시나리오 전면 재작성(장군님 지시)** — 앞서 제안한 "되돌리기(↶) 경로"는 **오판이었음, 소스로 확정 철회**: `fnWs20PushUndo(oFocus)` 호출 **43곳 전부 인자 없이 호출**(전수 grep) → `_snap.focus` 미생성 → undo 시 `sFocusUiatk=""` → `setSelectTreeItem(OBJID, undefined)` → `setAttrFocus(undefined)` 가 3224행 첫 가드에서 즉시 return → **되돌리기로는 접힘·선택·포커스 전부 미발생**. (원본 undoRedo.js:2278 `setSelectTreeItem(_sAttr.OBJID, _sAttr.UIATK)` → uiDesignArea.js:2561 `setAttrFocus(UIATK,TYPE)` 는 존재 → **HTML5 속성값변경 undo 의 "그 속성 줄로 이동" 자체가 미이식 = 별건 누락, 보고만**). **실제 도달 경로는 3개뿐이며 전부 별창**: Find 창(fnFindPopupOpen.js:168)·오류 목록 창(ws_fn_ipc.js:96)·UI5 미리정의 CSS 팝업 SELECT_UI(fnDialogPopupOpener.js:3534). → **커서(포커스)는 어느 경로로도 육안 확인 불가**(별창이 전면 유지, 원본 동일) → 테스트 제외. **선택 강조는 `.changeValue` 가 덮으므로**(원본도 동일) 이동 후 **그 값을 지워 changeValue 를 제거하면 드러나는지**로 검증(BR52-2, P2-1 재렌더 유지 로직도 동시 검증). **★테스트 항목 정리(장군님 지시 2026-08-30)** — 실화면에서 고치기 전/후 구분 불가한 항목 제외, **BR52-2·BR52-3(선택 표시) 2건만** 남김. 제외 사유: ①**위 영역 접힘**=이 화면의 접힘은 스크롤로 위 영역이 밀려 사라지는 것과 동일 동작(`_attrHeaderExpanded(false)` 가 scrollTop 조정) → 데려갈 대상(스타일클래스·이벤트·바인딩)이 전부 목록 아래쪽이라 스크롤만으로 같은 결과, 접기 유무가 화면에 차이를 못 만듦(장군님 지적, 소스 확인). ②**부드러운 스크롤**=접기의 즉시 scrollTop 이 선행해 뒤이은 smooth `scrollIntoView(block:"nearest")` 이동량 0(원본도 동일 순서). ③**커서**=도달 경로 3개가 전부 별창이라 전면 유지로 육안 확인 불가(원본 동일). ④**TYPE I/W**=이 경로로 미도달·무증상(코드만 원본 4종과 일치시킴). 실화면 `.works/ws20attrchange` BR52-2·BR52-3 대기 | ★**선택 표시 최종 철회(장군님 결정 2026-08-30)** — 원본 속성표는 클릭 선택이 켜진 sap.m.Table(319행)이라 그 기본 성질이 보인 것뿐이고 `getSelectedItem`/`selectionChange` 등 **선택 결과 소비처가 원본 전체 0곳**(전수 확인) → 화면상 무의미. 오류 인지는 값칸 빨간 테두리+안내 문구로 충분. attr.js 선택키 보관·재렌더 복원·해제 3지점 + ws20.css 선택 규칙 + edit.js `bNoRowSelect` 옵션까지 전부 제거(잔여 0 확인). 이동·접힘·커서·I/W 는 원본대로 유지. node --check 통과. 백업 `_*.br52selrmbak`. 실화면 확인 항목 0(전부 구분 불가) → 히스토리 이관, 그룹 종결. |
| br51 | ✅ | ❌ | ✅ | ✅ | ☐ | 03 취합: 코덱스 P1·P2 **둘 다 반영**(반론 0), 안티=통과였으나 코덱스가 옳음. **P1**(표면화 후 정상 흐름 복귀→검증 안 된 값이 변경 수집 prev._T_0015→저장 후보 유입, 원본은 예외 전파로 수집 미도달=fail-closed)=**반영**: catch 에서 표면화 후 `return;` 추가 — 후속 수집·스타일·재렌더 중단, 함수 전체 finally 가 busy·단축키·자식창·재진입 플래그 원복(autoGrowing 조기 return 3823행과 동일 기성 패턴, 잠금 고착 없음). throw 대신 return=표면화 이미 완료+미처리 예외 부수효과 회피. **P2**(내부 예외 원문 e.message 화면 노출)=**반영**: 화면 안내는 추적 코드 `WS20ATTR-CHK01` 만, 상세는 콘솔만. 정식 문구 키는 코덱스도 "담당자 지정 필요" 명시 → 장군님 번호 지정 대기. 안티 [P4] "불량 엔트리 즉시 제거" 서술은 점검 성공+RETCD=E 경우라 P1(점검 자체 미실행) 반박 아님. node --check 통과. 실화면 `.works/ws20attrchange` BR51-1~2(무회귀) 대기 |
| br60 | ✅ | ❌ | ❌ | ✅ | ✅ | 실화면 BR60-1~12 전부 통과·히스토리 이관(2026-08-21). ★테스트 중 정정: BR60-9 되돌리기 단축키를 Ctrl+Z 로 적었으나 이 화면은 Ctrl+Shift+Z(Ctrl+Z 는 목록에서 주석처리·미사용, 다시실행=Ctrl+Shift+X) — 장군님 지적으로 소스 재확인 후 항목 수정, 겸사 미리보기 제목 옆 자물쇠 표시(원본 1:1 기존재) 확인을 항목에 추가. 노션 상태변경 대기. 03 취합: **코덱스·안티 지적 동일(P1 2건·P2 1건) 전부 반영, 반론 0**. **P1-1**(재배치 분기에서 재로드 완료 리스너를 걸고도 블록 끝 즉시 해제가 잔존 → 저장 직후 잠금 풀림 + 재로드 완료 때 2회차 해제가 남의 잠금까지 해제)=**반영**: 즉시 해제 줄 제거(주석은 "재부착 없음"인데 실제 위치가 재부착 성공 블록 안 — 배치 실수 확인). **P1-2**(순서 무변경 경로에서 `fnWs20LoadPreview()` 직후 동기 해제 → 로더가 방금 인수한 화면잠금까지 파괴)=**반영**: 로더가 [진입 시 자기 잠금 ON → 미리보기 구성 완료/실패 시 스스로 OFF] 완결 계약을 이미 보유(소스 확인) → **화면잠금 소유권을 로더에 이전**(`_bOwnBusy=false`), 로더 미관리인 **단축키 잠금만 "미리보기 로드 종료" 알림**으로 해제. **P2**(빈 catch·멱등 없음)=**반영**: 전 지점 console.error 표면화 + **소유권 기반 1회 해제**(`_bOwnBusy`/`_bOwnShortcut`, 켜기 성공 시에만 소유 표시 → 남의 잠금 보호·중복 해제 차단). 부수: 재배치 경로에 iframe `error` 회수 추가(로더 기성 패턴 차용, busy 짝 규칙). prev.js 는 **기존 종료 지점 한 곳**(`_ws20ReleasePrevBusy` 맨 앞, 조기 return 앞 = 성공·실패 공통 경유)에 알림만 삽입 — 등록자 없으면 무동작, 타 호출부 영향 0. 종료 갈래 6곳 전수 짝 확인. **남은 한계(사실 기재)**: 재배치 경로의 iframe `load` 는 문서 로드 시점이라 그리기 완료와 불일치(코덱스 보강 지적 수용) — 그 재로드는 미리보기 iframe 내부가 진행해 부모 로더를 안 타고(BR49 의도), 정확한 완료 신호를 얻으려면 `design/preview/index.js`(**원본 파일·수정금지**) 를 고쳐야 함 → 원본 수정 허가 시 교체 가능, 지시 대기. **원본 조사 보고**: 원본은 저장 시 단축키를 잠그기만 하고 **이 흐름에서 다시 풀지 않음**(미리보기 소스 내 해제 0건 전수확인) = 원본 결함 추정 → 이슈 기대결과("끝나면 푼다")대로 푸는 쪽 구현. 맞바꿈 6조합 동치·취소 안내 001·확인창 잠금·런타임 실재·BR49 무충돌은 양측 통과·무변경. node --check 양 파일 통과. 실화면 `.works/디자인레이아웃변경팝업` BR60-1~12 대기 |
| br61 | ✅ | ✅ | ✅ | ✅ | ✅ | 실화면 BR61-1~5 전부 통과·히스토리 이관(2026-08-21). 노션=수정완료. 03 취합: **코덱스·안티 둘 다 통과·필수 수정 0건 → 반영 코드수정 0**. 본수정 3지점(문구 조회 실패 표면화 2곳 / 순서변경형 269 를 `_wsc`(ZMSG_WS_COMMON_001)→`_msg`(/U4A/MSG_WS) 정정 / 취소콜백에서 RCODE 02→KIND20 알림창·그 외→KIND10 토스트로 RTMSG 안내 복원)이 원본 호출측 블록과 1:1 확인됨. 메시지 클래스 전수점검(코덱스 7건·안티 `_wsc`12·`_msg`8 전수) 추가 오지정 0건. 잠금 대칭·showMessage 예외 시에도 _done 도달·회귀 4종·3경로 동등성 양측 통과. **[해명1=코덱스 조건부 "구조체판 원본 미확인"]** 검수자가 본 `design/js/uiDesignArea.js` 는 **작업폴더 사본**이며 원본 SSOT 는 U4A_WS_DESIGN — 그곳에 구조체판 실재(정독 확인, t01 반띐1과 동일 정정) → 조건부 단서 해소, 코드변경 0. **[해명2=별건·보고만]** 선택창 겹침 시 앞 창 취소콜백 미호출(코덱스가 필수에서 제외) — 진입부 잠금으로 정상조작 재현 불가 + 원본도 겹침방지 없음(임의창작 금지) → 미반영, 지시 시 별도 코드번호. 병행 t01 보완 후에도 BR61 4-hunk 생존 재확인, node --check 통과. 실화면 `.works/디자인트리컨텍스트메뉴` BR61-1~5 대기 |
| t01 | ✅ | ❌ | ❌ | ✅ | ✅ | 03 취합: 반영 2·반론 3. **[반영1=코덱스 P1]** 옛 UAI(작업폴더 design/UAI, `.length` 계약) 호환 — 반환 구조체에 `length`(=T_SEL 개수) 전 경로 동기화(`_fin`). 옛 UAI 는 `.length` 검사만(408·411, 원소접근 0)·원본 최신 UAI 는 이미 구조체 소비(WS_DESIGN parseAiLibraryData.js:434) → 덮어쓰기 시 자연 정합. **[반영2=안티 P2]** 방어가드(_LIB 체인·T_0022/23/27‖[]·UIADT‖""·prev 체인). **[반론1=코덱스 P1 "원본 부재"]** 구조체판 원본은 저장소 밖 SSOT(U4A_WS_DESIGN)에 실재(uiDesignArea.js:1596~1680·aggrSelectPopup.js:32·designChkSelLine:1474·UAI:434 정독) — 검수자가 저장소만 검색. 클로버도 실측 반박: 작업폴더 uiDesignArea/aggrSelectPopup 로드 배선 0건. **[반론2=코덱스 P2 0:1 노출]** 최신 구조체판에 0:1 제외 분기 자체 없음(의도된 제거, 점유 0:1은 다음 단계 개수점검 022가 차단) → 내 이식=최신 1:1, 옛 제외 부활은 원본과 달라져 미채택. **[반론3=안티 P3]** RTMSG 폴백은 소비부에 기존재(262/280), 하드코딩 문구는 임의생성 금지 규칙 위반. node --check 통과·length 논리 Node 검증. **실화면 T01-1~4 전부 통과·히스토리 이관(2026-08-21)** |
| br59 | ✅ | ✅ | ✅ | ✅ | ✅ | 03 취합: **코덱스·안티 둘 다 통과·필수 수정 0건 → 반영 코드수정 0**(구현이 원본 `contextMenuUiMove` 산식과 1:1). 그룹 위치 `_aggrPos`(원본 l_indx1/2)·UA026 감산 `_aggrPrevIndex`(원본 l_cnt)·`iAggrBefore!==iAggrAfter` 가드(원본 l_indx1!==l_indx2)·양 경로(트리·미리보기 Up/Down 위임, Move Position 0-based 위임)·미리보기 index 소비(insertAggregation)·T_9011 미로드 시 그룹index 유지·클로저 안전·경계(음수 index 이중가드)·Undo/splice/선택/busy try-finally 전부 양측 통과. 트리 splice(전체 배열)는 원본대로 유지. node --check 통과(코덱스 edit.js·prev.js 재확인). 안티 서브에이전트 2기 심층 팩트체크(공식 1:1·경계·D&D 동등성·동기 트랜잭션) 무결점 통과. **★장군님 실화면 지적(2026-08-21): 위/아래 이동 후 미리보기가 그 자리로 화면 이동(스크롤)을 안 함** → 원본 재대조로 원인 확정: 미리보기 선택표시(`fn_mark`, preview/index.js:4893)가 선택 UI 에 `focus()` 를 주어 브라우저가 그 자리로 스크롤하는 구조인데, 이동 경로가 **다시 그리기 완료 전에** 선택을 불러(그것도 await 없이) 옛 DOM/미배치 상태에 focus → 스크롤 무효. 원본 `contextMenuUiMove` 546~599 는 **①prevRemoveUiObject → ②reCreateUIObjInstance → ③setAfterRendering 등록 → ④moveUIObjPreView → ⑤rerender+await(+RTE await) → ⑥await setSelectTreeItem** 순서(=UI 추가 경로 designAddUIObject 5651~5681 과 동일 원리: 다 그린 뒤 await 선택). **반영(BR59-2차)**: 공용 `_movePreviewAndWait(oNode,aSib)` 신설로 ①~⑤ 원본 1:1 이식, `_moveUI`·`fnWs20MoveUIToIndex` 를 **async 화**하고 `await _selectNode` 로 교체(양 경로 공용). 기존 `_rerenderParentRTE` 에 midFn 인자 추가(원본 순서상 이동은 대기등록 뒤·재렌더 앞) — **midFn 은 조기종료 경로에서도 1회 보장 실행**(미리보기/렌더모듈 부재 시에도 이동 반영 유실 방지). 두 보조함수는 dnd.js 기정의(703·716, 원본 1:1)이며 로드순서상 edit.js 뒤라 실행시점 가용. busy 는 try/finally 유지(이제 렌더 완료 후 해제 = WP1 부합). 삽입 경로 호출(1220)은 midFn 미전달로 동작 불변. node --check 통과. 백업 `_ws_html5_ws20_edit.js.br59scrollbak`. **★장군님 실화면 지적 2(2026-08-21): 이동 되돌리기 후 선택표시가 옮긴 컬럼이 아닌 엉뚱한 줄로 감** → 원본 `undoRedo.js` 전수 대조로 **계약 위반 확정**: 원본은 **모든 액션이 '그 액션의 대상 UI'를 재선택**한다(MOVE=`contextMenuUiMove` 599 `await setSelectTreeItem(ls_tree.OBJID)` / INSERT 543 / DRAG_DROP 1758·1924 / CHANGE_ATTR 2278 / AI_INSERT 2872. DELETE 1012 만 예외적으로 '현재 선택 유지, 단 그게 삭제 대상이면 부모, 없으면 ROOT'). 내 스냅샷 복원은 앵커를 `s.sel`(=편집 **직전에 선택돼 있던** 노드)로 잡아, **우클릭은 선택을 바꾸지 않으므로**(원본 callDesignContextMenu 의 `setSelectTreeItem(OBJID)` 는 주석처리=선택 안 함) '딴 줄 선택 → 다른 줄 우클릭 이동 → 되돌리기' 시 강조가 딴 줄로 튐. **반영(BR59-3차)**: `fnWs20PushUndo` 가 `{OBJID}` 만으로도 대상 기록되게 완화(기존엔 `OBJID`+`UIATK` 둘 다 있어야 기록 → 사실상 사문화), 이동 두 경로가 `{OBJID: oNode.OBJID}` 전달 → 되돌리기·다시하기 모두 옮긴 UI 재선택(`_doApply` 가 반대 스택에도 focus 승계하므로 양방향 동일). **A-Z 재감사 결과(원본 1:1 확인된 것)**: 메뉴 Up/Down/Move Position 활성판정(자식 1건=전부 비활성 / 첫줄=Up 비활성 / 끝줄=Down 비활성) 원본 296~316 과 동일 · 같은그룹 index+UA026 감산 동일 · `l_indx1!==l_indx2` 가드 동일 · 위치이동 팝업 위치값이 '전체 자식 기준 1-based/총건수' 인 점 동일 · 펼침상태 사이드맵은 UI이름 키 단일맵(트리·선택경로 동일 객체, 분열 없음) · 선택강조·트리스크롤·펼침 모두 UI이름 기준(번호 기준 아님) 재확인 · DELETE 앵커(선택유지→부모→ROOT)는 이미 원본과 일치. **남은 원본 불일치(보고만, 지시 대기)**: ①되돌리기 앵커가 아직 `s.sel` 인 나머지 경로 = UI추가(edit 1137)·내패턴적용(dnd 1122)·끌어놓기 맞바꿈(dnd 1294)·끌어놓기 부모이동(dnd 1369)·복사붙여넣기(dnd 1586) → 원본은 각각 '삽입/끌어온 UI' 선택. ②속성값 변경 경로 전부 `fnWs20PushUndo()` 무인자 → 원본 CHANGE_ATTR 의 '값 바뀐 UI+그 속성줄' 재선택 미동작(BR52 에서 별건 보고된 것과 동일 뿌리, 이번 완화로 배선만 하면 동작). ③자식창 BUSY 방송에 안내문구 미탑재 — 원본 `sendBindPopupBusyOn` 은 전체 자식창엔 문구 없이(=내 것과 동일), **바인딩 팝업 채널에만** 문구(208 '디자인 화면에서 UI 변경 작업을 진행하고 있습니다.')를 실어 보냄. HTML5 는 그 두 번째 전송이 없음(edit.js 편집경로 공통). node --check 통과. **★반영(BR59-4, 장군님 지시 '되돌리기 ①② 정리')**: 원본 undoRedo 계약(액션 대상 UI 재선택)을 남은 전 경로에 이식. **①대상 미기록 5경로**: UI추가(edit `_showInsertPopup`)·내패턴적용(dnd `_applyP13nPattern`)·복사붙여넣기(dnd `designCopyUI`)는 **대상 이름이 편집 도중 생성**되므로 신설 `fnWs20SetUndoTarget(oFocus, oSnap)` 로 사후 각인, 끌어놓기 맞바꿈·부모이동(dnd)은 `i_drag.OBJID` 를 push 인자로 직접 전달(원본 CL_DRAG_DROP 1758·1924). 템플릿 마법사(wizard `_aggrCb`)도 동일 사후 각인(원본 CL_INSERT_UI 543). **②속성값 변경 9경로**: 신설 `_undoFocus(oAttr)`(행 OBJID, 없으면 현재 uiinfo 로 보정) 로 `{OBJID, UIATK}` 전달 — 값변경 본체 `fnWs20AttrChange`·autoGrowing 확인창·appF4 삭제·F4도움말 삭제 2곳·Reset 루프(첫 실제변경 = 원본 `T_ATTR[0]` 의미)·앱검색 팝업·F4검색도움말 팝업·동일속성 일괄적용·속성 우클릭 메뉴 = 원본 CL_CHANGE_ATTR 2278 `setSelectTreeItem(OBJID, UIATK)` 이식(그동안 43곳 전부 무인자라 사문화). **의도적 미변경 2종**: (a)UI 이름변경(attr 1732) = 원본 CL_CHANGE_OBJID 에 `setSelectTreeItem` **부재**(1343~1561 전수확인) → 무인자 유지. (b)삭제 단건·다건(edit 515·674) = 원본 CL_DELETE_UI 1012 `_SEL_OBJID`(현재 선택 유지 → 그게 삭제대상이면 부모 → 없으면 ROOT)와 기존 폴백이 이미 일치. **방향성은 복원측 존재확인이 자동 처리**(추가 되돌리기 = 대상이 트리에 없음 → 선택유지 폴백 = 원본의 '추가 되돌리기=삭제' 선택규칙과 동일 / 다시하기 = 대상 존재 → 그 UI 선택). **경쟁 차단**: `fnWs20PushUndo` 가 자기 스냅샷을 반환하도록 하고 사후 각인은 **그 객체**에 직접(맨 위가 아니라) — 각인 전 `await`(부모 재렌더 대기) 구간에 타 편집이 끼어도 오각인 없음. Node 하네스로 앵커 규칙 7케이스(이동 되돌/다시·추가 되돌/다시·옛선택 소멸시 부모·속성값·이름변경) **전건 통과**. 7개 파일 node --check 통과. 백업 `_ws_html5_ws20_{edit,dnd,attr}.js.br59undobak`. **★③(바인딩 팝업 안내문구) = 내 오보, 철회**(장군님 지시로 착수 후 수신측 확인 중 확정): 앞선 보고는 **발신측만 보고** 판단한 오류였다. 원본 WS20 는 BUSY_ON 을 2회 전송(ⓐ전체 자식창 `oMainBroad` 문구없음 ⓑ바인딩팝업 전용채널 `OPTION`=208 문구)하나, **수신측이 그 OPTION 을 원본에서도 버린다** — 원본 `U4A_WS_DESIGN/Popups/bindPopup/wsDesignHandler/broadcastChannelBindPopup.js` `responseBindPopupBusyOn` 은 `oAPP.fn.setBusy(true,{ISBROAD:true})` 만 호출(주석: WS20↔팝업 BUSY 핑퐁 방지 목적으로 의도적), 작업폴더 사본도 동일. 게다가 바인딩 팝업은 **ⓐ전체 자식창 채널도 직접 구독**(`Popups/bindPopup/frame.js` `_initBroadcast`, `index.js` `broadToChild.onmessage`)해 **동일한 `setBusy(true,{ISBROAD:true})`** 를 수행 → HTML5 의 ⓐ 단독 전송만으로 **원본과 화면 결과 완전 동일**(잠금+닫기버튼 비활성). 즉 원본에서도 208 문구는 이 방향에선 **표시되는 곳이 없는 사문화 인자**이며, DESC 가 실제로 살아나는 방향은 **팝업→WS20**(`TYPE:"DIALOG"`)뿐인데 그쪽은 HTML5 에 이미 배선돼 있음(`js/broadcast/ws_fn_broad.js` TITLE/DESC 파싱→`parent.setBusy("X",_sOption)`). → **코드 변경 0, 불일치 아님**. 교훈: 방송 계약은 발신·수신 양쪽을 봐야 판정 가능. **실화면 BR59-1~9 전부 통과·히스토리 이관(2026-08-21)**. 되돌리기 관련(옛 BR59-10~17)은 **범위가 달라 BR62 로 분리 등록**(노션 신규) 후 `.works/되돌리기다시하기/` 로 이관 — 통과분 7건은 그쪽 히스토리, 남은 검증은 BR62-1~8. **노션 BR59 = 수정완료 처리 완료(2026-08-21)**. |
| br46 | ✅ | ❌ | ❌ | ✅ | ✅ | 실화면 BR46-1~3 전부 통과·히스토리 이관(2026-08-19). 노션=수정완료. 03 취합: 반영 1(안티 P2=빈 catch → console.error 표면화, code.md 규칙). 반론 2건. **메인창 무잠금 P1**(코덱스·안티)=원본 designAddUIObject 도 await 전후 무잠금, WS20 비동기 전반 선재 → BR43/44와 동일 BR60(전역 공통 잠금)로 분리. **리스너 선등록 순서 P2**(코덱스)=원본 setOnAfterRender 확인, 부모 완료 Promise는 명시 rerender() 호출이 resolve 구동(등록 앞/뒤 무관, 누락·고착 없음)+형제 dnd.js _rerenderCore 와 1:1+RTE 자식수집 시점 원본 5651 동일 → 미채택. 코덱스 최초 빈catch P2 는 스스로 철회(reject 미발생), 규칙상 반영. node --check 통과. 실화면 .works/UI추가팝업 BR46-1~3 대기 |
| br49 | ✅ | ❌ | ✅ | ✅ | ☐ | Codex 재검수 P1·P2 둘 다 반영(안티=통과). **P1**(재부착 후 wireSplitter 예외→setDesignLayout false 축약→_doSave false분기서 참조정리 건너뛰고 명시 재로드→크래시 부활)=**반영**: 배선을 자체 try/catch(console.error)로 감싸 예외와 무관하게 return true(재부착 사실 보존). **P2**(새 iframe self-draw 완료 전 같은순서 재저장→진행중 재로드와 두번째 drawPreview 병렬→파괴 경쟁)=**반영**: 재배치 시 `__ws20PrevReloading` 표시 세움+iframe load 에 해제, 순서무변경 분기는 표시 켜져있으면 명시 재로드 생략(막는 건 같은순서=이미정확 경로뿐이라 무해). 근본 봉쇄(원본 busy 잠금 부재)는 BR60으로 분리 등록. 정상 self-heal·5종 참조정리·타 호출처·스토리지 정규화는 양측 통과. node --check 통과. 실화면 .works/디자인레이아웃변경팝업 BR49-1~3 대기 |
| br45 | ✅ | ❌ | ❌ | ✅ | ☐ | 코덱스·안티 동일 P1: attrUploadUrlException 기존행 보정 분기(attr.js 1481·1496) `UIAT` 오타→개인화/타앱 복붙 시 uploadUrl 이 현재 APPID 로 갱신 안 됨. **원본 대조: 오타는 원본(uiAttributeArea.js 7032·7047)에도 동일 → 내 이식은 1:1 충실.** ★장군님 결정(2026-08-18)=**미반영(원본 1:1 유지·원작자 회신)**. 실제 적용=호출 배선(삽입 루프)만→같은 앱 신규 추가는 정상. 오타 영향범위 조사(소비처 전수): `.UIAT` read 0건(보정 완전 무력화), 유효 소비=저장/개인화/복붙/속성표시 4곳, 미리보기 무관. 영향=cross-app(다른앱 개인화 적용·타앱 복붙·그 상태 저장)에서만 옛 앱 주소 서버 전파, 단일앱 무해. 테스트=`.works/UI추가팝업` BR45-E1~E4(영향도)+BR45-1~3(정상배선) 대기. node --check 통과 |
| br42 | ✅ | ❌ | ❌ | ✅ | ✅ | 실화면 BR42-1~3 전부 통과·히스토리 이관. 노션=수정완료. 코덱스·안티 동일 P1(Reset 루프에 dropAble/autoGrowing 연동 이벤트 초기화 attrSet*Exception(,false,true) 누락, 원본 uiAttributeArea.js:2222·2225) → **원본 2줄 1:1 복원 완료**(attr.js:4511~4517, 존재가드+console.error). 재검증: 두 예외처리 함수 본체 내부에 fnWs20PushUndo 없음 → BR42 단일 스냅샷 유지(첫 변경 직전 스냅샷 이후 이벤트 초기화 → Undo 1번에 프로퍼티+연동이벤트 함께 복원, 코덱스 T_CEVT 보존 확인). Undo 병합 자체는 양측 통과·무결. 반론 0. node --check 통과 |
| br47 | ✅ | ✅ | ✅ | ✅ | ✅ | 실화면 WC1~WC5 전부 통과·히스토리 이관(2026-08-18). ★검수 통과 후 실화면 WC1 재실패 → 진단(임시 로그)으로 원인 확정: 창(팝업)류는 미리보기에서 열리며 iframe 이 화면초점 선점, 메뉴 뜰 때 이미 active=IFRAME → 전역 iframe-blur-close 가 ≈2ms 뒤 합성 mousedown+.u4a-menu 직접제거로 즉시 닫음. [선택 await→틱→메뉴]만으론 불가(초점선점이 메뉴 표시시점에 이미 성립). **수정=메뉴 표시 시 oMenu.focus()로 active를 메뉴(부모문서)로 이동→teardown 가드(activeElement!==IFRAME) 조기 return→닫기 통째 skip**. 공통 u4a-ui.js 미수정(스코프내 edit.js). 상태기반이라 PC속도 무관(고정 ms 대기 없음, setTimeout(0)=순서양보). 코덱스·안티 둘 다 통과·필수수정 0. node --check 통과. 노션 상태변경 대기. 코덱스/안티 심층 검증 완료·지적 0. 1) [await 선택 → 한 틱 → 메뉴] 직렬화 복원이 매크로 태스크 큐(Macro Task Queue) 상에서 Iframe blur 이벤트를 선제적으로 완전 소진(Drain)시켜 메뉴 닫힘(Race Condition)을 원천 차단함이 입증됨. 2) await 이전 `e.clientX` 캐싱이 브라우저 이벤트 풀링 특성상 완벽한 Immutable 방어 조치이며, await 이후 노드(n)가 Stale 상태가 되더라도 순수 OBJID 기반 중앙 모델 재조회로 크래시를 원천 차단하는 방어적 로직(Defensive)임이 팩트체크됨. 무결점 통과 |
| br44 | ✅ | ❌ | ✅ | ✅ | ✅ | 실화면 BR44-1~3 전부 통과·히스토리 이관(2026-08-18). Codex P1 1건(독립 서브에이전트 교차확인)/안티 통과. UW03→UW08→UW10 정적 이식은 원본과 일치. 03 반영: P1(검사 뒤 프리셋 await 동안 대상 삭제→재개 시 화면엔 없고 prev/미리보기 고아→저장 payload 오염)=**await 후·fnWs20PushUndo 직전 대상 존재 재확인(_node) 추가**, 개인화 적용 경로에만, 없으면 조용히 취소(원본 동기 원자성 재현). 여섯 검사 전체 재검증=원본 1회검사라 과함·미반영, WS20 전체 잠금=원본 없는 방어·선재 공통이라 반론(별건, BR43 6c 동일). node --check 통과. 실화면 대기 |
| br43 | ✅ | ❌ | ✅ | ✅ | ✅ | 실화면 BR43-1~3 전부 통과·히스토리 이관. 노션=수정완료. 안티=통과, 코덱스=수정필요(P1). 정적 clamp·021·cardinality 상호배타는 양측 원본 일치. 03 반영: P1 핵심(undo push→`_readAttrPreset` await→삽입 사이 재진입 창)=**await 를 push 앞으로 이동**해 push~삽입 사이 await 제거(원자성 확보, 잠금 미추가). 변이 직전 재검증=await 사라져 불요(미반영). WS20 재진입 잠금·세대 토큰=원본에 없는 방어·선재 공통이라 반론(별건 권고). node --check 통과. 실화면 BR43-1~3 대기 |
| br41 | ✅ | ✅ | ✅ | ✅ | ✅ | 실화면 DE1~DE4 전부 통과·히스토리 이관. Codex/안티 둘 다 통과·필수지적0. Codex 서브에이전트 3축 재검수도 통과 유지(원본/현행 무작위 500상태 비교, 단건 부모+2단계 자손·멀티 VM PASS). 03 취합: 안티 제안2(삭제 호출부 조용한 catch→오류 표면화)=반영(edit.js 394·414 console.error, code.md 규칙 합치). 제안1(중복행 filter 전건삭제)=반론(원본 findIndex 1건 계약, 정상데이터 중복 없음). 제안3(uiDesignArea 늦은 로드 오버라이드)=반론(현행 미로드+typeof 등록가드, 가정적). 핵심 로직 변경0. node --check 통과. 노션 상태변경 대기 |
| br40 | ✅ | ❌ | ✅ | ✅ | ✅ | 실화면 DD1~DD4 전부 통과·히스토리 이관. 노션=수정완료. 03 취합: 두 검수 P1 동일(delDesc 함수 본체 부재→호출 no-op→T_DESC 잔존). 재확인=**현재 파일에 delDesc 본체 정상 존재**(attr.js:1400~1413, 원본 7783행 findIndex/-1 return/splice+T_DESC 가드). 검수본은 병행세션이 검수 중 되돌린 옛 상태(BR39 동일). 나머지(본인/하위 재귀 커버리지·삭제 경로 한정·Undo 스냅샷·멱등·원본 1:1·안티 P2 메모리 무위험)는 양측 통과·변경0. node --check 통과 |
| br39 | ✅ | ✅ | ❌ | ✅ | ✅ | 실화면 BR39-1~4 전부 통과·히스토리 이관. 노션=수정완료. 03 취합: 두 검수 모두 **코드는 정확·완벽** 일치(코덱스 통과, 안티 수정필요이나 권고안 a=문구정정). 쟁점=요청서·주석 "오류 UI 복귀 시 재표시" 문구가 원본과 다름. 원본 setUIInfo(uiAttributeArea.js:7462) 소스확인: UI 선택마다 uiinfo 새로 만들고 OBJID_stat 미보존 → **"다른 UI 이동 시 이전 오류 폐기"가 원본 계약, "복귀 시 재표시"는 원본에 없음**(이슈 기대결과도 재표시 요구 없음). 반영=주석/수용기준에서 "복귀 시 재표시" 삭제·정정, **코드 동작 변경 0**(현재 구현이 이미 원본 계약과 일치). 안티 P1(상태유실 결함)=반론: 별도 오류상태 Map은 원본에 없는 신규 동작이라 미채택(임의창작 금지). ①오류표시 동기화 ②`.data("OBJID")` 갱신은 양측 통과. ※검수 중 병행세션이 파일을 BR39 이전으로 되돌려 두 수정 재적용, node --check 통과. 실화면 .works/ws20attrchange BR39-1~4 대기 |
| br38 | ✅ | ✅ | ✅ | ✅ | ☐ | 코덱스/안티 심층 교차검증 완료·지적 0. 03 취합: 반영 코드수정 0(확정). 원인=미리보기 선택표시 함수 fn_mark 가 선택 강조·테두리만 하고 선택 UI 화면 안 이동이 빠짐 → scrollIntoView(block:nearest) 복원(밖이면 이동·보이면 유지). 근거 3종(이슈 기대결과·반대방향·원본 uiAttributeArea.js:8303). 1) 안티그래비티 초기 P1(Scroll Chaining 부모 튀김 결함) 오판 번복: WS20 메인 레이아웃이 겹겹의 `overflow: hidden` 고정 구조이므로 Iframe 연쇄 스크롤이 물리적으로 차단됨(안전). 2) `setTimeout` 0ms 트릭이 스크롤 이벤트 루프와 Overlay(선택 레이어) 좌표 갱신(Paint 1 & Paint 2)을 완벽히 동기화시켜 어긋남(Flickering)을 원천 차단함이 증명됨. 3) null/typeof 방어가드 완벽. 무결점 통과 |
| br37 | ✅ | ✅ | ✅ | ✅ | ☐ | 03 취합: 두 검수 통과·반영필요 0(추가 코드수정 없음). 원인=화면CSS(.u4aWs20InsRow display:flex)가 hidden 이겨 항상 표시, `.u4aWs20InsRow[hidden]{display:none}` 1줄로 원본 setVisible 의도 복원. 상태 5전이(최초·미선택/공백·단일·다건·복귀) 원본1:1, 명시도(0,2,0)>기본(0,1,0)·반응형 display 무재선언·데이터 상수1 주입 무결. 테스트 .works/UI추가팝업 BR37-1~5. 코덱스/안티 심층 교차검증 지적 0. 1) JS 토글 로직이 원본 UI5 `setVisible` 라이프사이클(초기/빈값/단일 숨김, 다건 노출)과 100% 1:1 일치함 입증. 2) 추가된 CSS `[hidden]`(명시도 0,2,0)이 기존 flex 규칙을 오버라이드하여 레이아웃 깨짐(Zero Side-effect) 없음. 3) 빈번한 토글 시에도 Reflow 프레임 드랍 전혀 없음. 4) 숨김 시 강제 초기화(1)가 확정 시점에 DOM을 바이패스하고 상수 1로 데이터 동기화되어 Data Sync Loss 0% 증명 완료. 무결점 통과 |
| br36 | ✅ | ✅ | ❌ | ✅ | ✅ | 코덱스 통과. 안티 P2(오류 catch에서 try안 선언 `_onEscKey` 참조→엄격모드 ReferenceError 삼킴→keydown 리스너 누수)=Node 재현으로 실재 확인 → **장군님 지시로 반영**: ESC 함수 이름칸만 try 밖(함수 스코프)에 선언(`var _onEscKey;`), 본체 배정은 try 안 유지(함제 `lf_cancel` 참조 보존). Node로 오류경로 리스너 정리·ESC 정상 재확인, node --check 통과. BR36 본수정(진입부 시작BUSY+짝해제)은 양측 정확성 통과. ★장군님 지적(2026-08-14): ESC에 취소 안내(001) 붙인 건 원본에 없음(원본 insertUIPopop.js:44~53 dialog=escapeHandler 없어 ESC는 기본 닫힘·안내 없음, 안내는 닫기88/취소782 press에만) → ESC 를 lf_close(무안내)로 원복했다가, ★장군님 최종 통일 결정(2026-08-14): 닫기 경로 일관성 위해 ESC 도 취소 안내(001) 뜨게 재변경(원본 UI5 ESC-무안내가 오히려 결함). 이제 ESC·닫기(X)·취소 모두 001 안내, 확정만 무안내. node --check 통과. 실화면 BR36-1~4 전부 통과·히스토리 이관. 노션=수정완료 |
| br34 | ✅ | ❌ | ❌ | 🔁 | ☐ | ★장군님 지시(2026-08-14) "원본 소스 동작과 똑같이": 툴팁 아닌 **원본 valueStateMessage(말풍선) 재현**. ①오류 시 빨간 테두리+오류 안내문구 팝오버(문서 최상단 fixed, _bindVsShow 방식), 값 확정 직후 자동표시+포커스/클릭 표시, 재렌더 정리, 툴팁=값(원본 uiAttributeArea.js:736·831). ②기본값 복구 원본 그대로(months=1 실화면 확인). ③지우기(X)는 원본대로 항상표시(앞서 넣었던 X숨김·비우기·안내 전부 철회). ④★실행(액티브) 후 값칸 빨간 잔존 — **진짜 원인 실측 확정(CDP로 살아있는 앱 직접 추적)**: 실행/체크가 부르는 오류점검 함수 **oAPP.fn.chkExcepionAttr·chkExcepUiTable 이 런타임에 undefined**(원본은 uiPreviewArea/미리보기 메인 안에서 정의하나 HTML5는 그 UI5 메인을 실행 안 함). ev_pressActivateBtn(ws_events.js:1528)은 `typeof chkExcepionAttr==="function"` 가드라 **점검 통째로 skip → attrClearErrorField 미호출 → 빨간 잔존 + 실제로 검증 없이 "액티브 성공"**. ev_pressSyntaxCheckBtn(1320)은 가드 없이 호출 → **undefined 예외로 busy 멈춤**. attrClearErrorField 자체는 정상(CDP로 직접 호출 시 데이터·DOM 다 지워짐 확인). → **두 함수를 원본(WS_DESIGN main.js:1315·1371) 1:1 이식**해 ws_html5_ws20_attr.js 에 가드 정의. CDP 실측 재검증: 오류 데이터 1→0, 화면 빨간 1→0(액티브가 실제 지움). 앞서 헛짚은 refresh 훅 재렌더는 되돌림. node --check 통과. **파일에 함수 있음≠런타임 로드됨**(감사 반성점). ⑤값칸에 onEnter=확정 배선(원본 sap.m.Input 엔터=change 재현) — 엔터로도 정상값 확정→오류 해제, 잘못된 값→오류 표시. ⑥★근본 버그 확정(서브에이전트 독립 검증): 값 변경 후 fnRenderWs20AttrRows(ROWS.innerHTML="")로 포커스 입력칸 제거 시 dirty change가 동기 재발화→fnWs20AttrChange 동기 중첩 재귀→안쪽이 attrClearErrorField로 방금 세운 valst 초기화+busy 조기해제→오류 표시 소실(범위밖 값 넣어도 오류 안 뜸). → fnWs20AttrChange 진입부 동기 재진입 방지 플래그(oAPP.attr._fnAttrChangeBusy) 추가, finally 해제. autoGrowing·M03·확인창 등 정상 재진입은 전부 비동기라 미차단(검증). 남은 참고(별건): 점검모듈 미로드 시 조용 skip(정상앱 무관), blur 후 refocus 포커스 되뺏김(테두리 정상). node --check 통과. 실화면 BR34-1·7·8 테스트 대기 |
| br32 | ✅ | ❌ | ❌ | ✅ | ✅ | 취합·반영(03): P1-1(최초 loadJs 실패 시 화면잠금 고착)=반영 — 디스패처 선-잠금 제거, 편집기 여는 함수가 스스로 잠금 관리(고착 여지 제거). P1-2(신규창 loadURL 실패·초기화 전 closed)=반론(별도 이슈): Edit메뉴 CSS/JS 편집기도 동일 선-잠금+위임 → 선재·공통, BR32 범위 밖. ★장군님 실화면 지적 반영: 원본은 값 전체가 아니라 **더블클릭한 그 단어(공백 기준)만** 취득해 편집기에 넘겨 그 단어를 찾아 블럭 처리 → 디스패처가 입력칸 커서 위치의 공백 토큰만 잘라 전달(편집기 검색은 원상복구). 실화면 BR32-1·1b·2~6 전부 통과·히스토리 이관. 노션=수정완료 |
| br33 | ✅ | ✅ | ✅ | ✅ | ☐ | 코덱스·안티+독립 반박 재검수 모두 통과·필수지적 0. 원본 가드·호출, 값칸 한정 제외, 중복방지, stale 참조, BR32 불교차 확인. 첫 click 비동기 조회와 dblclick 이동 공존도 원본 의도와 동일. 03의 공용 `execControllerClass` 동기 예외 엣지는 이식 오류가 아니어서 Pass 유지. 실화면 SAP GUI/느린 조회 UX 검증 권고 |
| br35 | ✅ | ❌ | ✅ | ✅ | ✅ | 실화면 BR35-1~3 전부 통과·히스토리 이관. 노션=수정완료. 판정 갈림: 안티=통과, 코덱스=수정필요(P1). 정상 경로(원본 UA040 인자·차단 순서 1:1, Undo 이전 차단=유령노드0, 팝업 잠금 대칭)는 양측 통과. P1=점검함수(dnd.js 정의) 부분 로드 실패 시 `typeof` 가드가 fail-open→금지 UI 재유입(트리 렌더러는 tree.js, 점검함수는 dnd.js로 파일 분리 확인). 03 반영: fail-open→**fail-closed**(함수 없으면 console.error 후 추가 취소, 원본 무조건 호출·붙여넣기 부재취소와 동일). 안티 P4(fail-open 강등 옹호)는 막는 점검엔 반대라 정정. 로더 실패후 WS시작 미차단=공통 인프라 선재이슈·범위밖 반론. node --check 통과. 실화면 BR35-1~3 대기 |
| br29 | ✅ | ✅ | ✅ | ✅ | ✅ | 코덱스·안티+독립 서브에이전트 재검수 통과·필수지적 0. 3종/9개 키·메시지283·확인/취소/바인딩건, 단일 스냅샷과 `_T_0015`+`T_CEVT` 복원, 재진입·ACTCD·BUSY 대칭 확인. 03=공통 잔여참고(확인 콜백 designRefershModel await) 반영(콜백 async+await, 원본 uiAttributeArea.js:3183 계약과 동일). 실화면 BR29-1~6 전부 통과·히스토리 이관. 노션=수정완료 |
| br31 | ✅ | ✅ | ✅ | ✅ | ☐ | 코덱스/안티 둘 다 통과·필수지적0(콤보 잠금 강건성·Undo 복원 무결). 03=코덱스 잔여참고2건 처리: (1)초기화 3개 호출 조용한 catch→console.error 표면화(규칙 합치, 반영), (2)요청서 autoGrowing 미이식 메모=BR29로 이미 이식됨(정정, 무관). 실화면 테스트 대기 |
| br28 | ✅ | ❌ | ❌ | ✅ | ✅ | 취합·반영(03): 과잉방어=해소확인(스크롤이 활성조건 밖·안티 거짓반박). 미바인딩 재오픈 옛 스크롤 잔존(P1)=반영(목록 맨위 초기화), 닫힘 후 rAF 정리=반영. 완전 비동기 직렬화(세대토큰 P2)=반론(원본에 없는 방어·BR28 직접원인 아님, 보장범위=예약 rAF취소로 한정·별도이슈 권고). 노션=수정완료 |
| br27 | ✅ | ❌ | ❌ | ✅ | ✅ | 코덱스·안티 P1(재오픈 세션경계 유령 선택) 반영: 새로 그리기 직전 선택 명시 해제(lf_renderTree)로 재오픈/새로고침/빈·오류 렌더 모두 초기화 → 유령 강조 차단. + 선택불가 행 클릭 해제 통일. ★활성(enable) 게이팅은 원본 스크롤(setFirstVisibleRow) 회귀라 미채택(반론1, 처음 넣었다 되돌림) — lf_setSelectTree 원본 그대로. 실화면 BR27-1~6 전부 통과·히스토리 이관. 노션=수정완료 |
| br26 | ✅ | ❌ | ❌ | ✅ | ✅ | 안티 지적(멀티 체크 삭제 경로 자식 prev 잔존) 반영: 체크 삭제 루프에도 `_purgePrevSubtree` 배선. 코덱스 P1(이벤트·설명·바인딩·팝업 수집정보 정리)은 반론=BR26 이전부터 최상위도 미정리인 선재·광범위 결손, 측정범위(prev) 밖 → 별도 이슈 권고. 노션=수정완료 |
| br25 | ✅ | ✅ | ✅ | ✅ | ✅ | Codex·안티 둘 다 통과·지적0(확인창 직후 BUSY 해제=원본 uiAttributeArea.js:4753·4831 1:1, YES 재잠금·공통 finally 해제, 취소·팝업 종료·BUSY 대칭). 코덱스 잔여참고(표준밖 window.confirm 폴백)=대상환경 미해당+원본에 없는 방어라 미반영(03). 실화면 BR25-1~3 전부 통과·히스토리 이관. 곁들여 SEL(모달 트리 줄 선택표시 소실)도 수정·통과. 노션=수정완료 |
| br24 | ✅ | ❌ | ✅ | ✅ | ✅ | 코덱스 P1(조회 실패 오판 방지)·P2(과도한 텍스트 색상 제거) 지적사항 전부 반영(03). 안티 최종 검수(04) 통과. 실화면·재검수 대기 완료 |
| br23 | ✅ | ✅ | ✅ | ✅ | ✅ | 코덱스·안티 재검수 둘 다 통과·지적0(A 모듈 선로드·OBJID·전 종료 잠금회수, B 툴바 경계, C 가상 트리 재사용, D 팝업 내부 드롭 차단). 03=반영/반론 없음. 실화면 개인화 팝업 위치·크기 7건(PFC1~7)+내 패턴 진입 5건(BR23-1~5) 전부 통과. 전체창/리사이즈·공통 팝업 리사이즈 화면가둠 추가 반영. 노션=수정완료, 커밋 8ed6da46 푸시 |
| br22 | ✅ | ✅ | ✅ | ✅ | ✅ | Codex·안티 둘 다 통과·지적0(공용 함수 공유, UI5 Dialog·서버조회 차단, BUSY·단축키 잠금 회수, 로드순서·null인자 검증). 실화면 BR22-1~4 전부 통과·히스토리 이관. 노션=수정완료 |
| br21 | ✅ | ❌ | ✅ | ✅ | ✅ | 코덱스2·안티3 지적 정리: APP폴백·노드부재폴백·화면잠금누수·_done중복가드 4건 반영, 비동기 재잠금 1건 반론(원본/트리 SSOT 미재잠금). 실화면 BR21-1~4 전부 통과·히스토리 이관. 노션=수정완료 |
| br20 | ✅ | ❌ | ✅ | ✅ | ✅ | 코덱스 수정필요 3건 전부 반영(03): 빈 설명이면 `_DESC` 미생성(원본1:1)·위임불가시 깨진원본 미호출+잠금만 해제·저장 실패 표면화. 안티 통과. 실화면 BR20-1~3 통과. 노션=수정완료 |
| br18 | ✅ | ❌ | ❌ | ✅ | ✅ | 장군님 지시"원본대로" 반영: A(오픈오류해제)·B(확정BUSY 복원)·D(Enter·닫기키)·E(미리강조 HTML5 재현) 반영, 미리보기+트리 팝업 통일. C=그냥두기, F·G 반론. 실화면 RS1~RS6·RS3b·MP1~MP7 전부 통과. 노션=수정완료. 커밋 f8435614 푸시 |
| br17 | ✅ | ✅ | ✅ | ✅ | ✅ | 미리보기 우클릭 위/아래 이동 예외·화면잠금 → 트리 이동으로 위임. 코덱스·안티 둘 다 통과(지적 없음). 노션=수정완료. 실화면 테스트 대기 |
| br19 | ✅ | ❌ | ❌ | ✅ | ✅ | 코덱스·안티 수정필요 2건(오류경로 해제·이중rAF) 전부 반영(03). 실화면 BR19-1~5 전부 통과 + 취소 안내 공통알림 수정. 노션=수정완료 |
| br16 | ✅ | ❌ | ❌ | ✅ | ✅ | 지적1(try 범위)·2(await) 반영. 지적3(공통 방송모듈 변경)은 미반영-공통코드·원본1:1 규칙상 반론. 실화면 통과·노션=수정완료 |
