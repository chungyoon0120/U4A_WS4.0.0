# 프로젝트 작업 규칙 (모든 agent 공통)

이 파일은 **Claude · codex · antigravity 등 어떤 agent 든** 이 프로젝트에서 일할 때 지키는 규칙이다.
Claude 는 `CLAUDE.md` 와 `.claude/rules/` 도 함께 읽는다. 내용이 어긋나면 **이 파일과 `.analy` 문서가 기준**이다.

## 시작하기 전에 반드시 읽는다

1. **`.docs/CURRENT.md`** — 지금 상태 · 진행 중 · 미해결 · 일하는 순서. **이것부터 읽는다**
2. `.docs/README.md` — 작업 이력 남기는 규칙
3. `.docs/history/` — **건드릴 화면 이름으로만** 지난 이력을 찾아 읽는다 (전체를 다 읽지 말 것)

---

## ★ 최우선 규칙

### 1. 원본(as-is) 소스 위치 — 두 곳, 읽기 전용

| | 어디 | 무엇 |
|---|---|---|
| ① | `C:\Users\socce\Documents\Github\U4A_WS3.0.0` | 전체 |
| ② | `C:\Users\socce\Documents\Github\U4A_WS_DESIGN` | **디자인 영역(`design/`) + 담당 팝업 5개** (값도움 OTR · 바인딩 · 테마 만들기 · 옵션 · WebDynpro 변환 기록) |

담당 개발자가 둘이라 영역별로 나뉘어 있다. **②가 상류이며 더 최신**이므로 그 영역은 ②를 먼저 본다.
폴더 구조는 작업 폴더와 같다. **읽기 전용 — 절대 쓰지 않는다.**

**작업 폴더 `www/` 를 원본으로 믿지 말 것** — 원본 파일 173개가 이미 고쳐져 섞여 있다.
`C:\Users\socce\Documents\U4A_WS3.0` 도 **원본이 아니다**(부분 변환본, 22개 수정됨).
상세 = `.analy/18_원본소스_위치_SSOT.md`

### 2. 원본 우선 — 임의로 만들어 넣지 않는다

원본에 있는 속성·동작·구조를 그대로 이식한다.
**원본에 없는 UX·요소·옵션·스타일·레이아웃을 임의로 추가하거나 바꾸지 않는다.**
(원본에 없는 아이콘 · 빈 화면 디자인 · 건수 표시 · 레이아웃 재해석 등 금지)

이상한 점·개선안이 보여도 **코드에 반영하지 말고 보고만** 한다. **별도 지시가 있을 때만** 적용한다.
확신이 없으면 만들지 말고 **먼저 묻는다.**

원본 UI5 가 자동으로 해 주던 동작은 **소스에 안 적혀 있어도 HTML5 에서 명시적으로 재현**해야 한다.
"소스에 없다 = 안 해도 된다" 가 아니다.

### 3. 작업 폴더 안은 전부 고쳐도 된다 — 사본 폴더 포함

진짜 원본은 다른 폴더·다른 git 으로 따로 관리하므로,
현재 프로젝트 폴더(`U4A_WS4.0.0`) 안은 **사본 폴더(`www/ws30/ws10_20/design/`)든 밖이든 수정 가능**하다.

**원본이 바뀌어도 소스를 통째로 옮기지 않는다.** 목표는 소스 동기화가 아니라 **「기능이 같으면 성공」**.
원본을 **참고**해서 사본 폴더 안 로직을 고친다.

새 로직은 되도록 `js/ws_html5_*.js` 계열에 두되, **사본 폴더 안을 고쳐야 하면 고친다.**
어느 쪽이든 **고치기 전 백업**(`_` 접두)과 **왜 고쳤는지 주석**을 남긴다.

### 4. 남이 만든 라이브러리는 한 글자도 고치지 않는다

글꼴 아이콘 · 글 편집기 · 암호 처리 · 내려받기 도구 등 **바깥에서 받아온 것 전부.**
문제가 있으면 **버전을 올리거나 다른 판을 받는 것이 답**이다.

**폴더 이름만으로 판별하지 말 것.** **파일 앞머리의 저작권 표시**와
**줄여 놓은 배포용 파일(`.min.js`)인지**까지 확인한다.
여러 파일을 한꺼번에 고치는 작업은 **대상 목록을 먼저 보고**한 뒤 실행한다.

### 5. 코드를 새로 쓰거나 고치면 로그를 같이 넣는다

**이 프로그램은 AI 가 로그만 보고 운영·수정한다. 로그 없는 신규 로직은 완료가 아니다.**
SSOT = `.works/DEV_STANDARD_로그.md`

- 오류 잡는 자리(catch) · 필요한 값이 없어 그만두는 자리 · 서버에 다녀오는 자리 **전부**
- **로그 글은 전부 영어.** 단 화면에 실제로 적힌 글자(버튼 이름 · 서버가 준 글)는 그대로 둔다
- 안 남길 것: 비밀번호 · 접속 열쇠 · 입력칸에 친 값 · 업무 자료 통째로 · 주소 물음표 뒤 · 마우스 움직임
- 양: 정상 동작 중 분당 수백 줄 금지. 같은 자리 반복은 처음 3번 + 500번마다
- **완료 판정**: 그 기능이 실패했을 때 **로그만 보고 어디가 실패했는지 알 수 있어야** 끝난 것이다

### 6. 서버에 다녀오는 일 · 저장하는 일은 시작 전에 busy 를 켠다

처리 중 사용자 조작을 막아 버리는 것이 가장 단순하고 안전하다.

- 팝업은 **뜨자마자 busy 부터 켜고**, 준비가 다 끝난 **맨 마지막에** 끈다. 중간에 끄지 않는다
- **겹치는 처리는 깊이(개수)를 세어** 깊이 0 일 때만 실제로 끈다
- busy 를 켠 모든 종료 갈래(early return · 취소 · 오류)에서 **끄는 짝이 반드시 있어야** 한다
- 부모 창이 방송으로 알려오는 바쁨은 깊이와 **섞지 말고 따로** 기억한다
- 통신 함수에는 **실패용 콜백을 반드시** 넘긴다. 안 넘기면 통신 실패 시 busy 가 안 풀린다

### 7. 브라우저 기본 대화상자 금지

**`window.confirm` · `window.alert` · `window.prompt` 절대 금지.**
테마·다국어·드래그가 안 먹고, 모달창 위에 딴판으로 뜨며, 앱 전체를 멈춰 세운다.

확인창은 **공통 확인창만** 쓴다. 공통이 없거나 모달 띄우기가 실패하면
**fallback 을 만들지 말고** `접두-번호` 오류코드로 표면화하고 **진행하지 않고 끝낸다**(= 취소로 처리).

### 8. 오류는 삼키지 않는다

SSOT = `.analy/19_예외처리_크리티컬오류_표준.md` · 실무 규약 = `.works/DEV_STANDARD_오류처리.md`

- 예측 가능한 입력·유효성 오류 → 안내 메시지 + 정상 return
- 예측 못 한 오류(catch 구간 · 필수 의존성 없음) → **`접두-번호` 내부 오류 코드로 표면화**
- 콘솔엔 `[코드] 어느 자리 + 예외 원문`, 화면엔 `[코드] + 기존 메시지 키 문구`
  (예외 원문·스택은 화면에 노출하지 않는다)
- 저장·실행처럼 반드시 성공해야 하는 일은 보내기 전 한 군데라도 터지면 **서버로 안 보내고 중단**
- 전수 개조 금지 — **신규·수정분부터** 점진 적용

### 9. 항상 느린 PC 도 고려한다

개발 PC 는 빠르지만 고객사 PC 는 아니다.

- **「N초 뒤에 하면 되겠지」로 못 박지 말 것.** **끝난 것을 확인하고** 넘어간다.
  기다림에는 상한을 두고, 상한에 닿으면 **왜 못 기다렸는지 로그를 남기고** 다음 길로 간다
- **큰 파일을 통째로 읽지 말 것.** 필요한 토막만 읽는다. **앱 켤 때 도는 코드는 특히**
- 무거운 일을 켜는 길목에 두지 않는다

### 10. 비동기 · 화면 전환

- **로딩 표시(busy)는 화면 그리기·데이터 구성·전환 애니메이션이 완전히 끝난 뒤에 끈다.**
  중간에 미리 끄면 그 틈에 버튼 연타가 들어와 상태가 꼬인다(백지 등)
- 예약한 뒷정리 타이머는 **핸들을 저장**하고, 다음 동작 시작 시 **먼저 취소**해 겹침을 막는다
- 화면 전환은 **완료를 콜백/Promise 로 알리고**, 후속(잠금 해제 · busy off · 복원)은 그 **완료 후**에만 한다

---

## 소스 탐색 규칙

- **폴더 이름이 `_`(언더바)로 시작하는 폴더는 탐색 대상에서 제외**한다.
  백업·구버전·실험/폐기 코드이며 현행 산출물이 아니다
- 검증·참조·변환 근거를 찾을 때 이 폴더들의 내용을 **현행으로 인용하지 않는다**

---

## UX / 화면 / 컨버전 작업 규칙

UX·화면·UI5→HTML5 컨버전 표준의 **단일 출처(SSOT)는 `.analy/` 문서 세트**다.

### 시작 전 반드시 읽기

- `.analy/00_개요_및_컨버전전략.md` — 문서 인덱스. 어떤 문서를 봐야 하는지 먼저 파악한다
- `.analy/13_AI_작업지시_가이드.md` — 작업 방식 · 지시 템플릿 · 가드레일
- **`.analy/16_공통_화면UX_표준.md`** — 모든 화면 공통 표준. 화면 작업 시 항상 함께 적용한다
- 작업 대상 기능 문서 + `.analy/12_테마_컨버전_전략.md` 를 끝까지 읽고 시작한다
  - 입력칸: `.analy/15_공통_입력UX_가이드.md`
  - 팝업/모달: `.analy/06_팝업.md`
  - 로그인/서버리스트(트리 포함): `.analy/02_로그인_서버리스트.md`
  - 디자인 영역: `.analy/05_디자인영역.md`
  - 그 밖의 매핑은 13번 문서 3절의 표를 본다

### 진행 방식

- **한 번에 작업 단위 하나.** "전부 컨버전" 이 아니라 해당 문서의 실행 사양 절 하나씩
- 문서의 작업 순서를 그대로 따르고, **산출물 트리에 명시된 파일만** 만들거나 고친다
- 모든 설계 결정은 **문서의 절 번호로 근거**를 댄다. 문서에 없으면 **추측하지 말고 묻는다**
- 모양·동작은 화면마다 새로 만들지 말고 **공통 자산을 소비**한다
  (`www/ws30/ws10_20/theme/` 의 `shell.css` · `u4a-ui.js` · `tokens.css` · `bootstrap-skin`)
- **공통 자산을 직접 고치지 않는다** → 화면 범위로 덧댄다(override)
- 색은 `theme/tokens.css` 의 의미 토큰만 쓴다. **하드코딩 hex 금지**, `color-mix` 금지(Chromium 93)
- **반드시 반응형** — 고정 px 폭 금지
- 팝업(`<dialog>`)을 만들면 **공통 3종을 항상 같이** 붙인다: 헤더 드래그 · 헤더 더블클릭 중앙 복귀 · 우하단 크기 조절
- 메시지 문구: 기존은 **원본(as-is) 키 참조**. 원본에 없고 지시도 없으면 공통 메시지 키를 쓴다.
  **임의 문구·키 생성 금지**, 필요한 키는 보고한다. **메시지 DB 를 직접 고치지 않는다**

### 저장 직전

- **`node --check`** 로 문법을 본다
- 오류 삼킴 · 조용한 catch 금지

---

## 작업 이력 남기기 (필수)

**파일을 고쳤으면 답변을 끝내기 전에 `.docs/history/YYYY/MM/` 에 이력을 남긴다.**

안 남기면 **답변 끝날 때 hook 이 막는다.** 형식·필수 섹션은 `.docs/README.md` 를 본다.
파일 이름의 화면 이름은 **소스 폴더 이름 그대로** 쓴다(나중에 이름으로 찾기 위한 것).
**실패·미검증·되돌린 것도 숨기지 않고 쓴다** — 다음 agent 가 같은 실수를 반복한다.

작업이 끝나면 **`.docs/CURRENT.md` 에서 바뀐 것을 반영**한다.

---

## 문서(.md) 편집 규칙

- 테스트 절차 · 화면 설명 · 버튼 동작은 **소스로 확인한 뒤** 적는다. 추측 금지, 확인 못 한 건 "미확인" 표기
- 용어는 **화면/원본(as-is)** 것만 쓴다. 코드 내부 라벨을 쓰지 않는다
- **문서 맨 위 = 「지금 할 것」.** 설명·근거·이력은 그 아래로 내린다
- 테스트 항목은 그 작업 폴더(`.works/<영역>/00_현황판.md`)에 **체크리스트 표**로 쓴다
  형식: `| 코드 | 동작 | 조작·확인 포인트 | O/X(☐) |`, 각 행 끝 ☐ / ✅ O / ❌ X
- 조작은 **①②③ 순서로 쪼개고**, 버튼·영역은 **화면에 적힌 이름 그대로**, 쉬운 말로
- **정상 조작으로 도달 못 하는 내부 방어는 테스트 항목에 넣지 않는다**
- 그룹 안에서 **항목 순서를 바꾸지 않는다.** 통과하면 그 자리에 ✅ O 표시만 한다
- 히스토리 이관은 **그룹 전체가 통과했을 때 그룹째** 한다

---

## 커밋 규칙

- 커밋 메시지 제목 **맨 앞에 `[YYYY-MM-DD HH:MM]`**
- 기본 브랜치면 **먼저 브랜치를 판다**
- **커밋·푸시는 지시가 있을 때만** 한다
- busy · 닫기 같은 **기초 결함을 미해결로 두고 커밋하지 않는다**

---

## 답변 규칙

- **팩트만 · 짧게 · 결과 위주.** 추측성 답변 금지
- **IT 용어는 영어 그대로 쓴다** — `tree` · `snapshot` · `node` · `model` · `filter` · `path` · `circular` · `DOM`.
  한글로 옮기지 않는다. 딱 맞는 한글이 없으면 **지어내지 말고 영어를 쓴다**
- `sap.ui.table.Table` · `columns` · `template` · `styleClass` 같은 **고유 이름은 원래 이름 그대로**
- 다만 **코드 내부 이름은 답에 복사하지 않는다** — CSS 클래스 이름 · 아이콘 이름 · 메시지 번호 · 주석 라벨.
  **화면에 보이는 글자/모양으로 바꿔** 말한다
- 어려운 한자어는 쉬운 말로 풀어 쓴다
